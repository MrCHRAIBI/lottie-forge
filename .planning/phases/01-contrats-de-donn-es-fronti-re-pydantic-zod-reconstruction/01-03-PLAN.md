---
phase: 01-contrats-de-donn-es-fronti-re-pydantic-zod-reconstruction
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - lottie_forge/domain/asset.py
  - tests/domain/test_asset.py
  - tests/bridge/fixtures.py
  - tests/bridge/test_asset_bridge.py
  - src/rpc/contracts/asset-spec.schema.ts
  - src/rpc/contracts/asset-spec.spec.ts
  - fixtures/rejection-cases/asset-spec.json
autonomous: true
requirements: [DM-03, DM-05]

estimate:
  tokens: 37500
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "asset_id matche ^a-\\d{3}$ exactement : chaîne vide, a-12 (2 chiffres), a-1234 (4 chiffres) rejetés des deux côtés avec loc/path [\"asset_id\"] (résolution probe empty DM-03 via §4.7 — 50 slots à exactement 3 chiffres)"
    - "style_ref pin name@version triple : version partielle (name@1.2), version à 4 segments et nom non-kebab rejetés des deux côtés — l'ancre de dérive STY-03 existe dès cette phase (§4.7)"
    - "content_hashes est un modèle clos à 2 champs : toute clé supplémentaire est rejetée, hash non hexadécimal, en majuscules ou de longueur ≠ 64 rejeté (§4.7 — pas de mapping ouvert, §2.5)"
    - "composition_meta.shape_group_names : liste vide rejetée (1..24), noms au pattern ^[a-z][a-z0-9-]{2,31}$ (3..32 caractères), rejet des deux côtés (résolution probe empty DM-03)"
    - "recipe_ref réutilise le vocabulaire clos de 01-02 : disco-spin rejeté au niveau asset (§4.7 — jamais re-déclaré)"
    - "Encodage clos : tous les patterns sont ASCII ancrés — un token non-ASCII (ex. contenant une lettre accentuée) est rejeté des deux côtés ; longueur = code points, divergence Py/zod structurellement impossible (résolution probe encoding DM-03)"
    - "Résolution probe DM-04 du rapport de couverture (item unclassified restant côté asset) : §4.7 définit chaque champ — traduit en critères d'acceptation ci-dessous, aucune hypothèse laissée ouverte"
  artifacts:
    - "lottie_forge/domain/asset.py : AssetSpec, CompositionMeta, ContentHashes (STRICT_CONFIG)"
    - "src/rpc/contracts/asset-spec.schema.ts : AssetSpecSchema (z.strictObject nested) + type AssetSpec"
    - "tests/bridge/fixtures.py : make_asset() ajouté"
    - "tests/bridge/test_asset_bridge.py : test_export_asset + test_reimport_asset + test paramétré harnais de rejet"
    - "fixtures/rejection-cases/asset-spec.json : cas de rejet commités (format D-08)"
  key_links:
    - "recipe_ref: RecipeId importé depuis domain/vocabulary.py (01-02) — vocabulaire clos réutilisé au niveau asset (§4.7)"
    - "make_asset() → fixtures/bridge/asset-spec.from-python.json → asset-spec.spec.ts → asset-spec.from-ts.json → test_reimport_asset"
    - "fixtures/rejection-cases/asset-spec.json → pytest parametrize ET vitest test.each (loaders de 01-01, D-06)"
    - "style_ref regex = l'ancre que la gate de re-validation STY-03 consommera en Phase 2"

prohibitions:
  - "Aucune seconde liste d'ids de recettes hardcodée en dehors de lottie_forge/domain/vocabulary.py et src/rpc/contracts/vocabulary.schema.ts (règle same-commit §4.2)"
  - "Aucune coercion numérique : patterns et bornes en strict, aucun cast implicite (§4.1 #3)"
  - "Aucune assertion de parité d'erreur sur le texte des messages — appartenance de chemin uniquement (D-08)"
  - "Aucun test skippé en CI : junitxml + skipped == 0 asserté (§4.2) ; côté TS, artifact bridge manquant = échec bruyant, pas un skip"
  - "Aucune licence de forme abonnement constructible (structurelle, gate Phase 1 — réalisation complète en 01-04)"
  - "Aucun fixture bridge committé : fixtures/bridge/ gitigné, généré au test (§4.3)"
  - "Aucune validation calendrier de la date de pack_id — forme nominale seulement (IN-07, réalisée en 01-04)"
  - "Aucun validateur Python fait main à la place de pydantic-core pour KebabToken (fix CR-01, §4.6)"
  - "Aucune valeur par défaut sur style_version (§4.5)"
  - "Aucune clé extra acceptée : extra=forbid Py / z.strictObject TS sur chaque modèle et modèle imbriqué (§4.1 #3) — content_hashes est un modèle clos, pas un mapping ouvert"
  - "Aucun hook pre-commit local (D-05) : ruff/biome/tsc enforce en CI uniquement + séquence README"
  - "Aucun dict[str, Any] traversant la frontière — modèles clos uniquement (§2.5)"
  - "dotlottie_sha256 n'existe PAS dans ContentHashes en Phase 1 (extension Phase 8 par édition même commit, §4.14)"
---

<objective>
Livrer le contrat `AssetSpec` complet des deux côtés : identité d'asset (`asset_id` a-\d{3}), pin de style (`style_ref` name@version — ancre STY-03), référence de recette sur le vocabulaire clos, métadonnées de composition et hashes de contenu en modèle clos — plus miroir zod, chaîne bridge et cas de rejet partagés.

Purpose: AssetSpec est l'unité de traçabilité que le manifest (01-04), le store (Phase 5) et le compiler (Phase 3) consomment ; ses patterns sont les ancres de toutes les gates en aval.
Output: asset.py + asset-spec.schema.ts + bridge vert + asset-spec.json committé.
</objective>

<execution_context>
@D:/projets/illustrations/systeme/.opencode/gsd-core/workflows/execute-plan.md
@D:/projets/illustrations/systeme/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@docs/project/04_Modeles.md
@.planning/phases/01-contrats-de-donn-es-fronti-re-pydantic-zod-reconstruction/01-01-SUMMARY.md
@.planning/phases/01-contrats-de-donn-es-fronti-re-pydantic-zod-reconstruction/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AssetSpec Pydantic strict (asset_id, style_ref pin, content_hashes clos) + suite domain</name>
  <files>lottie_forge/domain/asset.py, tests/domain/test_asset.py</files>
  <behavior>
    - asset_id "a-000" et "a-999" → acceptés ; "a-12", "a-1234", "b-123", "", "a-" → rejetés loc ["asset_id"]
    - style_ref "example-style@1.0.0" → accepté ; "example-style@1.2" (version partielle) → rejeté loc ["style_ref"] ; "example_style@1.0.0" (underscore) → rejeté ; "example-style@1.0.0.1" → rejeté
    - recipe_ref "fade" → accepté ; "disco-spin" → rejeté loc ["recipe_ref"] (vocabulaire clos réutilisé)
    - composition_meta.shape_group_names : 1 nom et 24 noms acceptés ; 0 nom et 25 noms rejetés loc ["composition_meta","shape_group_names"] ; nom "ab" (trop court) et "bg-shape" ok pattern 3..32
    - content_hashes : svg_sha256 + lottie_sha256 en 64 hex minuscules → accepté ; 63 caractères, 64 hex avec majuscule, caractère non-hex → rejetés loc ["content_hashes", champ] ; 3e champ quelconque → rejeté (modèle clos, extra=forbid)
    - token non-ASCII (ex. groupe de formes contenant une lettre accentuée) → rejeté des deux côtés (probe encoding résolu)
  </behavior>
  <files_note>Écrire tests/domain/test_asset.py d'abord (rouge), puis domain/asset.py jusqu'au vert.</files_note>
  <read_first>docs/project/04_Modeles.md §4.7 (table champ par champ + WR-01), §4.11 (schémas d'IDs/hashes), §4.14 (extensions différées) ; lottie_forge/domain/_shared.py + vocabulary.py (01-01/01-02) ; 01-CONTEXT.md D-01/D-02</read_first>
  <action>
Créer lottie_forge/domain/asset.py avec STRICT_CONFIG importé de _shared.py : AssetSpec{asset_id: str regex ^a-\d{3}$, style_ref: str regex ^[a-z][a-z0-9-]*@\d+\.\d+\.\d+$ (points échappés), recipe_ref: RecipeId importé de domain/vocabulary.py, composition_meta: CompositionMeta, content_hashes: ContentHashes} ; CompositionMeta{shape_group_names: list de str pattern ^[a-z][a-z0-9-]{2,31}$, 1..24 items} ; ContentHashes{svg_sha256: str pattern ^[a-f0-9]{64}$, lottie_sha256: str pattern ^[a-f0-9]{64}$} — modèle clos à exactement 2 champs, PAS de dotlottie_sha256 (Phase 8, §4.14). Écrire tests/domain/test_asset.py selon le bloc behavior, paramétré, assertions de loc par errors() normalisés, bornes testées à la limite exacte et un pas au-delà (liste 1/24, hashes 63/64/65). Cycle rouge→vert documenté en commits atomiques (D-09).
  </action>
  <verify>
    <automated>python -m pytest tests/domain/test_asset.py -q && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/domain/test_asset.py -q exit 0 après cycle rouge→vert (commits test(01-03) puis feat(01-03), D-09)
    - pytest -q -k disco_spin couvre le rejet au niveau asset (recipe_ref) en plus du niveau recipe
    - pytest -q -k accent exit 0 (cas non-ASCII / newline rejetés dans les champs asset)
    - Le modèle ContentHashes n'a que 2 champs (le test du 3e champ rejeté passe)
  </acceptance_criteria>
  <done>AssetSpec rejette tout payload invalide en strict avec loc précises ; les ancres asset_id/style_ref/hashes sont opérationnelles pour les phases en aval.</done>
</task>

<task type="auto">
  <name>Task 2: Miroir zod AssetSpec + bridge + cas de rejet partagés</name>
  <files>src/rpc/contracts/asset-spec.schema.ts, src/rpc/contracts/asset-spec.spec.ts, tests/bridge/fixtures.py, tests/bridge/test_asset_bridge.py, fixtures/rejection-cases/asset-spec.json</files>
  <read_first>docs/project/04_Modeles.md §4.7, §4.9 (miroirs), §4.2/§4.3 (parité + bridge) ; lottie_forge/domain/asset.py ; src/rpc/contracts/recipe.spec.ts (patron 01-02) ; loaders rejection (01-01) ; 01-CONTEXT.md D-06/D-07/D-08</read_first>
  <action>
Créer src/rpc/contracts/asset-spec.schema.ts : miroir z.strictObject exact (nested compositionMetaSchema + contentHashesSchema stricts, recipeIdSchema importé de vocabulary.schema.ts, regexes identiques échappées) + export type AssetSpec = z.infer. Ajouter make_asset() dans tests/bridge/fixtures.py (instance valide, hashes 64 hex minuscules réalistes, style_ref piné sur la version de make_style_spec()). Créer tests/bridge/test_asset_bridge.py : test_export_asset (asset-spec.from-python.json + asset-spec.schema-keys.json, patron 01-01/01-02), test_reimport_asset (skipif garde d'ordre, égalité canonique model_dump_json), test paramétré consommant rejection_loader sur le nouveau fichier de cas. Créer src/rpc/contracts/asset-spec.spec.ts : validate + deep-equal + parité de clés + re-émission + test.each sur les cas partagés (échec bruyant si artefact absent, jamais de skip).

Créer fixtures/rejection-cases/asset-spec.json (format D-08) : asset-id-deux-chiffres / asset-id-quatre-chiffres / asset-id-vide (ref DM-03, expect_paths ["asset_id"]), style-ref-version-partielle (expect_paths ["style_ref"]), recipe-ref-hors-catalogue (ref DM-02, expect_paths ["recipe_ref"]), shape-groups-vide (ref DM-03, expect_paths ["composition_meta","shape_group_names"]), shape-group-nom-court (expect_paths ["composition_meta","shape_group_names",0]), hash-majuscule / hash-longueur-63 (expect_paths ["content_hashes", champ]), content-hashes-champ-extra (expect_paths ["content_hashes", clé extra]), token-non-ascii (probe encoding, expect_paths sur le champ porteur). Rejets TOUJOURS assertés des deux côtés ; expect_paths par appartenance de chemin, jamais sur le texte des messages (D-08).
  </action>
  <verify>
    <automated>python -m pytest tests/ -q -k export && npx vitest run asset-spec && python -m pytest tests/ -q && npx tsc --noEmit && ruff check . && npx @biomejs/biome check .</automated>
  </verify>
  <acceptance_criteria>
    - Chaîne bridge asset-spec verte (export → vitest → re-import) exit 0
    - npx vitest run asset-spec exit 0 avec test.each dont les case_ids sont exactement ceux de fixtures/rejection-cases/asset-spec.json
    - python -m pytest tests/ -q -k asset exit 0 (tests bridge asset + domain asset)
    - fixtures/rejection-cases/asset-spec.json commité (D-07)
    - Suite globale verte : python -m pytest tests/ -q exit 0
  </acceptance_criteria>
  <done>AssetSpec validé/rejeté de façon identique des deux côtés via le harnais partagé ; les probes empty et encoding de DM-03 sont couverts par des critères concrets testés.</done>
</task>

</tasks>

<artifacts-this-phase-produces>
Inventaire complet de la Phase 1 : voir 01-01-PLAN.md (section dédiée). Symboles créés par CE plan :
- `AssetSpec`, `CompositionMeta`, `ContentHashes` (modèle clos 2 champs) — `lottie_forge/domain/asset.py`
- `AssetSpecSchema` + `type AssetSpec` — `src/rpc/contracts/asset-spec.schema.ts`
- `make_asset()` — ajout à `tests/bridge/fixtures.py`
- `fixtures/rejection-cases/asset-spec.json` (commité, format D-08)
- Fichiers : tests/domain/test_asset.py, tests/bridge/test_asset_bridge.py, src/rpc/contracts/asset-spec.spec.ts
</artifacts-this-phase-produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payload JSON → validateurs AssetSpec | Les specs d'asset produites par l'orchestrateur (futures) traversent ici ; hashes et ancres doivent être intègres |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | content_hashes (hash falsifié ou champ injecté) | medium | mitigate | modèle clos 2 champs + pattern 64 hex minuscules des deux côtés ; extension future uniquement par édition même commit (§4.11/§4.14) |
| T-01-06 | Repudiation | style_ref mal formé cassant l'ancre de re-validation STY-03 | medium | mitigate | regex triple name@version stricte des deux côtés + cas de rejet version partielle partagé |
</threat_model>

<verification>
- python -m pytest tests/ -q exit 0 · npx vitest run exit 0 · npx tsc --noEmit exit 0 · ruff check . exit 0 · npx @biomejs/biome check . exit 0
- Chaîne bridge asset-spec verte depuis état frais
- fixtures/rejection-cases/asset-spec.json commité et consommé par les deux loaders
</verification>

<success_criteria>
- DM-03 complet : asset_id, style_ref pin, recipe_ref clos, composition_meta, content_hashes clos — validés des deux côtés
- Probes DM-03 (empty, encoding) résolus en critères testés, zéro hypothèse ouverte
- Le patron bridge tient pour un 3e contrat sans déviation
</success_criteria>

<output>
Create `.planning/phases/01-contrats-de-donn-es-fronti-re-pydantic-zod-reconstruction/01-03-SUMMARY.md` when done
</output>
