---
phase: 01-contrats-de-donn-es-fronti-re-pydantic-zod-reconstruction
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lottie_forge/domain/vocabulary.py
  - lottie_forge/domain/recipe.py
  - tests/domain/test_vocabulary.py
  - tests/domain/test_recipe.py
  - tests/bridge/fixtures.py
  - tests/bridge/test_vocabulary_bridge.py
  - tests/bridge/test_recipe_bridge.py
  - src/rpc/contracts/vocabulary.schema.ts
  - src/rpc/contracts/vocabulary.spec.ts
  - src/rpc/contracts/recipe.schema.ts
  - src/rpc/contracts/recipe.spec.ts
  - fixtures/rejection-cases/recipe.json
autonomous: true
requirements: [DM-02, DM-05]

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "RecipeId dérive d'une liste close unique : RECIPE_IDS tuple (10 ids verrouillés, ordre canonique fade, slide, bounce, pulse, draw-on, rotate, scale-pop, float, wiggle, orbit) et RecipeId = Literal[*RECIPE_IDS] — get_args(RecipeId) == RECIPE_IDS asserté (§4.4)"
    - "Tout id hors catalogue (disco-spin) est rejeté par Pydantic strict ET zod safeParse (critère de succès ROADMAP n°2)"
    - "L'invariant 8 <= nombre d'ids <= 12 est asserté des deux côtés, aux deux bornes et un pas au-delà (résolution probe frontière DM-05 via §4.4)"
    - "Le cas piégeux 1200.0 (WR-06, D-02 #2) est documenté par un test bridge pinné : z.number().int() accepte le float integral là où Pydantic strict le rejette — Python est l'autorité la plus stricte, toute entrée TS est re-validée au re-import (§4.9 — résolution probe précision DM-05)"
    - "duration_ms en strict : la chaîne « 1200 » est rejetée des deux côtés ; bornes 100..10000 testées à la borne et un pas au-delà"
    - "accent\n fullmatch est asserté dans test_vocabulary.py (CR-01 côté vocabulaire) et la parité de rejet KebabToken Py/zod est vérifiée via theme_anchors (D-02 #1)"
    - "Règle same-commit (ADR-03) : recipe.schema.ts importe RECIPE_IDS depuis vocabulary.schema.ts et ne la redéclare jamais — test structurel + parité de vocabulaire via fixtures/bridge/vocabulary.json (§4.2)"
    - "Résolution probe DM-02 (unclassified) : §4.4 + §4.6 définissent chaque champ — traduit en critères d'acceptation ci-dessous, aucune hypothèse laissée ouverte"
  artifacts:
    - "lottie_forge/domain/vocabulary.py : RECIPE_IDS (tuple canonique), RecipeId = Literal[*RECIPE_IDS] (star-unpack 3.12), assert_recipe_count()"
    - "src/rpc/contracts/vocabulary.schema.ts : RECIPE_IDS as const, recipeIdSchema = z.enum(RECIPE_IDS), type RecipeId, assertion d'invariant runtime"
    - "lottie_forge/domain/recipe.py : MotionRecipe, MotionParams (nested STRICT_CONFIG)"
    - "src/rpc/contracts/recipe.schema.ts : MotionRecipeSchema (z.strictObject nested) + type MotionRecipe"
    - "tests/bridge/fixtures.py : make_recipe() ajouté (single source of fixture truth, §4.9)"
    - "tests/bridge/test_vocabulary_bridge.py : test_export_vocabulary écrit fixtures/bridge/vocabulary.json"
    - "tests/bridge/test_recipe_bridge.py : test_export_recipe + test_reimport_recipe + test bridge asymétrie WR-06"
    - "fixtures/rejection-cases/recipe.json : cas de rejet commités (format D-08)"
  key_links:
    - "recipe_id: RecipeId importé depuis domain/vocabulary.py — jamais redéclaré dans domain/recipe.py (§4.6)"
    - "recipeIdSchema construit sur RECIPE_IDS importé depuis vocabulary.schema.ts — jamais redéclaré (ADR-03 same-commit)"
    - "test_export_vocabulary → fixtures/bridge/vocabulary.json → vocabulary.spec.ts deep-equal + invariant 8..12"
    - "make_recipe() → fixtures/bridge/recipe.from-python.json → recipe.spec.ts → recipe.from-ts.json → test_reimport_recipe"
    - "fixtures/rejection-cases/recipe.json → pytest parametrize ET vitest test.each (loaders de 01-01, D-06)"

prohibitions:
  - "Aucune seconde liste d'ids de recettes hardcodée en dehors de lottie_forge/domain/vocabulary.py et src/rpc/contracts/vocabulary.schema.ts (règle same-commit §4.2) — test structurel en place"
  - "Aucune coercion numérique : la chaîne « 1200 » est rejetée pour duration_ms des deux côtés (§4.1 #3)"
  - "Aucune assertion de parité d'erreur sur le texte des messages — appartenance de chemin uniquement (D-08)"
  - "Aucun test skippé en CI : junitxml + skipped == 0 asserté (§4.2) ; côté TS, artifact bridge manquant = échec bruyant, pas un skip"
  - "Aucune licence de forme abonnement constructible (structurelle, gate Phase 1 — réalisation complète en 01-04)"
  - "Aucun fixture bridge committé : fixtures/bridge/ gitigné, généré au test (§4.3)"
  - "Aucune validation calendrier de la date de pack_id — forme nominale seulement (IN-07, réalisée en 01-04)"
  - "Aucun validateur Python fait main à la place de pydantic-core pour KebabToken (fix CR-01, §4.6)"
  - "Aucune valeur par défaut sur style_version (§4.5)"
  - "Aucune clé extra acceptée : extra=forbid Py / z.strictObject TS sur chaque modèle et modèle imbriqué (§4.1 #3)"
  - "Aucun hook pre-commit local (D-05) : ruff/biome/tsc enforce en CI uniquement + séquence README"
  - "Aucun dict[str, Any] traversant la frontière — modèles clos uniquement (§2.5)"
  - "theme_anchors >= 1 n'est PAS imposé en Phase 1 (extension niveau catalogue Phase 2, MOT-03, §4.14)"
  - "Le cas WR-06 (float integral en int) n'apparaît JAMAIS dans fixtures/rejection-cases/recipe.json — c'est une asymétrie pinnée documentée par des tests dédiés, pas un cas de rejet miroir (zod l'accepte)"
---

<objective>
Livrer le vocabulaire clos `RecipeId` des deux côtés (source unique + invariant 8–12 + parité) et le contrat `MotionRecipe`/`MotionParams` complet (Pydantic strict + miroir zod + chaîne bridge + cas de rejet partagés), avec l'asymétrie pinnée WR-06 documentée par test bridge.

Purpose: Le vocabulaire fermé est la gate ADR-03 dont dépendent RecipePicker (Phase 6) et le catalogue (Phase 2) ; MotionRecipe verrouille le seul canal de motion admissible (ADR-01, §4.1 #7).
Output: vocabulary.py + recipe.py + miroirs zod + bridge vert + recipe.json committé.
</objective>

<execution_context>
@D:/projets/illustrations/systeme/.opencode/gsd-core/workflows/execute-plan.md
@D:/projets/illustrations/systeme/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@docs/project/04_Modeles.md
@docs/project/03_Stack.md
@.planning/phases/01-contrats-de-donn-es-fronti-re-pydantic-zod-reconstruction/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vocabulaire clos RecipeId des deux côtés + invariant 8–12 + same-commit</name>
  <files>lottie_forge/domain/vocabulary.py, tests/domain/test_vocabulary.py, src/rpc/contracts/vocabulary.schema.ts, src/rpc/contracts/vocabulary.spec.ts, tests/bridge/test_vocabulary_bridge.py</files>
  <read_first>docs/project/04_Modeles.md §4.4 (vocabulaire clos), §4.2 (parité de vocabulaire + règle same-commit), §4.6 (KebabToken) ; lottie_forge/domain/_shared.py (créé en 01-01) ; 01-CONTEXT.md D-01/D-02 #1</read_first>
  <action>
Créer lottie_forge/domain/vocabulary.py : RECIPE_IDS = tuple des 10 ids dans l'ordre canonique de §4.4 (fade, slide, bounce, pulse, draw-on, rotate, scale-pop, float, wiggle, orbit) ; RecipeId = Literal[*RECIPE_IDS] avec star-unpack 3.12 ; assert_recipe_count(ids) levant si le compte sort de l'intervalle fermé 8..12 (utilisé par les tests aux bornes). Créer tests/domain/test_vocabulary.py : (a) les 10 ids paramétrés en positifs (chaque id accepté dans un MotionRecipe minimal en Task 2 — pour l'instant validés comme membres du Literal via get_args) ; (b) disco-spin rejeté (hors catalogue) ; (c) invariant aux deux bornes : une liste de 7 ids et une liste de 13 ids violent l'invariant assert_recipe_count, 8 et 12 passent (résolution probe frontière) ; (d) get_args(RecipeId) == list(RECIPE_IDS) — une seule source ; (e) test fullmatch explicite : le pattern de KebabToken en fullmatch rejette accent\n (CR-01, D-02 #1 — §4.6 exige le fullmatch dans test_vocabulary.py) ; (f) test structurel same-commit : scanner les fichiers .ts de src/rpc/contracts/ et assert que vocabulary.schema.ts est le seul à déclarer la liste d'ids, tous les autres fichiers l'important (ADR-03).

Créer src/rpc/contracts/vocabulary.schema.ts : export const RECIPE_IDS (as const, même ordre), export const recipeIdSchema = z.enum(RECIPE_IDS), export type RecipeId, et une assertion runtime de l'invariant (échec bruyant si la longueur sort de 8..12). Créer src/rpc/contracts/vocabulary.spec.ts : lit fixtures/bridge/vocabulary.json (échec bruyant si absent, jamais de skip), deep-equal avec RECIPE_IDS, invariant 8..12 aux bornes. Créer tests/bridge/test_vocabulary_bridge.py avec test_export_vocabulary écrivant fixtures/bridge/vocabulary.json (liste Python sérialisée).
  </action>
  <verify>
    <automated>python -m pytest tests/domain/test_vocabulary.py -q && python -m pytest tests/ -q -k export && npx vitest run vocabulary</automated>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/domain/test_vocabulary.py -q exit 0, incluant les tests bornes 7/8/12/13 et disco-spin rejeté
    - python -m pytest tests/ -q -k export exit 0 et fixtures/bridge/vocabulary.json existe
    - npx vitest run vocabulary exit 0 (deep-equal + invariant)
    - get_args(RecipeId) == list(RECIPE_IDS) asserté dans la suite
    - Le test structurel same-commit passe et échouerait si un second fichier .ts déclarait la liste (logique de scan présente dans test_vocabulary.py)
  </acceptance_criteria>
  <done>Vocabulaire clos à source unique des deux côtés, invariant 8–12 asserté aux deux bornes et un pas au-delà, parité de vocabulaire via bridge, CR-01 fullmatch verrouillé.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MotionRecipe + MotionParams Pydantic strict + suite domain</name>
  <files>lottie_forge/domain/recipe.py, tests/domain/test_recipe.py</files>
  <behavior>
    - recipe_id valide (chaque id du catalogue) → MotionRecipe construit ; recipe_id "disco-spin" → ValidationError avec loc ["recipe_id"]
    - duration_ms 100 et 10000 → acceptés ; 99 et 10001 → rejetés avec loc ["duration_ms"] ; "1200" (str) → rejeté (strict) ; 1200.0 (float) → rejeté par Pydantic strict (moitié Py de WR-06)
    - params nested : amplitude 0 et 1 acceptés, -0.1 et 1.1 rejetés loc ["params","amplitude"] ; direction Literal up/down/left/right/none, "diagonal" rejeté loc ["params","direction"] ; loops 1..10, 0 et 11 rejetés loc ["params","loops"]
    - theme_anchors : défaut [] (champ optionnel), liste de 16 KebabToken acceptée, 17 rejetée, item "accent\n" rejeté loc ["theme_anchors", idx] (CR-01 via KebabToken)
    - family et easing : kebab max 64 ; "Solid Color" (espace) rejeté ; 65 caractères rejetés (WR-04)
    - extra key top-level ou dans params → rejetée (extra=forbid nested compris)
  </behavior>
  <files_note>Comportement spécifié avant implémentation : écrire tests/domain/test_recipe.py d'abord, vérifier qu'il échoue (module absent), puis implémenter domain/recipe.py jusqu'au vert.</files_note>
  <read_first>docs/project/04_Modeles.md §4.6 (table champ par champ + contrainte KebabToken), §4.1 #3/#4 ; lottie_forge/domain/_shared.py ; docs/project/01_Vision.md ADR-01/ADR-03 (aucun canal motion ad-hoc) ; 01-CONTEXT.md D-01/D-02</read_first>
  <action>
Créer lottie_forge/domain/recipe.py avec STRICT_CONFIG (importé de _shared.py, jamais redéfini) : MotionRecipe{recipe_id: RecipeId importé de domain/vocabulary.py — jamais redéclaré (§4.6), family: KebabToken, duration_ms: int 100..10000 strict, easing: KebabToken (référencera easing_curves de la StyleSpec en Phase 2 — pas de cross-check maintenant), params: MotionParams, theme_anchors: list[KebabToken] max 16 avec défaut []} ; MotionParams{amplitude: float 0..1, direction: Literal["up","down","left","right","none"], loops: int 1..10} même config strict. NE PAS imposer theme_anchors >= 1 (extension catalogue Phase 2, §4.14). Écrire tests/domain/test_recipe.py selon le bloc behavior (paramétré, ids de test stables, assertion de loc via errors() normalisés). Vérifier rouge puis vert (tdd). Aucun float integral dans les fixtures de test (§4.1 #6).
  </action>
  <verify>
    <automated>python -m pytest tests/domain/test_recipe.py -q && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/domain/test_recipe.py -q exit 0 après cycle rouge→vert documenté dans les commits (test(01-02) puis feat(01-02), D-09)
    - pytest -q -k disco_spin exit 0 (rejet hors catalogue présent)
    - pytest -q -k wr06 exit 0 (la moitié Py de l'asymétrie est testée : float integral rejeté en strict)
    - pytest -q -k accent exit 0 (CR-01 via theme_anchors)
  </acceptance_criteria>
  <done>MotionRecipe rejette tout id hors catalogue et toute valeur hors bornes en strict, avec loc précises ; défauts et optionnalité conformes §4.6.</done>
</task>

<task type="auto">
  <name>Task 3: Miroir zod MotionRecipe + bridge + asymétrie pinnée WR-06 + cas de rejet partagés</name>
  <files>src/rpc/contracts/recipe.schema.ts, src/rpc/contracts/recipe.spec.ts, tests/bridge/fixtures.py, tests/bridge/test_recipe_bridge.py, fixtures/rejection-cases/recipe.json</files>
  <read_first>docs/project/04_Modeles.md §4.9 (miroirs + asymétrie pinnée WR-06), §4.2 (parité de rejet + clés), §4.3 (protocole bridge) ; lottie_forge/domain/recipe.py ; src/rpc/contracts/style-spec.spec.ts (patron à répliquer) ; tests/bridge/rejection_loader.py + rejection-cases.ts (loaders de 01-01) ; 01-CONTEXT.md D-02 #2, D-06/D-08</read_first>
  <action>
Créer src/rpc/contracts/recipe.schema.ts : miroir z.strictObject exact de MotionRecipe (nested motionParamsSchema strict, recipeIdSchema importé de vocabulary.schema.ts, regexes identiques, défaut [] sur theme_anchors, amplitude z.number().min(0).max(1), direction z.enum, loops z.number().int().min(1).max(10), duration_ms z.number().int().min(100).max(10000)) + export type MotionRecipe = z.infer. Ajouter make_recipe() dans tests/bridge/fixtures.py (instance valide, floats fractionnaires, recipe_id dans le catalogue). Créer tests/bridge/test_recipe_bridge.py : test_export_recipe (écrit fixtures/bridge/recipe.from-python.json + recipe.schema-keys.json, patron de 01-01) ; test_reimport_recipe (skipif garde d'ordre, égalité canonique). Test bridge WR-06 (D-02 #2) en deux moitiés appariées : côté TS (recipe.spec.ts) — safeParse d'un payload duration_ms float integral réussit, documenté en commentaire comme asymétrie pinnée ; côté Python (test_recipe_bridge.py) — le même payload re-émis est rejeté par model_validate strict, assertion que Python est l'autorité la plus stricte au re-import (§4.9). CE test n'utilise PAS le harnais de rejet partagé (voir interdit must_haves).

Créer fixtures/rejection-cases/recipe.json : cas {case_id, ref, model, payload, expect_paths?} couvrant recipe-id-hors-catalogue (ref DM-02, payload recipe_id disco-spin, expect_paths ["recipe_id"]), duration-ms-below-min / above-max (expect_paths ["duration_ms"]), duration-ms-string-strict (ref §4.1 #3, "1200", expect_paths ["duration_ms"]), amplitude-out-of-range / loops-out-of-range / direction-inconnu (expect_paths dans params), theme-anchor-accent-newline (ref CR-01, expect_paths ["theme_anchors", 0]), family-trop-long-65 (ref WR-04), extra-key-top / extra-key-nested (expect_paths sur la clé). Ajouter le test paramétré consommant le loader dans test_recipe_bridge.py (pytest) et le test.each dans recipe.spec.ts — plus la parité de clés schema-keys comme en 01-01. Re-émis recipe.from-ts.json consommé par test_reimport_recipe.
  </action>
  <verify>
    <automated>python -m pytest tests/ -q -k export && npx vitest run recipe && python -m pytest tests/ -q && npx tsc --noEmit && ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - Chaîne bridge recipe complète verte (export → vitest → re-import) exit 0
    - pytest -q -k wr06 exit 0 (test bridge asymétrie présent) ET le cas float-integral n'apparaît dans aucun fixtures/rejection-cases/*.json (grep du JSON ne retourne aucune occurrence de durée float intégrale)
    - npx vitest run recipe exit 0 incluant test.each sur les cas partagés (case_ids identiques côté pytest)
    - fixtures/rejection-cases/recipe.json commité ; recipe.schema.ts n'a aucune déclaration locale de liste d'ids (importe RECIPE_IDS)
    - python -m pytest tests/ -q exit 0 (suite globale toujours verte)
  </acceptance_criteria>
  <done>MotionRecipe validé/rejeté de façon identique des deux côtés via le harnais partagé ; WR-06 pinné et documenté par test bridge ; vocabulaire clos à import unique.</done>
</task>

</tasks>

<artifacts-this-phase-produces>
Inventaire complet de la Phase 1 : voir 01-01-PLAN.md (section dédiée). Symboles créés par CE plan :
- `RECIPE_IDS` (tuple 10 ids ordre canonique), `RecipeId` (Literal star-unpack), `assert_recipe_count()` — `lottie_forge/domain/vocabulary.py`
- `RECIPE_IDS` const + `recipeIdSchema` (z.enum) + `type RecipeId` — `src/rpc/contracts/vocabulary.schema.ts`
- `MotionRecipe`, `MotionParams` — `lottie_forge/domain/recipe.py`
- `MotionRecipeSchema` + `type MotionRecipe` — `src/rpc/contracts/recipe.schema.ts`
- `make_recipe()` — ajout à `tests/bridge/fixtures.py`
- `test_export_vocabulary` → `fixtures/bridge/vocabulary.json` (généré, gitigné)
- `fixtures/rejection-cases/recipe.json` (commité, format D-08)
- Fichiers : tests/domain/test_vocabulary.py, tests/domain/test_recipe.py, tests/bridge/test_vocabulary_bridge.py, tests/bridge/test_recipe_bridge.py, src/rpc/contracts/vocabulary.spec.ts, src/rpc/contracts/recipe.spec.ts
</artifacts-this-phase-produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payload JSON → validateurs MotionRecipe | Ids de recette émis par des agents (Phase 6) traverseront ici : le vocabulaire clos est la gate anti-invention |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | recipe_id (invention hors catalogue par un futur agent) | high | mitigate | RecipeId Literal/z.enum sur liste close à source unique + test structurel same-commit + rejet disco-spin testé des deux côtés (DM-02) |
| T-01-04 | Elevation of Privilege (drift de contrat) | dérive silencieuse du schéma entre Py et TS | medium | mitigate | parité de rejet (cas partagés) + parité de clés schema-keys + round-trip ordonné (§4.2) |
</threat_model>

<verification>
- python -m pytest tests/ -q exit 0 · npx vitest run exit 0 · npx tsc --noEmit exit 0 · ruff check . exit 0
- Chaîne bridge recipe verte depuis état frais (export d'abord)
- fixtures/rejection-cases/recipe.json commité, consommé par les deux loaders
</verification>

<success_criteria>
- Critère ROADMAP n°2 : disco-spin rejeté des deux côtés ; invariant 8 ≤ ids ≤ 12 asserté de part et d'autre
- WR-06 documenté : asymétrie pinnée, Python autorité au re-import
- Le patron bridge de 01-01 répliqué sans déviation (même règle same-commit pour vocabulary)
</success_criteria>

<output>
Create `.planning/phases/01-contrats-de-donn-es-fronti-re-pydantic-zod-reconstruction/01-02-SUMMARY.md` when done
</output>
