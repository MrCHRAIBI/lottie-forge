---
phase: 01-contrats-de-donn-es-fronti-re-pydantic-zod-reconstruction
plan: 05
type: execute
wave: 5
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - .github/workflows/verify.yml
  - scripts/assert-zero-skips.mjs
  - README.md
autonomous: true
requirements: [DM-05]

estimate:
  tokens: 27000
  raw_tokens: 18000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Le job CI verify (ubuntu-latest) exécute la séquence ordonnée de §3.6 : checkout → setup-python 3.12 → setup-node 20 → pip install -e \".[dev]\" → npm ci → ruff check . → npx @biomejs/biome check . → python -m pytest tests/ -q -k export → npx vitest run → python -m pytest tests/ -q, plus npx tsc --noEmit et l'assertion zéro-skip (§3.6 + §4.2)"
    - "Zéro test skippé est ASSERTÉ en CI : junitxml pytest + vitest parsés par scripts/assert-zero-skips.mjs, skipped cumulé == 0 sinon échec dur (§4.2 — critère ROADMAP n°1)"
    - "Le README quickstart = séquence CI byte-for-byte (§3.6) : les mêmes commandes dans le même ordre, exécutables locales Windows après venv + pip install -e \".[dev]\" (D-04/D-05)"
    - "La chaîne bridge complète passe verte depuis un fresh checkout simulé (clone temporaire → séquence 12 étapes → exit 0, skipped == 0) — preuve locale du critère ROADMAP n°1 avant le premier push CI"
    - "Résolution probe DM-05 (mécanisme CI) : la parité (vocabulaire, clés, rejet, round-trip) est enforce par la CI, jamais par convention (§4.2, doctrine « the gate is the gate » §1.8)"
  artifacts:
    - ".github/workflows/verify.yml : job verify, triggers push/pull_request, permissions contents: read, actions versions pinnées, les 12 étapes ordonnées"
    - "scripts/assert-zero-skips.mjs : parse les junitxml (pytest + vitest), somme des attributs skipped, exit 1 si > 0"
    - "README.md : quickstart = séquence CI byte-for-byte + protocole bridge (§4.3) + note ruff known-first-party (déterminisme fresh-checkout) + note pas-de-pre-commit (D-05)"
  key_links:
    - "verify.yml étape export → fixtures/bridge/*.from-python.json → étape vitest → *.from-ts.json → étape pytest final (ordre bloquant, même job)"
    - "addopts pytest + reporters vitest (configurés en 01-01) → junitxml → scripts/assert-zero-skips.mjs → gate zéro-skip"
    - "README quickstart ↔ verify.yml : même séquence commandes, vérifiée par le test fresh-clone"

prohibitions:
  - "Aucune seconde liste d'ids de recettes hardcodée en dehors de lottie_forge/domain/vocabulary.py et src/rpc/contracts/vocabulary.schema.ts (règle same-commit §4.2)"
  - "Aucune coercion numérique acceptée quelque part dans la chaîne CI (§4.1 #3)"
  - "Aucune assertion de parité d'erreur sur le texte des messages — appartenance de chemin uniquement (D-08)"
  - "Aucun test skippé en CI : junitxml + skipped == 0 asserté par un échec dur du job (§4.2) — un skip présent fait échouer verify"
  - "Aucune licence de forme abonnement constructible (gate structurelle livrée en 01-04, enforce par la même CI)"
  - "Aucun fixture bridge committé : fixtures/bridge/ gitigné, régénéré à chaque run CI (§4.3)"
  - "Aucune validation calendrier de la date de pack_id (IN-07)"
  - "Aucun validateur Python fait main à la place de pydantic-core (CR-01)"
  - "Aucune valeur par défaut sur style_version (§4.5)"
  - "Aucune clé extra acceptée des deux côtés (§4.1 #3)"
  - "Aucun hook pre-commit local (D-05) : la CI est le seul enforceur + le README documente la séquence manuelle équivalente"
  - "Aucun dict[str, Any] traversant la frontière (§2.5)"
---

<objective>
Enforcer le tout par CI : le job `verify` GitHub Actions avec la séquence ordonnée de 10 étapes de §3.6 + `tsc --noEmit` + l'assertion zéro-skip sur junitxml (§4.2), et le README quickstart = séquence CI byte-for-byte. Preuve locale finale : la chaîne complète verte depuis un fresh checkout simulé.

Purpose: Critère ROADMAP n°1 — la chaîne bridge ordonnée doit passer verte en CI depuis un fresh checkout avec zéro test skippé ; « the gate is the gate » (§1.8) : sans job CI, la parité ne serait qu'une convention.
Output: verify.yml + assert-zero-skips.mjs + README.md + preuve fresh-clone.
</objective>

<execution_context>
@D:/projets/illustrations/systeme/.opencode/gsd-core/workflows/execute-plan.md
@D:/projets/illustrations/systeme/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@docs/project/03_Stack.md
@docs/project/04_Modeles.md
@.planning/phases/01-contrats-de-donn-es-fronti-re-pydantic-zod-reconstruction/01-01-SUMMARY.md
@.planning/phases/01-contrats-de-donn-es-fronti-re-pydantic-zod-reconstruction/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Job CI verify (10 étapes ordonnées + tsc + zéro-skip)</name>
  <files>.github/workflows/verify.yml, scripts/assert-zero-skips.mjs</files>
  <read_first>docs/project/03_Stack.md §3.6 (séquence CI exacte), §3.1–§3.2 (versions setup) ; docs/project/04_Modeles.md §4.2 (gates CI : tsc + zéro-skip junitxml), §4.3 (ordre bridge) ; pyproject.toml + vitest.config.ts (junitxml configurés en 01-01)</read_first>
  <action>
Créer scripts/assert-zero-skips.mjs : script node sans dépendance qui prend en arguments les chemins des junitxml (fixtures/bridge/pytest-junit.xml, fixtures/bridge/vitest-junit.xml), somme tous les attributs skipped des éléments testsuite/testsuites, affiche le total et exit 1 si > 0 (sinon exit 0). Créer .github/workflows/verify.yml : name verify ; on push (branches main) + pull_request ; permissions contents: read ; concurrency group par ref ; jobs.verify runs-on ubuntu-latest, étapes dans l'ORDRE EXACT de §3.6 : (1) actions/checkout version pinnée ; (2) actions/setup-python version pinnée, python-version "3.12" ; (3) actions/setup-node version pinnée, node-version 20 ; (4) pip install -e ".[dev]" — chaîne QUOTÉE dans le YAML (§3.6) ; (5) npm ci (package-lock committé en 01-01, échoue sur drift) ; (6) ruff check . ; (7) npx @biomejs/biome check . ; (8) python -m pytest tests/ -q -k export ; (9) npx vitest run ; (10) python -m pytest tests/ -q ; puis (11) npx tsc --noEmit ; (12) node scripts/assert-zero-skips.mjs fixtures/bridge/pytest-junit.xml fixtures/bridge/vitest-junit.xml. Les junitxml sont produits par la configuration de 01-01 (addopts pytest + reporters vitest) — aucune commande CI n'est déviée de §3.6. Aucun secret requis. Pas de step de skip ni de continue-on-error.
  </action>
  <verify>
    <automated>node scripts/assert-zero-skips.mjs fixtures/bridge/pytest-junit.xml fixtures/bridge/vitest-junit.xml</automated>
  </verify>
  <acceptance_criteria>
    - Après une chaîne bridge locale complète, node scripts/assert-zero-skips.mjs ... exit 0 avec total affiché 0
    - Le script exit 1 si un junitxml contient un skip (testé temporairement avec un fichier junitxml fabriqué contenant skipped="1", supprimé ensuite)
    - verify.yml contient les 12 étapes dans l'ordre §3.6 (steps numérotés/nommés lisibles) ; la chaîne pip install -e ".[dev]" est quotée dans le YAML
    - Aucun continue-on-error ni if: always() sur les étapes 4..12
    - YAML bien formé (parse du fichier sans erreur)
  </acceptance_criteria>
  <done>Le job verify encode la séquence §3.6 + tsc + gate zéro-skip ; le script d'assertion est prouvé dans les deux sens (0 → vert, skip → rouge).</done>
</task>

<task type="auto">
  <name>Task 2: README quickstart = séquence CI byte-for-byte (D-04/D-05)</name>
  <files>README.md</files>
  <read_first>docs/project/03_Stack.md §3.6 (README = séquence CI byte-for-byte) ; docs/project/04_Modeles.md §4.3 (protocole bridge + déterminisme fresh-checkout ruff) ; .github/workflows/verify.yml (Task 1) ; 01-CONTEXT.md D-04 (pip+venv), D-05 (pas de pre-commit)</read_first>
  <action>
Créer README.md (langue du projet : français, termes techniques en anglais) : titre lottie-forge + une phrase de vision (usine de packs, cf. PROJECT.md) ; section Quickstart avec la séquence EXACTE du job verify dans le même ordre, précédée de la création du venv Windows/local (python -m venv venv ; activation ; pip install -e ".[dev]" ; npm ci) — chaque commande du job §3.6 apparaît verbatim dans le README (ruff check . ; npx @biomejs/biome check . ; python -m pytest tests/ -q -k export ; npx vitest run ; python -m pytest tests/ -q ; npx tsc --noEmit ; node scripts/assert-zero-skips.mjs ...) ; section Protocole bridge expliquant l'ordre export → validate/re-emit → strict re-import, l'ordre-garde skipif et le gitignore de fixtures/bridge/ (§4.3) ; section Structure du monorepo (src/ = TS déterministe, lottie_forge/ = Python, src/rpc/contracts/ = miroirs zod, fixtures/rejection-cases/ = cas de rejet partagés) ; note d'outillage : pas de hooks pre-commit (D-05), la CI est l'unique enforceur, le lint est identique local/CI grâce au ruff.toml known-first-party (§4.3) ; note stack : pins §3.1/§3.2 (pydantic 2.13.4 exact, zod ^4, etc.).
  </action>
  <verify>
    <automated>python -m pytest tests/ -q -k export; if ($?) { npx vitest run }; if ($?) { python -m pytest tests/ -q }</automated>
  </verify>
  <acceptance_criteria>
    - Chaque commande du job verify (étapes 4..12) apparaît verbatim dans README.md (grep de chaque chaîne de commande présent)
    - L'ordre des commandes dans le README == l'ordre du job (séquence §3.6 respectée dans le fichier)
    - README mentionne l'absence de hooks pre-commit et la séquence manuelle équivalente (D-05)
    - La chaîne quickstart du README exécutée dans l'ordre est verte (exit 0 sur les trois étapes bridge ci-dessus)
  </acceptance_criteria>
  <done>Un nouveau contributeur peut exécuter la vérification complète locale en suivant le README au pied de la lettre — identique à la CI (§3.6).</done>
</task>

<task type="auto">
  <name>Task 3: Preuve fresh-checkout — chaîne complète verte depuis un clone temporaire</name>
  <files></files>
  <read_first>.github/workflows/verify.yml (séquence exacte) ; README.md (séquence quickstart) ; ROADMAP.md critère de succès n°1</read_first>
  <action>
Simuler un fresh checkout : committer tout le travail de la phase (D-09 — l'arbre de travail doit être propre), puis git clone du dépôt local dans un répertoire temporaire (C:\Users\CHRAIBI\AppData\Local\Temp\opencode\lf-fresh-verify — répertoire pré-approuvé). Dans le clone : créer le venv, pip install -e ".[dev]", npm ci, puis exécuter les 12 étapes de verify.yml dans l'ordre. Assert : chaque commande exit 0 ; les junitxml produits montrent skipped cumulé == 0 via scripts/assert-zero-skips.mjs ; fixtures/bridge/ n'existe PAS dans le clone avant l'étape export (gitignore effectif) et est généré au test. Nettoyer le clone temporaire après la preuve. Toute divergence (dépendance manquante, ordre cassé, skip silencieux) est un bug de phase à corriger avant de conclure.
  </action>
  <verify>
    <automated>git status --porcelain (doit être vide avant le clone) ; la séquence 12 étapes du clone retourne exit 0 à chaque commande et assert-zero-skips exit 0</automated>
  </verify>
  <acceptance_criteria>
    - git status --porcelain vide (tout committé, D-09) avant le clone
    - Dans le clone temporaire : pip install -e ".[dev]" exit 0, npm ci exit 0 (pas de npm install), les 7 commandes de vérification exit 0
    - node scripts/assert-zero-skips.mjs exit 0 dans le clone (skipped == 0 sur les deux junitxml)
    - fixtures/bridge/ absent du clone frais avant l'étape export (preuve du gitignore, §4.3)
    - Répertoire temporaire supprimé après la preuve
  </acceptance_criteria>
  <done>Critère ROADMAP n°1 prouvé localement : la chaîne bridge ordonnée passe verte depuis un fresh checkout avec zéro test skippé — le premier push CI ne pourra que confirmer.</done>
</task>

</tasks>

<artifacts-this-phase-produces>
Inventaire complet de la Phase 1 : voir 01-01-PLAN.md (section dédiée). Symboles créés par CE plan :
- `.github/workflows/verify.yml` — job `verify` : 10 étapes ordonnées §3.6 + `npx tsc --noEmit` + gate zéro-skip
- `scripts/assert-zero-skips.mjs` — parseur junitxml sans dépendance, exit 1 si skipped > 0
- `README.md` — quickstart = séquence CI byte-for-byte + protocole bridge + structure monorepo + notes D-04/D-05
</artifacts-this-phase-produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub runner → code + dépendances | Le runner exécute pip/npm : surface supply chain CI |
| workflow → dépôt | Permissions minimales du token GITHUB_TOKEN |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Elevation of Privilege | token GITHUB_TOKEN du workflow | medium | mitigate | permissions contents: read explicites, aucun secret, aucun write |
| T-01-11 | Tampering | ordre du bridge contourné en CI (étapes réordonnées/ignorées) | high | mitigate | job unique avec étapes séquentielles, aucun continue-on-error, gate zéro-skip en dernière étape — une chaîne à moitié silencieuse ne peut pas passer verte (§4.2) |
| T-01-12 | Denial of Service | dépendance npm/PyPI compromise dans le lockfile | low | accept | packages top-tier pins §3.1/§3.2 approuvés au checkpoint 01-01 ; package-lock committé (npm ci échoue sur drift) ; re-vérification possible via /gsd-secure-phase |
</threat_model>

<verification>
- node scripts/assert-zero-skips.mjs exit 0 après chaîne complète locale
- Preuve fresh-clone : 12 étapes vertes + skipped == 0 dans le clone
- README = séquence CI byte-for-byte (chaque commande du job présente verbatim)
- git status propre : phase entièrement committée (D-09)
</verification>

<success_criteria>
- Critère ROADMAP n°1 prouvé : bridge ordonné vert depuis fresh checkout, zéro skippé asserté
- La parité DM-05 est enforce par CI (vocabulaire, clés, rejet, round-trip + gates statiques tsc/ruff/biome)
- Le job verify est le garde-fou permanent pour les Phases 2+ (patron réutilisable)
</success_criteria>

<output>
Create `.planning/phases/01-contrats-de-donn-es-fronti-re-pydantic-zod-reconstruction/01-05-SUMMARY.md` when done
</output>
