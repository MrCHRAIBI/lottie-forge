---
phase: 01-contrats-de-donn-es-fronti-re-pydantic-zod-reconstruction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - package.json
  - package-lock.json
  - tsconfig.json
  - biome.json
  - ruff.toml
  - vitest.config.ts
  - .gitignore
  - lottie_forge/__init__.py
  - lottie_forge/domain/__init__.py
  - lottie_forge/domain/_shared.py
  - lottie_forge/domain/style.py
  - tests/conftest.py
  - tests/bridge/fixtures.py
  - tests/bridge/test_style_spec_bridge.py
  - tests/bridge/rejection_loader.py
  - tests/domain/test_style_spec.py
  - src/rpc/contracts/style-spec.schema.ts
  - src/rpc/contracts/style-spec.spec.ts
  - src/rpc/contracts/rejection-cases.ts
  - fixtures/rejection-cases/style-spec.json
autonomous: false
requirements: [DM-01, DM-05]

estimate:
  tokens: 90000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "La chaîne bridge ordonnée (pytest -k export → npx vitest run → pytest -q) passe verte pour StyleSpec, avec zéro test skippé (§4.3)"
    - "Tout payload StyleSpec invalide (cross-fields, bornes, regex, clé extra, coercion str) est rejeté par Pydantic strict ET zod safeParse en suites miroirs paramétrées (critère de succès ROADMAP n°3, partie StyleSpec)"
    - "Le cas piégeux accent\n (CR-01, D-02) est rejeté par KebabToken des deux côtés — validation possédée par pydantic-core via StringConstraints, pas un validateur fait main (§4.6)"
    - "style_version est requis sans défaut : une StyleSpec non pinnée ne peut pas être construite (§4.5)"
    - "Parité de clés de schéma : Object.keys(Schema.shape) == clés properties de model_json_schema(), assertée via fixtures/bridge/style-spec.schema-keys.json (§4.2)"
    - "Chaque borne numérique de StyleSpec est testée à la borne (accepté) et un pas au-delà (rejeté) des deux côtés — résolution du probe de frontière DM-05 via §4.4/§4.5"
    - "Les floats des fixtures sont fractionnaires uniquement (2.5, 0.25 — jamais un float integral tel que 2.0) pour parité de formatage Py/JS (§4.1 #6 — résolution du probe de précision DM-05)"
    - "L'encodage est structuralement clos : tous les patterns sont ASCII ancrés (longueur = code points, divergence Py/zod impossible) — résolution du probe d'encodage via §4.5/§4.6"
  artifacts:
    - "pyproject.toml (pydantic==2.13.4 pin exact, requires-python >=3.12,<3.14, extras dev, pytest addopts junitxml)"
    - "package.json + package-lock.json committé (zod ^4, typescript ~5.9, vitest ^4, @biomejs/biome ^2, @types/node ^20, engines.node >=20)"
    - "tsconfig.json (strict, verbatimModuleSyntax: true) · biome.json (scope src/**) · ruff.toml (known-first-party lottie_forge, fixtures) · vitest.config.ts (reporters default+junit)"
    - "lottie_forge/domain/_shared.py : STRICT_CONFIG, TOKEN_NAME_PATTERN, KebabToken"
    - "lottie_forge/domain/style.py : StyleSpec, Size, StrokeWidths, CornerRadii, PaletteToken, EasingCurve"
    - "src/rpc/contracts/style-spec.schema.ts : StyleSpecSchema + type export z.infer"
    - "tests/bridge/fixtures.py : make_style_spec() (single source of fixture truth, §4.9)"
    - "tests/bridge/test_style_spec_bridge.py : test_export_style_spec + test_reimport_style_spec (skipif garde d'ordre)"
    - "tests/bridge/rejection_loader.py + src/rpc/contracts/rejection-cases.ts : loaders partagés du harnais de rejet (D-06/D-08)"
    - "fixtures/rejection-cases/style-spec.json : cas de rejet commités au format {case_id, ref, model, payload, expect_paths?} (D-07/D-08)"
    - "tests/domain/test_style_spec.py : suite domain StyleSpec (cross-fields, bornes, boundary, strict, extra=forbid)"
  key_links:
    - "make_style_spec() → pytest -k export → fixtures/bridge/style-spec.from-python.json"
    - "fixtures/bridge/style-spec.from-python.json → style-spec.spec.ts (zod strictObject parse + deep-equal) → fixtures/bridge/style-spec.from-ts.json"
    - "fixtures/bridge/style-spec.from-ts.json → test_reimport_style_spec (model_validate strict + égalité canonique model_dump_json)"
    - "fixtures/bridge/style-spec.schema-keys.json → parité de clés zod.shape vs model_json_schema().properties"
    - "fixtures/rejection-cases/style-spec.json → pytest parametrize ET vitest test.each (une seule source, zéro drift, D-06)"

prohibitions:
  - "Aucune seconde liste d'ids de recettes hardcodée en dehors de lottie_forge/domain/vocabulary.py et src/rpc/contracts/vocabulary.schema.ts (règle same-commit §4.2)"
  - "Aucune coercion numérique : la chaîne « 2.5 » est rejetée pour un float et « 1200 » pour un int, des deux côtés (§4.1 #3)"
  - "Aucune assertion de parité d'erreur sur le texte des messages — appartenance de chemin uniquement (D-08)"
  - "Aucun test skippé en CI : junitxml + skipped == 0 asserté (§4.2) ; côté TS, artifact bridge manquant = échec bruyant, pas un skip"
  - "Aucune licence de forme abonnement constructible (structurelle, gate Phase 1 — réalisation complète en 01-04)"
  - "Aucun fixture bridge committé : fixtures/bridge/ gitigné, généré au test (§4.3)"
  - "Aucune validation calendrier de la date de pack_id — forme nominale seulement (IN-07, réalisée en 01-04)"
  - "Aucun validateur Python fait main à la place de pydantic-core pour KebabToken (fix CR-01, §4.6)"
  - "Aucune valeur par défaut sur style_version (§4.5)"
  - "Aucune clé extra acceptée : extra=forbid Py / z.strictObject TS sur chaque modèle et modèle imbriqué (§4.1 #3)"
  - "Aucun hook pre-commit local (D-05) : ruff/biome/tsc enforce en CI uniquement + séquence README"
  - "Aucun dict[str, Any] traversant la frontière — modèles clos uniquement (§2.5)"
---

<objective>
Bootstrap le monorepo deux couches (§2.7) et livrer le tracer de bout en bout du harnais bridge : UN contrat (`StyleSpec`) construit des deux côtés (Pydantic strict + miroir zod strictObject), exporté via builder Python, validé/re-émis par vitest, ré-importé sous Pydantic strict — la chaîne ordonnée verte, plus le harnais de rejet partagé (D-06/D-07/D-08) et la suite domain StyleSpec complète.

D-03: aucun plan ne cible le comptage de tests de référence (94/50 indicatif) — la seule mesure est les critères de succès ROADMAP (bridge vert fresh-checkout, zéro test skippé, parité de rejets).

Purpose: Le schéma est le contrat (§2.8) — ce plan prouve l'architecture bridge complète sur un contrat réel avant expansion ; tous les plans suivants répliquent le patron prouvé ici.
Output: Scaffolding racine complet, `StyleSpec` opérationnel des deux côtés, harnais de rejet miroir en place, CR-01 verrouillé.
</objective>

<execution_context>
@D:/projets/illustrations/systeme/.opencode/gsd-core/workflows/execute-plan.md
@D:/projets/illustrations/systeme/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/project/04_Modeles.md
@docs/project/03_Stack.md
@docs/project/02_Architecture.md
</context>

<!-- Résolution des probes (fallback spec-less, $COVERAGE : 7/7 unresolved à l'entrée) :
  - DM-01 unclassified → résolu par §4.5 (table champ par champ = critères d'acceptation, Task 3)
  - DM-05 boundary → résolu : tests à la borne + un pas au-delà pour chaque champ borné (Task 3)
  - DM-05 precision → résolu : floats fractionnaires uniquement + strict (Task 3, fixtures)
  - DM-03 empty/encoding → résolus en 01-02/01-03 ; DM-02/DM-04 unclassified → résolus en 01-02/01-04
  Aucun probe laissé ouvert — zéro flagged assumption. -->

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: légitimité des packages (fallback [ASSUMED] — pas de table Package Legitimacy Audit)</name>
  <files></files>
  <read_first>docs/project/03_Stack.md §3.1–§3.2 (pins verrouillés par l'utilisateur)</read_first>
  <action>Presenter à l'utilisateur la liste des packages que ce plan va installer, avec leurs pins du cahier des charges et leurs pages registre : PyPI — pydantic 2.13.4 (pin exact, pypi.org/project/pydantic), pytest >= 8 (pypi.org/project/pytest), ruff latest (pypi.org/project/ruff) ; npm — zod ^4 (npmjs.com/package/zod), typescript ~5.9 (npmjs.com/package/typescript), vitest ^4 (npmjs.com/package/vitest), @biomejs/biome ^2 (npmjs.com/package/@biomejs/biome), @types/node ^20 (npmjs.com/package/@types/node). Politique fallback GSD : sans table RESEARCH.md Package Legitimacy Audit, tous les packages sont statut [ASSUMED] et requièrent une approbation humaine bloquante avant install (T-01-SC). Demander : confirmer chaque package, ou veto avec alternative. Ne pas installer avant réponse. Noter que ces pins sont déjà des décisions utilisateur verrouillées du cahier des charges (§3.1/§3.2) — le checkpoint est la gate d'approvisionnement, pas une re-décision.</action>
  <verify>
    <human-check>L'utilisateur a confirmé les 8 packages (ou fourni des alternatives validées)</human-check>
  </verify>
  <what-built>Rien encore — ce checkpoint précède toute installation. Il présente la liste des dépendances avec pins et liens registre.</what-built>
  <how-to-verify>Vérifier chaque package sur sa page registre (pypi.org / npmjs.com) : nom, version/pin, mainteneur, téléchargements. Confirmer que chaque pin correspond au cahier des charges §3.1/§3.2.</how-to-verify>
  <resume-signal>Répondre « packages confirmés » pour continuer, ou indiquer le(s) package(s) à remplacer avec l'alternative souhaitée.</resume-signal>
  <done>Approbation explicite des 8 packages [ASSUMED] enregistrée dans la conversation avant toute exécution de Task 2</done>
</task>

<task type="tracer">
  <name>Tracer: StyleSpec end-to-end — scaffold monorepo + chaîne bridge ordonnée verte</name>
  <files>pyproject.toml, package.json, package-lock.json, tsconfig.json, biome.json, ruff.toml, vitest.config.ts, .gitignore, lottie_forge/__init__.py, lottie_forge/domain/__init__.py, lottie_forge/domain/_shared.py, lottie_forge/domain/style.py, tests/conftest.py, tests/bridge/fixtures.py, tests/bridge/test_style_spec_bridge.py, src/rpc/contracts/style-spec.schema.ts, src/rpc/contracts/style-spec.spec.ts</files>
  <read_first>docs/project/04_Modeles.md §4.1 (principes), §4.2 (harnais de parité), §4.3 (protocole de bridge), §4.5 (StyleSpec champ par champ), §4.9 (miroirs zod) ; docs/project/03_Stack.md §3.1–§3.2 (pins), §3.6 (séquence CI) ; docs/project/02_Architecture.md §2.5/§2.7 ; .planning/phases/01-contrats-de-donn-es-fronti-re-pydantic-zod-reconstruction/01-CONTEXT.md (D-04, D-05, D-09, discrétion outillage)</read_first>
  <action>
Scaffold le monorepo deux couches (§2.7) à la racine du dépôt : pyproject.toml (build backend hatchling, name lottie-forge version 0.1.0 — discrétion, requires-python ">=3.12,<3.14", dependency pydantic==2.13.4 pin exact §3.1, optional-dependencies dev = pytest>=8 + ruff, [tool.pytest.ini_options] addopts="--junitxml=fixtures/bridge/pytest-junit.xml", commentaire IN-02 notant pyyaml réservé Phase 2) ; package.json (private, type module, engines.node ">=20", dependency zod ^4, devDependencies typescript ~5.9 + vitest ^4 + @biomejs/biome ^2 + @types/node ^20, scripts typecheck/test) ; committer package-lock.json (npm ci, §3.6) ; tsconfig.json (strict true, verbatimModuleSyntax true §3.2, module NodeNext, noEmit, include src) ; biome.json scope files.includes sur src/** (§3.2) ; ruff.toml (target py312, select E/F/I/UP, [lint.isort] known-first-party = ["lottie_forge", "fixtures"] — déterminisme fresh-checkout §4.3, D-04) ; vitest.config.ts (include src/**/*.spec.ts, environment node, reporters default + junit outputFile fixtures/bridge/vitest-junit.xml) ; .gitignore couvrant venv/, __pycache__/, node_modules/, dist/, fixtures/bridge/ (discrétion) ; tests/conftest.py créant fixtures/bridge/ au démarrage de session (l'addopts junitxml écrit dans ce répertoire).

Créer lottie_forge/domain/_shared.py avec STRICT_CONFIG = ConfigDict(extra="forbid", strict=True), TOKEN_NAME_PATTERN = "^[a-z][a-z0-9-]*$", et KebabToken = Annotated[str, StringConstraints(pattern=r"^[a-z][a-z0-9-]*$", max_length=64)] — validation possédée par pydantic-core, PAS un validateur fait main (fix CR-01, §4.6, D-02 #1).

Créer lottie_forge/domain/style.py : les 6 modèles de §4.5 — StyleConfig commun STRICT_CONFIG partout, nested compris ; style_version: str regex ^\d+\.\d+\.\d+$ points échappés, requis sans défaut ; viewBox: Size{width,height: int 16..2048} ; stroke_widths: StrokeWidths{thin,default,bold: float 0.25..16} avec model_validator mode="after" imposant strictement thin < default < bold ; corner_radii: CornerRadii{small,medium,large: float 0..48} avec model_validator small <= medium <= large (largeur inégale : strokes strict, radii inclusif) ; palette: list[PaletteToken] 2..16, PaletteToken{name: KebabToken, hex: str regex ^#[0-9a-fA-F]{6}$}, noms kebab uniques via model_validator avec loc précise ("palette", idx, "name") via ValidationError.from_exception_data + InitErrorDetails (pydantic_core) ; easing_curves: list[EasingCurve] 2..8, EasingCurve{name: KebabToken, control_points: exactement 4 floats 0..1}.

Créer src/rpc/contracts/style-spec.schema.ts : miroir z.strictObject exact (nested compris : sizeSchema, strokeWidthsSchema avec .check/.superRefine cross-field, cornerRadiiSchema idem, paletteTokenSchema, easingCurveSchema), regexes identiques échappées, exports type StyleSpec = z.infer — structure §4.9.

Créer tests/bridge/fixtures.py : make_style_spec() retournant une instance valide de référence, floats fractionnaires uniquement (0.25/2.5/15.5 — jamais de float integral), contenu couvrant toutes les bornes intérieures. Créer tests/bridge/test_style_spec_bridge.py : test_export_style_spec (écrit fixtures/bridge/style-spec.from-python.json via model_dump_json + fixtures/bridge/style-spec.schema-keys.json = liste triée des clés properties de model_json_schema()) ; test_reimport_style_spec avec pytest.mark.skipif si fixtures/bridge/style-spec.from-ts.json absent (garde d'ordre §4.3) — model_validate strict du JSON TS, égalité avec l'instance canonique de make_style_spec(), et model_dump_json() identique. Créer src/rpc/contracts/style-spec.spec.ts : lit fixtures/bridge/style-spec.from-python.json (échec bruyant si absent — jamais de skip côté TS), zod parse, deep-equal avec l'objet brut, assert parité de clés vs schema-keys.json, re-écrit fixtures/bridge/style-spec.from-ts.json via JSON.stringify(parsed).

Exécuter D-04 (pip + venv nus) : python -m venv + pip install -e ".[dev]" + npm install. Pas de hooks pre-commit (D-05). Commits atomiques directs sur main (D-09).
  </action>
  <verify>
    <automated>python -m pytest tests/ -q -k export && npx vitest run && python -m pytest tests/ -q && npx tsc --noEmit && ruff check . && npx @biomejs/biome check .</automated>
  </verify>
  <acceptance_criteria>
    - La chaîne complète ci-dessus s'exécute avec exit 0 (6 commandes)
    - fixtures/bridge/style-spec.from-python.json ET style-spec.from-ts.json existent après la chaîne
    - git check-ignore fixtures/bridge/style-spec.from-python.json exit 0 (artefact gitigné, §4.3)
    - git status propre : aucun fichier de fixtures/bridge/ staged
    - package-lock.json committé et présent
  </acceptance_criteria>
  <done>La chaîne bridge ordonnée StyleSpec est verte de bout en bout : export Python → validation/re-émission zod → re-import strict Python avec égalité canonique ; outillage racine (ruff/biome/tsc) vert ; aucune étape skippée.</done>
</task>

<task type="auto">
  <name>Task 2: Suite domain StyleSpec + harnais de rejet miroir (D-06/D-07/D-08)</name>
  <files>tests/domain/test_style_spec.py, tests/bridge/rejection_loader.py, src/rpc/contracts/rejection-cases.ts, fixtures/rejection-cases/style-spec.json, tests/bridge/test_style_spec_bridge.py, src/rpc/contracts/style-spec.spec.ts</files>
  <read_first>docs/project/04_Modeles.md §4.5 (champs/bornes/cross-fields), §4.6 (KebabToken CR-01), §4.1 #3/#4/#6 (strict, bornes anti-DoS, floats) ; 01-CONTEXT.md D-06/D-07/D-08 (format de cas verbatim) ; fichiers créés en Task 1 (style.py, style-spec.schema.ts)</read_first>
  <action>
Écrire tests/domain/test_style_spec.py : suite paramétrée couvrant (a) positif — StyleSpec de référence acceptée, bornes acceptées aux limites exactes (width/height 16 et 2048, floats 0.25/16 et 0/48, listes 2 et 16 / 2 et 8) — résolution probe frontière DM-05 ; (b) rejets Pydantic strict — cross-field stroke thin>=default / default>=bold avec loc ["stroke_widths"], cross-field radii small>medium>large avec loc ["corner_radii"], doublon de nom de palette avec loc ["palette", idx, "name"], hex invalide, style_version "1x0x0" / "1..0" / absent (résolution probe DM-01), contrôle_points 3 ou 5 éléments / hors 0..1, extra key top-level et nested, coercion "2.5" pour float rejetée, accent\n dans un nom de palette rejeté (CR-01 côté Python).

Créer le harnais de rejet partagé (D-06/D-07/D-08) : tests/bridge/rejection_loader.py — fonction load_rejection_cases(contract) lisant fixtures/rejection-cases/style-spec.json, retournant (case_id, payload, expect_paths) pour pytest.parametrize ; src/rpc/contracts/rejection-cases.ts — même loader pour vitest test.each. Format de cas EXACT (D-08) : {case_id, ref, model, payload, expect_paths?} — case_id stable humain préfixé par la référence (cr01-accent-newline, dm01-crossfield-stroke-order, dm01-strict-float-string, dm01-extra-key-nested, dm01-style-version-required, dm01-style-version-regex, dm01-palette-duplicate-name, dm01-viewbox-below-min, dm01-hex-invalid…). Règle d'assertion : rejet TOUJOURS asserté des deux côtés ; si expect_paths présent → appartenance : chemin attendu ∈ loc des errors() Pydantic (normalisés en liste de strings) ET ∈ path des issues zod — JAMAIS sur le texte des messages.

Ajouter dans test_style_spec_bridge.py un test paramétré par le loader (ids = case_id) et dans style-spec.spec.ts un test.each équivalent — une seule source JSON, zéro drift (D-06). Pour les cas cross-fields côté zod, les issues proviennent des .check/.superRefine des objets nested (chemin ["stroke_widths"] / ["corner_radii"] vu de la racine). Committer fixtures/rejection-cases/style-spec.json (D-07 — commités, au niveau fixtures/ avec les futurs répertoires style-specs/ et recipe-catalogue/ de Phase 2).
  </action>
  <verify>
    <automated>python -m pytest tests/domain/test_style_spec.py -q && python -m pytest tests/ -q -k accent && npx vitest run style-spec</automated>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/domain/test_style_spec.py -q exit 0
    - python -m pytest tests/ -q -k accent exit 0 — le cas cr01-accent-newline est présent dans les ids pytest ET dans les ids vitest test.each (même case_id, deux suites, D-06)
    - npx vitest run style-spec exit 0 avec le test.each sur les cas partagés
    - Aucune assertion de la suite ne compare le texte d'un message d'erreur — grep du fichier de test ne trouve aucune comparaison sur message, uniquement appartenance de chemins (D-08)
    - fixtures/rejection-cases/style-spec.json est commité (git status : tracked)
  </acceptance_criteria>
  <done>StyleSpec rejette tous les payloads invalides des deux côtés via un fichier de cas partagé unique ; CR-01 verrouillé parité Py/zod ; expect_paths par appartenance opérationnel.</done>
</task>

</tasks>

<artifacts-this-phase-produces>
Inventaire complet des symboles créés par la Phase 1 (répartition entre plans) :
- **01-01 (ce plan)** : `STRICT_CONFIG`, `TOKEN_NAME_PATTERN`, `KebabToken` (domain/_shared.py) ; `StyleSpec`, `Size`, `StrokeWidths`, `CornerRadii`, `PaletteToken`, `EasingCurve` (domain/style.py) ; `StyleSpecSchema` + `type StyleSpec` (style-spec.schema.ts) ; `make_style_spec()` (tests/bridge/fixtures.py) ; `load_rejection_cases()` Py + TS (rejection_loader.py, rejection-cases.ts) ; `fixtures/rejection-cases/style-spec.json` ; scaffolding racine (pyproject.toml, package.json, package-lock.json, tsconfig.json, biome.json, ruff.toml, vitest.config.ts, .gitignore, tests/conftest.py)
- **01-02** : `RECIPE_IDS`, `RecipeId`, `assert_recipe_count` (domain/vocabulary.py) ; `RECIPE_IDS` const + `type RecipeId` (vocabulary.schema.ts) ; `MotionRecipe`, `MotionParams` (domain/recipe.py) ; `MotionRecipeSchema` + `type MotionRecipe` (recipe.schema.ts) ; `make_recipe()` ; `fixtures/rejection-cases/recipe.json` ; `fixtures/bridge/vocabulary.json` (généré)
- **01-03** : `AssetSpec`, `CompositionMeta`, `ContentHashes` (domain/asset.py) ; `AssetSpecSchema` + `type AssetSpec` (asset-spec.schema.ts) ; `make_asset()` ; `fixtures/rejection-cases/asset-spec.json`
- **01-04** : `PackManifest`, `PackTotals`, `LicenseInfo` (domain/pack.py) ; `PackManifestSchema`, `LicenseInfoSchema` + types (pack-manifest.schema.ts) ; `make_pack()` ; `fixtures/rejection-cases/pack-manifest.json`
- **01-05** : `.github/workflows/verify.yml` (job verify, 10 étapes + tsc + zéro-skip) ; `scripts/assert-zero-skips.mjs` ; `README.md` (quickstart = séquence CI byte-for-byte)
</artifacts-this-phase-produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payload JSON → validateurs | Tout payload traversant la frontière est non fiable : rejets stricts (bornes anti-DoS §4.1 #4, extra=forbid, patterns ancrés) |
| registres npm/PyPI → machine dev/CI | Installation de dépendances = surface supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | DoS | champs string/liste/numériques des contrats | medium | mitigate | max_length sur toute chaîne (64), min/max sur toute liste (2..16, 2..8, 1..24, 1..50), bornes sur tout numérique (§4.1 #4) — testées en Task 2 |
| T-01-02 | Tampering | fixtures/rejection-cases/*.json partagés Py↔TS | low | mitigate | fichiers commités et relus ; les deux suites consomment la même source (D-06) — un edit biaisé casse la parité visible en CI |
| T-01-SC | Tampering | npm/pip installs (pydantic, pytest, ruff, zod, typescript, vitest, biome, @types/node) | high | mitigate | Package Legitimacy Gate : fallback [ASSUMED] + checkpoint:human-verify bloquant en Task 1 (pins §3.1/§3.2, liens registre fournis) |
</threat_model>

<verification>
- Chaîne ordonnée complète verte : python -m pytest tests/ -q -k export && npx vitest run && python -m pytest tests/ -q
- Gates statiques vertes : npx tsc --noEmit ; ruff check . ; npx @biomejs/biome check .
- Zéro artefact bridge committé ; package-lock.json committé
- Cas de rejet partagés consommés par les deux suites avec case_id identiques
</verification>

<success_criteria>
- StyleSpec construit, validé et rejeté de façon identique des deux côtés de la frontière
- Le patron bridge (builder → export → vitest validate/re-emit → strict re-import) est prouvé et réplicable tel quel pour les 3 contrats suivants
- Le harnais de rejet D-06/D-07/D-08 est en place et consommé par pytest ET vitest
</success_criteria>

<output>
Create `.planning/phases/01-contrats-de-donn-es-fronti-re-pydantic-zod-reconstruction/01-01-SUMMARY.md` when done
</output>
