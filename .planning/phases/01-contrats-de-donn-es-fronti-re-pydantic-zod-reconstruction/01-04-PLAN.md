---
phase: 01-contrats-de-donn-es-fronti-re-pydantic-zod-reconstruction
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - lottie_forge/domain/pack.py
  - tests/domain/test_pack.py
  - tests/bridge/fixtures.py
  - tests/bridge/test_pack_bridge.py
  - src/rpc/contracts/pack-manifest.schema.ts
  - src/rpc/contracts/pack-manifest.spec.ts
  - fixtures/rejection-cases/pack-manifest.json
autonomous: true
requirements: [DM-04, DM-05]

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Une licence de type abonnement est impossible à construire : terms hors Literal[\"perpetual-one-time\"], commercial_use=False ou attribution_required=True sont rejetés À L'INSTANCIATION des deux côtés — Literal côté Pydantic, z.literal + superRefine côté zod (critère ROADMAP n°4, gate structurelle LIC-01/02 Phase 1)"
    - "Les trois validateurs agrégés de PackManifest (unicité asset_id, cohérence totals.asset_count == len(assets), mono-style) rejettent les payloads invalides en suites miroirs paramétrées (critère ROADMAP n°3, partie pack)"
    - "Doublons asset_id rejetés, JAMAIS fusionnés (sonde d'adjacence IN-08, D-02 #4), avec chemins d'erreur précis [\"assets\", idx, \"asset_id\"] côté zod en stratégie collect-all ET côté Pydantic via loc précises (ValidationError.from_exception_data + InitErrorDetails)"
    - "Mono-style vérifié par opérations string : rsplit(\"@\", 1) + comparaison exacte du suffixe de version de chaque style_ref avec style_version du pack — AUCUNE regex re-dérivée (WR-01, D-02 #3)"
    - "Deux packs de contenu égal construits indépendamment sérialisent en model_dump_json() byte-identiques (critère ROADMAP n°5, déterminisme §4.8 — résolution probe précision DM-05)"
    - "pack_id : forme nominale ^pack-[a-z][a-z0-9-]*-\\d{4}-\\d{2}-\\d{2}$ seulement, PAS de validation calendrier (IN-07) — une date impossible comme 13e mois est acceptée si la forme matche, documenté par test de parité"
    - "assets 1..50 : pack vide rejeté, 51 assets rejetés, bornes testées à la limite exacte (1 et 50 acceptés) — résolution probe frontière DM-05"
    - "Résolution probe DM-04 (unclassified) : §4.8 définit chaque champ et chaque validateur — traduit en critères d'acceptation ci-dessous, aucune hypothèse laissée ouverte"
  artifacts:
    - "lottie_forge/domain/pack.py : PackManifest, PackTotals, LicenseInfo + 3 model_validators séparés (un invariant chacun, message pinpoint)"
    - "src/rpc/contracts/pack-manifest.schema.ts : PackManifestSchema, PackTotalsSchema, LicenseInfoSchema (z.literal perpetual-one-time + superRefine) + types"
    - "tests/bridge/fixtures.py : make_pack() ajouté (assemble make_style_spec/make_asset de façon cohérente mono-style)"
    - "tests/bridge/test_pack_bridge.py : test_export_pack + test_reimport_pack + test paramétré harnais de rejet + test déterminisme"
    - "fixtures/rejection-cases/pack-manifest.json : cas de rejet commités incluant les sondes expect_paths (doublons, compte, mono-style, licence)"
  key_links:
    - "make_pack() compose make_asset() (01-03) avec style_ref suffixé par la style_version du pack — cohérence mono-style par construction"
    - "3 validateurs PackManifest → loc précises ↔ issues zod collect-all avec paths identiques → expect_paths par appartenance (D-08)"
    - "LicenseInfo Literal + validateurs → la gate structurelle que Phase 10 exprimera en license.txt runtime"
    - "fixtures/rejection-cases/pack-manifest.json → pytest parametrize ET vitest test.each (loaders de 01-01, D-06)"

prohibitions:
  - "Aucune seconde liste d'ids de recettes hardcodée en dehors de lottie_forge/domain/vocabulary.py et src/rpc/contracts/vocabulary.schema.ts (règle same-commit §4.2)"
  - "Aucune coercion numérique : cost_eur/first_pass_yield en strict, chaînes rejetées (§4.1 #3)"
  - "Aucune assertion de parité d'erreur sur le texte des messages — appartenance de chemin uniquement (D-08)"
  - "Aucun test skippé en CI : junitxml + skipped == 0 asserté (§4.2) ; côté TS, artifact bridge manquant = échec bruyant, pas un skip"
  - "Aucune licence de forme abonnement constructible : terms hors Literal, commercial_use=False, attribution_required=True rejetés à l'instanciation des deux côtés (gate structurelle)"
  - "Aucun fixture bridge committé : fixtures/bridge/ gitigné, généré au test (§4.3)"
  - "Aucune validation calendrier de la date de pack_id — forme nominale seulement, regex plate (IN-07)"
  - "Aucun validateur Python fait main à la place de pydantic-core pour KebabToken (fix CR-01, §4.6)"
  - "Aucune valeur par défaut sur style_version (§4.5)"
  - "Aucune clé extra acceptée : extra=forbid Py / z.strictObject TS sur chaque modèle et modèle imbriqué (§4.1 #3)"
  - "Aucun hook pre-commit local (D-05) : ruff/biome/tsc enforce en CI uniquement + séquence README"
  - "Aucun dict[str, Any] traversant la frontière — modèles clos uniquement (§2.5)"
  - "Aucun doublon asset_id fusionné silencieusement ni dédupliqué à la validation — rejet avec sonde d'adjacence (IN-08)"
  - "Aucune comparaison de suffixe de version par regex re-dérivée dans le validateur mono-style — rsplit + comparaison exacte uniquement (WR-01)"
---

<objective>
Livrer `PackManifest` complet : totaux cohérents, trois validateurs agrégés séparés (unicité, compte, mono-style) avec chemins d'erreur précis en stratégie collect-all des deux côtés (IN-08), la licence perpétuelle one-time **structurelle** (Literal + validateurs — une licence abonnement ne peut pas être construite, critère ROADMAP n°4), la vérification WR-01 par rsplit, et la sonde de déterminisme byte-identique (critère n°5).

Purpose: PackManifest est l'agrégat qui « ne peut pas mentir » (MFT-02 en Phase 5 validera ses INSERT via ce modèle) ; la licence structurelle est la posture anti-subscription encode dans le type.
Output: pack.py + pack-manifest.schema.ts + bridge vert + pack-manifest.json committé + déterminisme prouvé.
</objective>

<execution_context>
@D:/projets/illustrations/systeme/.opencode/gsd-core/workflows/execute-plan.md
@D:/projets/illustrations/systeme/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@docs/project/04_Modeles.md
@docs/project/01_Vision.md
@.planning/phases/01-contrats-de-donn-es-fronti-re-pydantic-zod-reconstruction/01-01-SUMMARY.md
@.planning/phases/01-contrats-de-donn-es-fronti-re-pydantic-zod-reconstruction/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PackManifest + LicenseInfo structurelle + 3 validateurs + déterminisme (côté Python)</name>
  <files>lottie_forge/domain/pack.py, tests/domain/test_pack.py</files>
  <behavior>
    - LicenseInfo valide : license_id "pack-license-std", terms "perpetual-one-time", commercial_use=True, attribution_required=False → construite
    - terms "subscription-monthly" → ValidationError immédiate (Literal, loc ["license","terms"]) ; commercial_use=False → rejetée (validateur, loc ["license"]) ; attribution_required=True → rejetée (loc ["license"]) — une licence abonnement ne peut pas être construite
    - pack_id "pack-nature-2026-03-15" → accepté ; "pack-nature-2026-03-15-extra" → rejeté loc ["pack_id"] ; date impossible "pack-x-2026-13-45" → acceptée (forme nominale seulement, IN-07 — test de documentation)
    - assets vide → rejeté loc ["assets"] ; 1 et 50 assets → acceptés ; 51 → rejeté loc ["assets"]
    - totals.asset_count != len(assets) → rejeté loc ["totals","asset_count"] ; cost_eur -0.01 ou 1000.01 → rejeté loc ["totals","cost_eur"] ; first_pass_yield 0 et 1 acceptés, -0.1/1.1 rejetés
    - doublons asset_id (2 assets avec le même id) → rejetés, jamais fusionnés, loc ["assets", idx_du_doublon, "asset_id"] (sonde d'adjacence IN-08)
    - mono-style : un asset dont le suffixe style_ref != style_version du pack → rejeté loc ["assets", idx, "style_ref"] (vérification par rsplit("@", 1) + comparaison exacte)
    - déterminisme : deux instances de contenu égal construites indépendamment (ordres de construction différents) → model_dump_json() byte-identiques
  </behavior>
  <files_note>Écrire tests/domain/test_pack.py d'abord (rouge), puis domain/pack.py jusqu'au vert.</files_note>
  <read_first>docs/project/04_Modeles.md §4.8 (PackManifest + licence structurelle + 3 validateurs collect-all), §4.1 #6 (déterminisme), §4.7 (AssetSpec consommé) ; lottie_forge/domain/asset.py (01-03) ; 01-CONTEXT.md D-02 #3/#4, ROADMAP critères 3/4/5 ; docs/project/01_Vision.md ADR (posture licence)</read_first>
  <action>
Créer lottie_forge/domain/pack.py avec STRICT_CONFIG : LicenseInfo{license_id: str pattern ^[a-z0-9-]+$, terms: Literal["perpetual-one-time"], commercial_use: bool, attribution_required: bool} + model_validator mode="after" imposant commercial_use == True ET attribution_required == False — le Literal est la gate, le validateur la ceinture (§4.8) ; PackTotals{asset_count: int >= 1, cost_eur: float 0..1000, first_pass_yield: float 0..1} ; PackManifest{pack_id: str pattern ^pack-[a-z][a-z0-9-]*-\d{4}-\d{2}-\d{2}$ (forme nominale, PAS de validation calendrier — IN-07), style_version: str pattern ^\d+\.\d+\.\d+$, assets: list[AssetSpec] 1..50, totals: PackTotals, license: LicenseInfo}. TROIS model_validators agrégés SÉPARÉS (un invariant chacun, message pinpoint, §4.8) : unicité des asset_id ; cohérence totals.asset_count == len(assets) ; mono-style — implémenté par rsplit("@", 1) sur chaque style_ref + comparaison exacte avec style_version, AUCUNE regex re-dérivée (WR-01, D-02 #3). Pour des loc précises depuis les validateurs de niveau pack, lever via pydantic_core ValidationError.from_exception_data avec InitErrorDetails portant les loc exactes ("assets", idx, "asset_id") / ("assets", idx, "style_ref") / ("totals", "asset_count") — nécessaire à la parité d'appartenance expect_paths (D-08, IN-08 collect-all côté Py). Écrire tests/domain/test_pack.py selon le bloc behavior, y compris le test de déterminisme : deux packs de contenu égal construits indépendamment (mêmes valeurs, ordre de construction différent) → bytes de model_dump_json() identiques (critère ROADMAP n°5). Cycle rouge→vert en commits atomiques (D-09).
  </action>
  <verify>
    <automated>python -m pytest tests/domain/test_pack.py -q && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/domain/test_pack.py -q exit 0 après cycle rouge→vert (commits test(01-04) puis feat(01-04))
    - pytest -q -k license exit 0 : les trois voies de rejet licence (terms hors Literal, commercial_use False, attribution True) sont testées et échouent à l'instanciation
    - pytest -q -k in08 exit 0 : doublons rejetés, jamais fusionnés, loc précise ["assets", idx, "asset_id"] assertée par appartenance
    - pytest -q -k wr01 exit 0 : le mismatch mono-style est rejeté et la chaîne de pack valide passe (vérification rsplit)
    - pytest -q -k determinism exit 0 : model_dump_json() byte-identique pour deux constructions indépendantes
    - pytest -q -k in07 exit 0 : la date impossible de forme correcte est acceptée (pas de validation calendrier)
  </acceptance_criteria>
  <done>PackManifest valide agrégats, unicité, compte, mono-style et licence structurelle côté Python ; déterminisme de sérialisation prouvé ; loc précises opérationnelles pour la parité.</done>
</task>

<task type="auto">
  <name>Task 2: Miroir zod PackManifest (collect-all + z.literal licence) + bridge + cas de rejet partagés</name>
  <files>src/rpc/contracts/pack-manifest.schema.ts, src/rpc/contracts/pack-manifest.spec.ts, tests/bridge/fixtures.py, tests/bridge/test_pack_bridge.py, fixtures/rejection-cases/pack-manifest.json</files>
  <read_first>docs/project/04_Modeles.md §4.8 (3 validateurs + licence miroir zod), §4.9 (miroirs zod), §4.2/§4.3 (parité + bridge) ; lottie_forge/domain/pack.py ; src/rpc/contracts/asset-spec.spec.ts (patron 01-03) ; loaders rejection (01-01) ; 01-CONTEXT.md D-02 #3/#4, D-06/D-08</read_first>
  <action>
Créer src/rpc/contracts/pack-manifest.schema.ts : miroir z.strictObject exact — licenseInfoSchema (license_id regex, terms z.literal("perpetual-one-time"), commercial_use z.boolean(), attribution_required z.boolean()) + superRefine/check imposant commercial_use === true ET attribution_required === false avec issues path ["license"] ; packTotalsSchema (asset_count int >= 1, cost_eur 0..1000, first_pass_yield 0..1) ; packManifestSchema avec TROIS superRefine/check séparés en stratégie collect-all : unicité des asset_id (une issue PAR doublon, path ["assets", idx, "asset_id"] — jamais de fusion), cohérence totals.asset_count === assets.length (path ["totals","asset_count"]), mono-style (suffixe version de chaque style_ref extraite par rsplit/"@" split limité à 1 + comparaison exacte avec style_version — même opération string que Python, path ["assets", idx, "style_ref"]). Exporter les types z.infer. NE PAS utiliser de regex re-dérivée pour le suffixe de version (WR-01 symétrie TS).

Ajouter make_pack() dans tests/bridge/fixtures.py : assemble 2–3 assets de make_asset() (asset_id distincts, style_ref suffixé exactement par la style_version du pack), totals cohérents, licence valide — la référence mono-style par construction. Créer tests/bridge/test_pack_bridge.py : test_export_pack (pack-manifest.from-python.json + pack-manifest.schema-keys.json, patron des plans précédents), test_reimport_pack (skipif garde d'ordre, égalité canonique model_dump_json — le re-import strict est la dernière barrière WR-06 §4.9), test paramétré harnais de rejet, test déterminisme côté bridge (l'export écrit deux dumps de constructions indépendantes et assert l'égalité bytes). Créer src/rpc/contracts/pack-manifest.spec.ts : validate + deep-equal + parité de clés + re-émission + test.each cas partagés + assertion que les issues collect-all portent les paths précis pour les cas expect_paths (doublons : une issue par idx).

Créer fixtures/rejection-cases/pack-manifest.json (format D-08) : in08-doublons-asset-id (ref IN-08, 2 assets même id, expect_paths ["assets", 1, "asset_id"]), totals-compte-incohérent (expect_paths ["totals","asset_count"]), mono-style-mismatch (expect_paths ["assets", 0, "style_ref"], ref WR-01), assets-vide (expect_paths ["assets"]), assets-51 (expect_paths ["assets"]), license-terms-hors-literal (terms "subscription-monthly", expect_paths ["license","terms"]), license-commercial-use-false (expect_paths ["license"]), license-attribution-required-true (expect_paths ["license"]), cost-eur-hors-bornes (expect_paths ["totals","cost_eur"]), pack-id-forme-invalide (expect_paths ["pack_id"]). Rejets TOUJOURS assertés des deux côtés, appartenance de chemins uniquement (D-08).
  </action>
  <verify>
    <automated>python -m pytest tests/ -q -k export && npx vitest run pack-manifest && python -m pytest tests/ -q && npx tsc --noEmit && ruff check . && npx @biomejs/biome check .</automated>
  </verify>
  <acceptance_criteria>
    - Chaîne bridge pack-manifest verte (export → vitest → re-import) exit 0
    - npx vitest run pack-manifest exit 0 : le cas in08-doublons produit au moins une issue par doublon avec path ["assets", idx, "asset_id"] (collect-all asserté), jamais une seule issue agrégée
    - Le miroir zod rejette terms différent du literal ET les deux violations de validateur licence (3 voies testées côté TS)
    - python -m pytest tests/ -q exit 0 ; fixtures/rejection-cases/pack-manifest.json commité (D-07)
    - npx vitest run exit 0 sur toute la surface TS (les 4 contrats ensemble)
  </acceptance_criteria>
  <done>PackManifest et licence structurelle opérationnels des deux côtés avec collect-all et chemins précis ; les critères ROADMAP 3 (partie pack), 4 et 5 sont verts localement ; le jeu de contrats Phase 1 est complet.</done>
</task>

</tasks>

<artifacts-this-phase-produces>
Inventaire complet de la Phase 1 : voir 01-01-PLAN.md (section dédiée). Symboles créés par CE plan :
- `PackManifest`, `PackTotals`, `LicenseInfo` (Literal["perpetual-one-time"] + validateurs structurels) + 3 model_validators agrégés séparés — `lottie_forge/domain/pack.py`
- `PackManifestSchema`, `PackTotalsSchema`, `LicenseInfoSchema` (z.literal + superRefine, collect-all) + types z.infer — `src/rpc/contracts/pack-manifest.schema.ts`
- `make_pack()` (compose make_asset/make_style_spec, mono-style par construction) — ajout à `tests/bridge/fixtures.py`
- `fixtures/rejection-cases/pack-manifest.json` (commité, format D-08, sondes IN-08/WR-01/licence)
- Fichiers : tests/domain/test_pack.py, tests/bridge/test_pack_bridge.py, src/rpc/contracts/pack-manifest.spec.ts
</artifacts-this-phase-produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agrégat construit programmatique → PackManifest | L'agrégateur (Phase 5) ne pourra insérer que ce que le modèle valide — le manifest « ne peut pas mentir » |
| payload JSON → LicenseInfo | Toute tentative de licence abonnement est une escalade de privilege commerciale — bloquée par le type |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Elevation of Privilege | LicenseInfo (contournement de la posture anti-subscription) | high | mitigate | Literal["perpetual-one-time"] + validateurs commercial_use/attribution_required des deux côtés — constructible impossible (critère ROADMAP n°4) |
| T-01-08 | Repudiation | totals.asset_count mentant sur len(assets) | medium | mitigate | validateur de cohérence avec loc précise + cas partagé expect_paths (MFT-02 s'appuiera dessus en Phase 5) |
| T-01-09 | Tampering | doublons asset_id fusionnés/dédupliqués silencieusement | medium | mitigate | sonde d'adjacence IN-08 : rejet avec chemin précis, collect-all zod, jamais de fusion |
</threat_model>

<verification>
- python -m pytest tests/ -q exit 0 · npx vitest run exit 0 · npx tsc --noEmit exit 0 · ruff check . exit 0 · npx @biomejs/biome check . exit 0
- Chaîne bridge pack-manifest verte depuis état frais
- fixtures/rejection-cases/pack-manifest.json commité et consommé par les deux loaders
- Déterminisme : deux dumps indépendants byte-identiques (test dédié vert)
</verification>

<success_criteria>
- Critère ROADMAP n°4 : licence abonnement impossible à construire des deux côtés
- Critère ROADMAP n°3 complété : les 3 validateurs pack rejettent en suites miroirs paramétrées
- Critère ROADMAP n°5 : déterminisme model_dump_json byte-identique prouvé
- IN-08 et WR-01 (D-02 #3/#4) préservés verbatim
</success_criteria>

<output>
Create `.planning/phases/01-contrats-de-donn-es-fronti-re-pydantic-zod-reconstruction/01-04-SUMMARY.md` when done
</output>
