---
phase: 04-anim-qa-pinn-e
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/anim-qa/structural.ts
  - src/anim-qa/baseline.ts
  - src/anim-qa/report.ts
  - src/anim-qa/runner.ts
  - src/rpc/server.ts
  - src/rpc/server.spec.ts
  - src/anim-qa/__tests__/structural.spec.ts
  - src/anim-qa/__tests__/baseline-resolver.spec.ts
  - src/anim-qa/__tests__/report.spec.ts
  - tests/rpc/test_anim_qa_integration.py
autonomous: true
requirements: [QA-04]
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "anim_qa.run exists as a single RPC method chaining the 5 gate steps internally — the §7.3 ordering is a module invariant, not an calling convention (D-17)"
    - "A structurally invalid Lottie produces ok:true with a QAReport{passed:false, reason_codes:[structural:schema|feature:3D|stretch:negative|shape:layers]} — verdict is never a transport error (D-17/B5)"
    - "Steps 1–3 failure or a baseline resolution failure NEVER launches a browser — a spawn counter asserted at 0 proves it (m7)"
    - "An orphan asset_id resolves to the closed error baseline_missing; a sidecar hash mismatch resolves to baseline_stale — decided BEFORE any pixel work (D-26/m3)"
    - "QAReport JSON artifacts are written only through the deterministic serializers — fmt()/serializeDeterministicJson()/writeDeterministicJson() (P8)"
  artifacts:
    - "src/anim-qa/structural.ts — steps 1–3: LottieJSONSchema.safeParse + assertSupportedComposition reuse + structural diff, closed reason-code mapping"
    - "src/anim-qa/baseline.ts — pure resolver: sidecar predicate m3, baseline_missing/baseline_stale, configs at startup"
    - "src/anim-qa/report.ts — QAReport builder (injectable timestamp D-15) + deterministic bytes"
    - "src/anim-qa/runner.ts — gate orchestrator; playwright dynamic-imported inside the walk step only"
    - "src/rpc/server.ts — anim_qa.run dispatch branch + ServerContext QA configs + banner update + chromium spawn counter"
    - "tests/rpc/test_anim_qa_integration.py — verdict≠transport, exact 10-code parity, orphan baseline_missing"
  key_links:
    - "handleAnimQa → structural.ts (steps 1–3) → baseline.ts (sidecar predicate) → [walk step: plan 04-05] — order fixed by §7.3, fillable without architectural change"
    - "renderer_support arrives in the request and is NEVER re-derived server-side (D-18 anti-pattern)"
prohibitions:
  - statement: "renderer_support is never re-derived server-side via classify() — it arrives in the request from the compile envelope, single source (D-18)"
    status: enforced
    verification: explicit
---

<objective>
TRACER: one production-quality end-to-end slice of the QA gate — `anim_qa.run` on a compile-envelope input through structural steps 1–3 and the baseline resolver, terminating in a real envelope (`baseline_missing` on the happy-path input until baselines exist in wave 3; a structured `passed:false` QAReport on broken inputs) — with zero browser involvement. This proves the RPC seam, the gate ordering, the report builder and the closed codes before any pixel code exists, and it is the skeleton the browser plans (04-05/04-06/04-07) expand into.

Purpose: the architectural risk of Phase 4 is the gate ordering + envelope semantics, not the pixel math; prove it on the cheapest possible path (m7's no-spawn guarantee is the ordering proof).
Output: working anim_qa.run (steps 1–3 + resolver), spawn-safe by construction, integration-proven from Python.
</objective>

<execution_context>
@D:/projets/illustrations lottie/systeme/.opencode/gsd-core/workflows/execute-plan.md
@D:/projets/illustrations lottie/systeme/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-anim-qa-pinn-e/04-CONTEXT.md
@.planning/phases/04-anim-qa-pinn-e/04-RESEARCH.md
@.planning/phases/04-anim-qa-pinn-e/04-PATTERNS.md
@src/rpc/server.ts
@src/rpc/contracts/qa-report.schema.ts
@src/motion-compiler/feature-gate.ts
@src/motion-compiler/markers.ts
@src/shared/format.ts
@lottie_forge/rpc/client.py
@tests/rpc/test_rpc_integration.py
</context>

## Artifacts this phase produces

(Consolidated inventory in 04-01-PLAN.md — this plan contributes:) `src/anim-qa/structural.ts`, `src/anim-qa/baseline.ts` + `resolveBaseline()`, `src/anim-qa/report.ts`, `src/anim-qa/runner.ts` skeleton with `chromiumSpawnCount`, RPC method `anim_qa.run` dispatch, `ServerContext` extension (thresholds/darkTheme/captureConfig loaded at startup), banner listing the new method, Python integration suite `test_anim_qa_integration.py`.

<tasks>

<task type="tracer">
  <name>Task 1 (TRACER): anim_qa.run end-to-end browserless — structural gate → baseline resolver → envelope</name>
  <files>src/anim-qa/structural.ts, src/anim-qa/baseline.ts, src/anim-qa/report.ts, src/anim-qa/runner.ts, src/rpc/server.ts, tests/rpc/test_anim_qa_integration.py</files>
  <read_first>
    - docs/project/07_AnimQA.md §7.3 (5-step gate order) and §7.6 (report contract)
    - 04-PATTERNS.md "src/rpc/server.ts — MODIFY" section (dispatch pattern, ServerContext extension, verdict-≠-transport model handleSanitize, banner substring note, entry-guard isEntryModule + never import playwright at module top level)
    - 04-CONTEXT.md D-17, D-18, D-26, m3, m7, B5; 04-RESEARCH.md Q9 (processLine 271-282, buildDefaultContext 164-166, banner 370, client banner match substring-safe)
    - src/motion-compiler/feature-gate.ts lines 210-229 + 248-258 (assertSupportedComposition reused verbatim at step 2; classify() NOT called server-side)
    - src/shared/format.ts lines 45-92 (the ONLY allowed serializers)
    - tests/rpc/test_rpc_integration.py (module-scoped client fixture, fail-loud doctrine)
  </read_first>
  <action>
    Create `src/anim-qa/structural.ts` (pure, zero I/O, no browser): step 1 = LottieJSONSchema.safeParse on request.lottie; step 2 = reuse assertSupportedComposition from feature-gate.ts verbatim; step 3 = structural diff — layer count, layer ids shape, keyframe-count sanity vs the recipe contract, equality expected (§7.3 "diff structurel"), implemented as explicit checks with an exhaustive reason mapping into the closed codes: schema failure → structural:schema; UnsupportedFeatureError → feature:3D; negative-stretch/op&lt;ip → stretch:negative; layer/shape-count or id-shape mismatch → shape:layers. Any step 1–3 failure returns a structured QAReport{passed:false} through report.ts — never throws to the RPC layer, never touches step 4.
    Create `src/anim-qa/baseline.ts`: startup-side config loaders (parse fixtures/qa/capture-config.json, thresholds.json-when-present, dark-theme.json through the qa-configs schemas — thresholds tolerate absence pre-spike via an explicit "not yet calibrated" state that maps to baseline_missing semantics) + the PURE resolver `resolveBaseline(assetId, baselineRoot, expected)` returning {status: "ok", paths} | {status: "missing"} | {status: "stale"} from the sidecar baseline-frames/index.json per the exact m3 predicate: entry absent → missing; lottie_sha256 mismatch OR captureConfig_hash mismatch → stale; thresholds/dark-theme hashes recorded in the sidecar for traceability but NEVER triggering stale. sha256 via node:crypto. Decision logic pure (sidecar content passed in), I/O isolated at read edges.
    Create `src/anim-qa/report.ts`: buildQaReport(...) producing the §7.6+additive QAReport with timestamp as an INJECTED parameter (D-15), all numeric fields flowing through fmt(), serialization only via serializeDeterministicJson/writeDeterministicJson; also buildStructuralFailureReport(reasonCode, ...) for steps 1–3 rejects. Export `packVerdictFromReports(reports)` as the pure QA-03 policy: pack passed iff every asset report passed — NO averaging (unit-pinned in Task 2 and consumed by Phase 7).
    Create `src/anim-qa/runner.ts` as the gate orchestrator: exported module counter `chromiumSpawnCount` incremented at the ONLY future chromium.launch site; the walk step (steps 4–5: browser) loads playwright via await import INSIDE the function — never a top-level import (D-24 scan + m7). In THIS plan the walk step resolves baselines first (resolveBaseline) and throws the closed RpcErrorCode baseline_missing/stale as operational errors BEFORE any browser work; the browser capture itself is completed by plan 04-05 filling the same function — no architectural change. This fill-later boundary is the tracer's explicit stub allowance.
    Extend `src/rpc/server.ts`: ServerContext gains thresholds/darkTheme/captureConfig parsed at startup in buildDefaultContext (baselines stay lazy per-request); add the anim_qa.run dispatch branch calling handleAnimQa(id, params, ctx): parse via AnimQaRequestSchema (reject → validation_error, existing pattern); run structural gate — failure → okEnvelope(id, structural QAReport) (verdict ≠ transport, B5); resolver failure → errEnvelope with baseline_missing/baseline_stale; success path → the walk step (which, pre-04-05, deterministically surfaces baseline_missing on the 11 fixtures). Update the ready banner to list anim_qa.run (substring-safe per client.py:150). Keep stdout protocol-only (D-36/P5).
    Create `tests/rpc/test_anim_qa_integration.py` (host-runnable, no browser): module-scoped client fixture; test 1 calls motion.compile on a render-spec fixture then anim_qa.run with the envelope's lottie/renderer_support (D-18 source-of-truth flow) and asserts ok:false with code baseline_missing (proves steps 1–3 passed and resolver decided); test 2 sends a structurally broken lottie (extra key) and asserts ok:true with result.passed is False and reason_codes containing structural:schema; test 3 asserts the EXACT 10-code set parity against lottie_forge/rpc/client.py RPC_ERROR_CODES; test 4 asserts method_not_found for an unknown method naming the closed set including anim_qa.run.
  </action>
  <verify>
    <automated>npx vitest run &amp;&amp; python -m pytest tests/rpc/test_anim_qa_integration.py -q &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - test_anim_qa_integration.py exits 0: baseline_missing on valid input (orphan path), verdict-as-ok:true on structural reject, exact 10-code parity, updated banner matched by the Python client's substring
    - `npx vitest run` green including the extended server.spec.ts (dispatch branch, envelope shapes)
    - grep of src/anim-qa/runner.ts shows the playwright module specifier ONLY inside the walk function body (no top-level import line)
    - server.spec.ts asserts chromiumSpawnCount === 0 after a structural-failure request AND after a baseline_missing request (m7 — full proof in Task 2)
  </acceptance_criteria>
  <done>The one path — compile-envelope request → 5-step gate ordering → resolver decision → envelope — works end-to-end browserless and is committed. Architecture proven: expansion plans fill the walk step without restructuring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: m7 no-spawn matrix + resolver predicate matrix + structural rejection matrix (unit)</name>
  <files>src/rpc/server.spec.ts, src/anim-qa/__tests__/structural.spec.ts, src/anim-qa/__tests__/baseline-resolver.spec.ts, src/anim-qa/__tests__/report.spec.ts</files>
  <read_first>
    - 04-CONTEXT.md m7 (échec des étapes 1–3 = JAMAIS de spawn Chromium), D-26/m3 (stale predicate exact), D-08/m4 (frame_hashes producer order [canonique, 25%, 50%, 75%]), D-13 (closed reason codes)
    - src/rpc/server.spec.ts conventions (buildContext from COMMITTED fixtures, closed-set assertion style)
    - src/motion-compiler/__tests__/make-render-spec.ts (fixture builder for valid/invalid inputs)
    - 04-RESEARCH.md P8 (float serialization → flake-proof false negatives)
  </read_first>
  <action>
    structural.spec.ts: parametrized matrix producing each structural reason code from a minimal synthetic input — schema violation → structural:schema; a feature outside the supported set via the phase-3 gate error → feature:3D; op &lt; ip payload → stretch:negative; layer/id-shape mismatch → shape:layers; plus the happy path asserting NO reason codes and passed=true shape readiness. Every case asserts the gate returns a QAReport (never throws) and that step-4 inputs are never constructed (ordering proof: structural result short-circuits before resolver).
    baseline-resolver.spec.ts: pure matrix on synthetic sidecar objects — missing entry → missing; lottie_sha256 mismatch → stale; captureConfig_hash mismatch → stale; both hashes matching → ok with resolved paths; thresholds-hash DIFFERING but capture+lottie matching → still ok (m3: traceability-only hashes never trigger stale); assert the decision function performs zero I/O (pure over inputs).
    report.spec.ts: builder emits fields in §7.6+additive order, serializes byte-identically across two calls with the same injected timestamp, floats formatted through fmt(), timestamp omitted from identity comparison in a provided compareReports-excluding-timestamp helper (consumed later by the flake proof, D-25); packVerdictFromReports: all-passed → pack pass; ONE failing report → pack fail regardless of any mean/average computed across the set (QA-03, prohibition #1); empty set rejected (defensive).
    server.spec.ts additions: the m7 assertion — send a structural-broken request and a baseline-orphan request through processLine; after both, import runner.ts and assert chromiumSpawnCount === 0; also assert the envelope for the structural case carries ok:true + QAReport while the orphan case carries ok:false + code from the closed set.
  </action>
  <verify>
    <automated>npx vitest run src/anim-qa/__tests__ src/rpc/server.spec.ts &amp;&amp; npx @biomejs/biome check src/anim-qa src/rpc</automated>
  </verify>
  <acceptance_criteria>
    - structural.spec.ts covers all 4 structural reason codes + happy path; each asserts QAReport-shaped return (no throw)
    - baseline-resolver.spec.ts covers the 6-row predicate matrix including the thresholds-hash negative case (m3 exact)
    - report.spec.ts: two identical builds serialize to equal strings; the one-failing-asset pack case fails with zero averaging
    - server.spec.ts: chromiumSpawnCount === 0 asserted on both non-browser failure paths (m7 green)
  </acceptance_criteria>
  <done>Gate-ordering, resolver predicate, and report determinism pinned by unit matrices; the no-browser guarantee is a tested invariant, not a convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC caller → gate | Request zod-parsed (AnimQaRequestSchema) before any processing |
| Gate → browser | Steps 1–3 + resolver decide BEFORE any browser surface exists (m7) |
| Resolver → fixture files | Sidecar/config reads from repo-relative constants only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-01 | Tampering/Elevation | anim_qa.run request fields | low | mitigate | Closed request schema (plan 04-01 Task 2), validated at entry in handleAnimQa (Task 1) |
| T-4-09 | Tampering | Malicious "Lottie" payload driving a browser page | medium | mitigate | Steps 1–3 re-validate via LottieJSONSchema strictObject BEFORE the browser step exists; E2E chain input is the compiler's own re-validated output (D-16) |
| T-4-10 | Denial of Service | Resolver probe on attacker-chosen asset_id | low | mitigate | asset_id regex-gated ^a-\d{3}$ at entry; resolver reads only baseline-frames/ paths built from the validated id — no traversal surface |
| T-4-11 | Information Disclosure | Error envelope leaking filesystem layout | low | mitigate | baseline_missing/stale envelopes carry closed codes only, never resolved paths (Task 1 envelope shape) |
| T-4-12 | Repudiation | Silent gate-order violation | medium | mitigate | m7 spawn-counter assertion makes ordering violations red in verify (Task 2) |
</threat_model>

<verification>
- `npx vitest run` green (tracer + matrices); `python -m pytest tests/rpc -q` green (integration parity)
- `npx tsc --noEmit` clean; biome clean
- Full pytest suite still green (no regression to the bridge chain)
</verification>

<success_criteria>
- anim_qa.run live with steps 1–3 + resolver proven end-to-end browserless; verdict≠transport semantics locked by tests
- QA-04 ordering half ("validation structurelle avant tout diff pixel") enforced by the m7 spawn counter
- Expansion surface (walk step) isolated behind one dynamically-imported function — 04-05 fills it without architectural change
</success_criteria>

<output>
Create `.planning/phases/04-anim-qa-pinn-e/04-03-SUMMARY.md` when done
</output>
