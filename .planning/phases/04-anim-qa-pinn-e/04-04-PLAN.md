---
phase: 04-anim-qa-pinn-e
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - src/anim-qa/canonical-frame.ts
  - src/anim-qa/diff.ts
  - src/anim-qa/frame-walker.ts
  - src/anim-qa/page/qa-page.html
  - src/anim-qa/page/qa-page.js
  - scripts/qa-calibrate.mjs
  - fixtures/qa/thresholds.json
  - docs/qa.md
  - src/anim-qa/__tests__/diff.spec.ts
  - src/anim-qa/__tests__/canonical-frame.spec.ts
  - src/anim-qa/__tests__/capture.smoke.qa.spec.ts
  - src/anim-qa/__tests__/qa-contracts.spec.ts
autonomous: true
requirements: [QA-02]
user_setup:
  - service: docker
    why: "Calibration spike captures run inside the pinned container via qa:run"
    env_vars: []
    dashboard_config:
      - task: "Start Docker Desktop"
        location: "Docker Desktop application"
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "thresholds.json is a measured artifact, not an estimate: K=10 captures per fixture, pairwise pixelmatch noise, p95 × a fixed documented margin (D-07/m11) — and NO threshold-consuming consumer landed before it (D-07 first-delivered ordering)"
    - "canonicalFrame() is the single shared frame selector consumed by poster SVG, baseline generation and the frame walker, with the three-index equality test (D-08/m4)"
    - "frameWalkFrames() always returns exactly [canonique, 25%, 50%, 75%] in that fixed order (m4)"
    - "Frame capture waits only on lottie lifecycle events — never wall-clock waits (Q6 anti-pattern #1)"
    - "Captures are 400×300 at deviceScaleFactor 1 on an opaque #ffffff background from capture-config.json (D-22/D-23)"
    - "Re-running the calibration spike twice produces byte-identical thresholds.json (determinism)"
  artifacts:
    - "src/anim-qa/diff.ts + comparePngBuffers() + countRegionDiff() + computePixelDiffStats() (nearest-rank p95, policy fixed and unit-tested)"
    - "src/anim-qa/canonical-frame.ts + canonicalFrame() + frameWalkFrames() (reuses poseResolutionFor)"
    - "src/anim-qa/frame-walker.ts — capture core: launch args, event-driven lifecycle, goToAndStop(n, true) + setSpeed(0) + setSubframe(false)"
    - "src/anim-qa/page/qa-page.{html,js} — lottie-web 5.13 UMD served locally, no text in viewport, opaque background"
    - "scripts/qa-calibrate.mjs + fixtures/qa/thresholds.json + docs/qa.md (method + values + margin justification)"
  key_links:
    - "canonicalFrame() ← poseResolutionFor() (markers.ts) — enter → final frame, loop → frame 0, aligned with the Phase 3 poster rule"
    - "thresholds.json → ThresholdsSchema (04-01) validated at write and at every consumer load"
    - "spike captures → diff.ts pairwise noise → thresholds.json → docs/qa.md (the artifact chain, D-07)"
prohibitions:
  - statement: "Thresholds are never guessed or hand-set — every maxDiffPixels value derives from the committed spike protocol and is traceable to docs/qa.md (D-07)"
    status: enforced
    verification: explicit
---

<objective>
Deliver the D-07 calibration spike as the FIRST functional artifact of the phase (immediately after the environment pin; no threshold-enforcing consumer precedes it — the tracer's browserless path consumes none), plus the pure diff/frame primitives it and all later plans build on: pixelmatch wrappers with fixed stats policy, the shared canonical-frame selector, and the production-quality capture core of the frame walker.

Purpose: QA-02's "maxDiffPixels calibré" is only defensible if the threshold is measured noise × documented margin, committed as data.
Output: thresholds.json for all 11 fixtures + docs/qa.md + diff/canonical-frame/walker-core modules, spike re-run deterministic.

> **Lean execution (checker P4-005):** self-estimate 90k/100k tokens at confidence: low — batch the read_first reads, never re-read a file already in context, keep verify runs targeted.
</objective>

<execution_context>
@D:/projets/illustrations lottie/systeme/.opencode/gsd-core/workflows/execute-plan.md
@D:/projets/illustrations lottie/systeme/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-anim-qa-pinn-e/04-CONTEXT.md
@.planning/phases/04-anim-qa-pinn-e/04-RESEARCH.md
@.planning/phases/04-anim-qa-pinn-e/04-PATTERNS.md
@docs/project/07_AnimQA.md
@src/motion-compiler/markers.ts
@src/shared/format.ts
@src/rpc/contracts/qa-configs.schema.ts
@scripts/qa-run.mjs
</context>

## Artifacts this phase produces

(Consolidated inventory in 04-01-PLAN.md — this plan contributes:) `src/anim-qa/diff.ts` (`comparePngBuffers()`, `countRegionDiff()`, `computePixelDiffStats()` with nearest-rank p95), `src/anim-qa/canonical-frame.ts` (`canonicalFrame()`, `frameWalkFrames()`), `src/anim-qa/frame-walker.ts` (capture core), `src/anim-qa/page/qa-page.{html,js}`, `scripts/qa-calibrate.mjs`, `fixtures/qa/thresholds.json`, `docs/qa.md`, capture smoke qa-spec.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: diff.ts pixelmath + canonical-frame.ts shared selector (pure, unit-tested on synthetic PNGs)</name>
  <files>src/anim-qa/diff.ts, src/anim-qa/canonical-frame.ts, src/anim-qa/__tests__/diff.spec.ts, src/anim-qa/__tests__/canonical-frame.spec.ts</files>
  <read_first>
    - 04-RESEARCH.md Q4 (pixelmatch 7.2.0 sync API verbatim, pngjs sync read/write, crop-based per-region compare, nearest-rank p95 policy note), P8
    - 04-PATTERNS.md "src/anim-qa/diff.ts — CREATE" delta block and Shared Patterns #2/#5 (fmt authority, pure module + exhaustive switches)
    - src/motion-compiler/markers.ts lines 75-109 (poseResolutionFor + svgPoseFrameFor — the data source and the equality target)
    - fixtures/recipe-catalogue/catalogue.json (10 recipes for the equality sweep)
    - 04-CONTEXT.md D-06, D-08, m4
  </read_first>
  <action>
    Create `src/anim-qa/diff.ts` (pure, no I/O beyond buffer args): `comparePngBuffers(a, b, opts)` decoding via pngjs sync, running pixelmatch with options taken from an explicit opts object (threshold from capture-config's versioned field), returning the mismatch count; `countRegionDiff(a, b, width, bbox)` implementing the crop-based region compare — row-sliced RGBA subarray copies per bbox row, per-crop pixelmatch with diff null, summed count (A2; exact semantics pinned by unit test); `computePixelDiffStats(perFrameCounts, appliedMaxDiffPixels)` returning the PixelDiffStats shape — max, mean (float through fmt() semantics), p95 by nearest-rank over the per-frame counts with the policy FIXED and documented in the module docstring, frames_above_tolerance counted against appliedMax. No JSON.stringify on any float-bearing value.
    Create `src/anim-qa/canonical-frame.ts` (pure): import poseResolutionFor from markers.ts (reuse, no redeclaration); `canonicalFrame(recipeId, op)` → enter-pose recipes (finale) → op, loop recipes → 0 (D-08); `frameWalkFrames(recipeId, op)` → exactly [canonical, floor(0.25·op), floor(0.5·op), floor(0.75·op)] in that order, length 4 always (m4). The poster SVG consumer stays on svgPoseFrameFor untouched — D-08's three-consumer canonicalFrame() alignment is test-enforced via the equality sweep rather than by rewiring the poster (zero domain churn, D-21 reconciliation); record this decision in the module docstring.
    diff.spec.ts: synthetic PNG pairs built in-test via pngjs (solid colors, known-diff patterns) — identical buffers → 0; a K-pixel perturbation → exactly K (bbox known); region crop counts only pixels inside the bbox (A2 pin); stats on a fixed 4-count input pin max/mean/p95/frames_above_tolerance including the nearest-rank edge (4 samples → p95 = 4th ordered value) and a zeros case.
    canonical-frame.spec.ts: table-driven over all 10 locked recipes — canonical index equals svgPoseFrameFor(recipe, op) for every recipe (the D-08 three-consumer equality: poster SVG consumer vs this selector); frameWalkFrames order + length + monotonicity for representative op values (48 from the golden, plus edge op=1).
  </action>
  <verify>
    <automated>npx vitest run src/anim-qa/__tests__/diff.spec.ts src/anim-qa/__tests__/canonical-frame.spec.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - diff.spec.ts green with the A2 region-semantics pin and the nearest-rank p95 policy pin (both named in doc-comments)
    - canonical-frame.spec.ts green over all 10 recipes with the svgPoseFrameFor equality sweep
    - Neither module imports playwright (D-24 scan will fail otherwise) and neither mutates its inputs
  </acceptance_criteria>
  <done>Pure diff + frame-selection primitives locked with pinned policies; zero browser involvement.</done>
</task>

<task type="auto">
  <name>Task 2: Frame-walker capture core + QA page (production-quality, container-verified)</name>
  <files>src/anim-qa/frame-walker.ts, src/anim-qa/page/qa-page.html, src/anim-qa/page/qa-page.js, src/anim-qa/__tests__/capture.smoke.qa.spec.ts</files>
  <read_first>
    - 04-RESEARCH.md Q5 (loadAnimation animationData deep-clone warning, goToAndStop(value, isFrame) semantics, setSpeed/setSubframe), Q6 (determinism mitigations: deviceScaleFactor 1, event-driven lifecycle — the wall-clock wait ban, fonts/no-text rule, dev-shm flags), Q3 (launch args)
    - 04-PATTERNS.md "frame-walker + page + runner — CREATE" delta block
    - fixtures/qa/capture-config.json (viewport/dsf/background/threshold — the versioned source)
    - 04-CONTEXT.md D-22, D-23, D-08; src/motion-compiler/__tests__/goldens/a-001.fade.golden.json (emit shape ground truth)
  </read_first>
  <action>
    Create `src/anim-qa/page/qa-page.html` + `qa-page.js`: a minimal deterministic QA page served locally by the walker — lottie-web 5.13.0 loaded from the container's own node_modules UMD build (never a CDN), the animation mounted in a container with the opaque background from capture-config, viewport-strict (no text nodes or captions inside the captured viewport — fonts rule, Q6), and a JS bridge exposing exactly: load(animationData deep-cloned), walk(frames) performing setSpeed(0) + setSubframe(false) then goToAndStop(n, true) per frame with a readiness signal per frame, and capture() returning the canvas-region PNG bytes. Readiness waits ONLY on lottie lifecycle events (DOMLoaded / per-frame render settle via requestAnimationFrame-after-goToAndStop signal) — the page contains no wall-clock wait call of any kind.
    Create `src/anim-qa/frame-walker.ts` (executed only in-container): launch chromium with args --no-sandbox --disable-dev-shm-usage; context at viewport/captureConfig values (400×300, deviceScaleFactor 1, background #ffffff); serve qa-page via a local route/file URL; expose `captureFrames(lottie, recipeId, op, config)` → exactly 4 PNG buffers in frameWalkFrames order, waiting on the page's readiness signals only. This module increments the wave-1 spawn counter imported from src/anim-qa/spawn-counter.ts (created in 04-01 T2) at its launch site (single launch site per D-17/m7 architecture) — deliberately NOT an import from runner.ts, which is produced by same-wave plan 04-03 (P4-002). Playwright import stays inside the function bodies.
    Create `src/anim-qa/__tests__/capture.smoke.qa.spec.ts` (qa project — container-only): compile the a-001 fixture on the fly (Phase 3 compile in-process, D-16 pattern), captureFrames, assert 4 buffers each decoding via pngjs to exactly 400×300, opaque white corners, and non-uniform ink somewhere in the canonical frame (the render actually happened).
  </action>
  <verify>
    <automated>npm run qa:run -- npx vitest run --config vitest.qa.config.ts capture.smoke</automated>
  </verify>
  <acceptance_criteria>
    - The capture smoke qa-spec exits 0 inside the pinned container: 4 PNGs, each exactly 400×300, canonical frame shows ink
    - grep of src/anim-qa/page/ and frame-walker.ts finds zero wall-clock wait calls and zero CDN URLs (lottie-web resolved from node_modules only)
    - The spec runs through the same qa:run invocation path (lock-parsed image) — local/CI path identity (D-02/B1)
  </acceptance_criteria>
  <done>Real captures of a real fixture inside the pinned container through the production walker core — the exact machinery the spike and later E2E reuse.</done>
</task>

<task type="auto">
  <name>Task 3: Calibration spike → thresholds.json + docs/qa.md (D-07/m11, first delivered functional artifact)</name>
  <files>scripts/qa-calibrate.mjs, fixtures/qa/thresholds.json, docs/qa.md, src/anim-qa/__tests__/qa-contracts.spec.ts</files>
  <read_first>
    - 04-CONTEXT.md D-07, m11 (protocol: K ≥ 10 captures of the reference asset; noise = pairwise pixelmatch between captures of the same input; threshold = p95 observé × marge, marge fixée ET justifiée dans docs/qa.md; critère d'acceptation = seuils dérivés passent la preuve identité ×10; script committé, paramétré par asset_id, appliqué aux 11 fixtures)
    - docs/project/07_AnimQA.md §7.5 (seuils & calibration table)
    - scripts/update-goldens.mjs (spawn discipline + deterministic ordering analogs); scripts/qa-run.mjs (in-container execution path)
    - src/rpc/contracts/qa-configs.schema.ts (ThresholdsSchema — write-side validation)
  </read_first>
  <action>
    Create `scripts/qa-calibrate.mjs` (executed INSIDE the container through qa:run; parameterized by asset_id, with a default run over ALL 11 fixtures sorted by asset_id — m11): for each fixture, compile on the fly (D-16), run K=10 capture passes of the 4 standard frames via the walker core; per frame-slot compute the 45 pairwise comparePngBuffers counts (threshold from capture-config); derive per-fixture threshold = max-over-slots(p95 of the 45 counts) × MARGIN with MARGIN = 2 (planner pick within D-07 discretion — headroom for single-row antialiasing outliers; the choice and its justification MUST be written in docs/qa.md); write `fixtures/qa/thresholds.json` = { default: max over fixtures of derived values, by_asset: { per-fixture derived } } through writeDeterministicJson with ThresholdsSchema validation before write (D-06: per-asset + default). All diagnostics to stderr, deterministic summary to stdout.
    Create `docs/qa.md`: the calibration method (protocol, K, pairwise noise definition, nearest-rank p95, margin value and justification, capture-config linkage), the resulting per-fixture values table, the Node-24-in-image note (research P4), and the identity-×10 acceptance statement (D-25 link — the full proof automation lands in plan 04-08; at spike time the acceptance is exercised by the spike's own 10 identical captures producing counts ≤ derived thresholds, recorded in the doc).
  </action>
  <verify>
    <automated>npm run qa:run -- node scripts/qa-calibrate.mjs &amp;&amp; npx vitest run src/anim-qa/__tests__/qa-contracts.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spike exits 0 over all 11 fixtures; fixtures/qa/thresholds.json exists, parses through ThresholdsSchema (extend qa-contracts.spec.ts to load it now that it exists), and has by_asset entries for all 11 asset ids
    - Running the spike a second time overwrites thresholds.json with BYTE-identical content (git status clean after re-run — determinism acceptance, D-07)
    - docs/qa.md documents K=10, the pairwise-noise definition, the p95 policy, MARGIN=2 WITH its written justification, and per-fixture values matching the committed thresholds.json
    - No threshold-consuming module existed before this task's commit ordering within the phase (tracer/structural path consumes none — D-07 first-delivered ordering)
  </acceptance_criteria>
  <done>Thresholds are measured artifacts committed as versioned data with a documented method — QA-02's "calibré" is satisfied by construction, not assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser page ← animation data | Page loads only locally-served lottie-web + compiler-produced JSON |
| Container → network | Zero CDN fetches — wasm/lottie-web resolved from node_modules inside the image run |
| Spike → versioned data | thresholds.json written through schema validation + deterministic serializer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-13 | Tampering | CDN supply-chain drift at render time | medium | mitigate | lottie-web served from container-local node_modules (Task 2 action explicitly bans remote URLs); wasm concern handled identically in plan 04-06 |
| T-4-14 | Tampering | Threshold manipulation masking regressions | medium | mitigate | thresholds.json is versioned data + schema-validated + spike-deterministic (re-run byte-identity); manual edits visible in diff and inconsistent with docs/qa.md |
| T-4-15 | Denial of Service | Flaky capture loop stalling CI | medium | mitigate | Event-driven lifecycle only (no wall-clock waits — acceptance-grepped); container testTimeout bounded (plan 04-02) |
| T-4-16 | Information Disclosure | QA page exposing repo paths | low | accept | Page is a local fixture served inside the container for a batch QA run; no external surface (§7.2 threat model: un-pinned rendering is the risk, not local serving) |
</threat_model>

<verification>
- `npx vitest run` green (pure matrices); capture smoke + spike green inside the pinned container via qa:run
- thresholds.json × docs/qa.md consistency; spike re-run byte-identical
- `npx tsc --noEmit` + biome clean
</verification>

<success_criteria>
- Calibration artifact chain complete: measurements → thresholds.json → docs/qa.md (QA-02 "calibré", D-07/m11)
- Walker capture core + QA page production-quality and reused by waves 3–5 (no throwaway spike code)
- Frame selection unified across poster/baseline/walker via canonicalFrame() equality proof (D-08/m4)
</success_criteria>

<output>
Create `.planning/phases/04-anim-qa-pinn-e/04-04-SUMMARY.md` when done
</output>
