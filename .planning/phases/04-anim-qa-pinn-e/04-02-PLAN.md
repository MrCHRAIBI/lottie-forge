---
phase: 04-anim-qa-pinn-e
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - qa-container.lock
  - vitest.config.ts
  - vitest.qa.config.ts
  - scripts/qa-run.mjs
  - .gitignore
  - src/anim-qa/__tests__/env-hygiene.spec.ts
  - src/anim-qa/__tests__/capture-canary.qa.spec.ts
autonomous: false
requirements: [QA-01]
user_setup:
  - service: docker
    why: "Local pinned-container QA execution (qa:run) — qa.yml in CI does not depend on the host daemon"
    env_vars: []
    dashboard_config:
      - task: "Start Docker Desktop (engine is not auto-running on this host)"
        location: "Docker Desktop application"
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The QA container image is pinned by tag AND sha256 digest in qa-container.lock, the single source consumed by both qa:run and qa.yml — no tag@digest reference anywhere else (D-01/B1)"
    - "npm playwright version equals the version extracted from the lock tag, asserted by a test collected by verify (m1)"
    - "qa:run executes npm ci INSIDE the container with a named volume shadowing node_modules — host Windows node_modules is never mounted and zero browsers are installed on the host (D-02/m8)"
    - "QA specs (*.qa.spec.ts) are excluded from the default vitest project and collected only by vitest.qa.config.ts — non-collected is not skipped (D-24/B2)"
    - "A unit-spec scan proves no verify-project spec imports playwright (D-24)"
    - "out/qa/ is gitignored; fixtures/bridge/ is untouched (D-19/m12, M1 audit F-4)"
  artifacts:
    - "qa-container.lock — image=mcr.microsoft.com/playwright, tag=v1.62.1-noble, digest=sha256:dcc5531e97840b9b5e794f2814476b21571c5124a3fca2267d73041f56e7580e, node=24 + refresh procedure comment"
    - "scripts/qa-run.mjs — docker run wrapper, lock parser, engine-down readable failure"
    - "vitest.qa.config.ts — QA-only project, junit fixtures/bridge/vitest-qa-junit.xml, raised testTimeout"
    - "npm aliases qa:run / baseline:update / qa"
    - "src/anim-qa/__tests__/env-hygiene.spec.ts — m1 parity + D-24 scan + default-config exclusion assertions"
  key_links:
    - "qa-container.lock → scripts/qa-run.mjs AND (later) .github/workflows/qa.yml — one source, two consumers (B1)"
    - "playwright npm pin 1.62.1 ↔ lock tag v1.62.1-noble — version-match constraint from official docs (m1)"
prohibitions:
  - statement: "Playwright is never installed or run bare-metal on the host — the npm package exists on the host only so the in-container npm ci resolves the matching version (D-02, §7.2)"
    status: enforced
    verification: explicit
---

<objective>
Pin the QA execution environment: legitimize and install the exact npm dep batch, write the image lock (tag + digest single source), build the shared local/CI docker-run path, and segregate the vitest projects so browser specs never leak into verify. After this plan, every container task in waves 2–5 runs through one invocation path.

Purpose: QA-01's reproducibility is a property of the environment before it is a property of code — same image local/CI, browser build byte-locked by digest.
Output: pinned deps + lock + qa:run + vitest segregation. First plan with a blocking human gate (new npm deps).
</objective>

<execution_context>
@D:/projets/illustrations lottie/systeme/.opencode/gsd-core/workflows/execute-plan.md
@D:/projets/illustrations lottie/systeme/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-anim-qa-pinn-e/04-CONTEXT.md
@.planning/phases/04-anim-qa-pinn-e/04-RESEARCH.md
@.planning/phases/04-anim-qa-pinn-e/04-PATTERNS.md
@vitest.config.ts
@scripts/update-goldens.mjs
@package.json
</context>

## Artifacts this phase produces

(Consolidated inventory in 04-01-PLAN.md — this plan contributes:) `qa-container.lock`; `scripts/qa-run.mjs` (npm alias `qa:run`); `vitest.qa.config.ts`; the exact-pin devDep batch (playwright 1.62.1, pixelmatch 7.2.0, pngjs 7.0.0, @lottiefiles/dotlottie-web 0.79.2, lottie-web 5.13.0, lottie-react 3.1.1, @lottiefiles/dotlottie-vue 0.11.27, react/react-dom/vue peers); `src/anim-qa/__tests__/env-hygiene.spec.ts`; `out/qa/` gitignore entry.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Legitimacy gate — new npm dependency batch (7 direct pins + 3 peers)</name>
  <files>—</files>
  <read_first>
    - 04-RESEARCH.md "Package Legitimacy Audit" table (11 packages, 4 SUS keep-verdicts: @lottiefiles/dotlottie-web, lottie-react, @lottiefiles/dotlottie-vue, vue — publish-recency heuristics on official-org repos, zero postinstall scripts)
    - 04-CONTEXT.md D-09, D-27 (the human-verify gate is mandated by the locked decisions themselves)
    - Phase 3 precedent: svgo+tsx human-verify gate (03-01)
  </read_first>
  <action>
    Present the batch to the human for legitimacy approval BEFORE any install: playwright@1.62.1, pixelmatch@7.2.0, pngjs@7.0.0, @lottiefiles/dotlottie-web@0.79.2, lottie-web@5.13.0, lottie-react@3.1.1, @lottiefiles/dotlottie-vue@0.11.27, plus peers react/react-dom (latest stable 19.x) and vue (latest stable 3.x, exact-pinned at install). This checkpoint is NEVER auto-approvable regardless of auto_advance. The human verifies on npmjs.com/package/&lt;name&gt;: repository owner (microsoft / mapbox / pngjs / LottieFiles / airbnb / Gamote / facebook / vuejs), absence of install scripts, and download volume consistent with the research table.
  </action>
  <what-built>Nothing yet — this is a pre-install legitimacy gate. The deliverable is an explicit human decision on the 10-package batch.</what-built>
  <how-to-verify>For each package, open npmjs.com/package/&lt;name&gt; and confirm: (1) repository points to the official org (microsoft/playwright, mapbox/pixelmatch, pngjs/pngjs, LottieFiles/dotlottie-web, airbnb/lottie-web, Gamote/lottie-react, facebook/react, vuejs/core); (2) no install/postinstall scripts; (3) weekly downloads in the range of the 04-RESEARCH.md legitimacy table. The four SUS-flagged packages (dotlottie-web, lottie-react, dotlottie-vue, vue) are publish-recency heuristics on official repos — the human decides keep/strike.</how-to-verify>
  <resume-signal>Human replies with explicit approval of the batch (or names struck packages). Approval unblocks Task 2 (install). Strike of any package halts the plan and forces replanning of dependents (04-04/04-06/04-07). Never auto-approved.</resume-signal>
  <verify>
    <automated>Human approval recorded in the checkpoint response (blocking; execute-plan halts until approved)</automated>
  </verify>
  <done>Human explicitly approves the 10-package batch (or strikes specific packages — struck packages force replanning of the dependent plans 04-04/04-06/04-07).</done>
</task>

<task type="auto">
  <name>Task 2: Install exact pins + write qa-container.lock (single source B1)</name>
  <files>package.json, package-lock.json, qa-container.lock</files>
  <read_first>
    - 04-RESEARCH.md Q2 (image tag v1.62.1-noble + OCI index digest, refresh procedure verbatim), "Recommended Stack Pins" table, and the single npm install command
    - 04-CONTEXT.md D-01, D-02, m8
    - package.json scripts block (goldens:update alias pattern to replicate)
  </read_first>
  <action>
    Run the research install command with --save-exact: playwright@1.62.1 pixelmatch@7.2.0 pngjs@7.0.0 @lottiefiles/dotlottie-web@0.79.2 lottie-web@5.13.0 lottie-react@3.1.1 @lottiefiles/dotlottie-vue@0.11.27 — pixelmatch and pngjs as regular dependencies (backbone QA primitives, §3.2 research note), the rest as devDependencies; add react/react-dom/vue exact-pinned devDeps for the scaffold peers. package-lock.json committed same-commit. Verify post-install that the host has NO playwright browser download (D-02: the npm package is host-side only so the in-container npm ci resolves the matching version; never run any browser-install command on the host).
    Write `qa-container.lock` at repo root, flat key=value per the research template: image=mcr.microsoft.com/playwright, tag=v1.62.1-noble, digest=sha256:dcc5531e97840b9b5e794f2814476b21571c5124a3fca2267d73041f56e7580e, node=24, plus the refresh procedure as a comment (curl with the OCI-index Accept header against the MCR manifests endpoint → Docker-Content-Digest). This file is THE single source of the image (B1): scripts and CI parse it; no other file may embed tag@digest.
    Add npm scripts: qa:run → node scripts/qa-run.mjs; baseline:update → node scripts/baseline-update.mjs (script lands in plan 04-05 — alias references the future path intentionally, matching the goldens:update alias pattern); qa → node scripts/qa-run.mjs (default full QA suite).
  </action>
  <verify>
    <automated>node -e "const l=require('fs').readFileSync('qa-container.lock','utf8'); if(!/tag=v1\.62\.1-noble/.test(l)||!/digest=sha256:[0-9a-f]{64}/.test(l)) process.exit(1)" && npm ls playwright pixelmatch pngjs --depth=0 && git status --porcelain package-lock.json</automated>
  </verify>
  <acceptance_criteria>
    - qa-container.lock contains tag=v1.62.1-noble and the sha256 digest from RESEARCH Q2, plus the documented refresh comment
    - npm ls prints playwright@1.62.1, pixelmatch@7.2.0, pngjs@7.0.0 exactly; lottie-react 3.1.1 and dotlottie-vue 0.11.27 present in devDependencies
    - package-lock.json modified and committed in the same commit as package.json (verify-chain doctrine)
    - Host node_modules contains no chromium browser directory (package is driver-only on host)
  </acceptance_criteria>
  <done>Exact-pin batch installed with committed lockfile; qa-container.lock is the single image source; npm aliases in place.</done>
</task>

<task type="auto">
  <name>Task 3: qa-run wrapper + vitest segregation + hygiene gates</name>
  <files>scripts/qa-run.mjs, vitest.config.ts, vitest.qa.config.ts, .gitignore, src/anim-qa/__tests__/env-hygiene.spec.ts, src/anim-qa/__tests__/capture-canary.qa.spec.ts</files>
  <read_first>
    - 04-RESEARCH.md Q3 (docker run pattern: --rm --init --ipc=host, named volume for /work/node_modules, npm ci inside, Node 24 finding), Q7 (in-container default shell is sh — no bashisms), P2, P3, P4, P5
    - scripts/update-goldens.mjs lines 1-2, 23-26, 46-47, 106-141 (shebang/SPDX, stdlib-only doctrine, REPO_ROOT from import.meta.url, spawnSync discipline)
    - vitest.config.ts entire file (12 lines — the analog to modify)
    - 04-CONTEXT.md D-02, D-03, D-24, B2, m1, m8; RESEARCH "Runtime State Inventory" (Docker engine may be down — fail readable)
    - scripts/assert-zero-skips.mjs lines 14-15 (usage — consumed by qa.yml in plan 04-08)
  </read_first>
  <action>
    Create `scripts/qa-run.mjs` (stdlib-only, shebang + SPDX header): parse qa-container.lock from REPO_ROOT (import.meta.url discipline) extracting tag and digest; optional passthrough args forwarded to the in-container vitest invocation; preflight `docker info` (or equivalent engine probe) and on failure exit 1 with a readable message naming the fix (start Docker Desktop) — never a stack trace; then exec docker run --rm --init --ipc=host with the repo bind-mounted at /work, workdir /work, the named volume lottie_forge_qa_node_modules shadowing /work/node_modules (m8: protects host win32 native binaries from the in-container npm ci), image ref as image@digest, and sh -c "npm ci && <command>" using sh-compatible syntax only. Diagnostics to stderr; summary to stdout. No hardcoded tag or digest anywhere in the script — only the parsed lock values.
    Modify `vitest.config.ts`: add exclude as [configDefaults.exclude, "src/**/*.qa.spec.ts"] (B2 — QA specs under src/ would otherwise be collected by verify; non-collected must never mean skipped).
    Create `vitest.qa.config.ts`: include only ["src/**/*.qa.spec.ts"], environment node, junit reporter to fixtures/bridge/vitest-qa-junit.xml (joins the gitignored bridge family), testTimeout raised (120000) for browser work, invoked ONLY inside the container by qa.yml/qa:run.
    Append `out/qa/` to .gitignore (D-19/m12 run artifacts). NEVER touch the fixtures/bridge/ entry (M1 audit F-4: intentional, bridge byte-compare depends on it).
    Create `src/anim-qa/__tests__/env-hygiene.spec.ts` (default project): (a) m1 parity — read qa-container.lock, extract the version from the tag via the leading-v regex, read playwright's package.json version from node_modules, assert equality (D-01/m1, real constraint per playwright docker docs); (b) D-24 scan — glob src/**/*.spec.ts (excluding *.qa.spec.ts) and assert none contains a playwright import specifier; (c) exclusion — assert the default resolved config's exclude covers the qa-spec pattern (read vitest.config.ts and assert the pattern string present, plus a canary path assertion).
    Create `src/anim-qa/__tests__/capture-canary.qa.spec.ts` — a trivial passing assertion proving the QA project collects qa specs and ONLY qa specs (this file is the wiring proof; real qa specs arrive in waves 2–5).
  </action>
  <verify>
    <automated>npx vitest run && npx vitest run --config vitest.qa.config.ts && docker info &gt;NUL 2&gt;&1 &amp;&amp; node scripts/qa-run.mjs --check</automated>
  </verify>
  <acceptance_criteria>
    - Default `npx vitest run` is green and does NOT collect the canary (its filename absent from the default run output)
    - `npx vitest run --config vitest.qa.config.ts` collects capture-canary.qa.spec.ts and exits 0 (host-side wiring proof; real browser specs run in-container later)
    - env-hygiene.spec.ts: parity assertion passes (1.62.1 == 1.62.1); scan finds zero playwright imports in unit specs
    - `node scripts/qa-run.mjs --check` exits 0 with the engine running and prints the resolved image@digest parsed from the lock; with the engine stopped it exits 1 with the readable message (verify both states if feasible)
    - `grep -c "out/qa" .gitignore` == 1 and the fixtures/bridge/ entry is byte-unchanged
    - grep of scripts/qa-run.mjs finds zero literal occurrences of the image tag or digest (lock-parsed only)
  </acceptance_criteria>
  <done>One invocation path (qa:run) parses the lock and runs npm ci inside the pinned container with the volume shadow; vitest projects segregated with wiring proof; hygiene gates collected by verify.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → container | Repo bind-mounted read-write into the pinned image; npm ci executes inside |
| Registry → host/CI | mcr.microsoft.com image pulled by digest; npm registry packages installed |
| CI → workflow inputs | qa.yml (plan 04-08) will consume the lock; no secrets surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-SC | Tampering | npm/pip/cargo installs (7 direct + 3 peer packages) | high | mitigate | Package Legitimacy Audit table present in 04-RESEARCH.md; 4 SUS keep-verdicts → blocking checkpoint:human-verify BEFORE install (Task 1, never auto-approvable); zero postinstall verified by research |
| T-4-05 | Tampering | Container image supply-chain drift | high | mitigate | Digest pin (not tag-only) in qa-container.lock consumed by every invocation; refresh procedure documented in the lock |
| T-4-06 | Tampering | Host node_modules corruption via in-container npm ci (win32 native binaries) | medium | mitigate | Named volume lottie_forge_qa_node_modules shadows /work/node_modules (D-02/m8, Task 3) — host binaries never overwritten |
| T-4-07 | Elevation | Container privileges | low | accept | Runs the official Playwright image on trusted repo code, root user per upstream default, no docker.sock mounted, --init for process reaping (research Q3) |
| T-4-08 | Denial of Service | Docker engine down mid-phase | low | mitigate | qa-run preflight probe with readable failure (research Runtime State Inventory) |
</threat_model>

<verification>
- `npx vitest run` green with canary NOT collected; `npx vitest run --config vitest.qa.config.ts` green with canary collected
- `node scripts/qa-run.mjs --check` resolves image@digest from the lock alone
- `npx tsc --noEmit` + biome clean on touched files
</verification>

<success_criteria>
- Pinned environment reproducible local/CI from one lock file (QA-01 environment half, D-01/D-02/D-03 foundations)
- Dependency batch human-legitimized before install (T-4-SC closed)
- Test segregation guarantees verify never touches a browser and qa specs are never silently skipped
</success_criteria>

<output>
Create `.planning/phases/04-anim-qa-pinn-e/04-02-SUMMARY.md` when done
</output>
