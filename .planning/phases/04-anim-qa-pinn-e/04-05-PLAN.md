---
phase: 04-anim-qa-pinn-e
plan: 05
type: execute
wave: 3
depends_on: ["04-03", "04-04"]
files_modified:
  - scripts/baseline-update.mjs
  - fixtures/style-specs/example-style/baseline-frames/index.json
  - fixtures/style-specs/example-style/baseline-frames/a-001.canonical.png
  - src/anim-qa/runner.ts
  - src/anim-qa/__tests__/baseline-update.spec.ts
  - src/anim-qa/__tests__/gate-e2e.qa.spec.ts
  - src/anim-qa/__tests__/pack-verdict.spec.ts
autonomous: true
requirements: [QA-01, QA-02, QA-03]
user_setup:
  - service: docker
    why: "Baseline generation and E2E gate runs execute inside the pinned container"
    env_vars: []
    dashboard_config:
      - task: "Start Docker Desktop"
        location: "Docker Desktop application"
estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Each of the 11 fixtures walks its 4 frames via goToAndStop(n, true) + setSpeed(0) in the pinned container and diffs against its baseline with the per-asset calibrated maxDiffPixels (QA-01/QA-02, SC-1/SC-2)"
    - "Baselines are generated by a local-only refresh that refuses when CI=true, writes atomically, and commits PNGs + sidecar index.json (D-05/D-26)"
    - "A sidecar hash mismatch produces baseline_stale and a missing entry baseline_missing — never a fake pixel:* verdict (D-26/m3)"
    - "Canonical-frame failure rejects the asset immediately without reading the other frames (§7.4/D-08)"
    - "Every completed run persists a QAReport JSON plus relative-pointer PNG artifacts under out/qa/{asset_id}/ with deterministic naming (D-15/D-19/m12)"
    - "packVerdictFromReports: one asset over threshold ⇒ pack failed — no averaging survives a test (QA-03)"
    - "The container tag from qa-container.lock appears in every emitted QAReport.qa_container_tag (SC-1)"
  artifacts:
    - "scripts/baseline-update.mjs (npm alias baseline:update) + baseline-frames/{asset_id}.{frame-tag}.png ×44 + index.json sidecar"
    - "src/anim-qa/runner.ts completed — steps 4–5 wired: walk → capture → diff vs baseline → PixelDiffStats → QAReport"
    - "src/anim-qa/__tests__/gate-e2e.qa.spec.ts — 11-fixture gate E2E in-container"
    - "out/qa/{asset_id}/ artifact layout (gitignored) with relative pointers in the RPC response (D-19/m12)"
  key_links:
    - "baseline:update → walker capture core (04-04) + canonicalFrame — baselines and QA captures identical by construction (same config, same container)"
    - "anim_qa.run walk step (tracer stub) → filled here with NO architectural change (tracer boundary honored)"
    - "QAReport.passed + reason_codes → Phase 7 retry_router consumption (D-17); packVerdictFromReports → orchestrator rollup (QA-03)"
prohibitions:
  - statement: "CI never regenerates baselines — the refresh exits 1 when CI=true; qa.yml (plan 04-08) compares only (D-05/P7)"
    status: enforced
    verification: explicit
---

<objective>
Fill the tracer's walk step with the real browser pipeline and generate the committed baselines it diffs against: frame walk + capture + calibrated pixelmatch on all 11 fixtures, canonical-immediate-reject economics, stale/missing baseline triage before any pixel cost, run artifacts with relative pointers, and the per-asset verdict policy that makes "one bad asset fails the pack" structural.

Purpose: this is QA-01/QA-02/QA-03 becoming executable behavior — the gate measures instead of promising.
Output: 44 committed baseline PNGs + sidecar; completed runner; green in-container E2E over the full fixture set.
</objective>

<execution_context>
@D:/projets/illustrations lottie/systeme/.opencode/gsd-core/workflows/execute-plan.md
@D:/projets/illustrations lottie/systeme/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-anim-qa-pinn-e/04-CONTEXT.md
@.planning/phases/04-anim-qa-pinn-e/04-RESEARCH.md
@.planning/phases/04-anim-qa-pinn-e/04-PATTERNS.md
@docs/project/07_AnimQA.md
@src/anim-qa/runner.ts
@src/anim-qa/frame-walker.ts
@src/anim-qa/baseline.ts
@src/anim-qa/diff.ts
@scripts/update-goldens.mjs
@src/shared/format.ts
</context>

## Artifacts this phase produces

(Consolidated inventory in 04-01-PLAN.md — this plan contributes:) `scripts/baseline-update.mjs` (alias baseline:update); `fixtures/style-specs/example-style/baseline-frames/` — 44 PNGs + `index.json` sidecar; the completed `src/anim-qa/runner.ts` (steps 4–5, out/qa artifact writer); `packVerdictFromReports` policy pin; `gate-e2e.qa.spec.ts` (11-fixture in-container E2E); `pack-verdict.spec.ts`.

<tasks>

<task type="auto">
  <name>Task 1: baseline:update — CI-guarded local regeneration + committed sidecar (D-05/D-26)</name>
  <files>scripts/baseline-update.mjs, fixtures/style-specs/example-style/baseline-frames/index.json</files>
  <read_first>
    - scripts/update-goldens.mjs lines 144-154 (CI=true refusal — copy near-verbatim), 68-80 (asset_id-sorted ordering), 169-201 (atomic collect-then-write two-pass)
    - 04-CONTEXT.md D-05, D-26, m3; 04-PATTERNS.md "scripts/baseline-update.mjs — CREATE" section
    - src/anim-qa/frame-walker.ts (capture core), src/anim-qa/canonical-frame.ts (frame order), src/shared/format.ts (sidecar bytes)
    - fixtures/qa/capture-config.json + src/anim-qa/baseline.ts (sidecar field contract: lottie_sha256 + captureConfig_hash + thresholds/dark-theme hashes traceability-only)
  </read_first>
  <action>
    Create `scripts/baseline-update.mjs` executed INSIDE the pinned container via the qa:run path (D-02/D-05 same execution path): refuse when CI=true with the near-verbatim goldens-style refusal naming the doctrine (CI compares, never regenerates); iterate fixtures/render-specs sorted by asset_id; compile on the fly per fixture (D-16 chain compile → sanitize in-process); compute lottie sha256 via node:crypto; capture the 4 frames through the walker core with the versioned capture-config; write PNGs to fixtures/style-specs/example-style/baseline-frames/{asset_id}.{canonical|p25|p50|p75}.png; collect everything in memory and write only after ALL fixtures succeed (atomic two-pass); write `index.json` sidecar via writeDeterministicJson — per asset: lottie_sha256 + captureConfig_hash, plus thresholds-hash and dark-theme-hash recorded for traceability (these two NEVER feed the stale predicate, m3). Deterministic ordering and bytes throughout; exit 0 with a stdout summary.
    Run the refresh locally (Docker precondition) and COMMIT the 44 baseline PNGs + index.json — baselines are versioned data born in Phase 4 (D-05/Ph2 D-05).
  </action>
  <verify>
    <automated>npm run qa:run -- node scripts/baseline-update.mjs &amp;&amp; node scripts/baseline-update.mjs &amp;&amp; git status --porcelain fixtures/style-specs/example-style/baseline-frames/</automated>
  </verify>
  <acceptance_criteria>
    - baseline-frames/ contains exactly 44 PNGs (11 × 4 frame tags) + index.json, all committed
    - Running the refresh twice leaves git status clean (byte-stable regeneration)
    - CI=true node scripts/baseline-update.mjs exits 1 with the refusal message (P7 guard proven)
    - index.json parses through the sidecar shape and records captureConfig_hash per asset; thresholds/dark-theme hashes present but non-gating (m3 matrix from 04-03 already pins the semantics)
  </acceptance_criteria>
  <done>Baselines exist as committed, byte-stable, CI-irregenerable versioned data with a hash sidecar enabling clean triage.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the runner — walk → capture → calibrated diff → QAReport + out/qa artifacts (fills tracer stub)</name>
  <files>src/anim-qa/runner.ts, src/anim-qa/__tests__/gate-e2e.qa.spec.ts</files>
  <read_first>
    - 04-CONTEXT.md D-08 (canonical immediate reject), D-15, D-16, D-17, D-19, m12, §7.3/§7.4
    - 04-RESEARCH.md architecture data-flow diagram (§ Architecture Patterns) and P5 (stdout discipline), P8 (float determinism)
    - src/anim-qa/runner.ts (tracer skeleton — fill the walk step ONLY, no restructuring), src/anim-qa/{frame-walker,diff,baseline,report}.ts signatures
    - 04-RESEARCH.md Q4 (stats semantics), 04-03-PLAN.md Task 1 (the stub boundary being filled)
  </read_first>
  <action>
    Fill the tracer's walk step in `src/anim-qa/runner.ts` (same function signature, no architectural change): after steps 1–3 pass and resolveBaseline returns ok — resolve the recipe (op/duration and keyframe_shape) for frame selection via canonicalFrame/frameWalkFrames; launch the walker once per asset; capture the 4 frames; diff canonical FIRST — count &gt; applied_max_diff_pixels (thresholds.json by_asset falling back to default) → immediate reject with reason pixel:canonical and NO further frames read (§7.4 economics); otherwise diff the 3 sampled frames, compute PixelDiffStats via computePixelDiffStats, and fail with pixel:p95 when stats breach (frames_above_tolerance &gt; 0 or p95 &gt; applied threshold — pick the frames_above_tolerance rule and document it in the module doc-comment). On pass or pixel-fail, build the full QAReport (passed, renderer literal, frame_count 4, frame_hashes = sha256 of each captured PNG in m4 order, pixel_diff, theme smoke placeholder-free — theme step wired in plan 04-06 with its own codes, qa_container_tag from the lock, applied_max_diff_pixels, theme_smoke_path, injected timestamp) and write run artifacts: out/qa/{asset_id}/{frame-tag}.png + diff PNGs + report.json (writeDeterministicJson), returned as RELATIVE pointers in the response envelope (D-19/m12 — deterministic naming, gitignored directory). Output dir fixed at server activation, never per-request (D-18/D-19).
    Create `src/anim-qa/__tests__/gate-e2e.qa.spec.ts` (qa project, in-container): spawn-free in-process E2E — for each of the 11 fixtures: compile on the fly, call the runner pipeline, assert passed=true, frame_count=4, frame_hashes length 4 lowercase-hex, qa_container_tag equals the lock tag, applied_max_diff_pixels matches thresholds.json, report.json on disk re-parses through QAReportSchema, and relative pointers exist under out/qa/. Assert two consecutive full runs produce byte-identical report.json files (timestamp injected constant for the test — D-15/D-25 groundwork).
  </action>
  <verify>
    <automated>npm run qa:run -- npx vitest run --config vitest.qa.config.ts gate-e2e &amp;&amp; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - gate-e2e.qa.spec.ts exits 0 with 11/11 assets passed inside the pinned container
    - Every emitted QAReport.qa_container_tag == v1.62.1-noble (SC-1) and applied_max_diff_pixels == thresholds.json value for that asset
    - Canonical-immediate-reject: a spec case with a perturbed canonical frame returns pixel:canonical with zero captures of the sampled frames (economics asserted, §7.4)
    - out/qa/ artifacts use relative paths under the fixed output root; report.json bytes identical across two identical runs
    - Default vitest project stays green (unit suite untouched by the browser code)
  </acceptance_criteria>
  <done>The full gate walks, captures, diffs and reports on all 11 fixtures deterministically in the pinned container — QA-01/QA-02 behavior live end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: QA-03 verdict policy + stale-baseline triage proof (no averaging; no fake pixel codes)</name>
  <files>src/anim-qa/__tests__/pack-verdict.spec.ts, src/anim-qa/__tests__/gate-e2e.qa.spec.ts</files>
  <read_first>
    - 04-CONTEXT.md D-26/m3 (mismatch ≠ broken asset — sidecar triages BEFORE any diff), QA-03 definition in REQUIREMENTS.md, §7.5 pack row
    - src/anim-qa/report.ts (packVerdictFromReports — matrix pinned in 04-03 Task 2; extend here with the E2E-level proof)
    - src/anim-qa/baseline.ts + the real committed index.json (stale-path E2E)
  </read_first>
  <action>
    Extend `pack-verdict.spec.ts`: policy-level cases — a set where one asset exceeds its own threshold fails the pack even when every other asset passes with zero diffs AND the arithmetic mean across assets is far below any threshold (the averaging prohibition made red); all-pass ⇒ pass; assert the policy function never reads pixel counts beyond per-asset thresholds (no global normalization constant exists — grep-level assertion that no pack-level mean/p95 is computed).
    Extend `gate-e2e.qa.spec.ts` with the triage cases: (a) temporarily point the resolver's expected lottie hash at a wrong value (synthetic stale input via the pure resolver seam) → envelope ok:false code baseline_stale with ZERO png captures taken (assert no out/qa writes for the asset, D-26 "before any diff"); (b) unknown asset_id → baseline_missing; (c) restore → full pass. These prove the retry_router-facing diagnostics are clean codes, never fabricated pixel verdicts.
  </action>
  <verify>
    <automated>npx vitest run src/anim-qa/__tests__/pack-verdict.spec.ts &amp;&amp; npm run qa:run -- npx vitest run --config vitest.qa.config.ts gate-e2e</automated>
  </verify>
  <acceptance_criteria>
    - pack-verdict.spec.ts proves one-over-threshold ⇒ pack failed with an explicit "mean-below-threshold" counter-case (QA-03, prohibition #1)
    - Stale/missing triage cases return the closed codes with zero captures and zero artifact writes for the affected asset
    - 11/11 pass case still green after the triage cases restore state
  </acceptance_criteria>
  <done>QA-03's pack policy and D-26's clean triage are test-pinned at both unit and E2E levels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Baseline files → gate | Committed PNGs + sidecar are the trust anchor for comparison; hashes triage before diffs |
| Runner → filesystem | Writes confined to the server-activated output root with regex-gated asset ids |
| Container → repo | Baseline refresh mutates only fixtures/style-specs/example-style/baseline-frames/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-17 | Tampering | Baseline poisoning (quietly regenerating to mask regressions) | high | mitigate | CI=true refusal (Task 1, P7) + refresh only via the local container path + baselines are committed diff-visible data (D-05) |
| T-4-18 | Tampering | Path traversal via asset_id into out/qa/{asset_id} | low | mitigate | asset_id regex ^a-\d{3}$ at the RPC entry; output naming built only from the validated id + fixed frame tags (m12) |
| T-4-19 | Repudiation | Fake pixel verdict masking a stale baseline | medium | mitigate | Sidecar predicate decides BEFORE any capture/diff (Task 3 triage cases assert zero captures on stale) |
| T-4-20 | Denial of Service | Canonical-reject economy bypassed → cost blowup on broken sets | low | mitigate | Immediate-reject after canonical diff asserted by spec (§7.4) |
| T-4-21 | Information Disclosure | Artifact pointers leaking absolute paths | low | mitigate | D-19/m12: relative paths only, deterministic naming; asserted by the E2E pointer checks |
</threat_model>

<verification>
- `npm run qa:run` full container suite green (gate-e2e 11/11 + smoke); `npx vitest run` unit green
- Baseline set committed and byte-stable across regeneration; CI refusal proven
- Full bridge chain green (`python -m pytest tests/ -q` after report-contract consumers changed nothing)
</verification>

<success_criteria>
- SC-1 half: every Lottie walks frames in the pinned container with the tag recorded (QA-01)
- SC-2: calibrated per-asset diffs with canonical + 3 sampled frames (QA-02); QA-03 policy structural
- SC-4 behavior: reports written only after structural validation, persisted as deterministic run artifacts (QA-04)
</success_criteria>

<output>
Create `.planning/phases/04-anim-qa-pinn-e/04-05-SUMMARY.md` when done
</output>
