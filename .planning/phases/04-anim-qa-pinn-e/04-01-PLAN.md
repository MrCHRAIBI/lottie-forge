---
phase: 04-anim-qa-pinn-e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lottie_forge/domain/qa_report.py
  - src/rpc/contracts/qa-report.schema.ts
  - src/rpc/contracts/qa-report.spec.ts
  - src/rpc/contracts/qa-configs.schema.ts
  - src/rpc/contracts/rejection-cases.ts
  - src/rpc/server.ts
  - src/rpc/server.spec.ts
  - lottie_forge/rpc/client.py
  - tests/bridge/rejection_loader.py
  - tests/bridge/test_qa_report_bridge.py
  - fixtures/rejection-cases/qa-report.json
  - fixtures/qa/capture-config.json
  - fixtures/qa/dark-theme.json
  - src/anim-qa/__tests__/qa-contracts.spec.ts
  - src/anim-qa/spawn-counter.ts
autonomous: true
requirements: [QA-04]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every QAReport emitted this phase re-parses in strict Pydantic AND strict zod with zero drift — ordered bridge chain green (QA-04, D-14)"
    - "reason_codes is non-empty if and only if passed === false, enforced by cross-field invariant on BOTH sides (SC-4, D-13)"
    - "The anim_qa.run request schema accepts only {lottie, asset_id, renderer_support} — zero path fields, unknown keys reject (D-18)"
    - "The RPC error-code set is exactly 10 literals, mirrored same-commit across server.ts, client.py and rejection-cases.ts, and the exact set is asserted by a test (D-17)"
    - "Running anim_qa.run twice on the same input yields byte-identical QAReports excluding timestamp — the report builder is pure and compile is byte-deterministic per Phase 3 D-26 (COVERAGE QA-04 idempotency probe, resolved explicit)"
    - "theme_diff_pct + theme_smoke_path are nullable ONLY in the pre-theming state (documented transient §7.6 deviation, P4-001 resolution (a)); plan 04-06 tightens both sides same-commit when gate step 5 lands — phase-final state is §7.6-verbatim + D-20"
    - statement: "Per-asset QA runs serially (§7.11): an interrupted or concurrent scenario never corrupts a later identical run — artifacts are run-local under out/qa/ and deterministic per D-15 (COVERAGE QA-04 concurrency probe, resolved backstop)"
      verification: backstop
  artifacts:
    - "lottie_forge/domain/qa_report.py — PixelDiffStats + QAReport §7.6 verbatim + applied_max_diff_pixels + theme_smoke_path/theme_diff_pct (nullable until 04-06's same-commit tightening, then §7.6-verbatim)"
    - "src/rpc/contracts/qa-report.schema.ts — QAReportSchema + PixelDiffStatsSchema + ReasonCodeSchema(7) + ThemeSmokePathSchema(2) + AnimQaRequestSchema"
    - "src/rpc/contracts/qa-configs.schema.ts — ThresholdsSchema + DarkThemeSchema + CaptureConfigSchema"
    - "fixtures/rejection-cases/qa-report.json — shared rejection harness cases (D-08 format)"
    - "fixtures/qa/capture-config.json + fixtures/qa/dark-theme.json — versioned QA data (D-22/D-23/D-10)"
    - "RPC_ERROR_CODES extended 8→10 with baseline_missing + baseline_stale (3 sites same-commit)"
    - "src/anim-qa/spawn-counter.ts — wave-1-owned chromium spawn counter (m7), imported by runner (04-03) and frame-walker (04-04) — removes the undeclared same-wave 04-03↔04-04 coupling (P4-002)"
  key_links:
    - "QAReportSchema.zod ↔ QAReport Pydantic — field-for-field, snake_case identical, same-commit extension rule §4.14"
    - "AnimQaRequestSchema.renderer_support ↔ RendererSupportSchema (motion-compiler.schema.ts) — imported, never redeclared (single source)"
    - "DarkThemeSchema keys ↔ THEME_ANCHOR_IDS closed vocabulary (vocabulary.schema.ts)"
prohibitions:
  - statement: "Pixel diffs are never averaged across assets — one asset over its own maxDiffPixels fails, and no mean or pack-level average ever relaxes a verdict (QA-03)"
    status: enforced
    verification: explicit
  - statement: "CI never regenerates baselines — the baseline refresh refuses when CI=true and qa.yml compares only (D-05)"
    status: enforced
    verification: explicit
  - statement: "QA never repairs and never suggests fixes — QAReports carry only closed-vocabulary reason codes, never free-form remediation text (§7 principle 6)"
    status: enforced
    verification: explicit
---

<objective>
Freeze every Phase 4 contract BEFORE any QA code exists: the QAReport crossing contract (Pydantic §7.6 verbatim + zod strictObject mirror + shared rejection harness — theme fields nullable until 04-06's same-commit tightening, a documented pre-theming deviation per P4-001 resolution (a)), the closed `anim_qa.run` request shape (zero paths, D-18), the RPC error-code extension 8→10, and the three versioned QA config contracts with their fixtures. This is the Phase-3 "contrats gelés" pattern replicated: downstream plans (tracer, calibration, runner, theming, CI) all consume these closed shapes.

Purpose: QA-04's "validation structurelle avant tout diff pixel" starts with the contract itself; the Phase 5 store (MFT-01) will re-parse a contract already proven both sides.
Output: 4 contracts + 2 fixtures + rejection harness extension + 10-code parity, bridge chain green.

> **Lean execution (checker P4-005):** self-estimate 70k/100k tokens at confidence: low — batch the read_first reads, never re-read a file already in context, keep verify runs targeted.
</objective>

<execution_context>
@D:/projets/illustrations lottie/systeme/.opencode/gsd-core/workflows/execute-plan.md
@D:/projets/illustrations lottie/systeme/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-anim-qa-pinn-e/04-CONTEXT.md
@.planning/phases/04-anim-qa-pinn-e/04-RESEARCH.md
@.planning/phases/04-anim-qa-pinn-e/04-PATTERNS.md
@docs/project/07_AnimQA.md
@src/rpc/contracts/motion-compiler.schema.ts
@src/rpc/server.ts
@lottie_forge/rpc/client.py
@src/rpc/contracts/rejection-cases.ts
@tests/bridge/rejection_loader.py
</context>

## Artifacts this phase produces

Modules `src/anim-qa/{structural,baseline,report,diff,canonical-frame,theming,frame-walker,runner}.ts`; pure functions `canonicalFrame()`, `frameWalkFrames()`, `comparePngBuffers()`, `countRegionDiff()`, `computePixelDiffStats()`, `packVerdictFromReports()`, `resolveBaseline()`, `slotizeForTheme()`, `applyTheme()`; RPC method `anim_qa.run`; RPC codes `baseline_missing`/`baseline_stale` (set 8→10); contracts `QAReportSchema`, `PixelDiffStatsSchema`, `ReasonCodeSchema`, `ThemeSmokePathSchema`, `AnimQaRequestSchema`, `ThresholdsSchema`, `DarkThemeSchema`, `CaptureConfigSchema`; Pydantic `QAReport`/`PixelDiffStats`; fixture `fixtures/rejection-cases/qa-report.json`; data `fixtures/qa/{thresholds,dark-theme,capture-config}.json`; baselines `fixtures/style-specs/example-style/baseline-frames/` (+`index.json`); scripts `scripts/{qa-run,qa-calibrate,baseline-update,qa-flake-proof}.mjs`; npm aliases `qa:run`/`baseline:update`; config `qa-container.lock`, `vitest.qa.config.ts`, `.github/workflows/qa.yml`; docs `docs/qa.md`; reason-code vocabulary `structural:schema, feature:3D, stretch:negative, shape:layers, pixel:canonical, pixel:p95, theme:noop`; theme-smoke vocabulary `dotlottie-setTheme | applyTheme-svg`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QAReport crossing contract — Pydantic §7.6 verbatim + zod mirror + shared rejection harness</name>
  <files>lottie_forge/domain/qa_report.py, src/rpc/contracts/qa-report.schema.ts, src/rpc/contracts/qa-report.spec.ts, fixtures/rejection-cases/qa-report.json, src/rpc/contracts/rejection-cases.ts, tests/bridge/rejection_loader.py, tests/bridge/test_qa_report_bridge.py</files>
  <read_first>
    - docs/project/07_AnimQA.md lines 79-105 (QAReport + PixelDiffStats §7.6 verbatim — copy field-for-field)
    - 04-PATTERNS.md sections "qa-report.schema.ts — CREATE" and "rejection-cases.ts + rejection_loader.py — MODIFY" (verbatim analogs + line refs)
    - lottie_forge/domain/style.py + lottie_forge/domain/_shared.py (STRICT_CONFIG, Annotated Field ge/le, model_validator conventions, docstring names TS mirror path)
    - src/rpc/contracts/motion-compiler.schema.ts lines 652-671 (strictObject + superRefine template) and lines 269-279 (closed enum template)
    - fixtures/rejection-cases/render-spec.json (case shape: case_id/ref/model/expect_code/expect_paths/payload — path-membership only, never message text, D-08)
    - 04-CONTEXT.md D-13, D-14, B3, m4
  </read_first>
  <action>
    Create `lottie_forge/domain/qa_report.py`: `PixelDiffStats` (max int ge 0, mean float ge 0, p95 int ge 0, frames_above_tolerance int ge 0) and `QAReport` (passed bool, renderer Literal["lottie-web-svg"], frame_count int ge 1, frame_hashes list of strings, pixel_diff: PixelDiffStats, theme_diff_pct float ge 0 le 100, reason_codes list default empty, qa_container_tag str, timestamp datetime) — §7.6 verbatim field names snake_case, STRICT_CONFIG on every model. Additive fields same-commit (pattern D-16 Ph 2 / B3): `applied_max_diff_pixels` int ge 0 (required). Theme fields NULLABLE as a documented transient deviation from §7.6-verbatim (P4-001 resolution (a)): `theme_diff_pct` float ge 0 le 100 | None and `theme_smoke_path` Literal["dotlottie-setTheme","applyTheme-svg"] | None, default None on BOTH sides (Pydantic `| None` + zod nullable) — the theme smoke measurement only exists once plan 04-06 wires gate step 5, and no unmeasured or sentinel value is ever emitted (D-20: the mechanism is measured, never skipped). Plan 04-06 restores the §7.6-verbatim required form on BOTH sides same-commit with step-5 landing; the phase-final state satisfies §7.6 verbatim + D-20. Cross-field `@model_validator(mode="after")`: reason_codes non-empty exactly when passed is False; frame_count equals len(frame_hashes) (planner invariant per D-08/m4). timestamp stays injectable and is excluded from any content hash (D-15). Module docstring names the TS mirror path.
    Create `src/rpc/contracts/qa-report.schema.ts`: `ReasonCodeSchema` = z.enum of exactly the 7 literals structural:schema, feature:3D, stretch:negative, shape:layers, pixel:canonical, pixel:p95, theme:noop (D-13 closed set); `ThemeSmokePathSchema` = z.enum of the 2 literals; `PixelDiffStatsSchema` and `QAReportSchema` as z.strictObject mirroring the Pydantic field-for-field, frame_hashes items matched against lowercase-sha256 regex, timestamp as ISO datetime string; one `.superRefine` carrying BOTH cross-field invariants (reason-codes-vs-passed biconditional; frame_count === frame_hashes.length). Import nothing duplicated: renderer Literal inline is fine here, but do NOT redeclare RendererSupportSchema.
    Create `fixtures/rejection-cases/qa-report.json` (D-08 format, case_id kebab-case with qa-report- prefix, model "QAReport", path-membership-only expect_paths): at minimum — unknown reason code; empty reason_codes with passed=false; non-empty reason_codes with passed=true; unknown theme_smoke_path value; negative applied_max_diff_pixels; uppercase frame hash; missing qa_container_tag; theme_diff_pct above 100; extra unknown top-level key; frame_count mismatch vs frame_hashes length. The bridge-chain VALID exported sample — exported by the new `tests/bridge/test_qa_report_bridge.py` (the repo's test_{contract}_bridge.py convention with a test_export_qa_report leg, matching the `-k qa_report` collection in this task's verify) — omits both theme fields (proving the nullable pre-theming state parses on both sides); no rejection case asserts their absence at this wave — that case is added by 04-06's tightening (P4-001 (a)). Register `"qa-report": "qa-report.json"` in BOTH loader maps same-commit (TS CONTRACT_FILES in rejection-cases.ts; Python CONTRACT_FILES in rejection_loader.py) — preserve each file's own existing inventory, add nothing else.
  </action>
  <verify>
    <automated>python -m pytest tests/ -k qa_report -q && npx vitest run src/rpc/contracts && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ -k qa_report -q` exits 0 with every fixture case parametrized (visible ids) and zero skipped
    - vitest test.each over the same qa-report.json cases exits 0, same count both sides (write the count into the spec doc-comment) — the test lives in the NEW `src/rpc/contracts/qa-report.spec.ts` (per-contract spec convention: follow `asset-spec.spec.ts`'s `loadRejectionCases` + `it.each` pattern; it is covered by this task's `npx vitest run src/rpc/contracts` verify path; 04-06 T2 extends it when the tightening lands)
    - The ordered bridge chain (pytest export → vitest → pytest re-import) is green including one VALID exported QAReport sample round-tripping byte-identically
    - `grep -c "strictObject" src/rpc/contracts/qa-report.schema.ts` ≥ 2 and the 7-code enum appears exactly once in the file
    - Theme fields absent in the exported VALID sample parse both sides (pre-theming nullable state, P4-001 resolution (a)); requiredness is restored by 04-06's same-commit tightening
  </acceptance_criteria>
  <done>QAReport + PixelDiffStats exist both sides §7.6-verbatim with the two additive fields, 10+ shared rejection cases green in mirrored suites, loader maps extended same-commit, full pytest suite green.</done>
</task>

<task type="auto">
  <name>Task 2: RPC codes 8→10 + AnimQaRequest closed schema (zero paths, D-18)</name>
  <files>src/rpc/server.ts, lottie_forge/rpc/client.py, src/rpc/contracts/rejection-cases.ts, src/rpc/server.spec.ts, src/rpc/contracts/qa-report.schema.ts, src/anim-qa/spawn-counter.ts</files>
  <read_first>
    - 04-PATTERNS.md "Shared Patterns #4" (three-site same-commit rule, server.ts:86-95 ↔ client.py:73-84 ↔ rejection-cases.ts:48-57) and server.spec.ts closed-set assertion convention
    - src/rpc/server.ts lines 86-97 (current 8-code set), lottie_forge/rpc/client.py lines 71-84 (tuple mirror + docstring rule)
    - 04-RESEARCH.md Q9 (dispatch/verdict-≠-transport context for later plans) and Q8 (REJECTION_EXPECT_CODES extension requirement)
    - 04-CONTEXT.md D-17 (code set extension), D-18 (request shape), B5
  </read_first>
  <action>
    Extend the closed RPC code set 8→10 by appending `baseline_missing` and `baseline_stale` — in src/rpc/server.ts RPC_ERROR_CODES, lottie_forge/rpc/client.py RPC_ERROR_CODES, and src/rpc/contracts/rejection-cases.ts REJECTION_EXPECT_CODES — all three in ONE commit (the client.py docstring rule). Update the server.spec.ts exact-set assertion to expect the 10 literals (the old 8-literal assertion breaks otherwise).
    Add `AnimQaRequestSchema` to qa-report.schema.ts: z.strictObject with exactly three keys — lottie (reuse LottieJSONSchema imported from motion-compiler.schema.js), asset_id (the existing ASSET_ID_PATTERN regex from asset-spec.schema.js, reused not redeclared), renderer_support (RendererSupportSchema imported from motion-compiler.schema.js). Zero filesystem-path fields — this is D-18's injection-free surface and it is consumed verbatim by the orchestrator in Phase 7 (reversibility: costly). Export the inferred type. Unknown keys reject by construction; no .passthrough, no coercion anywhere.
    Create `src/anim-qa/spawn-counter.ts` — the wave-1 counter owner (P4-002): exported `chromiumSpawnCount` (int, starts 0) plus `incrementChromiumSpawnCount()`, zero dependencies. runner.ts (plan 04-03) and frame-walker.ts (plan 04-04) both import it instead of either wave-2 plan owning the counter — this removes the undeclared same-wave 04-03↔04-04 coupling flagged by the checker.
  </action>
  <verify>
    <automated>npx vitest run src/rpc && python -m pytest tests/rpc -q</automated>
  </verify>
  <acceptance_criteria>
    - server.spec.ts asserts the exact sorted 10-literal code set and passes
    - A spec case proves a request payload containing an extra output-path-like key is rejected by AnimQaRequestSchema (strictObject unrecognized key)
    - A spec case proves renderer_support accepts only the two closed enum values
    - Python parity test asserts client.RPC_ERROR_CODES has length 10 and matches the TS set (fixture-driven or duplicated literal assertion consistent with existing parity tests)
  </acceptance_criteria>
  <done>All three code-set sites extended same-commit, exact-set tests green both sides, AnimQaRequestSchema closed with zero path surface.</done>
  <reversibility rating="costly">D-18: the request shape is consumed as-is by the Phase 7 orchestrator; changing it later means a two-sided transport migration. Flagged, not blocking.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Versioned QA config contracts + fixtures (thresholds/dark-theme/capture-config)</name>
  <files>src/rpc/contracts/qa-configs.schema.ts, fixtures/qa/capture-config.json, fixtures/qa/dark-theme.json, src/anim-qa/__tests__/qa-contracts.spec.ts</files>
  <read_first>
    - 04-CONTEXT.md D-06, D-10, D-22, D-23, m3 (captureConfig_hash feeds the sidecar), m6 (dark-theme schema spec)
    - src/rpc/contracts/vocabulary.schema.ts (THEME_ANCHOR_IDS closed list — import source)
    - fixtures/recipe-catalogue/catalogue.json + its startup zod parse pattern (versioned-data-as-source-of-truth)
    - fixtures/render-specs/ listing (11 fixtures — anchor coverage input for the m6 spec)
    - 04-RESEARCH.md Q2 (pixelmatch threshold 0..1 default 0.1) and D-07 support note (threshold recorded in captureConfig)
  </read_first>
  <action>
    Create `src/rpc/contracts/qa-configs.schema.ts` with three strict schemas, all strictObject, no coercion: `ThresholdsSchema` = { default: int ge 0, by_asset: record keyed by the asset-id regex of int ge 0 } (D-06); `DarkThemeSchema` = { scope: z.literal("qa-only"), colors: record keyed by ThemeAnchorId of 3-tuple 0..1 floats } with a superRefine that every color key is a member of the closed THEME_ANCHOR_IDS vocabulary (D-10/m6); `CaptureConfigSchema` = { viewport: {width literal 400, height literal 300}, deviceScaleFactor literal 1, background literal "#ffffff", pixelmatch_threshold number 0..1 } (D-22/D-23; the threshold field is versioned so the sidecar captureConfig_hash m3 changes whenever diff semantics change).
    Commit `fixtures/qa/capture-config.json` with exactly the locked values (400×300, dsf 1, background #ffffff, pixelmatch_threshold 0.1) and `fixtures/qa/dark-theme.json` with scope qa-only and strong-contrast colors covering EVERY anchor present across the 11 render-spec fixtures (luminances inverted relative to the neutral gray fills; QA-only, explicitly non-product — D-10). thresholds.json is deliberately NOT created here: it is spike-derived (D-07) and lands in plan 04-04.
    Create `src/anim-qa/__tests__/qa-contracts.spec.ts` (default vitest project, no playwright import): parse both fixtures from disk with their schemas (fail-loud), assert dark-theme color keys ⊆ THEME_ANCHOR_IDS, assert coverage of the anchor set actually used by the 11 fixtures, assert scope literal, and assert capture-config equals the locked values.
  </action>
  <verify>
    <automated>npx vitest run src/anim-qa/__tests__/qa-contracts.spec.ts && npx tsc --noEmit && npx @biomejs/biome check src/rpc/contracts src/anim-qa</automated>
  </verify>
  <acceptance_criteria>
    - qa-contracts.spec.ts exits 0 with the anchor-coverage assertion listing the covered anchors in its doc-comment
    - Both fixtures parse through their strict schemas; feeding an extra key to any of the three schemas in a spec case rejects
    - `fixtures/qa/thresholds.json` does NOT exist after this task (spike-derived, D-07)
  </acceptance_criteria>
  <done>Three config contracts + two versioned fixtures committed with spec coverage; threshold data intentionally deferred to the calibration spike.</done>
</task>

</tasks>

## Flagged Assumptions (source-audit probes + RESEARCH assumptions log)

The deterministic edge-coverage probe left three requirement rows `unclassified` (QA-01/02/03 — no probe pattern matched). Per spec-less protocol they stay UNRESOLVED and are surfaced here as explicit flagged assumptions, never silently dropped:

- **QA-01 (unclassified)**: container-render determinism rests on the digest-pinned image + software rasterizer + event-driven capture; the residual antialiasing noise is MEASURED empirically by the D-07 spike (plan 04-04) rather than assumed — if the spike observes non-zero noise, thresholds absorb it; if captures ever diverge beyond thresholds, the D-25 identity proof reddens (plan 04-08).
- **QA-02 (unclassified)**: crop-based region diff (row-sliced RGBA subarray + per-crop pixelmatch) is assumed equal to "pixels in region" semantics — RESEARCH A2; pinned by a dedicated unit test in 04-04 Task 1.
- **QA-03 (unclassified)**: the pack-level rollup ("one asset over threshold = pack failed") is enforced per-asset THIS phase via `packVerdictFromReports`; the orchestrator (Phase 7) is the consumer of `passed` + `reason_codes` — no pack aggregation exists here to average over (D-17 flow).
- **A1 (RESEARCH)**: dotlottie-web 0.79.2's bundled wasm assumed to behave like main on sid-slots — settled at the pinned version by the first theming container run (04-06); documented same-commit refresh to 0.80.0 if it differs.
- **A3 (RESEARCH)**: `renderer.elements[i]` ↔ layers order assumed for DOM mask resolution — pinned by the mask-vs-ink sanity spec (04-06); analytic bboxes from RenderSpec geometry is the documented fallback.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC caller → server | NDJSON request lines parsed and zod-validated at entry |
| Request fields → filesystem | D-18: request carries zero path fields; nothing caller-controlled resolves to a path |
| Fixture files → contracts | Versioned JSON loaded at startup/test time — fail-loud parsing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-01 | Tampering/Elevation | anim_qa.run request fields | low | mitigate | Closed AnimQaRequestSchema with zero path fields (D-18); asset_id regex-gated; implemented in Task 2 |
| T-4-02 | Repudiation | reason_codes semantics | medium | mitigate | Biconditional reason_codes ⟺ passed enforced structurally both sides (Task 1 superRefine/model_validator) — a report cannot fail silently |
| T-4-03 | Tampering | Closed code vocabulary drift | medium | mitigate | 10-code set mirrored same-commit across 3 sites + exact-set assertions (Task 2) |
| T-4-04 | Information Disclosure | Free-form text in reports | low | accept | reason_codes is a closed 7-literal enum; no free-text field exists in the contract (prohibition #3) |
| T-4-SC | Tampering | npm/zod supply chain | high | mitigate | Covered by the phase-level human-verify gate in plan 04-02 (blocking checkpoint before install); no new deps installed in this plan |
</threat_model>

<verification>
- Ordered bridge chain green: `python -m pytest tests/ -k export -q` → `npx vitest run` → `python -m pytest tests/ -q` (zero skips)
- `npx tsc --noEmit` clean; `npx @biomejs/biome check .` clean
- server.spec.ts exact-set assertion expects 10 literals
</verification>

<success_criteria>
- QAReport contract proven both sides with shared rejection harness (QA-04 contract half)
- anim_qa.run request closed, zero paths, renderer_support reused from compile envelope vocabulary (D-18)
- Config contracts + fixtures versioned and spec-checked; thresholds deferred to the spike per D-07
</success_criteria>

<output>
Create `.planning/phases/04-anim-qa-pinn-e/04-01-SUMMARY.md` when done
</output>
