---
phase: 04-anim-qa-pinn-e
plan: 07
type: execute
wave: 4
depends_on: ["04-05"]
files_modified:
  - src/anim-qa/scaffold/scaffold-manifest.ts
  - src/anim-qa/scaffold/react-page.html
  - src/anim-qa/scaffold/vue-page.html
  - src/anim-qa/scaffold/scaffold.qa.spec.ts
  - src/anim-qa/__tests__/scaffold-manifest.spec.ts
autonomous: true
requirements: [QA-01]
user_setup:
  - service: docker
    why: "Scaffold passes (lottie-react/dotlottie-vue) run real renders inside the pinned container"
    env_vars: []
    dashboard_config:
      - task: "Start Docker Desktop"
        location: "Docker Desktop application"
estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "lottie-react 3.1.1 and @lottiefiles/dotlottie-vue 0.11.27 render the same fixture Lottie in real passes inside the pinned container — runs or manifest rows, never skipped tests (D-27)"
    - "A scaffold pass failing on a renderer_support=all asset is RED — a gate/reality contradiction must never pass silently (D-27)"
    - "Flutter (lottie 3.5.1) appears as a manifest-only row for every asset with zero run attempts — no skip markers anywhere (D-27, AQA-02 deferred)"
    - "Scaffold results land as deterministic run artifacts: scaffold-manifest.json with one renderer row-set per asset, serialized through the deterministic serializers (D-15/D-27)"
    - "The main QAReport verdict remains lottie-web-svg — scaffold passes are additional evidence, never parallel verdicts (D-27/§7.6)"
  artifacts:
    - "src/anim-qa/scaffold/scaffold-manifest.ts + buildScaffoldManifest() (pure, deterministic)"
    - "src/anim-qa/scaffold/react-page.html + vue-page.html (real player passes, local assets)"
    - "src/anim-qa/scaffold/scaffold.qa.spec.ts (container E2E, zero skips)"
    - "Renderer column data ready for Phase 5 store persistence (MFT-01) via the scaffold manifest artifact"
  key_links:
    - "Scaffold captures → same baselines + same per-asset thresholds as the main gate (D-27: mêmes fixtures, mêmes seuils)"
    - "scaffold-manifest.json rows → Phase 5 manifest column persistence (MFT-01) + Phase 8 EXP-01/EXP-02 consumption"
    - "renderer_support (feature gate, D-11 Ph 3) remains the source of truth for exclusion semantics"
prohibitions:
  - statement: "No test is skipped to simulate an absent renderer — Flutter is a manifest row, real players really run (D-27 zero-skip doctrine)"
    status: enforced
    verification: explicit
---

<objective>
Build the §7.7 multi-renderer scaffold: real passes for the two web players (lottie-react, dotlottie-vue) over the same fixtures and thresholds inside the same pinned container, the Flutter manifest-only column, and the deterministic scaffold-manifest artifact that carries the renderer column data to Phase 5/8.

Purpose: D-27 future-proofs the QA surface without parallel verdicts — the main QAReport stays lottie-web-svg; the scaffold proves the fixture set renders identically through the player components the packager will ship.
Output: two real player passes E2E-green + scaffold-manifest.json + contradiction (gate vs reality) red test.
</objective>

<execution_context>
@D:/projets/illustrations lottie/systeme/.opencode/gsd-core/workflows/execute-plan.md
@D:/projets/illustrations lottie/systeme/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-anim-qa-pinn-e/04-CONTEXT.md
@.planning/phases/04-anim-qa-pinn-e/04-RESEARCH.md
@.planning/phases/04-anim-qa-pinn-e/04-PATTERNS.md
@docs/project/07_AnimQA.md
@src/anim-qa/frame-walker.ts
@src/anim-qa/diff.ts
@src/shared/format.ts
</context>

## Artifacts this phase produces

(Consolidated inventory in 04-01-PLAN.md — this plan contributes:) `src/anim-qa/scaffold/` — `scaffold-manifest.ts` (`buildScaffoldManifest()`), `react-page.html`, `vue-page.html`, `scaffold.qa.spec.ts`; `src/anim-qa/__tests__/scaffold-manifest.spec.ts` (unit).

<tasks>

<task type="auto">
  <name>Task 1: scaffold-manifest builder + player pages (react/vue real passes, local assets)</name>
  <files>src/anim-qa/scaffold/scaffold-manifest.ts, src/anim-qa/scaffold/react-page.html, src/anim-qa/scaffold/vue-page.html, src/anim-qa/__tests__/scaffold-manifest.spec.ts</files>
  <read_first>
    - 04-CONTEXT.md D-27 (verbatim: real passes same fixtures/seuils; Flutter manifest-only; renderer column; verdict stays lottie-web-svg)
    - 04-RESEARCH.md Q5 (exact pins: lottie-react 3.1.1 peers react 19; dotlottie-vue 0.11.27 peer vue 3 — installed in plan 04-02; players load from local node_modules), Q1 (dotlottie wasm local rule applies here too)
    - docs/project/07_AnimQA.md §7.7 (scaffold contract) — note: §7.5's per-player 2% row is superseded by the locked D-27 "mêmes fixtures, mêmes seuils" (CONTEXT authoritative); document this in the module docstring
    - src/anim-qa/diff.ts + canonical-frame.ts (capture/compare reuse), src/shared/format.ts (artifact bytes)
  </read_first>
  <action>
    Create `src/anim-qa/scaffold/scaffold-manifest.ts` (pure): `buildScaffoldManifest(entries)` producing the deterministic scaffold-manifest object — per asset: asset_id, rows for lottie-web-svg (verdict echo from the main report), lottie-react (pass/fail + diff count), dotlottie-vue (pass/fail + diff count), flutter (manifest-only literal marker — present for EVERY asset, no run), serialized only via the deterministic serializers; insertion-order stable so the artifact is byte-comparable across runs (D-15).
    Create `react-page.html` + `vue-page.html`: minimal deterministic pages serving the player components from the container's node_modules (react + lottie-react UMD/ESM build; vue + @lottiefiles/dotlottie-vue; for the vue/dotlottie path the local-wasm rule from 04-06 applies), rendering the fixture animation at the capture-config viewport with the opaque background, exposing the same capture/readiness bridge conventions as the QA pages (event-driven, no wall-clock waits).
    `scaffold-manifest.spec.ts` (unit): builder purity, flutter row present for every asset, byte-stable serialization across two builds with identical inputs, and the assertion that NO row carries a skip-style marker (rows are pass/fail/manifest-only — the zero-skip doctrine at the data level).
  </action>
  <verify>
    <automated>npx vitest run src/anim-qa/__tests__/scaffold-manifest.spec.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - scaffold-manifest.spec.ts green: deterministic bytes, flutter rows universal, no skip markers
    - Both pages reference only node_modules-local player assets (grep: zero CDN hosts)
    - Module docstring documents the D-27-over-§7.5 threshold decision (mêmes seuils, CONTEXT authoritative)
  </acceptance_criteria>
  <done>Manifest builder + player pages ready; renderer column data shape frozen for Phase 5 persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold E2E — real passes over the 11 fixtures + gate/reality contradiction red test</name>
  <files>src/anim-qa/scaffold/scaffold.qa.spec.ts</files>
  <read_first>
    - 04-CONTEXT.md D-27 (failing pass on an all-asset = rouge; zero skipped; scaffold = preuves additionnelles)
    - src/anim-qa/scaffold/scaffold-manifest.ts + the page bridges (Task 1), src/anim-qa/__tests__/gate-e2e.qa.spec.ts (in-container conventions, baseline paths, thresholds source)
    - fixtures/style-specs/example-style/baseline-frames/ (committed baselines — the comparison target)
  </read_first>
  <action>
    Create `src/anim-qa/scaffold/scaffold.qa.spec.ts` (qa project, in-container): for each of the 11 fixtures — compile on the fly, capture the canonical frame through the react page and the vue page, compare each against the SAME committed baseline PNG with the SAME per-asset threshold from thresholds.json (D-27), collect pass/fail + counts; write scaffold-manifest.json to out/qa/ (gitignored run artifact) via the builder; assert all 22 player passes green (11 × 2) with the dotlottie-vue path honoring the local-wasm rule; assert the flutter rows exist manifest-only; assert two consecutive spec runs produce byte-identical scaffold-manifest.json; include the contradiction case — a perturbed capture (seeded synthetic mismatch above threshold) fed through the pass evaluator on an all-class asset turns the scaffold RED (proving a gate/reality contradiction cannot pass silently); the main QAReport verdict is untouched and remains lottie-web-svg (assert one report's renderer literal in the same run).
  </action>
  <verify>
    <automated>npm run qa:run -- npx vitest run --config vitest.qa.config.ts scaffold &amp;&amp; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - scaffold.qa.spec.ts exits 0 with 22/22 player passes + 11 flutter manifest rows recorded
    - scaffold-manifest.json byte-identical across consecutive runs; written under out/qa/ (gitignored — git status clean)
    - Contradiction case demonstrably RED through the evaluator (documented in the spec as the seeded-failure proof, restored to green in the same run)
    - Zero skipped tests in the scaffold suite; main-report renderer literal lottie-web-svg asserted unchanged
  </acceptance_criteria>
  <done>The renderer column is measured evidence in deterministic artifacts — scaffold complete without parallel verdicts or skips (D-27 fully honored).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Player components → page | react/vue/lottie players resolved from node_modules inside the pinned container |
| Scaffold → baseline trust anchor | Player renders compared against the SAME committed baselines as the main gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-13 | Tampering | CDN player/wasm drift | medium | mitigate | Local node_modules assets only (grep acceptance, Task 1); extends the 04-06 rule to scaffold pages |
| T-4-26 | Repudiation | Silent scaffold failure on shipped players | medium | mitigate | Contradiction red test (Task 2) — a failing player pass on an all-class asset cannot pass silently (D-27) |
| T-4-27 | Tampering | Skip-markers masquerading as coverage | medium | mitigate | Zero-skip junit gate (plan 04-08 consumes this project's junit) + data-level no-skip-marker assertion (Task 1 spec) |
| T-4-28 | Information Disclosure | Renderer matrix leaking beyond run artifacts | low | accept | scaffold-manifest.json lives in gitignored out/qa/ like all run artifacts (D-19/m12 family) |
</threat_model>

<verification>
- `npm run qa:run` container suite green including scaffold (22 player passes + flutter rows)
- scaffold-manifest.json deterministic; out/qa/ stays gitignored
- `npx vitest run` unit green (builder purity)
</verification>

<success_criteria>
- D-27 delivered: real lottie-react + dotlottie-vue passes, Flutter manifest column, zero skips, no parallel verdicts (§7.7)
- Renderer evidence deterministic and Phase-5-persistable (MFT-01) and Phase-8-consumable (EXP-01/EXP-02)
</success_criteria>

<output>
Create `.planning/phases/04-anim-qa-pinn-e/04-07-SUMMARY.md` when done
</output>
