---
phase: 04-anim-qa-pinn-e
plan: 08
type: execute
wave: 5
depends_on: ["04-06", "04-07"]
files_modified:
  - .github/workflows/qa.yml
  - scripts/qa-flake-proof.mjs
  - src/anim-qa/__tests__/qa-yml-gates.spec.ts
autonomous: true
requirements: [QA-01]
user_setup:
  - service: docker
    why: "Local rehearsal of the flake proof (10× identity) before CI"
    env_vars: []
    dashboard_config:
      - task: "Start Docker Desktop"
        location: "Docker Desktop application"
      - task: "Merge qa.yml to the default branch before relying on workflow_dispatch — the manual trigger only fires when the workflow file is on the default branch (research P9)"
        location: "GitHub repository settings"
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The flake criterion < 1 % is operationalized as D-25 strict identity: 10 consecutive QA runs on the same input produce byte-identical QAReports excluding timestamp — any flake reddens (QA-01/SC-1, D-25)"
    - "qa.yml is executable from a fresh checkout with zero secrets: image resolved by parsing qa-container.lock into an env var, never hardcoded (SC-5, D-03/B1)"
    - "CI runs the SAME invocation path as local qa:run — npm ci inside the pinned container, vitest qa project, assert-zero-skips on the QA junitxml (D-02/D-03/D-24)"
    - "out/qa/ artifacts upload with if: always() so failed runs still ship their diagnostics (D-19/m12)"
    - "Triggers per D-04: pull_request (1 run), push main + nightly schedule (10-run proof), workflow_dispatch (m2 on-demand, P9 caveat documented)"
    - "verify.yml remains byte-identical — all Phase 4 CI additions live exclusively in qa.yml (D-03)"
  artifacts:
    - ".github/workflows/qa.yml — dedicated CI workflow (lock-parsed image, artifacts upload, flake-proof step)"
    - "scripts/qa-flake-proof.mjs — 10× strict-identity proof (deep-compare minus timestamp, counter logged for the metric)"
    - "src/anim-qa/__tests__/qa-yml-gates.spec.ts — m13 grep gate + B1 no-hardcode gate + trigger/structure assertions (verify-collected)"
  key_links:
    - "qa.yml → qa-container.lock (parse step → GITHUB_ENV) — the B1 single-source rule extends to CI"
    - "qa.yml flake step → scripts/qa-flake-proof.mjs → report.ts compare-excluding-timestamp helper (D-25)"
    - "qa.yml zero-skip step → scripts/assert-zero-skips.mjs on fixtures/bridge/vitest-qa-junit.xml (D-24)"
prohibitions:
  - statement: "CI never regenerates baselines and never refreshes the container lock — qa.yml compares and runs only (D-05/D-37, B1)"
    status: enforced
    verification: explicit
---

<objective>
Make the gate CI-real: the dedicated qa.yml workflow (fresh-checkout, no secrets, digest-pinned via the lock), the 10× strict-identity flake proof as the measurable form of "< 1 %", and the verify-collected hygiene gates that keep the workflow honest (m13 + B1 + triggers).

Purpose: SC-5 — reproducibility is only proven when CI, from a fresh checkout, runs the same container path and the flake identity holds.
Output: qa.yml + qa-flake-proof.mjs + gate specs; verify.yml untouched.
</objective>

<execution_context>
@D:/projets/illustrations lottie/systeme/.opencode/gsd-core/workflows/execute-plan.md
@D:/projets/illustrations lottie/systeme/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-anim-qa-pinn-e/04-CONTEXT.md
@.planning/phases/04-anim-qa-pinn-e/04-RESEARCH.md
@.planning/phases/04-anim-qa-pinn-e/04-PATTERNS.md
@.github/workflows/verify.yml
@scripts/qa-run.mjs
@scripts/assert-zero-skips.mjs
@src/anim-qa/report.ts
</context>

## Artifacts this phase produces

(Consolidated inventory in 04-01-PLAN.md — this plan contributes:) `.github/workflows/qa.yml`; `scripts/qa-flake-proof.mjs`; `src/anim-qa/__tests__/qa-yml-gates.spec.ts`. This plan creates no new runtime modules — it wires and proves what waves 1–4 built.

<tasks>

<task type="auto">
  <name>Task 1: qa-flake-proof.mjs — 10× strict identity minus timestamp (D-25)</name>
  <files>scripts/qa-flake-proof.mjs</files>
  <read_first>
    - 04-CONTEXT.md D-25 (identité stricte ×10 hors timestamp; compteur dans le log pour la métrique), D-04 (cadence), D-15 (timestamp injectable)
    - 04-RESEARCH.md P8 (float serialization false negatives — identity holds because reports flow through the deterministic serializers) and Q6 (residual flake surface = environment, mitigated by the pin)
    - scripts/update-goldens.mjs (stdlib/spawn discipline), scripts/qa-run.mjs (container path to reuse), src/anim-qa/report.ts (the compare-excluding-timestamp helper exported in 04-03)
  </read_first>
  <action>
    Create `scripts/qa-flake-proof.mjs` (stdlib-only, executed inside the container via the qa:run path — CI and local share it): run 10 consecutive full QA passes over the fixture set with a FIXED injected timestamp (D-15); after each pass, load every per-asset report.json; deep-compare all 10 passes pairwise with the compare-excluding-timestamp semantics (the helper from report.ts, or its byte-equality equivalent since timestamps are fixed); ANY difference → exit 1 naming the asset, field and differing values; identity holds → exit 0 with the run counter logged to stdout as the flake metric line (D-25 keeps the counter in the log). Deterministic asset ordering; diagnostics to stderr.
  </action>
  <verify>
    <automated>npm run qa:run -- node scripts/qa-flake-proof.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The proof exits 0: 10 passes × 11 assets, byte-identical reports excluding timestamp (P8's serialization discipline makes this a meaningful check)
    - A seeded-difference dry run (temporarily perturbing one captured byte in a scratch copy) makes the proof exit 1 with the differing asset/field named — the checker provably catches flakes
    - The metric counter line appears on stdout (D-25 log requirement)
  </acceptance_criteria>
  <done>The flake criterion is a runnable identity proof, not a statistic — green means zero observed flake across 10 identical runs.</done>
</task>

<task type="auto">
  <name>Task 2: qa.yml — dedicated CI workflow (lock-parsed image, artifacts, triggers, zero-skip)</name>
  <files>.github/workflows/qa.yml</files>
  <read_first>
    - 04-CONTEXT.md D-03 (workflow dédié, fresh checkout, sans secrets; verify.yml byte-intact; lock = source unique, aucune référence tag@digest en dur dans le YAML; chemin d'exécution unique), D-04 + m2 (triggers), D-19/m12 (upload out/qa/ if always())
    - 04-RESEARCH.md Q7 (workflow syntax findings: container job image from env, default shell sh — no bashisms, workflow_dispatch default-branch caveat P9, upload-artifact versions, permissions contents: read) and the qa.yml skeleton; Q2 (lock fields)
    - 04-PATTERNS.md ".github/workflows/qa.yml — CREATE" (verify.yml structural conventions: permissions block, zero-skip step shape)
    - .github/workflows/verify.yml (READ-ONLY reference — must remain byte-identical, D-03)
  </read_first>
  <action>
    Create `.github/workflows/qa.yml` following the research skeleton: name qa; triggers exactly per D-04 — pull_request, push limited to main, one nightly schedule cron, workflow_dispatch with a top-comment documenting the P9 default-branch caveat; workflow-level permissions contents: read (least privilege, mirrors verify.yml). First step parses qa-container.lock (tag/digest lines) and exports the full image reference via GITHUB_ENV — the YAML contains NO hardcoded registry tag or digest (B1). The main step runs the SAME command path as local: docker run --rm --init --ipc=host with the workspace bind-mount, the named node_modules volume, the image from the env var, executing sh-compatible steps: npm ci, then the QA vitest project, then the zero-skip gate on the QA junitxml (assert-zero-skips.mjs reuse, D-24). Upload step: out/qa/ with if: always() and a warn-if-empty policy (D-19/m12). Flake-proof step: gated by condition to push-main + schedule + workflow_dispatch (D-04 cadence: PRs get 1 run; the 10-run proof runs on main/nightly/on-demand), invoking the flake script through the same container invocation. Default shell stays sh — every run block sh-compatible. Do not modify verify.yml in any way.
  </action>
  <verify>
    <automated>npx vitest run src/anim-qa/__tests__/qa-yml-gates.spec.ts &amp;&amp; git status --porcelain .github/workflows/verify.yml</automated>
  </verify>
  <acceptance_criteria>
    - qa-yml-gates.spec.ts (Task 3) green against the authored workflow
    - git status shows verify.yml untouched (byte-identical, D-03)
    - Every run block is sh-compatible (no bash-only syntax — reviewable by the gate spec's bashism scan)
  </acceptance_criteria>
  <done>SC-5's CI half is authored: fresh-checkout, secret-free, digest-pinned via the lock, same execution path as local.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: qa.yml hygiene gates (m13 + B1 + triggers + zero-skip wiring) — verify-collected spec</name>
  <files>src/anim-qa/__tests__/qa-yml-gates.spec.ts</files>
  <read_first>
    - 04-CONTEXT.md m13 (the qa.yml must not reference the credential-access pattern — gate as a spec collected by verify or a CI check), B1 (no hardcoded image refs outside the lock), D-04 (trigger set), D-24 (assert-zero-skips on the QA junitxml)
    - 04-RESEARCH.md Q7 (exact trigger keys, permissions shape, artifact step semantics)
    - src/rpc/contracts/rejection-cases.ts fail-loud shape guard convention (vacuous-green prevention applies to file-scanning specs)
  </read_first>
  <action>
    Create `src/anim-qa/__tests__/qa-yml-gates.spec.ts` (default vitest project, reads the workflow file from disk, fail-loud when the file is missing): (a) m13 — zero occurrences of the forbidden credential-access token fixed by D-03/m13 in qa.yml (the exact literal lives in the CONTEXT decision; the spec encodes it as the GitHub-Actions context accessor for secrets); (b) B1 — zero hardcoded occurrences of the image registry host with tag/digest suffixes; the parse step and env-var consumption present; (c) triggers — all four D-04 triggers present with push restricted to main; (d) zero-skip — the assert-zero-skips invocation targets the QA junitxml path; (e) artifacts — the upload step targets out/qa/ under an always() condition; (f) bashism scan — no bash-only process substitution or double-bracket conditionals in run blocks (sh compatibility); (g) vacuous-green guard — the spec fails if qa.yml shrinks below the asserted structural minimum (step count/named steps), so deleting the workflow cannot pass the gate.
  </action>
  <verify>
    <automated>npx vitest run src/anim-qa/__tests__/qa-yml-gates.spec.ts &amp;&amp; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - All gate assertions green against the Task-2 workflow; deliberately breaking one assertion in a scratch edit flips the spec red (checker-works proof, restored before commit)
    - The full default vitest project stays green (gates collected by verify, per m13's "spec collectée par verify")
  </acceptance_criteria>
  <done>The workflow's honesty properties are test-enforced in verify — drift in secrets hygiene, image pinning, or trigger shape reddens the ordinary suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → registry | Image pulled BY DIGEST from the lock — no mutable tag pulls |
| Workflow → repository | permissions contents: read; no secret contexts anywhere in qa.yml |
| CI → artifacts | out/qa/ uploaded as run diagnostics; no environment data included |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-29 | Information Disclosure | Secrets exposure via workflow | high | mitigate | m13 grep gate as a verify-collected spec (Task 3a) + permissions contents: read (Task 2); the workflow genuinely needs no secrets (D-03 fresh-checkout design) |
| T-4-05 | Tampering | Image tag re-push attack in CI | high | mitigate | Digest-form reference resolved from the lock into the job env (B1); CI never pulls by mutable tag (Task 2) |
| T-4-30 | Denial of Service | Nightly flake noise burning trust | medium | mitigate | D-25 identity proof makes any flake a hard red with the differing field named (Task 1) — noise is actionable, not ignorable |
| T-4-31 | Repudiation | Silent gate weakening (workflow edits removing steps) | medium | mitigate | Vacuous-green structural minimum in the gate spec (Task 3g) |
| T-4-32 | Elevation | Workflow privilege creep | low | mitigate | contents: read only; no deployment/auth scopes (mirrors verify.yml conventions) |
</threat_model>

<verification>
- `npm run qa:run -- node scripts/qa-flake-proof.mjs` green locally (Docker precondition) — the CI step's exact script
- `npx vitest run` green including qa-yml-gates; verify.yml byte-identical (git status)
- Fresh-checkout simulation: the workflow's run block mirrors the qa:run command exercised all phase long (single-path doctrine)
</verification>

<success_criteria>
- SC-5: full QA executable in CI from fresh checkout, pinned image, zero secrets (QA-01/SC-1 container story complete)
- Flake < 1 % operationalized as 10× strict identity (D-25) with the proof script reused local/CI
- verify.yml untouched; workflow hygiene gates permanent in verify
</success_criteria>

<output>
Create `.planning/phases/04-anim-qa-pinn-e/04-08-SUMMARY.md` when done
</output>
