---
phase: 02-style-verrouill-catalogue-de-recettes
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-04"]
files_modified:
  - fixtures/rejection-cases/catalogue.json
  - tests/bridge/rejection_loader.py
  - src/rpc/contracts/rejection-cases.ts
  - tests/bridge/test_catalogue_bridge.py
  - src/rpc/contracts/catalogue.spec.ts
  - tests/domain/test_vocabulary.py
  - lottie_forge/gates/__init__.py
  - lottie_forge/gates/stale_pins.py
  - tests/domain/test_stale_pins.py
autonomous: true
requirements: [MOT-01, MOT-02, MOT-03, STY-03]
estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Les rejets intrinsèques du catalogue (disco-spin, theme_anchors vide, doublons, bornes, extra keys) passent par le harnais partagé : un JSON, deux loaders, parametrize pytest + it.each vitest, membership de chemins uniquement"
    - "L'audit de couverture §5.6 est BLOQUANT dans le job verify : (A) tout état de la map mappe vers >= 1 recipe_id existant — état orphelin ou id inconnu = rouge ; (B) tout recipe_id du catalogue apparaît dans >= 1 mapping — recette morte = rouge (D-14)"
    - "La règle same-commit ADR-03 est étendue à 4 fichiers : set(catalogue ids) == RECIPE_IDS est asserté structurellement — un swap qui touche vocabulary.py sans catalogue.json (ou l'inverse) rougit la CI (D-14C)"
    - "scan_stale_pins(pins, current_version) est une fonction PURE à source de pins injectable ; flags = modèle Pydantic strict SANS miroir zod (D-06/D-08) ; bump_class dérivé in-fonction par diff semver, scope déclaratif patch→sampled / minor→tokens_touched / major→all (D-09)"
    - "Deux tests bloquants STY-03 : (a) bump simulé paramétré — pins sur version antérieure → flags attendus (PATCH échantillonné, MINOR tokens_touched, MAJOR all) ; (b) garde permanente — tout style_ref extrait d'un payload contractuellement VALIDE (baseline make_asset/make_pack incluse) ≠ version courante de la fixture style chargée = verify rouge ; les payloads de rejet des fixtures (dont le full-format example-style@2.0.0 de pack-manifest.json) sont rejection-only, exclus du scan de pins (D-07)"
    - "Edge résolu par le contexte (sonde MOT-03 empty) : theme_anchors vide rejette le chargement — cas partagé au harnais avec loc recipes.idx.theme_anchors"
    - "Edge résolu par le contexte (sonde MOT-01 ordering) : scan_stale_pins retourne les flags dans l'ordre d'entrée (stable), un flag par pin stale, jamais de fusion de doublons"
  artifacts:
    - "fixtures/rejection-cases/catalogue.json (cas intrinsèques D-08)"
    - "MOD tests/bridge/rejection_loader.py + src/rpc/contracts/rejection-cases.ts (entrée catalogue)"
    - "lottie_forge/gates/stale_pins.py — scan_stale_pins(), PinRecord, StalePinFlag, BumpClass, StaleScope, BUMP_SCOPE"
    - "tests/domain/test_stale_pins.py (bump simulé + garde permanente)"
    - "tests de l'audit D-14 A/B + scan same-commit C (dans test_catalogue_bridge.py + test_vocabulary.py étendu)"
  key_links:
    - "scan_stale_pins ↔ STYLE_REF_PATTERN (asset.py, consommé verbatim — parsing rsplit(@, 1), WR-01)"
    - "garde permanente ↔ load_style_spec().style_version (version courante de la fixture, plan 02-01)"
    - "audit A/B ↔ catalogue.json + coverage-map.json (plan 02-04)"
    - "harnais catalogue ↔ les deux loaders étendus par 02-02 (style-refinement) — même dict CONTRACT_FILES"
  prohibitions:
    - ".github/workflows/verify.yml reste byte-identique (D-18) — les gates sont des tests pytest/vitest ramassés par les étapes existantes"
    - "pas de miroir zod pour StalePinFlag/PinRecord — la gate vit côté Python, rien ne traverse la frontière (D-08, règle §4.10)"
    - "pas de rapport informatif : les deux tests STY-03 sont bloquants (échec = verify rouge), pas de warning (D-07)"
    - "pas de re-dérivation du pattern style_ref — STYLE_REF_PATTERN importé de asset.py verbatim"
    - "pas de résolution « quels assets utilisent quels tokens » en Phase 2 — le scope reste déclaratif (arrive avec le store Phase 5+, D-09)"
---

<objective>
Poser les **gates bloquantes** de la Phase 2 dans le job verify existant : harnais de rejet partagé du catalogue (MOT-04 rejet miroir), audit de couverture motion §5.6 (D-14 A/B), règle same-commit étendue à 4 fichiers (D-14C), et la gate de re-validation STY-03 — `scan_stale_pins` fonction pure + les deux tests bloquants D-07 (bump simulé + garde permanente zéro-pin-stale).

Purpose: ROADMAP critère 3 — un bump simulé de style_version flaggue tous les assets pinnés (PATCH échantillonné, MINOR tokens touchés, MAJOR tous) ; chaque membre du catalogue fermé justifie son slot.
Output: gates bloquantes vertes dans les étapes pytest/vitest existantes (zéro edit workflow), module `lottie_forge/gates/stale_pins.py` prêt pour le branchement store-backed Phase 5.
</objective>

<execution_context>
@.opencode/gsd-core/workflows/execute-plan.md
@.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-style-verrouill-catalogue-de-recettes/02-CONTEXT.md
@.planning/phases/02-style-verrouill-catalogue-de-recettes/02-PATTERNS.md
@docs/project/05_Style.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cas de rejet partagés catalogue.json + enregistrement dans les deux loaders</name>
  <files>fixtures/rejection-cases/catalogue.json, tests/bridge/rejection_loader.py, src/rpc/contracts/rejection-cases.ts, tests/bridge/test_catalogue_bridge.py, src/rpc/contracts/catalogue.spec.ts</files>
  <read_first>
    - fixtures/rejection-cases/style-spec.json (format D-08 verbatim : case_id/ref/model/payload complet une mutation/expect_paths)
    - tests/bridge/rejection_loader.py (CONTRACT_FILES — entrée style-refinement ajoutée au plan 02-02, ajouter catalogue à côté)
    - src/rpc/contracts/rejection-cases.ts (même mapping ts)
    - tests/bridge/test_style_spec_bridge.py lignes 78-89 + src/rpc/contracts/style-spec.spec.ts lignes 80-97 (consommation parametrize/it.each à répliquer)
    - tests/domain/test_catalogue.py (les locs exactes des validateurs Task 1 du plan 02-04 — les expect_paths doivent matcher)
  </read_first>
  <action>
    Créer fixtures/rejection-cases/catalogue.json au format D-08, model "RecipeCatalogue", chaque payload = catalogue valide complet de 10 recettes avec UNE mutation. Cas minimum : cat01-disco-spin-id (recette[0].id = "disco-spin", expect_paths [["recipes", 0, "id"]]) ; cat02-empty-theme-anchors (recette[0].theme_anchors = [], expect_paths [["recipes", 0, "theme_anchors"]]) ; cat03-duplicate-ids (recette[1].id = "fade" dupliqué, expect_paths [["recipes", 1, "id"]]) ; cat04-too-few-recipes (7 recettes, expect_paths [["recipes"]]) ; cat05-too-many-recipes (13 recettes, expect_paths [["recipes"]]) ; cat06-unknown-anchor (theme_anchors ["logo"], expect_paths [["recipes", 0, "theme_anchors"]]) ; cat07-intensity-unordered ([1.0, 0.2], expect_paths [["recipes", 0, "intensity_range"]]) ; cat08-intensity-out-of-bounds ([−0.1, 0.5] exprimé -0.1, expect_paths [["recipes", 0, "intensity_range"]]) ; cat09-unknown-shape (shapes_supported ["circle"], expect_paths [["recipes", 0, "shapes_supported"]]) ; cat10-unknown-trigger (trigger_points ["hover"], expect_paths [["recipes", 0, "trigger_points"]]) ; cat11-duration-below-pack-range (500, expect_paths [["recipes", 0, "duration_ms"]]) ; cat12-duration-above-pack-range (1600, expect_paths [["recipes", 0, "duration_ms"]]) ; cat13-unknown-keyframe-shape ("spin-around", expect_paths [["recipes", 0, "keyframe_shape"]]) ; cat14-bad-catalogue-version ("1.0", expect_paths [["catalogue_version"]]) ; cat15-extra-key-top-level (rejection-only).

    Enregistrer "catalogue": "catalogue.json" dans les DEUX loaders. Consommation : parametrize dans test_catalogue_bridge.py (ids case_id, ValidationError + membership expect_paths, paths seuls) ; it.each miroir dans catalogue.spec.ts (safeParse false + membership).
  </action>
  <verify>
    <automated>python -m pytest tests/bridge/test_catalogue_bridge.py -q</automated>
    <automated>npx vitest run catalogue</automated>
  </verify>
  <acceptance_criteria>
    - 15 cas consommés de chaque côté (+15 pytest parametrize, +15 vitest it.each)
    - chaque expect_paths trouve son loc/path dans les erreurs pydantic ET zod (membership, pas de message)
    - le harnais total compte 4 contrats phase 1 + style-refinement (02-02) + catalogue — même dict, même format
    - suites globales pytest + vitest vertes
  </acceptance_criteria>
  <done>Les rejets intrinsèques du catalogue sont verrouillés en miroir via le harnais partagé — ROADMAP critère 2 (volets disco-spin et theme_anchors vide) prouvé des deux côtés.</done>
</task>

<task type="auto">
  <name>Task 2: Audit de couverture bloquant (D-14 A/B) + same-commit 4 fichiers (C)</name>
  <files>tests/bridge/test_catalogue_bridge.py, tests/domain/test_vocabulary.py</files>
  <read_first>
    - 02-CONTEXT.md D-14 (audit bloquant A/B/C) et D-15 (cohérences attendues : 10 recettes couvertes, états exit présents, loops → orbit/float/pulse)
    - docs/project/05_Style.md lignes 155-159 (§5.6 — audit rejoué à chaque changement de catalogue)
    - fixtures/recipe-catalogue/catalogue.json + coverage-map.json (plans 02-04)
    - lottie_forge/domain/vocabulary.py (RECIPE_IDS — source du set)
    - tests/domain/test_vocabulary.py lignes 161-228 (patron des scans structurels à étendre)
    - lottie_forge/loading/catalogue.py (COVERAGE_MAP_PATH + load_catalogue_fixture)
  </read_first>
  <action>
    1. Audit A (dans test_catalogue_bridge.py) : charger coverage-map.json + catalogue.json ; pour chaque verticale/état : recipes NON VIDE (état orphelin = rouge, message explicitant la verticale et le state_id) et chaque id mappé ∈ set(catalogue ids) ET ∈ RECIPE_IDS (id inconnu = rouge).
    2. Audit B : l'union des ids mappés sur toutes les verticales == set des 10 ids du catalogue — toute recette absente (recette morte) = rouge avec la liste des ids non couverts dans le message. Vérifier aussi les cohérences D-15 documentaires : les états contenant exit → recettes à trigger exit (slide/fade présents), les états continus → recettes loop (orbit/float/pulse) — asserts directs sur la map committée.
    3. Règle C (dans test_vocabulary.py, à côté des scans existants) : assert set(ids du catalogue.json) == set(RECIPE_IDS) — un swap d'appartenance qui ne toucherait pas les 4 fichiers (vocabulary.py + vocabulary.schema.ts + catalogue.json + coverage-map.json) au même commit rougit. Étendre le scan structurel existant (celui qui interdit les secondes déclarations de RECIPE_IDS hors vocabulary.schema.ts) pour interdire aussi toute seconde déclaration de THEME_ANCHOR_IDS hors vocabulary.schema.ts. Un test miroir ts dans vocabulary.spec.ts assertra set(THEME_ANCHOR_IDS) == les 6 labels (lockstep de surface).
    4. Les trois gates sont des tests pytest ordinaires — ramassés par l'étape pytest du job verify existante, SANS modification du workflow (D-18).
  </action>
  <verify>
    <automated>python -m pytest tests/bridge/test_catalogue_bridge.py tests/domain/test_vocabulary.py -q</automated>
    <automated>npx vitest run vocabulary</automated>
  </verify>
  <acceptance_criteria>
    - l'audit A passe sur la map committée : 16 états, tous mappés vers des ids existants du catalogue
    - l'audit B passe : union des ids mappés == set des 10 ids (aucune recette morte) ; les cohérences exit/loop de D-15 sont assertées
    - le test C passe : set(catalogue ids) == set(RECIPE_IDS) ; aucune seconde déclaration de RECIPE_IDS ou THEME_ANCHOR_IDS hors les fichiers vocabulary
    - une mutation de test (retirer un mapping) ferait échouer l'audit — vérifié par la lisibilité des messages (ids/state_id cités)
    - ruff vert ; zéro modification de .github/workflows/verify.yml
  </acceptance_criteria>
  <done>L'audit de couverture §5.6 est bloquant (A/B) et la règle same-commit couvre 4 fichiers (C) — chaque membre du catalogue fermé justifie son slot à chaque changement.</done>
</task>

<task type="auto">
  <name>Task 3: Gate scan_stale_pins pure (D-06/D-08/D-09) + les deux tests bloquants (D-07)</name>
  <files>lottie_forge/gates/__init__.py, lottie_forge/gates/stale_pins.py, tests/domain/test_stale_pins.py</files>
  <read_first>
    - docs/project/05_Style.md lignes 77-88 (§5.4 — table PATCH/MINOR/MAJOR + mécanique Phase 2)
    - 02-CONTEXT.md D-06 (fonction pure injectable), D-07 (deux tests bloquants), D-08 (flags Pydantic strict SANS zod), D-09 (bump_class semver, scope déclaratif) + note sémantique Phase 5+
    - lottie_forge/domain/asset.py lignes 65, 100-111 (STYLE_REF_PATTERN verbatim + format name@X.Y.Z)
    - lottie_forge/domain/pack.py ligne ~270 (patron rsplit("@", 1) + comparaison exacte — WR-01)
    - lottie_forge/loading/style.py (load_style_spec — version courante de la fixture)
    - fixtures/rejection-cases/asset-spec.json + pack-manifest.json (payloads committés portant des style_ref — la garde n'extrait des pins que des payloads contractuellement valides ; les payloads de rejet sont exclusion-only)
    - tests/bridge/fixtures.py lignes 104-176 (make_asset / make_pack — payload de baseline contractuellement VALIDE de chaque contrat, style_ref dérivé de make_style_spec().style_version ; source de non-vacuité du scan)
  </read_first>
  <action>
    1. lottie_forge/gates/__init__.py (vide) + lottie_forge/gates/stale_pins.py :
       - PinRecord(BaseModel, STRICT_CONFIG) : asset_id: str pattern ^a-\d{3}$ ; style_ref: str pattern STYLE_REF_PATTERN (IMPORTÉ de lottie_forge.domain.asset — verbatim, pas de re-dérivation). Découplé d'AssetSpec exprès pour l'injectabilité (fixtures Phase 2, store Phase 5+).
       - StalePinFlag(BaseModel, STRICT_CONFIG) : asset_id: str ; pinned_version: str ; current_version: str ; bump_class: Literal["patch", "minor", "major"] ; scope: Literal["sampled", "tokens_touched", "all"]. Docstring : PAS de miroir zod — la gate vit côté Python, rien ne traverse la frontière (D-08, règle §4.10).
       - BUMP_SCOPE: Final dict déclaratif {"patch": "sampled", "minor": "tokens_touched", "major": "all"} (D-09 — la résolution tokens/assets arrive avec le store Phase 5+).
       - scan_stale_pins(pins: Sequence[PinRecord], current_version: str) -> list[StalePinFlag] : fonction PURE, zéro I/O ; pour chaque pin, version pinnée = style_ref.rsplit("@", 1)[1] (WR-01) ; si != current_version → un flag par pin, bump_class dérivé in-fonction par diff composant par composant (major d'abord, puis minor, puis patch ; premier composant différent = classe ; downgrade pinné > courant → classé major par sûreté, documenté) ; scope = BUMP_SCOPE[bump_class] ; ordre de sortie = ordre d'entrée (stable, un flag par pin, jamais de fusion).
    2. tests/domain/test_stale_pins.py — test (a) BUMP SIMULÉ (bloquant, paramétré) : pins sur 1.0.0 avec current 1.0.1 → flag unique bump_class patch / scope sampled ; current 1.1.0 → minor / tokens_touched ; current 2.0.0 → major / all ; pin == current → liste vide ; mélange 1.0.0 + 1.1.0 vs current 2.0.0 → deux flags major/all dans l'ordre d'entrée ; assert aussi que des pins identiques en double produisent DEUX flags (jamais de déduplication silencieuse — sonde d'adjacence).
       3. test (b) GARDE PERMANENTE (bloquant) : charger la version courante depuis load_style_spec()[0].style_version ; construire la liste de pins par extraction sous contrat : pour chaque fixture committée (asset-spec.json → modèle AssetSpec, pack-manifest.json → modèle PackManifest), json.loads des octets committés puis model_validate payload par payload — extraire le style_ref (regex STYLE_REF_PATTERN, parsing rsplit verbatim) UNIQUEMENT des payloads qui valident. Les payloads de rejet sont des données de rejet pures, hors du périmètre de pins — y compris le cas mono-style-mismatch au format pleinement valide dont le style_ref vaut example-style@2.0.0 (c'est la mutation testée par le harnais, pas un pin à surveiller) ; les versions partielles (ex. 1.2) et malformées (1.0.0.1, underscore) ne matchent de toute façon pas le pattern. Baseline valide : scanner aussi make_asset() et make_pack() (payload de baseline contractuellement valide de chaque contrat, style_ref dérivé de make_style_spec().style_version). Asserter que TOUTE version extraite == version courante — tout pin ≠ version courante = échec dur (discipline same-commit du bump dès maintenant) — ET que le total des pins est >= 1 (garde anti-scan-vide : une extraction silencieusement vide est un échec, jamais un vert). Documenter en docstring du test : fixtures de rejet = rejection-only, hors périmètre de pins.
       4. Aucun rapport informatif : les deux tests échouent le job verify (D-07). La sémantique Phase 5+ (store-backed, non-bloquant CI) est notée en docstring de module.
  </action>
  <verify>
    <automated>python -m pytest tests/domain/test_stale_pins.py -q</automated>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - scan_stale_pins est pure (aucune I/O, source injectable) : bump simulé patch/minor/major → flags avec bump_class et scope exacts sampled/tokens_touched/all (D-09)
    - pin à jour → zéro flag ; pins en double → un flag chacun, ordre d'entrée préservé
    - la garde permanente extrait les pins des seuls payloads qui valident sous leur contrat (AssetSpec / PackManifest, payload par payload) + baseline make_asset/make_pack — au moins un pin scanné au total (assert de non-vacuité)
    - les payloads de rejet des deux fixtures — y compris mono-style-mismatch full-format (example-style@2.0.0) — sont hors périmètre de pins, exclusion documentée en docstring ; versions partielles/malformées ignorées par le pattern
    - la logique d'échec est prouvée par le bump simulé (un pin à 0.9.9 produirait un flag) ; la garde est verte sur le dépôt committé
    - StalePinFlag/PinRecord en STRICT_CONFIG, STYLE_REF_PATTERN importé (pas re-déclaré), aucun schéma zod créé pour ces types
    - ruff vert ; suite globale verte ; verify.yml intouché
  </acceptance_criteria>
  <done>STY-03 verrouillée au niveau fixtures : la gate pure flaggue PATCH échantillonné / MINOR tokens_touched / MAJOR tous, et la garde permanente impose la discipline same-commit des versions pinnées — deux tests bloquants dans le job verify existant (D-18).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixtures committées → gates | catalogue.json / coverage-map.json / rejection-cases lus par les tests bloquants |
| pins injectés → scan_stale_pins | toute source future de pins (fixtures aujourd'hui, store Phase 5+) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Tampering | coverage-map falsifiée pour masquer un trou | medium | mitigate | l'audit A/B est bloquant et recoupe le catalogue + RECIPE_IDS ; la règle C lie les 4 fichiers au même commit — falsifier exige un commit incohérent détectable |
| T-02-09 | Tampering | contournement de la gate de bump | medium | mitigate | garde permanente : tout style_ref extrait d'un payload contractuellement valide (baseline make_asset/make_pack incluse, assert de non-vacuité) ≠ version courante rougit le verify — le bump ne peut pas passer en fraude sans commit visible |
| T-02-02 | Tampering | chemins des fixtures scannées | low | mitigate | constantes REPO_ROOT ; glob figés sur fixtures/ committées |
| T-02-04 | DoS | flags non bornés | low | mitigate | PinRecord/StalePinFlag stricts avec patterns bornés (asset_id a-\d{3}, versions X.Y.Z) ; la sortie est bornée par l'entrée |
| T-02-06 | Information Disclosure | messages d'audit | low | accept | messages citant ids/state_id — outil local mono-opérateur |
</threat_model>

<verification>
- python -m pytest tests/domain/test_stale_pins.py tests/bridge/test_catalogue_bridge.py tests/domain/test_vocabulary.py -q : vert
- python -m pytest tests/ -q : verte (toutes les gates bloquantes passent) ; npx vitest run : vert
- python -m pytest tests/ -q -k export → npx vitest run → python -m pytest tests/ -q : chaîne verte, zéro skip
- ruff check . / npx biome check . / npx tsc --noEmit : verts
- git diff --exit-code -- .github/workflows/verify.yml : vide (D-18 — les gates vivent dans les étapes existantes)
</verification>

<success_criteria>
ROADMAP critère 3 verrouillé : un bump simulé flaggue tous les pins anciens avec le bon bump_class/scope (PATCH échantillonné, MINOR tokens touchés, MAJOR tous) via la gate pure injectable sur l'ancre AssetSpec.style_ref. D-14 complet : audit de couverture bloquant (aucune recette morte, aucun état orphelin) + règle same-commit étendue à 4 fichiers. Les rejets catalogue sont prouvés en miroir via le harnais partagé.
</success_criteria>

<output>
Create `.planning/phases/02-style-verrouill-catalogue-de-recettes/02-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Symboles créés par LA PHASE entière (les items de CE plan sont marqués ★) — registre complet dans 02-01-PLAN.md :

- ★ Python gate : `lottie_forge.gates.stale_pins` — `scan_stale_pins()`, `PinRecord`, `StalePinFlag`, `BumpClass`, `StaleScope`, `BUMP_SCOPE` (+ `lottie_forge/gates/__init__.py`)
- ★ Data : `fixtures/rejection-cases/catalogue.json` (15 cas D-08)
- ★ MOD harness : `tests/bridge/rejection_loader.py` (entrée catalogue), `src/rpc/contracts/rejection-cases.ts`
- ★ Tests : `tests/domain/test_stale_pins.py` (bump simulé + garde permanente), audit D-14 A/B dans `tests/bridge/test_catalogue_bridge.py`, scan C dans `tests/domain/test_vocabulary.py`
- Autres artifacts de la phase : loader style + ThemeAnchorId (02-01) ; StyleRefinement (02-02) ; content_hashes 4 champs (02-03) ; catalogue + coverage-map + joint-load (02-04) ; prompt-fixture (02-06)
