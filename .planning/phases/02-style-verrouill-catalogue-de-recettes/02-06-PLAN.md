---
phase: 02-style-verrouill-catalogue-de-recettes
plan: 06
type: execute
wave: 3
depends_on: ["02-01", "02-03", "02-04"]
files_modified:
  - lottie_forge/prompts/__init__.py
  - lottie_forge/prompts/render.py
  - lottie_forge/prompts/templates/recipe_picker.system.md
  - tests/prompts/test_prompt_fixture.py
autonomous: true
requirements: [MOT-04]
estimate:
  tokens: 24000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Le template versionné recipe_picker.system.md porte les deux placeholders contractuels {{catalogue_json}} et {{catalogue_hash}} — présence assertée par test (D-13)"
    - "Le rendu injecte le catalogue VERBATIM (octets committés LF-normalisés décodés, jamais ré-sérialisés) + son sha256 au régime D-03 — le texte embarqué == les bytes hashés == le fichier committé (§5.1 principe 2)"
    - "Le rendu est déterministe : deux appels → bytes identiques (prérequis des snapshots Phase 6, zéro refactor du mécanisme prévu)"
    - "L'enregistrement du hash au manifest est en place : un AssetSpec construit avec content_hashes.style_sha256/catalogue_sha256 = vrais sha des fixtures se valide et round-trippe (ROADMAP critère 5, D-16)"
    - "Edge résolu par le contexte (sonde MOT-04 verbatim) : pas de reformulation, pas de troncature — le catalogue est embarqué en entier comme fixture de system prompt versionnée"
  artifacts:
    - "lottie_forge/prompts/render.py — render_recipe_picker_prompt(catalogue_json, catalogue_hash), load_catalogue_prompt_fixture()"
    - "lottie_forge/prompts/templates/recipe_picker.system.md (squelette versionné, 2 placeholders contractuels)"
    - "tests/prompts/test_prompt_fixture.py (placeholder + verbatim + hash + déterminisme + enregistrement manifest)"
  key_links:
    - "load_catalogue_prompt_fixture ↔ load_catalogue_fixture (plan 02-04) ↔ sha256_hex (plan 02-01, régime D-03)"
    - "test manifest ↔ make_asset (plan 02-03, 4 hash) + ContentHashes strict"
    - "template ↔ RecipePicker Phase 6 : la Phase 6 remplit le template, zéro refactor du mécanisme (D-13)"
  prohibitions:
    - ".github/workflows/verify.yml reste byte-identique (D-18)"
    - "pas de ré-sérialisation du catalogue dans le prompt — uniquement le texte brut committé (verbatim §5.1 #2)"
    - "pas d'appel réseau ni de dépendance agent dans ce plan — le mécanisme est pur, les agents arrivent en Phase 6"
    - "pas de miroir zod pour le prompt — payload purement Python, règle §4.10 (seul ce qui traverse a un miroir)"
---

<objective>
Câbler le catalogue comme **fixture de system prompt versionnée** (D-13, MOT-04) : module de rendu pur qui injecte le catalogue verbatim + son sha256 dans un template squelette portant les placeholders contractuels `{{catalogue_json}}` / `{{catalogue_hash}}`, avec tests bloquants (placeholder présent, hash injectable, déterminisme) et preuve d'enregistrement du hash au manifest via AssetSpec.content_hashes.

Purpose: ROADMAP critère 5 — le catalogue verbatim + son hash sont câblés comme fixture du template de system prompt versionné, placeholder asserté par test, enregistrement du hash au manifest en place. La Phase 6 remplira le template sans refactor du mécanisme.
Output: `lottie_forge/prompts/` (render + template) + suite de tests verte.
</objective>

<execution_context>
@.opencode/gsd-core/workflows/execute-plan.md
@.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-style-verrouill-catalogue-de-recettes/02-CONTEXT.md
@.planning/phases/02-style-verrouill-catalogue-de-recettes/02-PATTERNS.md
@docs/project/05_Style.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mécanisme prompt-fixture + template squelette (D-13)</name>
  <files>lottie_forge/prompts/__init__.py, lottie_forge/prompts/render.py, lottie_forge/prompts/templates/recipe_picker.system.md, tests/prompts/test_prompt_fixture.py</files>
  <read_first>
    - 02-CONTEXT.md D-13 (mécanisme + template squelette ; seuls les placeholders sont contractuels, wording libre) + specifics (embarqué verbatim, pas de reformulation/troncature §5.1 #2)
    - docs/project/05_Style.md lignes 13-15 (§5.1 principes 1-2 : catalogue = fixture de system prompt, embarqué en entier) et ligne 151 (§5.5.3 — template du RecipePicker, test placeholder + hash au manifest)
    - 02-PATTERNS.md section "Prompt-fixture mechanism (D-13) + template" (contrat du mécanisme, no analog — content locked)
    - lottie_forge/loading/catalogue.py (load_catalogue_fixture → (RecipeCatalogue, catalogue_sha256), plan 02-04)
    - lottie_forge/loading/style.py (normalize_lf + sha256_hex — le texte injecté doit être celui des bytes hashés)
  </read_first>
  <action>
    1. lottie_forge/prompts/__init__.py (vide) ; lottie_forge/prompts/templates/recipe_picker.system.md : squelette minimal VERSIONNÉ en français avec exactement les deux placeholders contractuels {{catalogue_json}} et {{catalogue_hash}} chacun présent UNE fois, plus un wording squelette court (rôle RecipePicker, consigne de choisir un id dans le catalogue — la Phase 6 remplira, zéro refactor attendu du mécanisme).
    2. lottie_forge/prompts/render.py :
       - RECIPE_PICKER_TEMPLATE_PATH = Path(__file__).resolve().parents[1] / "prompts" / "templates" / "recipe_picker.system.md" (chemin constant, aucune override — T-02-02).
       - render_recipe_picker_prompt(catalogue_json: str, catalogue_hash: str) -> str : fonction PURE qui lit le template, remplace {{catalogue_json}} par le texte fourni et {{catalogue_hash}} par le hash fourni (str.replace simple — pas de moteur de template, pas d'appel réseau) ; lève ValueError si un placeholder subsiste après rendu (garde anti-placeholder-résiduel).
       - load_catalogue_prompt_fixture() -> tuple[str, str] : charge la fixture via load_catalogue_fixture() du plan 02-04 mais retourne le TEXTE BRUT — read_bytes → normalize_lf → decode utf-8 (exactement les bytes hashés, garantissant embarqué == hashé == committé) — et le hash. Ne JAMAIS ré-sérialiser via model_dump_json (ce ne serait plus verbatim).
    3. tests/prompts/test_prompt_fixture.py : (a) le template contient {{catalogue_json}} et {{catalogue_hash}} exactement une fois chacun ; (b) le rendu de load_catalogue_prompt_fixture ne contient AUCUN placeholder résiduel ; (c) le rendu contient le texte catalogue ENTIER (assert catalogue_text in rendered — verbatim, pas de troncature) et le hash 64-hex ; (d) déterminisme : deux rendus → bytes identiques ; (e) render avec hash bidon correctement injecté (injectabilité du hash, D-13).
  </action>
  <verify>
    <automated>python -m pytest tests/prompts/test_prompt_fixture.py -q</automated>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - le template porte {{catalogue_json}} et {{catalogue_hash}} (une occurrence chacun) — assert bloquant
    - le rendu contient le texte intégral du catalogue.json committé (LF) + le sha256 (64 hex minuscules) au régime D-03, et zéro placeholder résiduel
    - deux rendus consécutifs sont byte-identiques (déterminisme)
    - render_recipe_picker_prompt est pure et sans I/O réseau ; le chemin template est une constante du module
    - ruff vert
  </acceptance_criteria>
  <done>Le mécanisme prompt-fixture existe, pur et déterministe, avec le template squelette versionné aux deux placeholders contractuels — prêt à être rempli par la Phase 6 sans refactor.</done>
</task>

<task type="auto">
  <name>Task 2: Enregistrement du hash au manifest (critère 5 / D-16) — test de bout en bout</name>
  <files>tests/prompts/test_prompt_fixture.py, tests/bridge/fixtures.py</files>
  <read_first>
    - lottie_forge/domain/asset.py (ContentHashes 4 champs, plan 02-03)
    - tests/bridge/fixtures.py (make_asset — construire un asset avec les VRAIS sha des fixtures)
    - lottie_forge/loading/style.py (load_style_spec → style_sha256, plan 02-01)
    - lottie_forge/loading/catalogue.py (load_catalogue_fixture → catalogue_sha256, plan 02-04)
    - docs/project/05_Style.md ligne 52 (§5.2.2 : hash sha256 du YAML enregistré dans chaque manifest et chaque prompt) + ligne 151 (hash au manifest)
  </read_first>
  <action>
    1. Dans tests/prompts/test_prompt_fixture.py, test d'enregistrement manifest : charger les vrais hash via load_style_spec()[1] et load_catalogue_fixture()[1] ; construire un AssetSpec via make_asset en surchargeant content_hashes avec ces quatre valeurs (svg/lottie inchangés de make_asset, style_sha256/catalogue_sha256 = vrais sha des fixtures) ; asserter la validation stricte OK, le model_dump_json round-trip fidèle (les 4 hash présents et intacts après re-import strict) et que le rendu prompt (Task 1) expose exactement le même catalogue_sha256 que celui enregistré dans l'asset — la boucle prompt ↔ manifest est fermée sur la même valeur.
    2. Si make_asset ne permet pas la surcharge des hash proprement, ajouter un paramètre optionnel content_hashes=None à make_asset (défaut = littéraux actuels) — garde la single source of fixture truth sans casser les appels existants ; ne PAS dupliquer un second builder.
    3. Aucun zod mirror, aucune modification du workflow : ce test vit dans l'étape pytest existante.
  </action>
  <verify>
    <automated>python -m pytest tests/prompts/test_prompt_fixture.py -q</automated>
    <automated>python -m pytest tests/ -q</automated>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - un AssetSpec porte les vrais style_sha256/catalogue_sha256 des fixtures et re-parse strictement à l'identique (round-trip)
    - le catalogue_sha256 enregistré dans l'asset == celui injecté dans le prompt par load_catalogue_prompt_fixture (même valeur, même régime de hash)
    - make_asset reste retro-compatible (appels existants des suites bridge inchangés et verts)
    - ruff / biome verts ; suite globale pytest + vitest verte ; verify.yml intouché
  </acceptance_criteria>
  <done>La boucle est fermée : catalogue verbatim + hash injectés au prompt, le même hash enregistrable dans chaque manifest d'asset — ROADMAP critère 5 complet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixtures committées → prompt rendu | le contenu du catalogue devient future entrée LLM (Phase 6) |
| template versionné → rendu | substitution de placeholders sur fichier committé |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | injection via catalogue embarqué | medium | mitigate | le texte injecté est le fichier produit committé verbatim, hashé D-03, verrouillé par l'audit de couverture + same-commit 4 fichiers (plan 02-05) — toute altération passe par un commit visible et cohérent |
| T-02-10 | Tampering | placeholder résiduel dans le prompt | low | mitigate | garde ValueError si un placeholder subsiste après rendu + test d'absence de résidu |
| T-02-02 | Tampering | chemin du template | low | mitigate | constante relative au module, aucune override env/user |
| T-02-06 | Information Disclosure | — | low | accept | aucun secret ni donnée externe dans le template squelette ; payloads purement Python (pas de miroir, §4.10) |
</threat_model>

<verification>
- python -m pytest tests/prompts/test_prompt_fixture.py -q : vert (placeholders, verbatim, hash, déterminisme, manifest)
- python -m pytest tests/ -q : verte ; npx vitest run : verte (aucun impact ts attendu)
- ruff check . / npx biome check . : verts
- git diff --exit-code -- .github/workflows/verify.yml : vide (D-18)
</verification>

<success_criteria>
ROADMAP critère 5 verrouillé : le catalogue verbatim + son hash sont câblés comme fixture du template de system prompt versionné (placeholders `{{catalogue_json}}`/`{{catalogue_hash}}` assertés par test, rendu déterministe) et l'enregistrement du hash au manifest est en place (AssetSpec 4 champs alimenté par les vrais sha des fixtures). Aucune dépendance agent — la Phase 6 remplit le template, zéro refactor du mécanisme (D-13).
</success_criteria>

<output>
Create `.planning/phases/02-style-verrouill-catalogue-de-recettes/02-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Symboles créés par LA PHASE entière (les items de CE plan sont marqués ★) — registre complet dans 02-01-PLAN.md :

- ★ Python module : `lottie_forge.prompts.render` — `render_recipe_picker_prompt()`, `load_catalogue_prompt_fixture()`, `RECIPE_PICKER_TEMPLATE_PATH`
- ★ Template : `lottie_forge/prompts/templates/recipe_picker.system.md` (placeholders contractuels {{catalogue_json}} / {{catalogue_hash}})
- ★ Tests : `tests/prompts/test_prompt_fixture.py` ; MOD `tests/bridge/fixtures.py` (paramètre optionnel content_hashes sur make_asset si nécessaire)
- Autres artifacts de la phase : loader style + ThemeAnchorId + pyyaml (02-01) ; StyleRefinement + harnais (02-02) ; content_hashes 4 champs (02-03) ; catalogue + coverage-map + joint-load (02-04) ; gates + audit + scan_stale_pins (02-05)
