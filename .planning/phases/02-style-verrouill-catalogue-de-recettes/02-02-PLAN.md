---
phase: 02-style-verrouill-catalogue-de-recettes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lottie_forge/domain/style_refinement.py
  - tests/domain/test_style_refinement.py
  - src/rpc/contracts/style-refinement.schema.ts
  - src/rpc/contracts/style-refinement.spec.ts
  - fixtures/rejection-cases/style-refinement.json
  - tests/bridge/rejection_loader.py
  - src/rpc/contracts/rejection-cases.ts
  - fixtures/bridge/style-refinement.schema-keys.json
autonomous: true
requirements: [STY-02]
estimate:
  tokens: 32000
  raw_tokens: 16000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "StyleRefinement est un modèle clos delta-only : exactement 5 champs (sub_palette, motif, stroke_pick, radius_pick, accent_weight), extra=forbid — aucun champ ne peut porter un hex libre, un path, un svg ou une épaisseur numérique hors Literal (§5.3)"
    - "KebabToken rejette #fff et <path dans sub_palette/motif — la structure même du type rend le SVG/path data inexprimable (STY-02 partial, ROADMAP critère 4)"
    - "Le miroir zod StyleRefinementSchema (z.strictObject, mêmes Literal/bornes) rejette les mêmes payloads — parité de rejet via le harnais partagé (D-06/D-07/D-08 phase 1 répliqué)"
    - "Edge résolu par le contexte (sonde STY-02) : le check sub_palette ⊆ palette chargée est l'affaire du Translator Phase 7 — la Phase 2 livre le type + les bornes, rien de plus (§5.3)"
  artifacts:
    - "lottie_forge/domain/style_refinement.py — StyleRefinement (STRICT_CONFIG + KebabToken importés, jamais redéclarés)"
    - "src/rpc/contracts/style-refinement.schema.ts — StyleRefinementSchema + type StyleRefinement"
    - "fixtures/rejection-cases/style-refinement.json — cas partagés (hex-like, svg-like, unknown Literal, bornes, extra key)"
    - "tests/bridge/rejection_loader.py + src/rpc/contracts/rejection-cases.ts — entrée style-refinement enregistrée"
    - "fixtures/bridge/style-refinement.schema-keys.json — artefact parité de clés"
  key_links:
    - "style_refinement.py ↔ _shared.py (STRICT_CONFIG + KebabToken réutilisés, CR-01)"
    - "style-refinement.json ↔ rejection_loader.py (pytest parametrize) ↔ rejection-cases.ts (vitest it.each) — mêmes expect_paths des deux côtés"
    - "StyleRefinementSchema ↔ StyleRefinement.model_json_schema properties (parité de clés)"
  prohibitions:
    - ".github/workflows/verify.yml reste byte-identique (D-18)"
    - "pas de validateur fait main pour les tokens — KebabToken/StringConstraints uniquement (CR-01)"
    - "pas de champ hex/path/svg/numérique libre dans le modèle — le modèle clos est la gate STY-02"
    - "pas de chaîne bridge ordonnée dump→re-emit pour ce contrat en Phase 2 — rien n'émet encore de StyleRefinement (l'agent arrive en Phase 6) ; la parité = clés + rejets, comme le mandaté §5.3"
---

<objective>
Livrer le type `StyleRefinement` delta-only (STY-02 partial) des deux côtés : modèle Pydantic strict clos (§5.3 verbatim), miroir zod `StyleRefinementSchema`, et les cas de rejet partagés dans le harnais existant (hex-like `#fff`, svg-like `<path`, Literal inconnus, bornes, extra key). Rien n'émet encore ce modèle — l'agent StyleRefiner arrive en Phase 6 — la Phase 2 livre le type + la gate structurelle.

Purpose: ROADMAP critère 4 — un hex libre, un path ou un svg dans un champ est rejeté des deux côtés à la construction.
Output: `style_refinement.py` + `style-refinement.schema.ts` + harnais de rejet miroir vert.
</objective>

<execution_context>
@.opencode/gsd-core/workflows/execute-plan.md
@.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-style-verrouill-catalogue-de-recettes/02-CONTEXT.md
@.planning/phases/02-style-verrouill-catalogue-de-recettes/02-PATTERNS.md
@docs/project/05_Style.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modèle Pydantic StyleRefinement delta-only + tests structurels</name>
  <files>lottie_forge/domain/style_refinement.py, tests/domain/test_style_refinement.py</files>
  <read_first>
    - docs/project/05_Style.md lignes 58-73 (§5.3 — squelette StyleRefinement exact + règles delta-only)
    - 02-PATTERNS.md section "lottie_forge/domain/style_refinement.py" (imports _shared, patron Literal, pattern optionnel motif)
    - lottie_forge/domain/_shared.py lignes 21-33 (STRICT_CONFIG + KebabToken — importer, JAMAIS redéclarer)
    - lottie_forge/domain/recipe.py lignes 50-62 (patron Literal-enum + bornes Annotated)
    - tests/domain/test_style_spec.py (patron de suite domain : asserts structurels + rejets)
  </read_first>
  <action>
    Créer lottie_forge/domain/style_refinement.py avec UN modèle public StyleRefinement(BaseModel), model_config = STRICT_CONFIG importé de lottie_forge.domain._shared. Champs EXACTS §5.3 : sub_palette: list[KebabToken] = Field(min_length=1, max_length=16) ; motif: KebabToken | None = None ; stroke_pick: Literal["thin", "default", "bold"] = "default" ; radius_pick: Literal["small", "medium", "large"] = "medium" ; accent_weight: Annotated[float, Field(ge=0.0, le=1.0)] = 0.5. Aucun autre champ, aucun champ porteur de visuel (pas de hex, pas de path, pas de nombre d'épaisseur hors les deux Literal et le poids borné). Docstring : renvoie à §5.3, note que sub_palette ⊆ palette est vérifié au Translator Phase 7 (STY-02 partial).

    Tests tests/domain/test_style_refinement.py : (1) test structurel modèle clos — set(model_json_schema()["properties"]) == exactement les 5 clés ci-dessus ; (2) KebabToken rejette "#fff" et "<path" dans sub_palette (ValidationError à la construction) et dans motif ; (3) extra key rejetée (hex_override quelconque → ValidationError) ; (4) stroke_pick "thick" et radius_pick "huge" rejetés ; (5) accent_weight 1.5 et -0.1 rejetés, 0.0 et 1.0 acceptés ; (6) sub_palette [] rejeté (min 1), 17 items rejeté (max 16) ; (7) construction valide par défaut (motif None, défauts Literal) + model_dump_json round-trip.
  </action>
  <verify>
    <automated>python -m pytest tests/domain/test_style_refinement.py -q</automated>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - set des propriétés du json schema == {"sub_palette", "motif", "stroke_pick", "radius_pick", "accent_weight"} — modèle clos asserté
    - StyleRefinement(sub_palette=["#fff"]) lève ValidationError ; idem "<path" ; idem dans motif
    - hex_override (extra key) → ValidationError ; stroke_pick "thick" → ValidationError ; accent_weight 1.5 → ValidationError
    - construction par défaut valide ; sub_palette [] et 17 tokens → ValidationError
    - STRICT_CONFIG et KebabToken importés de _shared (aucune redéclaration dans le fichier)
    - ruff check vert
  </acceptance_criteria>
  <done>StyleRefinement existe en modèle clos delta-only strict : la structure du type rend hex/path/svg inexprimables, rejets testés côté Pydantic.</done>
</task>

<task type="auto">
  <name>Task 2: Miroir zod StyleRefinementSchema + parité de clés</name>
  <files>src/rpc/contracts/style-refinement.schema.ts, src/rpc/contracts/style-refinement.spec.ts, fixtures/bridge/style-refinement.schema-keys.json, tests/domain/test_style_refinement.py</files>
  <read_first>
    - 02-PATTERNS.md section "src/rpc/contracts/style-refinement.schema.ts + catalogue.schema.ts" (header miroir, regex constants, parité de clés)
    - src/rpc/contracts/style-spec.schema.ts lignes 1-19 et 71-89 (anatomie complète du miroir : docstring renvoyant au modèle py, patterns exportés, strictObject, superRefine)
    - src/rpc/contracts/style-spec.spec.ts (patron : keys parity + rejets)
    - lottie_forge/domain/style_refinement.py (le modèle Task 1 — source de vérité à refléter champ par champ)
  </read_first>
  <action>
    Créer src/rpc/contracts/style-refinement.schema.ts : docstring renvoyant au modèle Pydantic style_refinement.py (DM-05) ; StyleRefinementSchema = z.strictObject({ sub_palette: z.array(z.string().regex(TOKEN_NAME_PATTERN).max(64)).min(1).max(16), motif: z.string().regex(TOKEN_NAME_PATTERN).max(64).nullable().optional(), stroke_pick: z.enum(["thin", "default", "bold"]).default("default"), radius_pick: z.enum(["small", "medium", "large"]).default("medium"), accent_weight: z.number().min(0).max(1).default(0.5) }) — TOKEN_NAME_PATTERN importé de style-spec.schema.js (extension .js obligatoire, verbatimModuleSyntax). export type StyleRefinement = z.infer<typeof StyleRefinementSchema>. Les bornes reflètent le py EXACTEMENT (une borne unilatérale = drift).

    Parité de clés (patron *.schema-keys.json existant) : ajouter dans test_style_refinement.py un test nommé test_export_style_refinement_schema_keys (contient export → ramassé par pytest -k export) qui écrit fixtures/bridge/style-refinement.schema-keys.json = {"properties": sorted(model_json_schema()["properties"])}. Dans style-refinement.spec.ts : lire cet artefact (hard-throw si absent) et asserter sorted(Object.keys(StyleRefinementSchema.shape)) == artefact.

    style-refinement.spec.ts : tests positifs (payload valide par défaut parse OK), rejets miroirs locaux : "#fff" et "<path" dans sub_palette → safeParse false ; stroke_pick "thick" → false ; accent_weight 1.5 → false ; extra key → false ; sub_palette vide → false.
  </action>
  <verify>
    <automated>python -m pytest tests/domain/test_style_refinement.py -q -k export</automated>
    <automated>npx vitest run style-refinement</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - StyleRefinementSchema.strictObject rejette toute clé inconnue ; les 5 clés du shape correspondent (triées) aux properties du model_json_schema via l'artefact schema-keys
    - safeParse rejette "#fff", "<path", "thick", accent_weight 1.5, extra key, sub_palette vide — même surface de rejet que pydantic
    - import TOKEN_NAME_PATTERN depuis ./style-spec.schema.js (pas de seconde déclaration du pattern)
    - npx tsc --noEmit vert ; biome check vert
  </acceptance_criteria>
  <done>Le miroir zod est complet et aligné champ par champ (parité de clés testée via artefact), avec rejets locaux miroirs — prêt à recevoir les cas partagés du harnais.</done>
</task>

<task type="auto">
  <name>Task 3: Cas de rejet partagés style-refinement.json + enregistrement dans les deux loaders</name>
  <files>fixtures/rejection-cases/style-refinement.json, tests/bridge/rejection_loader.py, src/rpc/contracts/rejection-cases.ts, tests/domain/test_style_refinement.py, src/rpc/contracts/style-refinement.spec.ts</files>
  <read_first>
    - fixtures/rejection-cases/style-spec.json lignes 1-21 + cas extra-key (format D-08 : case_id, ref, model, payload complet avec UNE mutation, expect_paths optionnel — chemins de loc identiques des deux côtés, JAMAIS de comparaison de message)
    - tests/bridge/rejection_loader.py lignes 25-33 (REPO_ROOT + dict CONTRACT_FILES)
    - src/rpc/contracts/rejection-cases.ts lignes 27-32 (même mapping côté ts)
    - tests/bridge/test_style_spec_bridge.py lignes 78-89 (consommation pytest parametrize)
    - src/rpc/contracts/style-spec.spec.ts lignes 80-97 (consommation vitest it.each avec expect_paths)
    - 02-PATTERNS.md section "Rejection fixtures + harness registration"
  </read_first>
  <action>
    Créer fixtures/rejection-cases/style-refinement.json au format D-08, model "StyleRefinement", chaque payload = objet valide complet avec UNE mutation ciblée. Cas minimum (case_id explicites) : sr01-hex-like-sub-palette (sub_palette[0] = "#fff", expect_paths [["sub_palette", 0]]) ; sr02-svg-like-motif (motif = "<path", expect_paths [["motif"]]) ; sr03-kebab-newline (sub_palette[0] = "accent\n", expect_paths [["sub_palette", 0]]) ; sr04-unknown-stroke-pick ("thick", expect_paths [["stroke_pick"]]) ; sr05-unknown-radius-pick ("huge", expect_paths [["radius_pick"]]) ; sr06-accent-weight-above (1.5, expect_paths [["accent_weight"]]) ; sr07-accent-weight-below (-0.1, expect_paths [["accent_weight"]]) ; sr08-empty-sub-palette (expect_paths [["sub_palette"]]) ; sr09-oversized-sub-palette (17 tokens, expect_paths [["sub_palette"]]) ; sr10-extra-key (champ hex_override en plus, rejection-only sans expect_paths — asymétrie de loc documentée Phase 1).

    Enregistrer dans tests/bridge/rejection_loader.py : ajouter l'entrée "style-refinement": "style-refinement.json" au dict CONTRACT_FILES. Même entrée dans src/rpc/contracts/rejection-cases.ts.

    Consommation : dans test_style_refinement.py, parametrize pytest (ids = case_id) chargeant load_rejection_cases("style-refinement") et assertant ValidationError + membership des expect_paths dans les chemins d'erreur (patron test_style_spec_bridge.py lignes 78-89) — paths seuls, jamais les messages. Dans style-refinement.spec.ts, it.each miroir (patron style-spec.spec.ts lignes 80-97) : safeParse false + membership expect_paths.
  </action>
  <verify>
    <automated>python -m pytest tests/domain/test_style_refinement.py -q</automated>
    <automated>npx vitest run style-refinement</automated>
    <automated>python -m pytest tests/ -q</automated>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - les 10 cas sont consommés en parametrize côté pytest ET en it.each côté vitest (compter les tests : +10 de chaque côté)
    - chaque cas expect_paths non vide trouve son chemin de loc dans les erreurs pydantic ET dans les issues zod (membership JSON.stringify, pas de comparaison de message)
    - les deux loaders (rejection_loader.py CONTRACT_FILES, rejection-cases.ts) exportent l'entrée style-refinement
    - suites complètes pytest + vitest vertes ; ruff / biome / tsc verts
  </acceptance_criteria>
  <done>Le harnais de rejet partagé (D-06/D-07/D-08 phase 1) est répliqué pour StyleRefinement : un JSON, deux loaders, parité de rejet bloquante des deux côtés — dans les étapes CI existantes sans aucun edit du workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payload entrant → modèle | tout dict/JSON soumis à StyleRefinement / StyleRefinementSchema (futur output LLM en Phase 6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | DoS | bornes des listes/strings | medium | mitigate | min_length 1 / max_length 16 sur sub_palette, max 64 sur tokens (KebabToken), bornes 0..1 sur accent_weight — anti-DoS principe §4.1 #4, côté py ET zod |
| T-02-05 | Tampering | injection de visuel via le delta | medium | mitigate | modèle clos extra=forbid : hex libre, path et svg inexprimables (rejets sr01/sr02 bloquants des deux côtés) |
| T-02-06 | Information Disclosure | messages d'erreur | low | accept | erreurs locales mono-opérateur, chemins de loc seuls comparés dans le harnais |
</threat_model>

<verification>
- python -m pytest tests/domain/test_style_refinement.py -q : vert (+10 cas de rejet partagés)
- npx vitest run style-refinement : vert (miroir +10)
- python -m pytest tests/ -q -k export → npx vitest run → python -m pytest tests/ -q : chaîne globale verte (l'artefact schema-keys s'intègre à l'étape export existante)
- ruff check . / npx biome check . / npx tsc --noEmit : verts
- git diff --exit-code -- .github/workflows/verify.yml : vide (D-18)
</verification>

<success_criteria>
ROADMAP critère 4 verrouillé côté type : StyleRefinement est un modèle clos delta-only des deux côtés — "#fff" et "<path" sont rejetés à la construction (pydantic ET zod), prouvé par le harnais partagé. Aucune chaîne bridge dump/re-emit pour ce contrat (rien ne l'émet avant Phase 6), conformément à §5.3.
</success_criteria>

<output>
Create `.planning/phases/02-style-verrouill-catalogue-de-recettes/02-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Symboles créés par LA PHASE entière (les items de CE plan sont marqués ★) — registre complet dans 02-01-PLAN.md :

- ★ Python model : `StyleRefinement` (lottie_forge/domain/style_refinement.py)
- ★ TS : `StyleRefinementSchema`, `type StyleRefinement` (style-refinement.schema.ts)
- ★ Fixtures : `fixtures/rejection-cases/style-refinement.json` ; `fixtures/bridge/style-refinement.schema-keys.json`
- ★ MOD harness : `tests/bridge/rejection_loader.py` (entrée style-refinement), `src/rpc/contracts/rejection-cases.ts`
- ★ Tests : `tests/domain/test_style_refinement.py`, `src/rpc/contracts/style-refinement.spec.ts`
- Autres artifacts de la phase : voir section "Artifacts this phase produces" de 02-01-PLAN.md (loader style + ThemeAnchorId = 02-01 ; content_hashes 4 champs = 02-03 ; catalogue + coverage-map = 02-04 ; gates + audit = 02-05 ; prompt-fixture = 02-06)
