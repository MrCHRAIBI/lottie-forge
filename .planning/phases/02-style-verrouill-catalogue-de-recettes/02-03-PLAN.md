---
phase: 02-style-verrouill-catalogue-de-recettes
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]  # tests/bridge/fixtures.py partagé : make_style_spec aligné §5.2.2 en 02-01 (seul propriétaire), builders make_asset/_make_asset_for_pack édités ICI ensuite — séquencé, pas parallèle même vague
files_modified:
  - lottie_forge/domain/asset.py
  - src/rpc/contracts/asset-spec.schema.ts
  - fixtures/rejection-cases/asset-spec.json
  - fixtures/rejection-cases/pack-manifest.json
  - tests/bridge/fixtures.py
  - tests/domain/test_asset.py
  - tests/bridge/test_asset_bridge.py
  - src/rpc/contracts/asset-spec.spec.ts
autonomous: true
requirements: [MOT-04, STY-01]
estimate:
  tokens: 32000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "AssetSpec.content_hashes porte 4 champs : svg_sha256, lottie_sha256, style_sha256, catalogue_sha256 — chaque manifest d'asset embarque l'intégralité de ce qu'il a consommé (D-16)"
    - "L'extension est faite par ÉDITION same-commit §4.14 des trois lieux (asset.py + asset-spec.schema.ts + fixtures/rejection-cases/asset-spec.json) — aucun 5e champ ne peut passer extra=forbid"
    - "make_asset() ET _make_asset_for_pack() (single source of fixture truth — make_pack construit via son helper) produisent des assets à 4 hash ; make_asset expose un override optionnel content_hashes=None (consommé tel quel par le plan 02-06 — aucun edit concurrent du builder en Wave 3) ; sans l'extension de _make_asset_for_pack, make_pack() lève une ValidationError ; la chaîne bridge asset-spec reste verte"
    - "Migration de données pack-manifest.json : chaque payload inline porte les 4 hash et chaque cas conserve exactement UNE mutation ciblée — la signature collect-all du harnais Phase 1 (in08-doublons, totals-compte-incoherent, mono-style-mismatch) est préservée (suite vitest complète, non filtrée, verte à l'issue de CE plan — avant Wave 3)"
    - "Edge résolu par le contexte (sonde MOT-04) : le hash enregistré au manifest est la contrepartie du chargement bilingue — style_sha256/catalogue_sha256 seront remplis par les loaders des plans 02-01/02-04"
  artifacts:
    - "lottie_forge/domain/asset.py — ContentHashes étendu à style_sha256 + catalogue_sha256 (Sha256Hex réutilisé, pas de second type de hash)"
    - "src/rpc/contracts/asset-spec.schema.ts — ContentHashesSchema à 4 champs identique"
    - "fixtures/rejection-cases/asset-spec.json — cas 4-champs verrouillés (5e clé rejetée, digest malformé sur les 2 nouveaux champs)"
    - "fixtures/rejection-cases/pack-manifest.json — content_hashes inline migrés 2→4 champs, chaque cas garde sa seule mutation"
    - "tests/bridge/fixtures.py — make_asset() à 4 hash + paramètre optionnel content_hashes=None ; _make_asset_for_pack() à 4 hash (make_pack reste constructible)"
  key_links:
    - "ContentHashes.style_sha256 ↔ sha256_hex(loading/style.py, plan 02-01) ↔ content du prompt (plan 02-06)"
    - "ContentHashes.catalogue_sha256 ↔ load_catalogue_fixture (plan 02-04)"
    - "rejection-cases asset-spec ↔ pytest parametrize + vitest it.each existants (aucune nouvelle suite à créer)"
  prohibitions:
    - ".github/workflows/verify.yml reste byte-identique (D-18)"
    - "pas de second type de hash — Sha256Hex (pattern 64 hex minuscules) réutilisé tel quel"
    - "pas de mapping ouvert dans ContentHashes — modèle clos à exactement 4 champs (extension Phase 8 dotlottie_sha256 se fera par édition same-commit, jamais par champ libre)"
---

<objective>
Étendre `AssetSpec.content_hashes` de 2 à 4 champs (`style_sha256`, `catalogue_sha256`) par édition same-commit §4.14 des deux côtés (D-16) : AssetSpec devient le 5e contrat traversant touché par la Phase 2. Mettre à jour la single source de fixture truth (make_asset + _make_asset_for_pack, le helper consommé par make_pack) et le harnais de rejet (cas 4-champs verrouillés) pour que les critères ROADMAP 1 et 5 (« hash enregistré dans chaque manifest ») aient leur colonne de destination.

Purpose: chaque manifest d'asset porte l'intégralité de ce qu'il a consommé (style + catalogue), traçabilité STY-01/MOT-04. Les payloads committés de pack-manifest.json sont migrés en conséquence pour préserver la signature de rejet collect-all du harnais Phase 1.
Output: ContentHashes 4 champs des deux côtés, harnais de rejet étendu, pack-manifest.json migré, chaîne bridge asset-spec verte.
</objective>

<execution_context>
@.opencode/gsd-core/workflows/execute-plan.md
@.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-style-verrouill-catalogue-de-recettes/02-CONTEXT.md
@.planning/phases/02-style-verrouill-catalogue-de-recettes/02-PATTERNS.md
@docs/project/04_Modeles.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContentHashes 4 champs — édition same-commit py + ts + tests modèle</name>
  <files>lottie_forge/domain/asset.py, src/rpc/contracts/asset-spec.schema.ts, tests/domain/test_asset.py, src/rpc/contracts/asset-spec.spec.ts</files>
  <read_first>
    - lottie_forge/domain/asset.py lignes 65-111 (STYLE_REF_PATTERN ligne 65, Sha256Hex, ContentHashes 2 champs + docstring §4.14 qui anticipe précisément cette extension)
    - docs/project/04_Modeles.md §4.7 ligne 108 + §4.14 (règle same-commit : l'extension se fait en éditant le modèle clos, pas en passant une clé en fraude)
    - src/rpc/contracts/asset-spec.schema.ts lignes 45-62 (SHA256_HEX_PATTERN, Sha256HexSchema, ContentHashesSchema strictObject)
    - tests/domain/test_asset.py (tests existants : les assertions de lock 2 champs deviennent des assertions de lock 4 champs)
    - src/rpc/contracts/asset-spec.spec.ts (miroir vitest à mettre à jour)
    - 02-PATTERNS.md section "MOD asset.py + asset-spec.schema.ts — content_hashes 4 champs"
  </read_first>
  <action>
    Édition same-commit des DEUX côtés dans la même passe (règle §4.14 — si l'un des deux côtés manque, la parité de rejet rougit) :
    1. asset.py : ajouter à ContentHashes deux champs style_sha256: Sha256Hex et catalogue_sha256: Sha256Hex — réutiliser le type Sha256Hex existant, NE PAS créer de second type de hash. Mettre à jour la docstring de la classe : le mécanisme anticipé (§4.14) est désormais exercé par la Phase 2 (style_sha256/catalogue_sha256) ; dotlottie_sha256 restera une édition same-commit en Phase 8 ; le modèle reste clos à exactement 4 champs.
    2. asset-spec.schema.ts : ajouter les deux mêmes champs à ContentHashesSchema avec Sha256HexSchema identique. Aucun autre changement du schéma.
    3. tests/domain/test_asset.py : mettre à jour les tests existants qui construisent des ContentHashes à 2 champs (constructeurs → 4 hash valides distincts en hex minuscules 64) ; transformer les assertions de « 3e clé rejetée » en « 5e clé rejetée » (par ex. dotlottie_sha256 injecté → ValidationError, extra=forbid) ; ajouter des rejets de digest malformé sur les NOUVEAUX champs : style_sha256 en majuscules, catalogue_sha256 à 63 chars, valeur non-hex → ValidationError avec loc content_hashes.<champ>.
    4. asset-spec.spec.ts : miroir exact des mêmes mises à jour (safeParse false pour 5e clé et digests malformés sur style_sha256/catalogue_sha256, parse OK pour 4 hash valides).
    5. NE PAS toucher fixtures/rejection-cases/pack-manifest.json dans cette tâche — la migration de ses payloads inline (2→4 champs) est la première étape de la tâche 2 (même plan).
  </action>
  <verify>
    <automated>python -m pytest tests/domain/test_asset.py -q</automated>
    <automated>npx vitest run asset-spec</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ContentHashes (py) et ContentHashesSchema (ts) déclarent exactement svg_sha256, lottie_sha256, style_sha256, catalogue_sha256 — parité de clés verte (artefact asset-spec.schema-keys.json re-généré par l'étape export)
    - un payload avec une 5e clé hash est rejeté des deux côtés (extra=forbid / strictObject)
    - style_sha256 "ABC..." (majuscules) et catalogue_sha256 63 chars → rejetés des deux côtés avec loc sur le champ
    - Sha256Hex réutilisé (aucun nouveau type/pattern de hash déclaré)
    - pytest tests/domain/test_asset.py et vitest asset-spec verts ; tsc vert
  </acceptance_criteria>
  <done>ContentHashes est un modèle clos à 4 champs des deux côtés, édité same-commit, avec rejets de verrou testés — prêt à recevoir les vrais sha des plans 02-01/02-04.</done>
</task>

<task type="auto">
  <name>Task 2: make_asset 4 hash + override optionnel + migration pack-manifest.json + cas de rejet partagés + chaîne bridge verte</name>
  <files>tests/bridge/fixtures.py, fixtures/rejection-cases/asset-spec.json, fixtures/rejection-cases/pack-manifest.json, tests/bridge/test_asset_bridge.py, src/rpc/contracts/asset-spec.spec.ts</files>
  <read_first>
    - tests/bridge/fixtures.py lignes 97-149 (make_asset — single source of fixture truth ; note style_ref pinné sur make_style_spec().style_version pour cohérence STY-03) ET _make_asset_for_pack (helper interne consommé par make_pack — même extension 4 champs requise, sinon make_pack lève)
    - fixtures/rejection-cases/asset-spec.json (format D-08 + cas existants : uppercase/short/non-hex sur les 2 champs actuels + lock 2 champs à faire évoluer)
    - fixtures/rejection-cases/pack-manifest.json (TOUS les content_hashes inline à migrer 2→4 champs : cas in08-doublons-asset-id, totals-compte-incoherent, mono-style-mismatch, assets-51 — 51 assets inline —, license-terms-hors-literal, license-commercial-use-false, license-attribution-required-true, cost-eur-hors-bornes, pack-id-forme-invalide ; chaque payload porte actuellement uniquement svg_sha256/lottie_sha256)
    - src/rpc/contracts/pack-manifest.spec.ts lignes 191-201 (harnais Phase 1 : assertions de MEMBRESHIP sur expect_paths — in08 [["assets",0,"asset_id"],["assets",1,"asset_id"]], totals [["totals","asset_count"]], mono-style [["assets",0,"style_ref"]] — ces signatures collect-all exigent que les payloads échouent UNIQUEMENT sur leur mutation ciblée, pas au niveau champ content_hashes)
    - tests/bridge/test_asset_bridge.py (consommation parametrize + export/import bridge)
    - 02-PATTERNS.md section "Rejection fixtures + harness registration" (nouveaux cas à ajouter)
  </read_first>
  <action>
     1. tests/bridge/fixtures.py : make_asset() produit des content_hashes à 4 champs — svg_sha256/lottie_sha256 gardent leurs littéraux hex valides existants ; style_sha256 et catalogue_sha256 reçoivent deux littéraux 64-hex minuscules VALIDES et distincts des deux premiers (placeholders deterministes — les vrais sha seront branchés par le plan 02-06 qui consomme make_asset ; ne pas importer les loaders ici pour garder fixtures.py sans dépendance aux fixtures produit). Signer make_asset avec un paramètre optionnel content_hashes=None (défaut = les littéraux 4-hash ci-dessus) : les consommateurs qui ont besoin des VRAIS sha (plan 02-06, test d'enregistrement manifest) passent les 4 hash via ce paramètre sans dupliquer un second builder — la single source of fixture truth est préservée, tous les appels existants restent inchangés et verts. Docstring mise à jour (4 hash + override optionnel). Étendre AUSSI _make_asset_for_pack (helper interne consommé par make_pack, même fichier) : ses content_hashes passent aux 4 champs avec les MÊMES littéraux deterministes que make_asset — sinon make_pack() lève une ValidationError dès la construction du PackManifest et le test d'export pack-manifest rougit.
    2. fixtures/rejection-cases/pack-manifest.json — MIGRATION DE DONNÉES obligatoire consécutive à l'extension ContentHashes de la tâche 1 : ajouter style_sha256 et catalogue_sha256 au bloc content_hashes de TOUS les assets inline de TOUS les payloads (les 3 cas à assets détaillés in08/totals/mono-style, les 51 assets du cas assets-51, et l'asset unique des 5 cas license-*/cost-eur/pack-id), en utilisant LES MÊMES deux littéraux 64-hex minuscules deterministes que make_asset (cohérence de la fixture truth). NE modifier AUCUN autre champ : chaque cas conserve exactement UNE mutation ciblée (doublon asset_id pour in08, totals.asset_count faux pour totals-compte-incoherent, style_ref example-style@2.0.0 pour mono-style-mismatch, terms hors literal, commercial_use false, attribution true, cost négatif, pack_id sur-formé, 51 assets) et les expect_paths existants restent inchangés. Justification : sans cette migration, chaque payload échoue au niveau champ (content_hashes.style_sha256 manquant) AVANT les validateurs collect-all, ce qui fait échouer par membership les trois signatures expect_paths du harnais Phase 1 listées en read_first.
    3. fixtures/rejection-cases/asset-spec.json : faire évoluer les cas de lock existants (l'ancien cas « 3e clé rejetée » devient « 5e clé rejetée » : dotlottie_sha256 smugglé → expect_paths [["content_hashes"]]) et ajouter au format D-08 : as01-style-hash-uppercase (style_sha256 majuscules, expect_paths [["content_hashes", "style_sha256"]]) ; as02-catalogue-hash-short (catalogue_sha256 63 chars, expect_paths [["content_hashes", "catalogue_sha256"]]) ; as03-catalogue-hash-non-hex (expect_paths [["content_hashes", "catalogue_sha256"]]). Les payloads restent des objets valides complets avec UNE mutation.
    4. tests/bridge/test_asset_bridge.py : le test d'export (nom contient export) ré-écrit l'artefact depuis make_asset — il passe avec 4 hash sans autre changement ; la consommation parametrize des cas étendus est automatique (load_rejection_cases("asset-spec")).
    5. asset-spec.spec.ts : le harnais it.each consomme les nouveaux cas automatiquement ; vérifier qu'aucune expectation codée en dur à 2 champs ne subsiste dans le spec (deep-equal sur l'artefact from-python).
    6. Rejouer la chaîne ordonnée complète pour ce contrat : pytest -k export → vitest run → pytest -q.
  </action>
  <verify>
    <automated>python -m pytest tests/bridge/test_asset_bridge.py -q</automated>
    <automated>python -m pytest tests/ -q -k export</automated>
    <automated>npx vitest run</automated>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - make_asset() construit un AssetSpec valide à 4 hash et accepte content_hashes en paramètre optionnel ; le test d'export bridge passe et l'artefact from-python porte les 4 clés
    - _make_asset_for_pack() construit des assets à 4 hash ; make_pack() reste constructible et le test d'export pack-manifest est vert
    - tous les payloads de fixtures/rejection-cases/pack-manifest.json portent des content_hashes à 4 champs avec les mêmes littéraux deterministes que make_asset ; chaque cas conserve exactement UNE mutation ciblée et ses expect_paths d'origine ; les trois signatures collect-all (in08, totals, mono-style) restent trouvées par membership dans les suites pack-manifest
    - les nouveaux cas as01/as02/as03 + le cas de lock 5e clé passent en parametrize pytest ET it.each vitest avec les expect_paths exactes
    - la chaîne ordonnée complète (export → vitest → re-import) est verte ; zéro skip au junitxml ; la suite vitest complète (non filtrée) est verte avant Wave 2
    - ruff / biome / tsc verts ; git diff --exit-code -- .github/workflows/verify.yml vide
  </acceptance_criteria>
  <done>AssetSpec traversant à jour des deux côtés : 4 champs de hash, fixture truth unique à jour (override optionnel inclus), pack-manifest.json migré à signature de rejet préservée, harnais de rejet étendu, chaîne bridge verte — sans aucun edit du workflow CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payload entrant → ContentHashes | tout manifest/JSON soumis au contrat AssetSpec |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | hash de contenu falsifiable | medium | mitigate | les 4 hash sont des Sha256Hex stricts (64 hex minuscules) dans un modèle clos extra=forbid — un 5e champ ou un digest malformé est rejeté des deux côtés ; les valeurs réelles seront produites par les loaders D-02/D-03 (reproductibles sha256sum) |
| T-02-04 | DoS | tailles des champs | low | mitigate | min_length/max_length 64 fixes sur chaque champ (Sha256Hex) |
| T-02-06 | Information Disclosure | messages d'erreur | low | accept | comparaison par chemins de loc uniquement dans le harnais, jamais par message |
</threat_model>

<verification>
- python -m pytest tests/ -q : verte (asset + bridge + tous les rejets étendus)
- npx vitest run : verte
- chaîne ordonnée complète : pytest -k export → npx vitest run → pytest -q, artefacts asset-spec.* byte-identiques, zéro skip
- ruff check . / npx biome check . / npx tsc --noEmit : verts
- git diff --exit-code -- .github/workflows/verify.yml : vide (D-18)
</verification>

<success_criteria>
D-16 livré : content_hashes = {svg_sha256, lottie_sha256, style_sha256, catalogue_sha256} des deux côtés, extension same-commit §4.14, verrou de fermeture testé (5e clé rejetée), fixture truth et chaîne bridge à jour. Les critères ROADMAP 1 et 5 ont leur destination de hash en place.
</success_criteria>

<output>
Create `.planning/phases/02-style-verrouill-catalogue-de-recettes/02-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Symboles créés par LA PHASE entière (les items de CE plan sont marqués ★) — registre complet dans 02-01-PLAN.md :

- ★ MOD `lottie_forge/domain/asset.py` — `ContentHashes` 4 champs (Sha256Hex réutilisé)
- ★ MOD `src/rpc/contracts/asset-spec.schema.ts` — `ContentHashesSchema` 4 champs
- ★ MOD `fixtures/rejection-cases/asset-spec.json` — cas de lock 4 champs + digests malformés
- ★ MOD `fixtures/rejection-cases/pack-manifest.json` — content_hashes inline migrés 2→4 champs (signature collect-all préservée)
- ★ MOD `tests/bridge/fixtures.py` — `make_asset()` à 4 hash + paramètre optionnel content_hashes (consommé par 02-06)
- ★ Tests : `tests/domain/test_asset.py`, `tests/bridge/test_asset_bridge.py`, `asset-spec.spec.ts` (à jour)
- Autres artifacts de la phase : loader style + ThemeAnchorId (02-01) ; StyleRefinement (02-02) ; catalogue + coverage-map + joint-load (02-04) ; gates + audit + scan_stale_pins (02-05) ; prompt-fixture (02-06)
