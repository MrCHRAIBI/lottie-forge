---
phase: 02-style-verrouill-catalogue-de-recettes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - fixtures/style-specs/example-style/style.yaml
  - fixtures/style-specs/example-style/palette.json
  - lottie_forge/loading/__init__.py
  - lottie_forge/loading/style.py
  - tests/bridge/fixtures.py            # make_style_spec aligné §5.2.2 — 02-01 SEUL propriétaire de cet edit (02-03 builders séquencé via depends_on)
  - fixtures/bridge/style-fixture.from-python.json
  - fixtures/bridge/style-fixture.from-ts.json
  - lottie_forge/domain/vocabulary.py
  - src/rpc/contracts/vocabulary.schema.ts
  - tests/bridge/test_style_fixture_bridge.py
  - src/rpc/contracts/style-fixture.spec.ts
  - tests/domain/test_vocabulary.py
  - src/rpc/contracts/vocabulary.spec.ts
autonomous: false
requirements: [STY-01, MOT-03]
estimate:
  tokens: 42000
  raw_tokens: 21000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Le LOADER accepte le YAML verbatim §5.2.2 — gate style_id (format KebabToken + == nom du répertoire fixture) puis champ retiré AVANT StyleSpec.model_validate du reste ; aucun contrat traversant modifié (lottie_forge/domain/style.py + src/rpc/contracts/style-spec.schema.ts intacts) ; le sha256 (octets bruts, LF normalisés avant hachage) est recala­culable au sha256sum hors usine (D-02)"
    - "La fixture style traverse le bridge ordonné pytest→vitest→pytest sans drift (deep-equal + parité de clés) ; le YAML ne traverse jamais la frontière Py↔TS, seul le JSON validé la traverse (§5.2.2)"
    - "palette.json est dérivé de style.yaml ; un test de sync byte-compare la dérivation au fichier commité — toute dérive rougit la CI (D-04)"
    - "ThemeAnchorId = Literal fermé de 6 labels (primary, secondary, accent, background, success, danger), déclaré en UN seul endroit par couche (vocabulary.py / vocabulary.schema.ts), lockstep get_args==tuple asserté des deux côtés (D-10/D-11)"
    - "Edge résolu par le contexte (sonde STY-01) : la fixture est l'unique source de vérité du style — son hash sera porté par content_hashes (plan 02-03) et le prompt (plan 02-06)"
    - "Edge résolu par le contexte (sonde MOT-03 encoding) : les labels d'ancres sont des tokens kebab ASCII validés par KebabToken pydantic-core (CR-01), pas une notion de longueur ad hoc"
  artifacts:
    - "fixtures/style-specs/example-style/style.yaml (contenu §5.2.2 verbatim)"
    - "fixtures/style-specs/example-style/palette.json (dérivé + commité)"
    - "lottie_forge/loading/style.py — load_style_spec() avec gate style_id (KebabToken + == répertoire fixture, strip avant model_validate), normalize_lf(), sha256_hex()"
    - "tests/bridge/fixtures.py — make_style_spec() aligné sur §5.2.2 (400×300, bold 4.0, radii 0.0/8.0/16.0, 4 tokens, courbes [0.2,0.0,0.2,1.0]/[0.0,0.0,0.2,1.0]) — 02-01 seul propriétaire de cet edit (02-03 builders séquencé)"
    - "tests/bridge/test_style_fixture_bridge.py + fixtures/bridge/style-fixture.from-python.json|.from-ts.json"
    - "src/rpc/contracts/style-fixture.spec.ts"
    - "THEME_ANCHOR_IDS + ThemeAnchorId dans lottie_forge/domain/vocabulary.py"
    - "THEME_ANCHOR_IDS + ThemeAnchorIdSchema dans src/rpc/contracts/vocabulary.schema.ts"
    - "pyproject.toml : pyyaml>=6.0.2,<7 en dependencies (IN-02 dé-réservée)"
  key_links:
    - "style.yaml (octets bruts LF) ↔ sha256_hex ↔ future colonne content_hashes.style_sha256 (plans 02-03/02-06)"
    - "style-fixture.from-python.json ↔ StyleSpecSchema.parse (zod) ↔ re-import strict Pydantic — chaîne ordonnée existante"
    - "THEME_ANCHOR_IDS (py) ↔ THEME_ANCHOR_IDS (ts) ↔ futur theme_anchors de CatalogRecipe (plan 02-04)"
  prohibitions:
    - ".github/workflows/verify.yml reste byte-identique (D-18) — aucun edit du workflow"
    - "pas de répertoire baseline-frames/ créé dans cette phase (D-05)"
    - "pas de hash calculé sur un JSON ré- sérialisé — uniquement les octets bruts committés, LF normalisés (D-02)"
    - "pas de validateur kebab fait main — KebabToken / StringConstraints réutilisés depuis _shared.py (CR-01)"
    - "pas de seconde liste d'ids/labels déclarée hors des deux fichiers vocabulary (règle same-commit ADR-03)"
---

<objective>
Poser les fondations données de la Phase 2 : la fixture style `fixtures/style-specs/example-style/` (style.yaml §5.2.2 verbatim + palette.json dérivé), le chargeur Python — gate style_id côté loader (décision utilisateur : le loader valide le style_id du YAML verbatim — format KebabToken + == répertoire fixture — puis le retire avant model_validate ; aucun contrat traversant modifié) avec le régime de hash sha256 sur octets bruts LF-normalisés (D-02), la traversal bridge ordonnée de la fixture (§5.2.2), et le vocabulaire clos `ThemeAnchorId` (D-10/D-11) dont dépend le catalogue du plan 02-04. Inclut la gate de légitimité PyYAML (seule dépendance ajoutée par la phase, paquet [ASSUMED] sans table d'audit RESEARCH.md).

Note de repli (scope — 12 fichiers édités + 2 artefacts bridge générés) : si l'exécution sature, le découpage de repli est de détacher la Task 3 (ThemeAnchorId, D-10/D-11) en plan autonome séquentiel — elle ne partage aucun fichier avec les Tasks 1-2.

Purpose: STY-01 — le style devient une donnée versionnée et hashée, unique source de vérité ; socle MOT-03 — labels d'ancres clos partagés par les deux couches.
Output: fixture style commitée + hashable hors usine, loader `lottie_forge/loading/style.py`, chaîne bridge style-fixture verte, `ThemeAnchorId` des deux côtés.
</objective>

<execution_context>
@.opencode/gsd-core/workflows/execute-plan.md
@.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-style-verrouill-catalogue-de-recettes/02-CONTEXT.md
@.planning/phases/02-style-verrouill-catalogue-de-recettes/02-PATTERNS.md
@docs/project/05_Style.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Gate de légitimité PyYAML + installation (T-02-SC)</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (dependencies — commentaire de réservation pyyaml ligne 12)
    - docs/project/04_Modeles.md §4.12 (note IN-02 : pyyaml réservé Phase 2)
    - AGENTS.md (stack pins — pydantic pin exact ; pyyaml non pinné)
  </read_first>
  <action>
    Aucune table d'audit de légitimité n'existe (pas de RESEARCH.md cette passe) → politique de repli : le paquet est traité [ASSUMED] et cette gate bloquante remplace l'audit. AVANT toute installation : ouvrir https://pypi.org/project/PyYAML/ et confirmer qu'il s'agit du paquet canonique PyYAML (projet PyYAML, librairie YAML de référence de l'écosystème Python, téléchargements massifs, pas un typosquatter). Si un doute subsiste, HALT et reporter à l'utilisateur.
    Ensuite : éditer pyproject.toml — remplacer la ligne de commentaire "# pyyaml reserved for Phase 2 (RecipeCatalogue fixture loading, IN-02)" par la dépendance "pyyaml>=6.0.2,<7" dans la liste dependencies. Puis installer : python -m pip install -e ".[dev]" dans le venv du dépôt. Confirmer l'import (python -c "import yaml; print(yaml.__version__)").
  </action>
  <verify>
    <human-check>https://pypi.org/project/PyYAML/ montre le paquet canonique PyYAML (mainteneurs du projet PyYAML, pas un homoglyphe) — confirmer avant de poursuivre</human-check>
    <automated>python -c "import yaml; assert tuple(int(p) for p in yaml.__version__.split('.')[:2]) >= (6, 0); print('pyyaml', yaml.__version__)"</automated>
  </verify>
  <done>pyyaml>=6.0.2 est dans dependencies de pyproject.toml (commentaire de réservation supprimé), importable dans le venv, légitimité confirmée sur pypi.org.</done>
  <what-built>pyyaml ajouté aux dependencies (pyproject.toml, commentaire IN-02 supprimé) et installé dans le venv via pip install -e ".[dev]" — légitimité confirmée au préalable</what-built>
  <how-to-verify>
    - Ouvrir https://pypi.org/project/PyYAML/ : confirmer le paquet canonique PyYAML (projet PyYAML, librairie de référence, pas un typosquatter)
    - Lancer python -c "import yaml; print(yaml.__version__)" : version >= 6.0.2
  </how-to-verify>
  <resume-signal>Tapez "approved" pour poursuivre, ou décrivez le problème constaté sur la page pypi</resume-signal>
  <reversibility rating="costly">Ajouter une dépendance de parsing YAML est réversible en soi, mais le régime de chargement qui en dépend (D-02, costly dans CONTEXT.md) s'y ancre — d'où la gate explicite avant installation.</reversibility>
</task>

<task type="tracer">
  <name>Task 2 (tracer): Fixture style end-to-end — style.yaml hashé → loader Py → bridge ordonné → zod → re-import</name>
  <files>fixtures/style-specs/example-style/style.yaml, fixtures/style-specs/example-style/palette.json, lottie_forge/loading/__init__.py, lottie_forge/loading/style.py, tests/bridge/fixtures.py, fixtures/bridge/style-fixture.from-python.json, fixtures/bridge/style-fixture.from-ts.json, tests/bridge/test_style_fixture_bridge.py, src/rpc/contracts/style-fixture.spec.ts</files>
  <read_first>
    - docs/project/05_Style.md lignes 22-55 (§5.2.1 arborescence + §5.2.2 style.yaml canonique verbatim + règles de chargement bilingue)
    - .planning/phases/02-style-verrouill-catalogue-de-recettes/02-PATTERNS.md section "fixtures/style-specs/example-style/ + loaders + palette.json"
    - lottie_forge/domain/style.py (forme exacte de StyleSpec : noms de champs palette/easing_curves, bornes — NOTER : le modèle n'a PAS de champ style_id, extra=forbid ; la gate vit dans le loader, ce fichier ne doit PAS être édité)
    - lottie_forge/domain/_shared.py (KebabToken — réutilisé par la gate style_id du loader, CR-01 ; pas de validateur kebab fait main)
    - tests/bridge/test_style_spec_bridge.py lignes 41-89 (patron bridge ordonné : export → assert re-readable ; et harnais de rejet)
    - src/rpc/contracts/style-spec.spec.ts lignes 31-97 (patron vitest : hard-throw si artefact absent, deep-equal, ré-émission)
    - src/rpc/contracts/style-spec.schema.ts (StyleSpecSchema existant — le TS n'a RIEN de nouveau à valider, il réutilise ce schéma)
    - .gitattributes (ligne "* text=auto eol=lf" — prérequis du régime D-02)
    - tests/bridge/fixtures.py (make_style_spec — pour l'assertion anti-drift fixture==builder)
  </read_first>
  <action>
    TRACER — un seul chemin, production-quality, de bout en bout : style.yaml committé → loader Python (validation stricte + sha256) → export bridge → validation zod TS → ré-émission → re-import strict Pydantic. Aucune autre voie (pas de CLI, pas de batch, pas de multi-fixture).

    1. Créer fixtures/style-specs/example-style/style.yaml avec le contenu EXACT de docs/project/05_Style.md lignes 35-49 (style_id example-style, style_version 1.0.0, viewBox 400x300, stroke_widths thin 1.5 / default 2.5 / bold 4.0, corner_radii small 0.0 / medium 8.0 / large 16.0, 4 tokens palette ink #1F2430 / accent #FF6B4A / surface #F5F1EA / success #3E9B6E, 2 courbes standard [0.2,0.0,0.2,1.0] et entrance [0.0,0.0,0.2,1.0], style flow YAML du doc). NE PAS créer baseline-frames/ (D-05).
    2. Créer lottie_forge/loading/__init__.py (vide) et lottie_forge/loading/style.py avec : STYLE_FIXTURE_PATH = Path(__file__).resolve().parents[2] / "fixtures" / "style-specs" / "example-style" / "style.yaml" ; normalize_lf(raw: bytes) -> bytes (remplace b"\r\n" par b"\n") ; sha256_hex(data: bytes) -> str (hashlib.sha256(...).hexdigest() — hex minuscules, 64 chars ; c'est l'implémentation UNIQUE du régime D-02/D-03, le plan 02-04 l'importera pour catalogue_sha256) ; load_style_spec(path: Path = STYLE_FIXTURE_PATH) -> tuple[StyleSpec, str] qui lit read_bytes, normalise LF AVANT hachage, calcule le sha256, puis yaml.safe_load (jamais yaml.load nu — menace T-02-01), puis applique la GATE style_id AVANT model_validate : (a) clé style_id absente du mapping → rejet explicite (erreur citant la cause) ; (b) valeur validée par KebabToken (importé de lottie_forge.domain._shared — CR-01, pas de validateur kebab fait main) ; (c) valeur == path.parent.name (nom du répertoire fixture, example-style) — toute divergence → rejet ; puis retrait du champ style_id du mapping et SEULEMENT ENSUITE StyleSpec.model_validate sur le mapping restant. Le YAML committé reste §5.2.2 VERBATIM (style_id: example-style y demeure) ; la gate vit UNIQUEMENT dans le loader — AUCUN edit de lottie_forge/domain/style.py ni de src/rpc/contracts/style-spec.schema.ts (décision utilisateur, option loader ; D-16 reste « 5e contrat »). Aucun chemin issu d'env/user input — constantes sous REPO_ROOT (T-02-02).
    3. Test hash (dans test_style_fixture_bridge.py) : deux appels load_style_spec → même sha ; le sha fait 64 chars hex minuscules ; le sha recalculé indépendamment dans le test (read_bytes + normalize_lf + hashlib) == celui du loader ; docstring documente la vérification manuelle sha256sum (D-02 « vérifiable hors usine »).
    4. Tests de rejet de la gate style_id (décision utilisateur : id ≠ répertoire → rejet ; id absent → rejet) : copier le YAML verbatim dans tmp_path sous un répertoire NOMMÉ example-style (pour isoler chaque mutation), puis paramétré : (a) style_id muté en "wrong-style" → load_style_spec(copie) lève (id ≠ nom du répertoire) ; (b) ligne style_id supprimée de la copie → lève (clé absente) ; (c) valeur hors format kebab (par ex. "Wrong_Style") → lève (format). Chaque message cite sa cause (clé absente / format / divergence répertoire).
    5. Test anti-drift : StyleSpec chargé du YAML == make_style_spec() (deep equality). Si inégal, corriger make_style_spec() vers les valeurs §5.2.2 (la fixture est le canon verbatim, jamais l'inverse — 400×300, bold 4.0, radii 0.0/8.0/16.0, 4 tokens, courbes [0.2,0.0,0.2,1.0]/[0.0,0.0,0.2,1.0]) et noter le delta dans le SUMMARY. 02-01 est le SEUL propriétaire de l'edit make_style_spec dans la phase — 02-03 (builders make_asset/_make_asset_for_pack du même fichier) dépend de 02-01 via depends_on : séquencé, pas parallèle même vague.
    6. palette.json (D-04) : dériver de la spec chargée — tableau JSON plat des tokens dans l'ordre du YAML, objets à clés name+hex ; sérialisation stable json.dumps(..., indent=2, ensure_ascii=False) + newline final ; COMMITER le fichier. Test test_palette_json_in_sync : re-dériver et byte-comparer au fichier commité — divergence = échec (pas une seconde source manuelle).
    7. Bridge ordonné de la fixture (§5.2.2) : test test_export_style_fixture (nom contient export → ramassé par pytest -k export) écrit fixtures/bridge/style-fixture.from-python.json = enveloppe {"style_sha256": sha, "spec": spec.model_dump_json() en objet} ; puis assert re-lecture sous le modèle strict. Test d'import en skipif si .from-ts.json absent (garde d'ordre, zéro skip en CI) : StyleSpec.model_validate_json == original + même sha. Côté TS : src/rpc/contracts/style-fixture.spec.ts — hard-throw si l'artefact from-python manque (patron style-spec.spec.ts), StyleSpecSchema.parse de l'objet spec, deep-equal, regex sha sur style_sha256 (importer SHA256_HEX_PATTERN depuis asset-spec.schema.js, verbatimModuleSyntax → extension .js), ré-émission style-fixture.from-ts.json.
  </action>
  <verify>
    <automated>python -m pytest tests/bridge/test_style_fixture_bridge.py -q</automated>
    <automated>python -m pytest tests/ -q -k export</automated>
    <automated>npx vitest run style-fixture</automated>
    <automated>python -m pytest tests/ -q</automated>
    <automated>git diff --exit-code -- lottie_forge/domain/style.py src/rpc/contracts/style-spec.schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - le LOADER accepte le YAML verbatim §5.2.2 : gate style_id (format KebabToken + == nom du répertoire fixture) puis champ retiré AVANT StyleSpec.model_validate du reste — aucune ValidationError sur le contenu verbatim ; id ≠ répertoire → rejet, id absent → rejet, id hors format kebab → rejet
    - lottie_forge/domain/style.py et src/rpc/contracts/style-spec.schema.ts inchangés (git diff --exit-code) — la gate vit côté loader uniquement, décision utilisateur option (b)
    - load_style_spec() retourne (spec, sha) avec sha == hashlib.sha256(normalize_lf(file_bytes)).hexdigest(), 64 hex minuscules, stable entre deux appels
    - le test anti-drift passe : fixture chargée == make_style_spec() (ou builder corrigé vers §5.2.2, delta noté au SUMMARY) — edit make_style_spec porté par CE plan (02-03 builders séquencé après)
    - palette.json committé == dérivation recomputée (test byte-compare vert)
    - la chaîne pytest -k export → npx vitest run → pytest -q est verte avec l'artefact style-fixture.* ; zéro test skippé au junitxml
    - ruff check / npx biome check / npx tsc --noEmit verts
    - .github/workflows/verify.yml non modifié (git diff --exit-code -- .github/workflows/verify.yml)
  </acceptance_criteria>
  <done>Le chemin complet style.yaml (verbatim, style_id gate côté loader) → StyleSpec validé + sha256 reproductible → bridge ordonné vert (deep-equal TS, re-import Py égal) est committé et tourne dans les étapes CI existantes, sans aucun changement du workflow ni des contrats traversants.</done>
  <reversibility rating="costly">Le régime de hash (octets bruts LF-normalisés, D-02) est coûteux à changer : il invaliderait tous les hash enregistrés dans les manifests et prompts futurs. D'où le verrouillage dès le tracer, avec test de reproductibilité sha256sum.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Vocabulaire clos ThemeAnchorId (6 labels) des deux côtés (D-10/D-11)</name>
  <files>lottie_forge/domain/vocabulary.py, src/rpc/contracts/vocabulary.schema.ts, tests/domain/test_vocabulary.py, src/rpc/contracts/vocabulary.spec.ts</files>
  <read_first>
    - lottie_forge/domain/vocabulary.py lignes 30-101 (bloc RecipeId : tuple Final + Literal EXPLICITE listant les 10 ids en toutes lettres — PAS de star-unpack de RECIPE_IDS à la définition du type, pour garder les arguments résolus du Literal faciles à inspecter via typing.get_args, docstring lignes 63-67 ; le lien RECIPE_IDS ↔ Literal est maintenu par le lockstep testé get_args(RecipeId) == tuple(RECIPE_IDS) ; assert_recipe_count, self-check import)
    - src/rpc/contracts/vocabulary.schema.ts lignes 18-51 (RECIPE_IDS as const, type, z.enum, invariant module-eval)
    - tests/domain/test_vocabulary.py lignes 66 et 161-228 (lockstep get_args == tuple + scans structurels)
    - src/rpc/contracts/vocabulary.spec.ts (miroir vitest des assertions de vocabulaire)
    - 02-CONTEXT.md décisions D-10/D-11
  </read_first>
  <action>
    Répliquer le bloc RecipeId EXACTEMENT, dans les mêmes fichiers (patron self) :
    1. vocabulary.py : ajouter THEME_ANCHOR_IDS: Final[tuple[str, ...]] = ("primary", "secondary", "accent", "background", "success", "danger") puis ThemeAnchorId = Literal["primary", "secondary", "accent", "background", "success", "danger"] — forme Littérale EXPLICITE énumérant les 6 labels, même technique que le bloc RecipeId : PAS de star-unpack de THEME_ANCHOR_IDS à la définition du type, afin de garder les arguments résolus du Literal faciles à inspecter (typing.get_args) ; le lien THEME_ANCHOR_IDS ↔ Literal est garanti par le lockstep testé (ci-dessous), pas par le star-unpack. Pas d'invariant de count (ensemble fixé à 6, D-10) — mais conserver le lockstep : test assert get_args(ThemeAnchorId) == THEME_ANCHOR_IDS dans test_vocabulary.py, et un test de rejet : RecipeCatalogue n'existe pas encore — asserter au niveau type qu'un label inconnu (par ex. "logo") n'appartient pas au Literal via un test de membership sur get_args.
    2. vocabulary.schema.ts : ajouter THEME_ANCHOR_IDS = [...les 6 labels...] as const, export type ThemeAnchorId = (typeof THEME_ANCHOR_IDS)[number], export const ThemeAnchorIdSchema = z.enum(THEME_ANCHOR_IDS). Pas d'invariant runtime de count (fixe 6).
    3. tests : vocabulary.spec.ts asserte THEME_ANCHOR_IDS.length == 6, l'appartenance des 6 labels, et le rejet ThemeAnchorIdSchema.safeParse("logo").success == false ; test_vocabulary.py fait le miroir (set(get_args(ThemeAnchorId)) == set des 6, rejet de "logo" par membership).
    4. Aucun autre fichier ne déclare ces listes (la règle same-commit 4 fichiers sera scannée au plan 02-05 ; ici, simplement ne pas dupliquer).
    Ces labels ne sont PAS cross-validés avec les noms de tokens palette (D-12 : namespaces distincts, aucune couplage inventé).
  </action>
  <verify>
    <automated>python -m pytest tests/domain/test_vocabulary.py -q</automated>
    <automated>npx vitest run vocabulary</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ThemeAnchorId est un Literal de exactement 6 labels : primary, secondary, accent, background, success, danger (D-10)
    - get_args(ThemeAnchorId) == THEME_ANCHOR_IDS (py) et z.enum(THEME_ANCHOR_IDS) construit depuis la même const (ts) — lockstep vert des deux côtés
    - ThemeAnchorIdSchema.safeParse("logo") échoue ; le membership test py rejette "logo"
    - une seule déclaration par couche (vocabulary.py / vocabulary.schema.ts), zéro duplication ailleurs
    - ruff / biome / tsc verts ; suite vocabulary complète verte
  </acceptance_criteria>
  <done>ThemeAnchorId existe des deux côtés en vocabulaire clos same-commit, lockstep testé, prêt à être consommé par CatalogRecipe.theme_anchors (plan 02-04) — sans aucun edit du workflow CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dépôt → loader | fixtures committées (style.yaml) parsées et hashées au chargement |
| pip → venv | installation du paquet pyyaml (chaîne d'approvisionnement) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | pip install pyyaml | high | mitigate | gate blocking-human : légitimité confirmée sur pypi.org/project/PyYAML avant installation (paquet [ASSUMED], pas de table d'audit RESEARCH.md) |
| T-02-01 | Tampering | yaml parsing (loading/style.py) | medium | mitigate | yaml.safe_load obligatoire (jamais yaml.load nu) — pas de construction d'objets arbitraires depuis les fixtures |
| T-02-02 | Tampering | chemins de fixtures | low | mitigate | chemins constants sous REPO_ROOT dans loading/style.py, aucune override env/user input |
| T-02-03 | Tampering | régime de hash sha256 | medium | mitigate | normalisation LF avant hachage + .gitattributes eol=lf + test de reproductibilité (deux lectures → même sha) |
| T-02-04 | Repudiation | messages d'erreur Pydantic | low | accept | les erreurs citent l'input — outil local mono-opérateur, pas de surface exposée |
</threat_model>

<verification>
- python -m pytest tests/ -q : vert, zéro skip (junitxml)
- npx vitest run : vert
- python -m pytest tests/ -q -k export → npx vitest run → python -m pytest tests/ -q : chaîne ordonnée verte avec les artefacts style-fixture.*
- ruff check . / npx biome check . / npx tsc --noEmit : verts
- git diff --exit-code -- .github/workflows/verify.yml : vide (D-18)
- git diff --exit-code -- lottie_forge/domain/style.py src/rpc/contracts/style-spec.schema.ts : vide (gate style_id côté loader uniquement — décision utilisateur, contrats traversants intacts)
- sha256 du style.yaml recala­culable manuellement au sha256sum (après normalisation LF) == sha256_hex du loader
</verification>

<success_criteria>
STY-01 pose sa fondation : le style est une donnée versionnée, hashée (régime D-02 verrouillé), chargée sans drift des deux côtés via le bridge existant — le YAML verbatim §5.2.2 (avec son style_id) est accepté par le LOADER (gate + strip + model_validate), sans aucun edit des contrats traversants. Le vocabulaire ThemeAnchorId (D-10/D-11) est clos et partagé. Aucun edit du workflow CI (D-18). La légitimité PyYAML est confirmée par un humain avant installation (T-02-SC).
</success_criteria>

<output>
Create `.planning/phases/02-style-verrouill-catalogue-de-recettes/02-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Symboles créés par LA PHASE entière (cette liste est le registre complet — les items de CE plan sont marqués ★) :

- ★ Python : `lottie_forge.loading.style` — `STYLE_FIXTURE_PATH`, `normalize_lf()`, `sha256_hex()`, `load_style_spec()` (gate style_id loader-side : KebabToken + == répertoire fixture → strip → model_validate)
- ★ Python types : `THEME_ANCHOR_IDS`, `ThemeAnchorId` (vocabulary.py)
- ★ TS : `THEME_ANCHOR_IDS`, `ThemeAnchorId`, `ThemeAnchorIdSchema` (vocabulary.schema.ts)
- ★ Data : `fixtures/style-specs/example-style/style.yaml`, `fixtures/style-specs/example-style/palette.json`, `fixtures/bridge/style-fixture.from-python.json` / `.from-ts.json` (artefacts bridge générés par le test export)
- ★ `pyyaml` ajouté aux dependencies (pyproject.toml, IN-02)
- Python models : `StyleRefinement` (style_refinement.py, plan 02-02) ; `CatalogRecipe`, `RecipeCatalogue`, `KeyframeShape` (catalogue.py, plan 02-04) ; `ContentHashes` 4 champs (asset.py, plan 02-03)
- Python modules : `lottie_forge.loading.catalogue` (plan 02-04) ; `lottie_forge.gates.stale_pins` — `scan_stale_pins()`, `PinRecord`, `StalePinFlag` (plan 02-05) ; `lottie_forge.prompts.render` — `render_recipe_picker_prompt()`, template `recipe_picker.system.md` (plan 02-06)
- TS schemas : `StyleRefinementSchema` (02-02) ; `CatalogRecipeSchema`, `RecipeCatalogueSchema`, `JointCatalogueStyleSchema`, `KEYFRAME_SHAPES` (02-04) ; `ContentHashesSchema` 4 champs (02-03)
- Fixtures : `fixtures/recipe-catalogue/catalogue.json`, `fixtures/recipe-catalogue/coverage-map.json` (02-04) ; `fixtures/rejection-cases/style-refinement.json` (02-02), `fixtures/rejection-cases/catalogue.json` (02-05) ; MOD `fixtures/rejection-cases/asset-spec.json` (02-03)
- ★ Fixtures py partagées : MOD `tests/bridge/fixtures.py` — `make_style_spec()` aligné §5.2.2 porté par CE plan (02-03 : builders make_asset/_make_asset_for_pack, séquencé via depends_on)
- Tests : `tests/bridge/test_style_fixture_bridge.py` ★, `src/rpc/contracts/style-fixture.spec.ts` ★, MOD `tests/domain/test_vocabulary.py` ★ / `vocabulary.spec.ts` ★ ; `tests/domain/test_style_refinement.py` + `style-refinement.spec.ts` (02-02) ; MOD `test_asset.py` / `asset-spec.spec.ts` (02-03) ; `tests/domain/test_catalogue.py` + `catalogue.spec.ts` + `tests/bridge/test_catalogue_bridge.py` (02-04) ; `tests/domain/test_stale_pins.py` + MOD loaders de rejet (02-05) ; `tests/prompts/test_prompt_fixture.py` (02-06)
