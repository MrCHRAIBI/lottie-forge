---
phase: 02-style-verrouill-catalogue-de-recettes
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - lottie_forge/domain/catalogue.py
  - lottie_forge/loading/catalogue.py
  - tests/domain/test_catalogue.py
  - fixtures/recipe-catalogue/catalogue.json
  - fixtures/recipe-catalogue/coverage-map.json
  - src/rpc/contracts/catalogue.schema.ts
  - src/rpc/contracts/catalogue.spec.ts
  - tests/bridge/test_catalogue_bridge.py
  - fixtures/bridge/catalogue.from-python.json
  - fixtures/bridge/catalogue.schema-keys.json
autonomous: true
requirements: [MOT-01, MOT-02, MOT-03, MOT-04]
estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "catalogue.json contient exactement 10 recettes (ordre canonique RECIPE_IDS) déclarant id, family, duration_ms, easing, keyframe_shape, intensity_range, shapes_supported, trigger_points, theme_anchors — contenu = table §5.5.2 × matrice D-01, verbatim (D-01)"
    - "RecipeCatalogue (py) et RecipeCatalogueSchema (ts) chargent le JSON committé DIRECTEMENT, sans drift — parité deep-equal + parité de clés (MOT-04 chargement bilingue, §5.5.3 l.150)"
    - "Un id hors vocabulaire (disco-spin) est rejeté à la construction ; l'invariant 8..12 est borné au niveau liste ; family reste une STRING LIBRE — pas de Literal (§5.9)"
    - "theme_anchors >= 1 par recette, labels ∈ ThemeAnchorId clos (MOT-03 + D-10) — anchors et noms de tokens palette ne sont JAMAIS cross-validés (D-12)"
    - "La cross-référence easing ∈ StyleSpec.easing_curves est validée au chargement CONJOINT côté py ET côté ts (superRefine) — un easing hors StyleSpec rejette le chargement des deux côtés (D-17, ROADMAP critère 2)"
    - "Edge résolu par le contexte (sonde MOT-01 adjacency) : deux recettes d'id égal → rejet avec un issue par doublon et loc precise (« recipes », idx, « id ») — jamais de fusion silencieuse"
    - "Edge résolu par le contexte (sonde MOT-01 empty) : un catalogue de 7 recettes est rejeté (invariant 8..12) ; 13 recettes rejetées"
    - "Edge résolu par le contexte (sonde MOT-01 ordering) : l'ordre des recettes = l'ordre du tableau committé (canonique RECIPE_IDS) — les loaders ne réordonnent rien et les ids dupliqués sont interdits, la question d'ordre à égalité est donc close par construction"
    - "coverage-map.json (D-15 verbatim : 3 verticales fintech/dev-tools/e-commerce) existe comme donnée produit — son audit bloquant arrive au plan 02-05 ; aucun miroir zod nécessaire (ne traverse pas la frontière, règle §4.10)"
  artifacts:
    - "lottie_forge/domain/catalogue.py — CatalogRecipe, RecipeCatalogue, KeyframeShape (validateurs agrégés collect-all §5.5.3 1/2/4, patron pack.py)"
    - "lottie_forge/loading/catalogue.py — CATALOGUE_FIXTURE_PATH, COVERAGE_MAP_PATH, load_catalogue_fixture(), load_catalogue_with_style(), validate_easing_cross()"
    - "fixtures/recipe-catalogue/catalogue.json (catalogue_version 1.0.0 + 10 recettes verbatim)"
    - "fixtures/recipe-catalogue/coverage-map.json (D-15 verbatim)"
    - "src/rpc/contracts/catalogue.schema.ts — CatalogRecipeSchema, RecipeCatalogueSchema, JointCatalogueStyleSchema, KEYFRAME_SHAPES/SHAPE_NAMES/TRIGGER_POINTS"
    - "tests/domain/test_catalogue.py, tests/bridge/test_catalogue_bridge.py, catalogue.spec.ts"
    - "fixtures/bridge/catalogue.from-python.json + catalogue.schema-keys.json (parité)"
  key_links:
    - "CatalogRecipe.id ↔ RecipeId (importé du vocabulary, jamais redéclaré — ADR-03)"
    - "CatalogRecipe.theme_anchors ↔ ThemeAnchorIdSchema (plan 02-01, D-11)"
    - "load_catalogue_with_style ↔ load_style_spec (plan 02-01) — joint load D-17"
    - "catalogue.json ↔ catalogue_sha256 (sha256_hex du plan 02-01, régime D-03 identique à D-02)"
    - "coverage-map.json ↔ audit bloquant (plan 02-05, D-14)"
  prohibitions:
    - ".github/workflows/verify.yml reste byte-identique (D-18)"
    - "pas de Literal sur family — le catalogue est la source des familles (§5.9, D-01 note)"
    - "pas de re-déclaration des ids de recette hors vocabulary.py (import RecipeId obligatoire)"
    - "pas de cross-validation entre theme_anchors et les noms de tokens palette (D-12)"
    - "pas de cohérence trigger fine dans l'audit Phase 2 (affaire du contrat RecipePicker Phase 6, note D-15)"
    - "pas de ré-ordonnancement des recettes par les loaders — l'ordre committé fait foi"
---

<objective>
Faire du mouvement des **données versionnées** : modèles `CatalogRecipe`/`RecipeCatalogue` Pydantic strict + miroir zod avec les validateurs agrégés §5.5.3 (collect-all, patron pack.py), fixtures produit `catalogue.json` (10 recettes verbatim D-01 × §5.5.2) et `coverage-map.json` (D-15 verbatim), chargement bilingue sans drift (MOT-04), et cross-référence easing conjointe des deux côtés (D-17).

Note de repli (scope — 10 fichiers au seuil, estimate 52 %) : si l'exécution sature, le découpage de repli propre est de détacher la Task 3 (chargement conjoint D-17) en plan séquentiel dédié — les Tasks 1-2 restent autonomes.

Purpose: ROADMAP critère 2 — le catalogue fermé rejette disco-spin, un easing hors StyleSpec et theme_anchors vide, des deux côtés.
Output: catalogue chargé bilingue, validateurs agrégés bloquants, fixtures produit commitées (contenu verrouillé = données produit, pas d'invention d'agent).
</objective>

<execution_context>
@.opencode/gsd-core/workflows/execute-plan.md
@.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-style-verrouill-catalogue-de-recettes/02-CONTEXT.md
@.planning/phases/02-style-verrouill-catalogue-de-recettes/02-PATTERNS.md
@docs/project/05_Style.md
@docs/project/04_Modeles.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modèles CatalogRecipe + RecipeCatalogue avec validateurs collect-all §5.5.3</name>
  <files>lottie_forge/domain/catalogue.py, tests/domain/test_catalogue.py</files>
  <read_first>
    - docs/project/05_Style.md lignes 94-151 (§5.5.1 schéma JSON — clé « id » PAS recipe_id ; §5.5.3 modèle + les 4 validateurs agrégés)
    - 02-PATTERNS.md sections "lottie_forge/domain/catalogue.py" et "Shared Patterns / Collect-all validators"
    - lottie_forge/domain/pack.py lignes 183-294 (stratégie collect-all : InitErrorDetails + ValidationError.from_exception_data + locs precis — copier la stratégie exacte)
    - lottie_forge/domain/recipe.py lignes 44, 65-82 (imports vocabulary + enveloppe bornée : duration_ms 100..10000, theme_anchors max 16)
    - lottie_forge/domain/vocabulary.py (RECIPE_IDS/RecipeId/ThemeAnchorId du plan 02-01 — importer, jamais redéclarer)
    - lottie_forge/domain/_shared.py (STRICT_CONFIG + KebabToken)
  </read_first>
  <action>
    Décision de structure (documenter en docstring) : §5.5.3 dit que CatalogRecipe « étend » MotionRecipe, mais le JSON §5.5.1 utilise la clé « id » (pas recipe_id) et ne porte PAS de champ params. CatalogRecipe est donc un modèle standalone STRICT_CONFIG dont les champs partagent les types de MotionRecipe par import (RecipeId, KebabToken, ThemeAnchorId) — la « extension » est au niveau des types partagés, pas de l'héritage Python, car hériter imposerait params. Le nom de champ est id: RecipeId (fidèle au JSON verrouillé).

    Types locaux catalogue.py : KeyframeShape = Literal des 10 valeurs §5.5.2 (opacity-ramp, translate-in, overshoot-settle, scale-breath, trim-path, angular-in, pop-settle, sine-drift, damped-oscillation, circular-path) avec tuple Final KEYFRAME_SHAPES en lockstep ; ShapeName = Literal[rect, ellipse, path, polyline, polystar] ; TriggerPoint = Literal[enter, exit, loop] ; IntensityBound = Annotated[float, Field(ge=0.0, le=1.0)] ; intensity_range: tuple[IntensityBound, IntensityBound].

    CatalogRecipe (STRICT_CONFIG) : id: RecipeId ; family: KebabToken (string libre — PAS de Literal, §5.9) ; duration_ms: Annotated[int, Field(ge=100, le=10000)] ; easing: KebabToken ; keyframe_shape: KeyframeShape ; intensity_range: tuple ordonné (voir validator) ; shapes_supported: list[ShapeName] Field(min_length=1, max_length=5) ; trigger_points: list[TriggerPoint] Field(min_length=1, max_length=3) ; theme_anchors: list[ThemeAnchorId] Field(min_length=1, max_length=16) (MOT-03 >= 1 — au niveau champ, loc identique au catalogue).

    RecipeCatalogue (STRICT_CONFIG) : catalogue_version: str pattern ^\d+\.\d+\.\d+$ (points échappés) ; recipes: list[CatalogRecipe] Field(min_length=8, max_length=12) (invariant 8..12 niveau liste).

    UN model_validator(mode=after) collect-all (patron pack.py verbatim) couvrant les invariants agrégés §5.5.3, un InitErrorDetails par violation avec loc precise : (1) unicité des id — un issue par occurrence dupliquée, type duplicate_recipe_id, loc (« recipes », idx, « id ») ; (2) plage pack des durées — 600 <= duration_ms <= 1500 (bornes modèle 100..10000 conservées au champ), type duration_out_of_pack_range, loc (« recipes », idx, « duration_ms ») ; (3) intensité ordonnée — intensity_range[0] <= intensity_range[1], type intensity_range_unordered, loc (« recipes », idx, « intensity_range »). Le validator easing ∈ StyleSpec.easing_curves n'est PAS ici : il vit au chargement conjoint (Task 3, D-17). theme_anchors >= 1 est au champ (min_length=1) — loc identique, documenté en docstring comme équivalent du validateur 2 §5.5.3.
    Note de conformité strict : intensity_range étant un tuple, la validation du JSON committé passera par model_validate_json (mode JSON — les arrays deviennent tuples) ; les builders en code Python passent des tuples.

    Tests tests/domain/test_catalogue.py : builder de recette valide en code (une par id des 10) + catalogue valide 10 recettes ; rejets : disco-spin (loc recipes.idx.id) ; doublon fade×2 (DEUX issues ? non — un issue par occurrence en excès, assert membership des locs) ; 7 recettes (loc recipes) ; 13 recettes ; theme_anchors [] (loc recipes.idx.theme_anchors) ; ancre inconnue logo (loc recipes.idx.theme_anchors) ; intensity [1.0, 0.2] (loc recipes.idx.intensity_range) ; intensity 1.5 hors borne (loc champ) ; shapes ["circle"] ; triggers ["hover"] ; duration 500 et 1600 (hors plage pack, loc duration_ms) ; duration 99999 (hors bornes modèle) ; keyframe_shape "spin-around" ; family "Opacity" (majuscule → KebabToken rejette) ; catalogue_version "1.0" ; extra key top-level et nested.
  </action>
  <verify>
    <automated>python -m pytest tests/domain/test_catalogue.py -q</automated>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - un catalogue valide de 10 recettes se construit ; RecipeCatalogue.model_validate_json accepte une payload JSON array→tuple pour intensity_range
    - disco-spin → ValidationError ; doublon → au moins un issue avec loc (« recipes », idx, « id ») ; 7 et 13 recettes → ValidationError (bornes 8..12)
    - theme_anchors vide → ValidationError loc theme_anchors ; duration 500 → ValidationError loc duration_ms (plage pack 600..1500) ; intensity [1.0, 0.2] → loc intensity_range
    - family reste une string libre kebab (une nouvelle famille quelconque comme "blur" passe la validation du champ — pas de Literal)
    - les types RecipeId/ThemeAnchorId/KebabToken/STRICT_CONFIG sont importés (zéro re-déclaration locale des listes d'ids)
    - ruff check vert
  </acceptance_criteria>
  <done>Les modèles catalogue existent avec les validateurs agrégés §5.5.3 en collect-all (locs precis), MOT-01/02/03 enforce à la construction côté Python.</done>
</task>

<task type="auto">
  <name>Task 2: Fixtures produit catalogue.json + coverage-map.json (verbatim D-01/D-15) + miroir zod + parité bilingue</name>
  <files>fixtures/recipe-catalogue/catalogue.json, fixtures/recipe-catalogue/coverage-map.json, src/rpc/contracts/catalogue.schema.ts, src/rpc/contracts/catalogue.spec.ts, tests/bridge/test_catalogue_bridge.py, fixtures/bridge/catalogue.from-python.json, fixtures/bridge/catalogue.schema-keys.json</files>
  <read_first>
    - 02-CONTEXT.md D-01 (matrice intensity_range/shapes_supported/trigger_points verbatim) et D-15 (coverage-map verbatim)
    - docs/project/05_Style.md lignes 96-151 (§5.5.1 enveloppe JSON + §5.5.2 table des 10 recettes id/family/duration_ms/easing/keyframe_shape/theme_anchors)
    - 02-PATTERNS.md section "fixtures/recipe-catalogue/catalogue.json + coverage-map.json" (consommation directe des deux côtés — PAS la chaîne bridge à 3 artefacts pour le catalogue)
    - src/rpc/contracts/style-spec.schema.ts lignes 71-89 (superRefine — gabarit des validateurs miroirs)
    - src/rpc/contracts/recipe.schema.ts lignes 1-3, 32-38 (import RecipeIdSchema depuis vocabulary.schema.js)
    - lottie_forge/loading/style.py (sha256_hex — calculer catalogue_sha256 au même régime D-03)
  </read_first>
  <action>
    1. fixtures/recipe-catalogue/catalogue.json : enveloppe §5.5.1 — catalogue_version "1.0.0", recipes tableau des 10 recettes dans l'ordre canonique RECIPE_IDS (fade, slide, bounce, pulse, draw-on, rotate, scale-pop, float, wiggle, orbit). Champs par recette = table §5.5.2 (fade/opacity/800/standard/opacity-ramp/[primary, accent] ; slide/transform/1000/standard/translate-in/[primary] ; bounce/transform/1200/entrance/overshoot-settle/[primary, accent] ; pulse/scale/900/standard/scale-breath/[accent] ; draw-on/stroke/1200/entrance/trim-path/[accent] ; rotate/transform/1100/standard/angular-in/[primary] ; scale-pop/scale/700/entrance/pop-settle/[primary, accent] ; float/transform/1400/standard/sine-drift/[primary, background] ; wiggle/transform/800/standard/damped-oscillation/[accent] ; orbit/transform/1500/standard/circular-path/[primary, accent]) croisée avec la matrice D-01 (fade [0.0,1.0] rect,ellipse,path [enter,exit] ; draw-on [0.2,1.0] path,polyline [enter] ; slide [0.2,1.0] rect,ellipse,path [enter,exit] ; bounce [0.3,1.0] rect,ellipse,path,polyline [enter] ; pulse [0.1,0.8] rect,ellipse,polystar [enter,loop] ; rotate [0.2,1.0] path,polyline,polystar [enter] ; scale-pop [0.2,1.0] rect,ellipse,polystar [enter] ; float [0.1,0.8] rect,ellipse,path [loop] ; wiggle [0.1,0.5] rect,ellipse,path,polyline [enter] ; orbit [0.3,1.0] ellipse,path,polystar [loop]). Formatage : json indent 2 + newline final, commité. Cohérence D-17 : tous les easing valent standard ou entrance (les 2 courbes de la fixture style) — vérifié au Task 3.
    2. fixtures/recipe-catalogue/coverage-map.json (D-15 verbatim) : structure {"verticals": [{"name": "fintech", "states": [{"state_id": "...", "recipes": [...]}]}, ...]} avec les mappings EXACTS de D-15 en state_id kebab — fintech : payment-success [draw-on, scale-pop], payment-declined [wiggle, slide], alert-dismissed [slide, fade], recurring-sync [orbit], balance-milestone [pulse, scale-pop] ; dev-tools : deploy-success [draw-on, bounce], pipeline-running [orbit, float], build-failed [wiggle, slide], commit-incoming [fade, slide], env-badge-ready [scale-pop, pulse] ; e-commerce : cart-added [scale-pop, slide], favorite-added [pulse], order-confirmed [draw-on, fade], promo-banner [float, slide], form-error [wiggle], product-carousel [rotate, orbit]. Pas de miroir zod pour ce fichier (ne traverse pas la frontière, §4.10).
    3. src/rpc/contracts/catalogue.schema.ts : const KEYFRAME_SHAPES (10 valeurs) + KeyframeShapeSchema = z.enum, SHAPE_NAMES + z.enum, TRIGGER_POINTS + z.enum (lockstep testé avec py via schema-keys) ; CatalogRecipeSchema = z.strictObject({ id: RecipeIdSchema (import vocabulary.schema.js), family: z.string().regex(TOKEN_NAME_PATTERN).max(64), duration_ms: z.number().int().min(100).max(10000), easing: token, keyframe_shape: KeyframeShapeSchema, intensity_range: z.tuple([z.number().min(0).max(1), z.number().min(0).max(1)]), shapes_supported: z.array(ShapeNameSchema).min(1).max(5), trigger_points: z.array(TriggerPointSchema).min(1).max(3), theme_anchors: z.array(ThemeAnchorIdSchema).min(1).max(16) }) ; RecipeCatalogueSchema = z.strictObject({catalogue_version: regex, recipes: z.array(CatalogRecipeSchema).min(8).max(12)}).superRefine miroir des invariants agrégés avec les MÊMES chemins : unicité id (path ["recipes", idx, "id"]), plage durée 600..1500 (path ["recipes", idx, "duration_ms"]), intensité ordonnée (path ["recipes", idx, "intensity_range"]). Export type RecipeCatalogue = z.infer.
    4. Parité bilingue MOT-04 (chargement DIRECT des deux côtés, pas de bridge 3-artefacts) : dans tests/bridge/test_catalogue_bridge.py — test_export_catalogue (contient export) écrit fixtures/bridge/catalogue.from-python.json (model_dump_json du catalogue chargé du fichier committé) + catalogue.schema-keys.json (clés triées de RecipeCatalogue) ; test de parité : RecipeCatalogue.model_validate_json(committed) == modèle re-importé de l'artefact. Dans catalogue.spec.ts : lire le fichier committé catalogue.json directement, RecipeCatalogueSchema.parse, deep-equal avec JSON.parse de l'artefact from-python, + keys parity vs schema-keys + lockstep KEYFRAME_SHAPES/SHAPE_NAMES/TRIGGER_POINTS vs artefact schema-keys.
    5. Test hash (même régime D-03) : dans test_catalogue_bridge.py, catalogue_sha256 = sha256_hex(normalize_lf(bytes committés)) — 64 hex, stable entre deux lectures, importé de loading/style.py.
  </action>
  <verify>
    <automated>python -m pytest tests/bridge/test_catalogue_bridge.py -q</automated>
    <automated>python -m pytest tests/ -q -k export</automated>
    <automated>npx vitest run catalogue</automated>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - catalogue.json : exactement 10 recettes, ordre canonique, tous les champs D-01/§5.5.2 verbatim ; RecipeCatalogue.model_validate_json ET RecipeCatalogueSchema.parse l'acceptent (chargement bilingue vert)
    - deep-equal : parse ts(committed) == JSON.parse(artefact from-python) ; keys parity verte ; KEYFRAME_SHAPES lockstep py/ts
    - catalogue_sha256 stable, 64 hex minuscules, recala­culable au sha256sum sur le fichier committé (régime D-03)
    - coverage-map.json : 16 états au total (5 fintech + 5 dev-tools + 6 e-commerce), mappings exacts D-15
    - superRefine ts rejette doublon/durée 500/intensité inversée avec les mêmes chemins que py (tests locaux de mutation dans catalogue.spec.ts)
    - ruff / biome / tsc verts
  </acceptance_criteria>
  <done>Le catalogue est une donnée produit versionnée chargée SANS drift par les deux couches, avec hash au régime D-03 ; la coverage-map D-15 est commitée prête pour l'audit du plan 02-05.</done>
</task>

<task type="auto">
  <name>Task 3: Chargement conjoint easing ∈ StyleSpec.easing_curves — py ET ts (D-17)</name>
  <files>lottie_forge/loading/catalogue.py, tests/bridge/test_catalogue_bridge.py, src/rpc/contracts/catalogue.schema.ts, src/rpc/contracts/catalogue.spec.ts</files>
  <read_first>
    - 02-CONTEXT.md D-17 (cross-référence validée au chargement conjoint des deux côtés ; parité de rejet MOT-04 inclut « easing inconnu »)
    - docs/project/05_Style.md ligne 147 (§5.5.3 validateur 3 — cross-référence au chargement conjoint catalogue + style)
    - lottie_forge/domain/style.py (EasingCurve.name — la source des noms valides)
    - lottie_forge/loading/style.py (load_style_spec du plan 02-01)
    - src/rpc/contracts/style-spec.schema.ts (StyleSpecSchema — importé par le schema conjoint ts)
  </read_first>
  <action>
    1. lottie_forge/loading/catalogue.py : CATALOGUE_FIXTURE_PATH et COVERAGE_MAP_PATH (constantes REPO_ROOT, chemins figés — T-02-02) ; load_catalogue_fixture(path = CATALOGUE_FIXTURE_PATH) -> tuple[RecipeCatalogue, str] (read_bytes → normalize_lf → sha256_hex → RecipeCatalogue.model_validate_json) ; fonction PURE validate_easing_cross(catalogue: RecipeCatalogue, easing_names: AbstractSet[str]) -> None en collect-all (un InitErrorDetails par recette offense, type unknown_easing, loc (« recipes », idx, « easing »)) — réutilisable par les tests avec des paires arbitraires ; load_catalogue_with_style() -> tuple[RecipeCatalogue, StyleSpec, str, str] qui charge les deux fixtures et appelle validate_easing_cross(catalogue, {c.name pour c dans spec.easing_curves}) — échoue dur si un easing ne correspond pas.
    2. Test conjoint py : load_catalogue_with_style() sur les fixtures committées → vert (les 10 easings valent standard/entrance) ; mutations : catalogue avec easing "overshoot" → ValidationError loc (« recipes », idx, « easing ») ; style avec easing_curves amputée de entrance → draw-on/bounce/scale-pop rejetés (ceux qui référencent entrance).
    3. TS : dans catalogue.schema.ts, exporter JointCatalogueStyleSchema = z.strictObject({ catalogue: RecipeCatalogueSchema, style: StyleSpecSchema (import style-spec.schema.js) }).superRefine — pour chaque recette dont easing n'appartient pas au set des noms de style.easing_curves, ctx.addIssue path ["catalogue", "recipes", idx, "easing"].
    4. catalogue.spec.ts : parité de rejet conjointe — les deux mêmes mutations (easing inconnu ; style sans entrance) → safeParse false avec les chemins attendus ; le couple committé (catalogue.json + style-fixture exporté au plan 02-01) parse vert. C'est la parité MOT-04 « easing inconnu » (D-17).
    5. Ne PAS enregistrer ces cas dans le harnais de rejet partagé (le harnais catalogue du plan 02-05 couvre les rejets intrinsèques ; la parité conjointe vit dans ces suites dédiées) — le documenter dans le SUMMARY.
  </action>
  <verify>
    <automated>python -m pytest tests/bridge/test_catalogue_bridge.py -q</automated>
    <automated>npx vitest run catalogue</automated>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - load_catalogue_with_style() sur les fixtures committées → (catalogue, style, style_sha256, catalogue_sha256) sans erreur ; les 10 easing valent standard ou entrance
    - easing "overshoot" injecté → ValidationError ET safeParse false, avec loc/path (« recipes », idx, « easing ») des deux côtés (parité D-17/MOT-04)
    - style amputé de la courbe entrance → les recettes draw-on/bounce/scale-pop rejetées des deux côtés
    - validate_easing_cross est une fonction pure réutilisable (pas d'I/O)
    - JointCatalogueStyleSchema importe StyleSpecSchema (pas de redéclaration du style côté ts)
    - ruff / biome / tsc verts ; chaîne pytest/vitest globale verte
  </acceptance_criteria>
  <done>Le chargement conjoint rejette un easing hors StyleSpec des deux côtés avec parité de chemins — ROADMAP critère 2 (volet easing) verrouillé, et les deux fixtures produit sont désormais chargables ensemble avec leurs hash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixtures committées → loaders (py + ts) | catalogue.json / coverage-map.json parsés au chargement |
| payload entrant → modèles | tout dict/JSON soumis à CatalogRecipe / RecipeCatalogue |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Tampering | catalogue.json comme future entrée de prompt | medium | mitigate | contenu produit verrouillé verbatim (D-01/D-15, revu au plan), hash D-03 reproductible, audit de couverture + règle same-commit bloquants (plan 02-05) — toute altération du catalogue exige un commit visible cohérent sur 4 fichiers |
| T-02-04 | DoS | tailles non bornées | medium | mitigate | bornes partout : recipes 8..12, anchors 1..16, shapes 1..5, triggers 1..3, durées 100..10000 (pack 600..1500), tokens max 64 — des deux côtés |
| T-02-02 | Tampering | chemins de fixtures | low | mitigate | constantes REPO_ROOT dans loading/catalogue.py, aucune override |
| T-02-01 | Tampering | parsing JSON/YAML | low | mitigate | json/YAML safe (model_validate_json / JSON.parse) sur fichiers committés ; aucun eval, aucune exécution |
| T-02-06 | Information Disclosure | messages d'erreur collect-all | low | accept | chemins de loc seuls comparés dans les tests, jamais les messages |
</threat_model>

<verification>
- python -m pytest tests/domain/test_catalogue.py tests/bridge/test_catalogue_bridge.py -q : vert
- python -m pytest tests/ -q -k export → npx vitest run → python -m pytest tests/ -q : chaîne verte (artefacts catalogue.*)
- npx vitest run catalogue : vert (parité bilingue + superRefine + joint load)
- ruff check . / npx biome check . / npx tsc --noEmit : verts
- git diff --exit-code -- .github/workflows/verify.yml : vide (D-18)
</verification>

<success_criteria>
ROADMAP critère 2 verrouillé : catalogue.json = 10 recettes (invariant 8..12 borné) avec tous les champs mandatés ; disco-spin, un easing hors StyleSpec (joint load D-17) et theme_anchors vide rejettent le chargement des deux côtés. MOT-04 chargement bilingue sans drift prouvé (deep-equal + keys parity). Les 10 recettes du catalogue fermé justifient leur slot via la coverage-map D-15 commitée (audit bloquant au plan 02-05).
</success_criteria>

<output>
Create `.planning/phases/02-style-verrouill-catalogue-de-recettes/02-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Symboles créés par LA PHASE entière (les items de CE plan sont marqués ★) — registre complet dans 02-01-PLAN.md :

- ★ Python models : `CatalogRecipe`, `RecipeCatalogue`, `KeyframeShape` (lottie_forge/domain/catalogue.py)
- ★ Python loaders : `lottie_forge.loading.catalogue` — `CATALOGUE_FIXTURE_PATH`, `COVERAGE_MAP_PATH`, `load_catalogue_fixture()`, `validate_easing_cross()`, `load_catalogue_with_style()`
- ★ Data : `fixtures/recipe-catalogue/catalogue.json` (10 recettes verbatim), `fixtures/recipe-catalogue/coverage-map.json` (D-15)
- ★ TS : `CatalogRecipeSchema`, `RecipeCatalogueSchema`, `JointCatalogueStyleSchema`, `KEYFRAME_SHAPES`, `SHAPE_NAMES`, `TRIGGER_POINTS` (catalogue.schema.ts)
- ★ Tests : `tests/domain/test_catalogue.py`, `tests/bridge/test_catalogue_bridge.py`, `src/rpc/contracts/catalogue.spec.ts`
- ★ Artefacts parité : `fixtures/bridge/catalogue.from-python.json`, `fixtures/bridge/catalogue.schema-keys.json`
- Autres artifacts de la phase : loader style + ThemeAnchorId (02-01) ; StyleRefinement (02-02) ; content_hashes 4 champs (02-03) ; gates + audit + scan_stale_pins (02-05) ; prompt-fixture (02-06)
