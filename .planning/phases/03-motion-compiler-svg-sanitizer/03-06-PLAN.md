---
phase: 03-motion-compiler-svg-sanitizer
plan: 06
type: execute
wave: 6
depends_on: ["03-05"]
files_modified:
  - scripts/compile-stdin.ts
  - scripts/update-goldens.mjs
  - package.json
  - src/motion-compiler/__tests__/compiler.spec.ts
  - src/motion-compiler/__tests__/determinism.spec.ts
  - src/motion-compiler/__tests__/ids.spec.ts
  - src/motion-compiler/__tests__/goldens/a-001.fade.golden.json
  - src/motion-compiler/__tests__/goldens/a-002.slide.golden.json
  - src/motion-compiler/__tests__/goldens/a-003.bounce.golden.json
  - src/motion-compiler/__tests__/goldens/a-004.pulse.golden.json
  - src/motion-compiler/__tests__/goldens/a-005.draw-on.golden.json
  - src/motion-compiler/__tests__/goldens/a-006.rotate.golden.json
  - src/motion-compiler/__tests__/goldens/a-007.scale-pop.golden.json
  - src/motion-compiler/__tests__/goldens/a-008.float.golden.json
  - src/motion-compiler/__tests__/goldens/a-009.wiggle.golden.json
  - src/motion-compiler/__tests__/goldens/a-010.orbit.golden.json
  - src/motion-compiler/__tests__/goldens/a-011.galerie.golden.json
autonomous: true
requirements: [COM-01, SAN-03]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Two independent compiler processes (double spawn, ≥ 1 s apart) produce byte-identical output for every golden fixture — three-way diff A/B/golden (COM-01 concurrency edge, D-26/D-37)"
    - "All 11 goldens compare byte-for-byte via Buffer.compare against exactly the delivered bytes (compact JSON + final newline, D-24)"
    - "IDs assigned by the compiler are identical between two regenerations on the SANITIZED output — diff = ∅ (SAN-03, tested post-sanitize per Pitfall 6)"
    - "Every golden shows visible ink (non-dégénérescence) and the D-15 pose (one-shot → finale, loop → t=0) (D-37)"
  artifacts:
    - "scripts/compile-stdin.ts — stdin RenderSpec → golden-envelope bytes on stdout (tsx-run, reused by determinism proof + goldens:update)"
    - "scripts/update-goldens.mjs — refuses CI=true, regenerates the 11 goldens atomically (D-25/D-37)"
    - "11 goldens as combined envelopes { lottie, svg, renderer_support } (planning decision on Open Question 2 — covers COM-01 'sorties identiques' in the full sense)"
    - "compiler.spec.ts + determinism.spec.ts + ids.spec.ts"
  key_links:
    - "Goldens are committed in the SAME commit as any format change (D-25 same-commit discipline); CI only compares, never regenerates"
    - "goldens share the make_render_spec fixtures (plan 03-03) — same inputs by construction"
  prohibitions:
    - statement: "Compiler output must never embed timestamps, random values, or environment-derived nondeterminism — the emit path stays free of clock/random/performance sources; byte-for-byte determinism IS the product (COM-01, D-23/D-26)"
      category: values
      status: unverified
      verification: test
      flagged: true
---

<objective>
Deliver COM-01's proof surface: the golden pipeline (compile-stdin entry, CI-guarded update script) and the 11 byte-exact goldens (10 recipes + galerie, D-03), the double-process determinism proof with three-way diff, the SAN-03 stable-ID regeneration test on sanitized output, and the per-golden ink/pose sanity assertions.

Purpose: "même RenderSpec → mêmes bytes" becomes a CI-enforced fact, not a claim — and the golden bytes become the rebuild doctrine's Phase 10 anchor.

Output: scripts + 11 goldens + 3 spec suites green; verify.yml untouched.
</objective>

<execution_context>
@.opencode/gsd-core/workflows/execute-plan.md
@.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-motion-compiler-svg-sanitizer/03-CONTEXT.md
@.planning/phases/03-motion-compiler-svg-sanitizer/03-RESEARCH.md
@.planning/phases/03-motion-compiler-svg-sanitizer/03-PATTERNS.md
@src/motion-compiler/compiler.ts
@src/motion-compiler/__tests__/make-render-spec.ts
@src/svg-sanitizer/sanitize.ts
@scripts/assert-zero-skips.mjs (house script conventions)
</context>

<artifacts_this_phase_produces>
This plan creates: `scripts/compile-stdin.ts`, `scripts/update-goldens.mjs`, the package.json `goldens:update` script, the 11 golden envelope files under `src/motion-compiler/__tests__/goldens/`, and the specs `compiler.spec.ts`, `determinism.spec.ts`, `ids.spec.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Golden pipeline — compile-stdin entry + CI-guarded update script + 11 goldens</name>
  <files>scripts/compile-stdin.ts, scripts/update-goldens.mjs, package.json, src/motion-compiler/__tests__/goldens/</files>
  <read_first>scripts/assert-zero-skips.mjs (house script style: header, stdlib-only, stdout-data/stderr-errors/exit-code discipline), src/motion-compiler/compiler.ts, src/motion-compiler/__tests__/make-render-spec.ts, src/shared/format.ts, package.json, .planning/phases/03-motion-compiler-svg-sanitizer/03-PATTERNS.md (scripts section)</read_first>
  <action>Create `scripts/compile-stdin.ts` (runs under npx tsx — Pitfall 8): reads a RenderSpec JSON from stdin, loads the committed catalogue + style fixtures once, runs compile() + the explicit sanitizeSvg chain (same order the RPC uses, D-17), and writes the golden ENVELOPE to stdout via the deterministic serializer — planning decision on Open Question 2: one envelope file per fixture { lottie, svg (raw compiler SVG), renderer_support } compact + final newline (D-24), so COM-01 "sorties identiques" is proven byte-exactly for BOTH artifacts in 1 file per fixture (11 files). Create `scripts/update-goldens.mjs` following assert-zero-skips.mjs conventions (stdlib-only, header comment, stderr for errors): first line of defense `if (process.env.CI === "true")` → stderr message + exit 1 (D-37 — CI never regenerates); otherwise regenerate all 11 envelopes from the committed fixtures IN MEMORY first, then write atomically (write all files only after every compile succeeded — never a half-regenerated set), using "\n" never os.EOL (Pitfall 9), file names {asset_id}.{recipe_id}.golden.json under src/motion-compiler/__tests__/goldens/. Add package.json script `goldens:update` = `npx tsx scripts/compile-stdin.ts --all` style wiring or direct node invocation per the chosen split (keep it one command for the executor). Run it once locally and COMMIT the 11 goldens in the same commit as the script (D-25 same-commit discipline). Do NOT touch .gitattributes (LF rule already committed).</action>
  <verify>
    <automated>node scripts/update-goldens.mjs; if ($?) { node -e "const fs=require('fs');const fs2=fs.readdirSync('src/motion-compiler/__tests__/goldens');if(fs2.length!==11)process.exit(1);for(const f of fs2){const b=fs.readFileSync('src/motion-compiler/__tests__/goldens/'+f);if(b[b.length-1]!==0x0a)process.exit(1)}" }; if ($?) { $env:CI = 'true'; node scripts/update-goldens.mjs 2>$null; if ($LASTEXITCODE -eq 0) { exit 1 } }; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - src/motion-compiler/__tests__/goldens/ contains exactly 11 .golden.json files, each ending with byte 0x0a
    - `node scripts/update-goldens.mjs` exits 0 locally and produces a clean git diff (idempotent regeneration)
    - `CI=true node scripts/update-goldens.mjs` exits non-zero with a stderr refusal (D-37 gate)
    - update-goldens.mjs writes via the "\n" literal (no os.EOL reference in the file)
    - package.json contains a goldens:update script and the 11 goldens + scripts are committed together
  </acceptance_criteria>
  <done>Golden pipeline operational: local regeneration scripted and CI-guarded, 11 combined-envelope goldens committed byte-exactly.</done>
</task>

<task type="auto">
  <name>Task 2: COM-01/SAN-03 proofs — golden byte-compare, double-spawn determinism, stable IDs</name>
  <files>src/motion-compiler/__tests__/compiler.spec.ts, src/motion-compiler/__tests__/determinism.spec.ts, src/motion-compiler/__tests__/ids.spec.ts</files>
  <read_first>src/rpc/contracts/recipe.spec.ts (fail-loud missing-artifact pattern, lines 42-46), scripts/compile-stdin.ts, src/svg-sanitizer/sanitize.ts, src/motion-compiler/__tests__/make-render-spec.ts, .planning/phases/03-motion-compiler-svg-sanitizer/03-RESEARCH.md (D-26/D-37 + Alternatives: explicit Buffer compare, never snapshot-auto-create)</read_first>
  <action>Create `compiler.spec.ts`: for each of the 11 fixtures — compile via the real compile() + sanitize chain, serialize with the deterministic writer, read the committed golden as a Buffer and assert Buffer.compare === 0 (explicit byte compare; NEVER toMatchFileSnapshot — it auto-creates missing goldens and rewrites via --update, contradicting D-25); a missing golden throws with a message pointing at `node scripts/update-goldens.mjs` (fail-loud, never skip); per golden assert non-dégénérescence (ink visible: sanitized SVG contains path/shape geometry with a fill or stroke — not an empty canvas) and the D-15 pose (one-shot recipes resolve opacity/trim/position at finale values; loop recipes at t=0 values — assert via the compiled lottie's animated property endpoints matching the pose rule). Create `determinism.spec.ts` (D-26/D-37): spawn TWO independent processes running compile-stdin on the same fixture input (tsx), with ≥ 1 s delay between spawns (anti-horodatage), then THREE-WAY Buffer diff — process A bytes === process B bytes === committed golden bytes; run over at least 3 representative fixtures (one one-shot, one loop, the galerie) to bound runtime. Create `ids.spec.ts` (SAN-03, tested on the SANITIZED output per Pitfall 6): compile each fixture twice in-process AND once through compile-stdin; extract all id attributes from the sanitized SVGs and all layer nm + shape ids from the lottie; assert every id matches the 2/3-segment scheme ({asset_id}_{component} for g elements; {asset_id}_{component}_{role} for shape elements, role ∈ ThemeAnchorId ∪ neutral); assert the ID multisets are IDENTICAL across regenerations (diff = ∅); assert (component, role) pairs are unique per asset (D-32 mirror at the output level).</action>
  <verify>
    <automated>npx vitest run src/motion-compiler/__tests__/compiler.spec.ts src/motion-compiler/__tests__/determinism.spec.ts src/motion-compiler/__tests__/ids.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - compiler.spec.ts runs 11 byte-compare tests, all green via Buffer.compare against committed goldens
    - determinism.spec.ts spawns two real processes with a >= 1 s gap and asserts three-way byte equality on >= 3 fixtures
    - ids.spec.ts asserts scheme conformance + cross-regeneration ID set equality (diff empty) on sanitized output for all 11 fixtures
    - A deleted golden produces a thrown error naming the update script (guard test or documented fail-loud path), never a skip
    - Full suite `npx vitest run` stays green (no cross-suite interference)
  </acceptance_criteria>
  <done>COM-01 and SAN-03 are CI-proven: byte-identical double-process output, byte-exact goldens, stable sanitized IDs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Golden bytes <-> compiler output | Any silent format drift invalidates the rebuild doctrine (Phase 10 depends on these bytes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06a | Tampering | golden regeneration in CI | medium | mitigate | update-goldens refuses CI=true (D-37); CI compares only; same-commit discipline (D-25) |
| T-03-06b | Repudiation | hidden nondeterminism (clocks/random/env) | high | mitigate | Prohibition P1 + double-process spawn with >= 1 s delay + three-way diff catches state/time leaks (D-26) |
| T-03-06c | Tampering | ID drift on sanitized output | medium | mitigate | ids.spec asserts sanitized-output scheme + cross-regeneration equality (Pitfall 6: cleanupIds neutralized upstream) |
</threat_model>

<verification>
- `npx vitest run src/motion-compiler` green (compiler/determinism/ids/pipeline/fixtures all pass)
- `node scripts/update-goldens.mjs` idempotent locally; refuses under CI=true
- Goldens committed same-commit with scripts; verify.yml untouched (specs collected by existing steps)
</verification>

<success_criteria>
- 11 byte-exact goldens committed (combined envelopes), CI compares and never regenerates
- Double-spawn three-way determinism proof green (COM-01, D-26/D-37)
- SAN-03 stable-ID proof green on sanitized output
</success_criteria>

<output>
Create `.planning/phases/03-motion-compiler-svg-sanitizer/03-06-SUMMARY.md` when done
</output>
