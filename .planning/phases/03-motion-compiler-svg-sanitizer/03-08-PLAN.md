---
phase: 03-motion-compiler-svg-sanitizer
plan: 08
type: execute
wave: 7
depends_on: ["03-06", "03-07"]
files_modified:
  - src/rpc/server.ts
  - src/rpc/contracts/no-llm-imports.spec.ts
  - lottie_forge/rpc/__init__.py
  - lottie_forge/rpc/client.py
  - tests/rpc/test_rpc_integration.py
autonomous: true
requirements: [COM-02]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A Python pytest spawns the TS server via tsx and drives motion.compile then svg.sanitize over NDJSON with the closed error envelope — the §6.6 integration passes in Phase 3 (D-30)"
    - "A malformed stdin line yields a protocol_error envelope with null id and the server STAYS ALIVE for the next request (D-36)"
    - "Every render-spec rejection case pushed through the RPC returns error.code == its fixture expect_code (D-29/D-37 closure)"
    - "The CI grep gate blocks any of the three LLM SDK names from package.json, tsconfig.json and all src/** sources (COM-02) — an ordinary vitest spec, verify.yml byte-identical"
  artifacts:
    - "src/rpc/server.ts — NDJSON lockstep server, methods motion.compile + svg.sanitize, closed code set (8 codes D-28/D-36), stdout protocol-only"
    - "lottie_forge/rpc/client.py — transport-only client (spawn, cold-start, NDJSON, generic envelope; zero business types per D-30, typed mirror arrives Phase 7)"
    - "tests/rpc/test_rpc_integration.py — §6.6 integration incl. expect_code parity over the shared rejection fixtures"
    - "src/rpc/contracts/no-llm-imports.spec.ts — COM-02 static-scan gate with a self-test proving scanner teeth"
  key_links:
    - "server.ts request parsing reuses CompileRequestSchema / SanitizeRequestEnvelopeSchema (plan 03-02) — no second validation truth"
    - "client.py transport is the contract every later Python consumer reuses (Phases 4/7/8 — D-27 costly reversibility)"
  flagged_assumptions:
    - "COM-02 probe row (category unclassified) — unresolved by the edge probe; the requirement itself is fully gated by no-llm-imports.spec.ts (this plan, Task 3); no additional edge predicate derivable"
    - "COM-04 probe row (category unclassified) — unresolved; COM-04 is enforced by the plan 03-02 schema pins + plan 03-05 feature-gate suite; no additional edge predicate derivable"
    - "SAN-02 probe row (category unclassified) — unresolved; SAN-02 is enforced by the plan 03-07 adversarial matrix (raster + base64 cases); no additional edge predicate derivable"
    - "SAN-03 probe row (category unclassified) — unresolved; SAN-03 is enforced by schema uniqueness (03-02), compiler assignment + stabilize-ids (03-04/03-07) and cross-regeneration ID equality (03-06); no additional edge predicate derivable"
    - "SAN-04 probe row (category unclassified) — unresolved; SAN-04 is enforced by the locked SVGO config + ADR-02 regression suite (03-04/03-07); no additional edge predicate derivable"
    - "SAN-05 probe row (category unclassified) — unresolved; SAN-05 is enforced by the adversarial matrix + collect-before-mutate order (03-07); no additional edge predicate derivable"
  prohibitions:
    - statement: "The deterministic backbone must never import or vendor an LLM SDK — not directly, not transitively, not behind a lazy/conditional import (COM-02; the grep gate scans for the three SDK names enumerated in REQUIREMENTS.md COM-02)"
      category: values
      status: unverified
      verification: test
      flagged: true
---

<objective>
Close the phase: the JSON-over-stdio boundary (Pattern 5) — NDJSON server exposing `motion.compile` + `svg.sanitize` with the closed error envelope — and the Phase 3 Python transport client with the §6.6 integration test; then the COM-02 static-scan gate that structurally keeps the backbone LLM-free.

Purpose: this is the seam Phases 4/7/8 extend (D-27: framing reused; D-30: transport reused) — get it byte-clean now; and COM-02 is the phase's last unproven requirement.

Output: server + client + integration suite + grep gate green; full 12-step verify chain green locally.
</objective>

<execution_context>
@.opencode/gsd-core/workflows/execute-plan.md
@.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-motion-compiler-svg-sanitizer/03-CONTEXT.md
@.planning/phases/03-motion-compiler-svg-sanitizer/03-RESEARCH.md
@.planning/phases/03-motion-compiler-svg-sanitizer/03-PATTERNS.md
@src/rpc/contracts/motion-compiler.schema.ts
@src/rpc/contracts/sanitizer.schema.ts
@src/rpc/contracts/render-spec-rejection.ts
@src/motion-compiler/compiler.ts
@src/svg-sanitizer/sanitize.ts
@lottie_forge/loading/catalogue.py (module conventions)
@tests/bridge/test_catalogue_bridge.py (pytest conventions)
</context>

<artifacts_this_phase_produces>
This plan creates: `src/rpc/server.ts` (+ exported pure line-handler for unit tests), `lottie_forge/rpc/__init__.py`, `lottie_forge/rpc/client.py` (spawn/call/close transport), `tests/rpc/test_rpc_integration.py`, and `src/rpc/contracts/no-llm-imports.spec.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: NDJSON RPC server — methods motion.compile + svg.sanitize (D-27/D-28/D-36)</name>
  <files>src/rpc/server.ts</files>
  <read_first>src/rpc/contracts/motion-compiler.schema.ts (CompileRequestSchema), src/rpc/contracts/sanitizer.schema.ts (SanitizeRequestEnvelopeSchema), scripts/assert-zero-skips.mjs (stdout/stderr/exit discipline), .planning/phases/03-motion-compiler-svg-sanitizer/03-RESEARCH.md (Pattern 5 + envelope Code Example + D-36)</read_first>
  <action>Implement `src/rpc/server.ts` per Pattern 5: readline over stdin (terminal false), one NDJSON message per line, correlation by numeric id. Per line: JSON.parse in try/catch — malformed line → write { id: null, ok: false, error: { code: "protocol_error", message } } and CONTINUE (never crash, never exit — D-36); known-shape but schema-invalid → validation_error; dispatch: "motion.compile" → parse CompileRequestSchema → load the committed catalogue + style fixtures ONCE at startup → run compile() then return the CompileResult (the sanitizer is a SEPARATE method — explicit chaining stays with the caller, D-17); "svg.sanitize" → parse SanitizeRequestEnvelopeSchema → sanitizeSvg() → result or sanitize_rejected error; anything else → method_not_found. Error mapping (closed set of exactly the 8 codes): schema rejection → validation_error; typed compile errors → compile_error except feature-gate rejections → unsupported_feature; sanitizer violations → sanitize_rejected; unexpected throw → internal (message only, stack to stderr). Envelope: { id, ok: true, result } | { id, ok: false, error: { code, message, details? } } — details carries the structured violation report for sanitize_rejected and the zod issues for validation_error. stdout carries PROTOCOL ONLY — every log/diagnostic goes to stderr (assert-zero-skips discipline); pipelining is protocol-legal (independent line processing) but the Phase 3 client is lockstep (D-36). Structure for testability: export a pure `processLine(line, ctx)` handler (ctx: loaded catalogue/style + method map) plus a thin main() — the unit spec drives processLine directly. Add `src/rpc/server.spec.ts`-style coverage inside this plan's spec: protocol_error on malformed line, method_not_found, validation_error carries issue paths in details, compile happy-path returns a gate-passing result, sanitize_rejected carries the violation report. Docblock: framing reused by Phases 4/7/8 (D-27 costly).</action>
  <verify>
    <automated>npx vitest run src/rpc && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - processLine unit tests cover: malformed line → protocol_error with id null (handler does not throw); unknown method → method_not_found; invalid RenderSpec → validation_error with zod issue paths in details; a-001 compile → ok true with LottieJSONSchema-valid result; violating SVG → sanitize_rejected with structured violations in details
    - The exported code set is exactly the 8 closed codes (validation_error, unsupported_feature, compile_error, sanitize_rejected, internal, protocol_error, method_not_found + parse_error alias per D-28/D-36)
    - No console.log anywhere in server.ts — diagnostics use the stderr stream only
    - `npx vitest run src/rpc` and `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>The stdio boundary speaks the closed-envelope NDJSON protocol with total error mapping — ready for pytest-driven integration.</done>
  <reversibility rating="costly">The NDJSON framing + envelope is implemented on both sides and reused by Phases 4/7/8 (D-27) — changing transport later rewrites the TS and Python sides.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Python transport client + §6.6 integration test (D-30)</name>
  <files>lottie_forge/rpc/__init__.py, lottie_forge/rpc/client.py, tests/rpc/test_rpc_integration.py</files>
  <read_first>lottie_forge/loading/catalogue.py (module opening conventions: __future__ annotations, __all__, docblock), tests/bridge/test_catalogue_bridge.py (pytest conventions, REPO_ROOT pattern), src/rpc/server.ts, fixtures/rejection-cases/render-spec.json, .planning/phases/03-motion-compiler-svg-sanitizer/03-CONTEXT.md (D-30), docs/project/06_Backbone.md §6.5-§6.6</read_first>
  <action>Create `lottie_forge/rpc/__init__.py` (package marker following loading/__init__.py) and `lottie_forge/rpc/client.py` — TRANSPORT ONLY per D-30: zero business types (the typed mirror + re-validation land in Phase 7 without refactoring this transport). API: a client class that (1) spawns the server as a subprocess with the command array defaulting to the tsx invocation of src/rpc/server.ts with cwd at the repo root (Pitfall 8 — Node 20 cannot execute TS natively; the command is a constructor parameter so Phase 4/7/8 can pin differently), (2) `call(method, params, timeout)` — lockstep: write one compact NDJSON request line with a monotonic numeric id, read exactly one response line, correlate by id, return the parsed envelope (dict) — raising a typed transport error on timeout or process death, (3) `send_raw(line)` + `read_response()` exposed for the malformed-line test, (4) `close()` terminating the child. Module opens with __future__ annotations + docblock stating transport-only status + __all__ at end; full type annotations; ruff-clean. Create `tests/rpc/test_rpc_integration.py` (docstring notes run-order: requires npm ci + node_modules present; spawns via the client): cold-start `motion.compile` with the a-001 fixture → envelope ok true and result.lottie reparses as JSON with v pinned (generic assertions — no business models, D-30); `svg.sanitize` with the compiled raw SVG → ok true, report ok true, zero violations; EVERY case in fixtures/rejection-cases/render-spec.json pushed through motion.compile → envelope ok false AND error.code == case expect_code (the D-29 closure proven over the wire, D-37); a malformed raw line → response id None + code protocol_error AND a subsequent valid call still succeeds (server survived — D-36); unknown method → method_not_found; assert stdout of the child never mixes logs (protocol-only — a log line would break NDJSON parsing and fail the suite by construction).</action>
  <verify>
    <automated>python -m pytest tests/rpc/test_rpc_integration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/rpc/test_rpc_integration.py -q` exits 0 (server spawned via tsx, cold-start compile + sanitize green)
    - Every render-spec rejection case asserts error.code == expect_code over the RPC (parametrized)
    - The malformed-line test asserts id None + protocol_error + server survival on the next call
    - client.py contains zero imports of lottie_forge.domain (transport-only, D-30) and passes ruff
    - The integration suite is collected by the existing full pytest run (tests/rpc/ under the existing rootdir — no pytest.ini change)
  </acceptance_criteria>
  <done>The §6.6 integration passes in Phase 3: Python drives the deterministic backbone end-to-end over the closed protocol.</done>
  <precondition>Local Python env has the project installed (pip install -e .[dev] in .venv) and node_modules present (npm ci) — environment facts plan ordering does not guarantee; the test docstring documents the run-order.</precondition>
</task>

<task type="auto">
  <name>Task 3: COM-02 grep gate — no-llm-imports.spec.ts</name>
  <files>src/rpc/contracts/no-llm-imports.spec.ts</files>
  <read_first>src/rpc/contracts/recipe.spec.ts (assert-style + fail-loud doctrine), package.json, tsconfig.json, REQUIREMENTS.md (COM-02 row — the three SDK names), .planning/phases/03-motion-compiler-svg-sanitizer/03-PATTERNS.md (no-llm section)</read_first>
  <action>Create `src/rpc/contracts/no-llm-imports.spec.ts` (COM-02): a vitest spec that reads package.json, tsconfig.json, and walks every .ts file under src/ via node:fs (repo-root resolution from __dirname per house convention), scanning each file's CONTENT against the regex union of the three LLM SDK names enumerated verbatim in REQUIREMENTS.md COM-02 (case-insensitive); ANY match throws with the file path (fail-loud doctrine — the suite is collected by existing CI step 09, verify.yml stays byte-identical). Include a self-test proving the scanner has teeth: export the scan function and assert it FLAGs a synthetic in-memory string containing each of the three names (one assert per name) and does NOT flag clean source content — without ever writing a violating file into src/ (in-memory only, no fixture pollution). Docblock cites COM-02 + §6.1 #2 and states the scan covers the whole src tree (new subdirectories auto-covered by the walk).</action>
  <verify>
    <automated>npx vitest run src/rpc/contracts/no-llm-imports.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/rpc/contracts/no-llm-imports.spec.ts` exits 0 (clean tree: zero SDK-name matches in package.json, tsconfig.json, src/**)
    - The self-test asserts the scanner flags each of the three names individually in synthetic in-memory content (teeth proven per name) and passes clean content
    - The scan walks src/ recursively (new subdirs auto-covered — asserted by the walk implementation, e.g. entry count > 0)
    - verify.yml is byte-identical to its pre-phase state (no new workflow steps; `git diff .github/workflows/verify.yml` empty at phase end)
  </acceptance_criteria>
  <done>COM-02 enforced as an ordinary CI-collected spec with proven teeth — the backbone is structurally LLM-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdin lines -> server dispatch | Potentially hostile NDJSON lines (spawned by any process) — parse guarded, no eval |
| Python client -> TS server | Process boundary; the envelope is the only legal channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08a | DoS/Tampering | hostile NDJSON line | medium | mitigate | Parse in try/catch → protocol_error without crash (D-36); server survival asserted by test; zod at every method entry (ASVS V5) |
| T-03-08b | Tampering | LLM dependency sneaking into the backbone | high | mitigate | COM-02 grep gate with self-tested scanner teeth; prohibition P2; CI-collected ordinary spec |
| T-03-08c | Information Disclosure | error details leakage | low | accept | details carries zod paths/violations only — structured, no stacks on stdout (stacks → stderr, local single-operator process) |
</threat_model>

<verification>
- `npx vitest run` FULL suite green (all Phase 3 specs)
- `python -m pytest tests/ -q` full suite green (integration included)
- `npx tsc --noEmit` + `ruff check .` + `npx @biomejs/biome check .` green
- Local mirror of the 12-step chain: ruff → biome → pytest -k export → vitest → pytest → tsc → assert-zero-skips — all green
</verification>

<success_criteria>
- §6.6 integration green over the real spawn (tsx), closed envelope, expect_code parity over the wire
- COM-02 gate green with proven teeth
- The full local verify chain (12-step equivalent) is green — the phase is CI-ready with verify.yml byte-identical
</success_criteria>

<output>
Create `.planning/phases/03-motion-compiler-svg-sanitizer/03-08-SUMMARY.md` when done
</output>
