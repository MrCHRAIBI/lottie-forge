---
phase: 03-motion-compiler-svg-sanitizer
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/motion-compiler/__tests__/make-render-spec.ts
  - src/motion-compiler/__tests__/fixtures.spec.ts
  - fixtures/render-specs/fade.json
  - fixtures/render-specs/slide.json
  - fixtures/render-specs/bounce.json
  - fixtures/render-specs/pulse.json
  - fixtures/render-specs/draw-on.json
  - fixtures/render-specs/rotate.json
  - fixtures/render-specs/scale-pop.json
  - fixtures/render-specs/float.json
  - fixtures/render-specs/wiggle.json
  - fixtures/render-specs/orbit.json
  - fixtures/render-specs/galerie.json
autonomous: false
requirements: [COM-01]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A single TS builder make_render_spec() is the source of fixture truth (D-04, patron tests/bridge/fixtures.py) and every committed fixture re-derives from it"
    - "All 11 RenderSpec fixtures parse against RenderSpecSchema and are cross-ref-compatible with the committed catalogue + style (shape ∈ recipe.shapes_supported, intensity ∈ intensity_range, easing declared)"
    - "The draw-on fixture uses paint stroke with stroke_width_token (never a free float) — trim-path is visible only on a stroke layer (D-14)"
  artifacts:
    - "src/motion-compiler/__tests__/make-render-spec.ts — canonical-default builder with stable defaults, deliberately fractional floats"
    - "fixtures/render-specs/*.json — 10 per-recipe fixtures (canonical shape from shapes_supported, D-03) + 1 galerie fixture per the decision checkpoint"
    - "src/motion-compiler/__tests__/fixtures.spec.ts — schema + cross-ref consistency assertions over the committed files"
  key_links:
    - "Fixtures pin recipe_id/easing/shapes against fixtures/recipe-catalogue/catalogue.json and fixtures/style-specs/example-style/style.yaml (D-05 pre-check at fixture level; the compiler re-checks at entry)"
---

<objective>
Create the positive RenderSpec fixture set (D-03/D-04): the single-source TS builder `make_render_spec()` and 11 committed fixtures — 10 per recipe (canonical shape drawn from that recipe's `shapes_supported`) + 1 "galerie" fixture. The galerie exposes a conflict between two LOCKED decisions — D-03 (cover the 5 shape generators in ONE asset) and D-05 (hard cross-ref: component shapes ⊆ recipe `shapes_supported`) — because no recipe in the locked catalogue supports all 5 shapes (max coverage 4: bounce/wiggle). A `checkpoint:decision` lets the user resolve it; nothing else in the phase is blocked (the tracer and all recipe fixtures are decision-independent).

Purpose: goldens (plan 03-06), the tracer (plan 03-04) and every compiler test compile THESE files — fixtures are the phase's concrete contracts-in-use.

Output: builder + 11 fixtures + consistency spec, decision recorded in the summary.
</objective>

<execution_context>
@.opencode/gsd-core/workflows/execute-plan.md
@.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-motion-compiler-svg-sanitizer/03-CONTEXT.md
@.planning/phases/03-motion-compiler-svg-sanitizer/03-RESEARCH.md
@.planning/phases/03-motion-compiler-svg-sanitizer/03-PATTERNS.md
@src/rpc/contracts/motion-compiler.schema.ts
@fixtures/recipe-catalogue/catalogue.json
@fixtures/style-specs/example-style/style.yaml
</context>

<artifacts_this_phase_produces>
This plan creates: `make_render_spec()` builder, 11 committed RenderSpec fixtures (`fade.json` … `orbit.json` + `galerie.json`), and `fixtures.spec.ts` (fixture↔catalogue/style consistency gate).
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: DECISION — galerie fixture vs the D-05 hard cross-ref (D-03 × D-05 conflict)</name>
  <files>[]</files>
  <decision>How to shape the 11th "galerie" golden fixture when D-03 (5 shape generators in ONE asset) collides with D-05 (hard cross-ref: component shapes ⊆ recipe shapes_supported) — no recipe in the locked catalogue supports all 5 shapes.</decision>
  <context>Both D-03 and D-05 are LOCKED user decisions from 03-CONTEXT.md. The committed catalogue gives bounce and wiggle the widest coverage (rect, ellipse, path, polyline — 4 shapes); polystar appears only in pulse, rotate, scale-pop and orbit. A 5-generator single-asset galerie therefore cannot pass the D-05 cross-ref against any existing recipe, and the planner may not silently reduce a locked decision — the user chooses. Downstream impact: plan 03-06 goldens need the resolution; plans 03-04/03-05/03-07/03-08 do not (the tracer compiles a-001/fade).</context>
  <options>
    <option id="option-a">
      <name>Extend one recipe's shapes_supported to all 5 (recommendation: wiggle + "polystar")</name>
      <pros>Fully honors D-03's letter — 5 generators in one asset, one galerie golden covering the complete shape-builder surface</pros>
      <cons>Touches locked Phase 2 data: catalogue.json edit + regenerate the verbatim prompt-fixture embedded copy and its recorded sha (02-06 D-13 chain) + re-export bridge artifacts, all same-commit; risks churn in pinned Phase 1/2 hash chains</cons>
    </option>
    <option id="option-b">
      <name>Galerie under wiggle with 4 components (rect, ellipse, path, polyline); polystar coverage rides on the pulse/rotate/scale-pop/orbit recipe goldens</name>
      <pros>Zero Phase 2 churn; 11 goldens intact; D-03 honored at set level — all 5 generators remain golden-covered across the fixture set</pros>
      <cons>The single-asset galerie covers 4/5 generators (polystar not in that one asset) — a literal reading of D-03 is not satisfied</cons>
    </option>
  </options>
  <resume-signal>Reply "option-a" or "option-b" (or your own variant — it will be recorded verbatim). The executor halts the phase here if no answer is available; record the choice verbatim in 03-03-SUMMARY.md before Task 2.</resume-signal>
  <read_first>.planning/phases/03-motion-compiler-svg-sanitizer/03-CONTEXT.md (D-03, D-05, Discretion), fixtures/recipe-catalogue/catalogue.json, docs/project/06_Backbone.md §6.3.5</read_first>
  <action>HALT and ask the user to choose between the two options above (do NOT pick silently). Recommended: option-b (zero Phase-2 churn, set-level D-03 coverage). Record the user's choice verbatim in the plan summary and proceed to Task 2 under that choice. If the user is unavailable, the executor halts the phase here — Task 2 and plan 03-06 depend on the resolution; plans 03-04/03-05/03-07/03-08 do not.</action>
  <verify>User choice recorded verbatim in 03-03-SUMMARY.md (option letter + one-line rationale)</verify>
  <acceptance_criteria>
    - 03-03-SUMMARY.md contains a "Galerie decision" entry naming Option A or Option B and the user's words
    - No fixture file is committed before the choice is recorded
  </acceptance_criteria>
  <done>The D-03 × D-05 conflict is resolved by an explicit user decision, recorded and traceable.</done>
</task>

<task type="auto">
  <name>Task 2: make_render_spec() builder + 11 committed fixtures + consistency spec</name>
  <files>src/motion-compiler/__tests__/make-render-spec.ts, src/motion-compiler/__tests__/fixtures.spec.ts, fixtures/render-specs/fade.json, fixtures/render-specs/slide.json, fixtures/render-specs/bounce.json, fixtures/render-specs/pulse.json, fixtures/render-specs/draw-on.json, fixtures/render-specs/rotate.json, fixtures/render-specs/scale-pop.json, fixtures/render-specs/float.json, fixtures/render-specs/wiggle.json, fixtures/render-specs/orbit.json, fixtures/render-specs/galerie.json</files>
  <read_first>src/rpc/contracts/motion-compiler.schema.ts, tests/bridge/fixtures.py (builder patron, lines 84-105 + fractional-float doctrine lines 28-30), fixtures/recipe-catalogue/catalogue.json, fixtures/style-specs/example-style/style.yaml, .planning/phases/03-motion-compiler-svg-sanitizer/03-PATTERNS.md (make-render-spec section)</read_first>
  <action>Create `src/motion-compiler/__tests__/make-render-spec.ts` as the single source of fixture truth (D-04), mirroring tests/bridge/fixtures.py: one exported `makeRenderSpec(recipeId)` with a canonical default pose, optional overrides only where a fixture needs variety, and a docblock listing every default and why. Floats deliberately fractional (2.5, 0.25 — never integral 2.0) per the Phase 1 bridge doctrine so the D-35 formatter is exercised. Defaults satisfy every cross-ref against the COMMITTED data: recipe_id from the argument; canonical shape per recipe drawn from that recipe's `shapes_supported` (executor discretion per D-03 — document each choice in the docblock; draw-on MUST take a `path` shape with paint stroke + stroke_width_token "default" so trim-path is visible per D-14; roles drawn from the recipe's theme_anchors plus "neutral" per D-02; intensity within the recipe's intensity_range; params amplitude/direction/loops coherent with the recipe family); asset_id `a-001`..`a-010` for recipe fixtures. Component counts vary 1..3 across fixtures so z-order inversion (D-10) and multi-layer ID assignment are exercised; every coordinate/param within the D-06 closed ranges; transform deltas within the D-34 ranges. Then generate and commit the 10 recipe fixtures as `fixtures/render-specs/{recipe_id}.json` (compact JSON + trailing newline, written via the plan 03-01 deterministic writer or equivalent \n-literal writer — these bytes are NOT golden bytes, they are inputs, but keep the LF regime). Build the galerie fixture per the Task 1 decision: Option A -> 5 components (one per generator) under the extended recipe, asset_id a-011; Option B -> 4 components (rect, ellipse, path, polyline) under wiggle, asset_id a-011, with the docblock noting polystar golden coverage rides on pulse/rotate/scale-pop/orbit. Create `src/motion-compiler/__tests__/fixtures.spec.ts`: every committed file parses against RenderSpecSchema; cross-ref consistency per fixture — recipe_id exists in the committed catalogue, the fixture's component shapes ⊆ that recipe's shapes_supported (D-05 fixture-level pre-check), intensity within intensity_range, recipe easing declared in the committed style's easing_curves, roles ⊆ recipe theme_anchors ∪ {neutral}; exactly 11 files present (fail-loud pointing at make-render-spec.ts if one is missing — never skip); draw-on fixture asserts paint.stroke_width_token present and no numeric stroke width key anywhere (D-14).</action>
  <verify>
    <automated>npx vitest run src/motion-compiler/__tests__/fixtures.spec.ts; if ($?) { npx tsc --noEmit }; if ($?) { node -e "const fs=require('fs');const n=fs.readdirSync('fixtures/render-specs').filter(f=>f.endsWith('.json')).length;if(n!==11)process.exit(1)" }</automated>
  </verify>
  <acceptance_criteria>
    - fixtures/render-specs/ contains exactly 11 .json files (10 recipes + galerie)
    - `npx vitest run src/motion-compiler/__tests__/fixtures.spec.ts` exits 0 (schema + cross-ref + count + D-14 assertions)
    - make-render-spec.ts exports a single makeRenderSpec builder and its docblock lists all defaults and the per-recipe canonical shape choices
    - Every fixture file ends with a single 0x0a byte
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>11 positive RenderSpec fixtures committed from a single builder source, cross-ref-consistent with the locked catalogue and style, galerie shaped by the recorded user decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Committed fixture files -> compiler tests | Fixture bytes are data, never type guarantees (validate-then-use at load) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03a | Tampering | fixtures/render-specs/*.json | medium | mitigate | fixtures.spec.ts re-validates every committed file against the frozen schema + cross-refs at test time (fail-loud, never skip) |
| T-03-03b | Tampering | catalogue.json edit (Option A only) | high | mitigate | Same-commit discipline: catalogue + prompt-fixture embedded copy + recorded sha regenerated together; Phase 2 suites re-run green before commit |
</threat_model>

<verification>
- `npx vitest run src/motion-compiler/__tests__/fixtures.spec.ts` green
- 11 fixture files committed, all LF-terminated
- Galerie decision recorded in the plan summary
</verification>

<success_criteria>
- make_render_spec() is the single fixture source of truth (D-04)
- 11 fixtures committed, cross-ref-consistent with catalogue + style (D-05 pre-check at fixture level)
- D-03 × D-05 conflict resolved by explicit user decision, not planner discretion
</success_criteria>

<output>
Create `.planning/phases/03-motion-compiler-svg-sanitizer/03-03-SUMMARY.md` when done (must include the galerie decision record)
</output>
