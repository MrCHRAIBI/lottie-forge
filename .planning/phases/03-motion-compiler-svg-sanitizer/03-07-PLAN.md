---
phase: 03-motion-compiler-svg-sanitizer
plan: 07
type: execute
wave: 6
depends_on: ["03-05"]
files_modified:
  - src/svg-sanitizer/sanitize.ts
  - src/svg-sanitizer/constraint-report.ts
  - src/svg-sanitizer/plugins/forbid-structure.ts
  - src/svg-sanitizer/plugins/stabilize-ids.ts
  - src/svg-sanitizer/__tests__/sanitize.spec.ts
  - src/svg-sanitizer/__tests__/svgo-regression.spec.ts
  - src/svg-sanitizer/__tests__/self-consistency.spec.ts
autonomous: true
requirements: [SAN-01, SAN-02, SAN-04, SAN-05]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The adversarial rejection matrix blocks text/tspan, embedded raster + base64 data URIs, foreignObject, script, on* handlers, js: URIs and external xlink:href — every case a structured violation, never a silent strip (SAN-01/02/05)"
    - "svg.sanitize on empty or null input returns a structured validation rejection, never a pass or a crash (SAN-01 empty edge)"
    - "SVG gates match exact case-sensitive XML names — any element not in the allow-list (including case variants like <TEXT>) is rejected: allow-list, not blacklist (SAN-01 encoding edge)"
    - "viewBox, <title> AND <desc> survive SVGO 4 optimization, and IDs survive unchanged — the ADR-02 regression test is green (SAN-04 + Pitfalls 5/6)"
    - "For every fixture, sanitize(raw compiler SVG) reports ZERO rejected elements, the Lottie↔SVG pairing is isomorphic, and ink is visible (D-31/D-37)"
  artifacts:
    - "src/svg-sanitizer/__tests__/sanitize.spec.ts — parametrized adversarial matrix (>= 16 cases)"
    - "src/svg-sanitizer/__tests__/svgo-regression.spec.ts — ADR-02 regression (viewBox + title + desc + stable IDs survive optimize)"
    - "src/svg-sanitizer/__tests__/self-consistency.spec.ts — 11-fixture zero-rejection + Lottie↔SVG isomorphism + ink"
    - "Hardened forbid-structure (allow-list enforcement) + stabilize-ids (assertion-only)"
  key_links:
    - "Plugin order is the gate: collectors BEFORE preset-default (raw tree), stabilize-ids AFTER (final assertion) — order asserted in a test"
    - "Self-consistency compiles fixtures IN-TEST via make_render_spec + compile (decoupled from plan 03-06 goldens; same inputs by construction)"
  prohibitions:
    - statement: "The sanitizer must never silently remove or strip a violating element — every violation is collected across all gates and rejected (sanitize_rejected) before any mutation; silent cleaning is the exact failure the gate exists to prevent"
      category: safety
      status: unverified
      verification: test
      flagged: true
    - statement: "stabilize-ids must never rewrite, rename or shorten an ID to force conformance — it only ASSERTS the compiler-assigned 2/3-segment scheme and records a violation on mismatch (D-32)"
      category: safety
      status: unverified
      verification: test
      flagged: true
---

<objective>
Complete the sanitizer's proof surface: the full adversarial rejection matrix (SAN-01/02/05 + D-31 structural gates), the ADR-02 regression test (SAN-04), and the self-consistency/isomorphism/ink proofs over all 11 fixtures (D-31/D-37).

Purpose: the sanitizer is the phase's security gate — SVG ships to third parties (packs sold), so rejection (never silent cleaning) must be demonstrably total, and the SVGO overrides must be provably load-bearing.

Output: 3 spec suites green; sanitizer seam hardened; verify.yml untouched.
</objective>

<execution_context>
@.opencode/gsd-core/workflows/execute-plan.md
@.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-motion-compiler-svg-sanitizer/03-CONTEXT.md
@.planning/phases/03-motion-compiler-svg-sanitizer/03-RESEARCH.md
@.planning/phases/03-motion-compiler-svg-sanitizer/03-PATTERNS.md
@src/svg-sanitizer/sanitize.ts
@src/svg-sanitizer/config.ts
@src/svg-sanitizer/plugins/forbid-text.ts
@src/svg-sanitizer/plugins/forbid-raster.ts
@src/svg-sanitizer/plugins/forbid-foreignobject.ts
@src/svg-sanitizer/plugins/forbid-structure.ts
@src/svg-sanitizer/plugins/stabilize-ids.ts
@src/motion-compiler/__tests__/make-render-spec.ts
</context>

<artifacts_this_phase_produces>
This plan completes: the enforcing allow-list inside `forbid-structure.ts`, assertion-only `stabilize-ids.ts`, full `constraint-report.ts` typing, and the three sanitizer suites (`sanitize.spec.ts`, `svgo-regression.spec.ts`, `self-consistency.spec.ts`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Sanitizer hardening — allow-list enforcement, assertion-only IDs, report typing</name>
  <files>src/svg-sanitizer/sanitize.ts, src/svg-sanitizer/constraint-report.ts, src/svg-sanitizer/plugins/forbid-structure.ts, src/svg-sanitizer/plugins/stabilize-ids.ts</files>
  <read_first>src/svg-sanitizer/config.ts, src/svg-sanitizer/sanitize.ts, src/rpc/contracts/sanitizer.schema.ts, .planning/phases/03-motion-compiler-svg-sanitizer/03-RESEARCH.md (Pattern 2 + Pitfalls 5/6), docs/project/06_Backbone.md §6.4.1-§6.4.3</read_first>
  <action>Harden the tracer's sanitizer seam into the final gate. `forbid-structure.ts`: add the ELEMENT ALLOW-LIST check — every element enter compares against the closed allow-list (svg, title, desc, g, path, rect, ellipse, polyline, polygon — exactly what svg-builder can emit, D-31); any element NOT in the list is a structure violation with the element name recorded (this is what catches case variants, unknown SVG elements and everything the other gates don't name); attribute check completes: XML comments, data-* attributes, width/height on the ROOT svg element only (coherent with D-22), and ANY prefixed element or attribute name (colon present — single default xmlns namespace only; no prefixed namespace declarations) — case-sensitive exact name matching (XML names are case-sensitive; the allow-list makes case variants unreachable). `stabilize-ids.ts`: stays pure ASSERTION — walk the optimized tree, assert every g id matches the 2-segment scheme, every shape-element id matches the 3-segment scheme AND equals its parent g id + "_" + role segment; a mismatch records an ids violation — the plugin NEVER renames or rewrites an id (prohibition P5, D-32). `constraint-report.ts`: finalize the typed report structures mirroring sanitizer.schema.ts (gate closed enum, violation counts per gate, ok flag) — the report shape the RPC returns and Phase 5 persists. `sanitize.ts`: assert the enforced ORDER — collectors run before preset-default and stabilize-ids after (the config array order from the tracer is normative; add a config-order self-check so a future reorder fails fast); collect ALL violations across all gates before deciding (collect-all, one report per call — never first-fail); any violation → ok false report (never mutate + return the mutated tree with a warning). All docblocks cite SAN ids + D-31/D-32.</action>
  <verify>
    <automated>npx vitest run src/svg-sanitizer && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - forbid-structure.ts contains the closed allow-list (9 element names) and rejects any element outside it (name recorded in the violation)
    - stabilize-ids.ts contains no id-assignment or rename logic (assertion + violation recording only)
    - sanitize.ts (or config.ts) self-checks the plugin order: collectors → preset-default → stabilize-ids
    - sanitizeSvg collects all violations across gates in one report (a fixture violating 2 gates yields 2 violations)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The sanitizer is the final enforcing gate: allow-list total, IDs assertion-only, order enforced, collect-all reports.</done>
</task>

<task type="auto">
  <name>Task 2: Adversarial rejection matrix (SAN-01/02/05 + D-31 structure gates)</name>
  <files>src/svg-sanitizer/__tests__/sanitize.spec.ts</files>
  <read_first>src/svg-sanitizer/sanitize.ts, src/rpc/contracts/sanitizer.schema.ts, src/rpc/contracts/recipe.spec.ts (it.each harness), .planning/phases/03-motion-compiler-svg-sanitizer/03-RESEARCH.md (SAN-01/02/05 matrix row)</read_first>
  <action>Create `sanitize.spec.ts` as a parametrized it.each matrix over hand-authored adversarial SVG strings, each case asserting sanitizeSvg returns ok false AND the expected gate recorded on at least one violation (gate-level assertion, never message text — D-08 discipline): text element; tspan element; embedded image element; base64 data URI in an href; foreignObject; script element; an on* event-handler attribute (e.g. the load-handler family on a g element); a href carrying the js: URI scheme; external xlink:href pointing at a remote URL (also caught by the prefixed-attribute gate — assert BOTH violations recorded, defense-in-depth proof); XML comment; data-* attribute; width AND height present on the root svg; a prefixed element from a foreign namespace; a case-variant text element (uppercase TEXT — caught by the allow-list, proving allow-list-not-blacklist); an unknown-but-harmless element (e.g. a foreign web component name — still rejected: allow-list); empty-string input → structured rejection via the schema/gate path with ok false (SAN-01 empty edge — never a pass, never a crash); a CLEAN minimal svg (one g + one path, proper ids, title + desc + viewBox) → ok true with zero violations (the control case proving the matrix teeth are selective, not a total rejector). Plus an order-proof test: a fixture violating two gates simultaneously yields both violations in one report (collect-all).</action>
  <verify>
    <automated>npx vitest run src/svg-sanitizer/__tests__/sanitize.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - sanitize.spec.ts contains >= 16 adversarial cases + 1 clean control, each adversarial case asserting ok false + expected gate
    - The case-variant element case asserts rejection via the allow-list gate (allow-list semantics proven)
    - Empty-string input asserts structured ok false (no throw escaping sanitizeSvg's contract)
    - The two-gate fixture yields both violations in a single report (collect-all asserted)
    - Suite exits 0
  </acceptance_criteria>
  <done>The full SAN-01/02/05 matrix + D-31 structural gates are parametrized, green, and gate-level-asserted.</done>
</task>

<task type="auto">
  <name>Task 3: ADR-02 regression + self-consistency/isomorphism/ink over 11 fixtures</name>
  <files>src/svg-sanitizer/__tests__/svgo-regression.spec.ts, src/svg-sanitizer/__tests__/self-consistency.spec.ts</files>
  <read_first>src/svg-sanitizer/config.ts (locked overrides), src/motion-compiler/__tests__/make-render-spec.ts, src/motion-compiler/compiler.ts, .planning/phases/03-motion-compiler-svg-sanitizer/03-RESEARCH.md (Pitfall 5/6, SAN-04 verified findings), docs/project/06_Backbone.md §6.4.2</read_first>
  <action>Create `svgo-regression.spec.ts` (SAN-04, ADR-02): take a raw compiler SVG (from a representative fixture compiled in-test), run the optimizer, and assert viewBox survives, the title element survives, the desc element survives (removeDesc override proven load-bearing — Pitfall 5), the root carries NO width/height after optimize (D-22 preserved), and every input id survives UNCHANGED in the output (cleanupIds override proven: no shortened ids like single letters — Pitfall 6). Include a config-level regression guard: assert the locked config still disables the three named overrides and still does NOT re-add removeViewBox/removeTitle-style plugin entries (in v4 those plugins are ABSENT from the preset — re-adding them would RE-ACTIVATE the v3 behavior, the inverse of intent; guard the config shape itself). Create `self-consistency.spec.ts` (D-31/D-37): for EACH of the 11 fixtures — compile in-test via make_render_spec + compile() (same inputs as goldens by construction, decoupled from plan 03-06 artifacts), take the raw SVG, run sanitizeSvg, and assert: (1) zero violations (the compiler's own output always passes its own gate — D-31 self-consistency); (2) Lottie↔SVG ISOMORPHISM — layer count equals g count, the g sequence order corresponds to the reversed component order mirroring the lottie layers array, every layer nm equals the role segment of its counterpart g's shape ids, and per-component shape types match between the lottie shape items and the SVG geometry elements; (3) ink visible — the sanitized SVG contains at least one geometry element carrying a paint attribute (non-dégénérescence per fixture, D-37).</action>
  <verify>
    <automated>npx vitest run src/svg-sanitizer/__tests__/svgo-regression.spec.ts src/svg-sanitizer/__tests__/self-consistency.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - svgo-regression.spec.ts asserts viewBox + title + desc + id-preservation survive optimize, and root has no width/height post-optimize
    - The config-shape guard asserts the three overrides stay disabled and no removed-from-preset viewbox/title plugin entries are re-added
    - self-consistency.spec.ts iterates all 11 fixtures asserting zero violations, isomorphism (layer/g pairing + nm == role segment), and ink visible
    - Both suites exit 0; `npx vitest run src/svg-sanitizer` fully green
  </acceptance_criteria>
  <done>SAN-04 regression + D-31/D-37 self-consistency proven across the whole fixture set; the gate is demonstrably total and selectively so.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adversarial SVG strings -> sanitizeSvg | The phase's primary attack surface: SVG ships inside sold packs to third-party pages |
| Compiler output -> own gate | Self-consistency proves the producer and the gate agree (no false rejects / false accepts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07a | Tampering/Elevation | XSS via SVG (script/handlers/js: URIs) | high | mitigate | Adversarial matrix (Task 2) + collect-before-mutate order + removeScripts preset as defense-in-depth; rejection, never silent cleaning |
| T-03-07b | Tampering | raster/base64 smuggling | medium | mitigate | forbid-raster matrix cases (image element + base64 data URIs) — SAN-02 |
| T-03-07c | Tampering | ID renames by optimizer | medium | mitigate | cleanupIds override + id-preservation regression test on optimized output (Pitfall 6) |
| T-03-07d | Information Disclosure | a11y elements stripped silently | low | mitigate | desc/title survival regression (Pitfall 5); derived-only content (D-18) upstream |
| T-03-07e | Tampering | gate bypass via case/prefix tricks | medium | mitigate | Allow-list (case-sensitive exact names) + prefixed-name rejection matrix cases |
</threat_model>

<verification>
- `npx vitest run src/svg-sanitizer` green (matrix + regression + self-consistency)
- `npx tsc --noEmit` + `npx @biomejs/biome check .` green
- verify.yml untouched — all gates are ordinary specs collected by existing CI step 09
</verification>

<success_criteria>
- SAN-01/02/05 adversarial matrix total and gate-level-asserted
- SAN-04 ADR-02 regression proves viewBox/title/desc/IDs survive SVGO 4 with the locked overrides
- D-31/D-37 self-consistency + isomorphism + ink proven over all 11 fixtures
</success_criteria>

<output>
Create `.planning/phases/03-motion-compiler-svg-sanitizer/03-07-SUMMARY.md` when done
</output>
