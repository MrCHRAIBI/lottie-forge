---
phase: 03-motion-compiler-svg-sanitizer
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - src/motion-compiler/keyframe-emitter.ts
  - src/motion-compiler/shape-builder.ts
  - src/motion-compiler/transform-builder.ts
  - src/motion-compiler/markers.ts
  - src/motion-compiler/feature-gate.ts
  - src/motion-compiler/__tests__/feature-gate.spec.ts
  - src/motion-compiler/__tests__/keyframe-emitter.spec.ts
  - src/motion-compiler/__tests__/shape-builder.spec.ts
autonomous: true
requirements: [COM-04]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "keyframe-emitter covers all 10 locked keyframe shapes via an exhaustive switch with NO default branch — a new enum member fails tsc, never silently emits (D-37)"
    - "Phase 3 implements no bake convention and no expression path — expressions in input are hard-rejected with unsupported_feature (P3, D-33 deliberate §6.3.4 deviation)"
    - "The svg-only branch of the feature gate is forced by a synthetic classification fixture (masks/matting base) and returns renderer_support svg-only (D-33; planning decision on Open Question 3)"
    - "Draw-on emits trim-path in Lottie percentages (s/e 0..100, m 1) with the 0→100 animation driven by keyframe_shape, never a float stroke width (D-14, Pitfall 2)"
  prohibitions:
    - statement: "Phase 3 must not implement the D-33 bake-marker convention or ANY expression-baking path — an expression found in input is hard-rejected with unsupported_feature, never baked; the bake mechanism is deferred to v2 (deliberate deviation from §6.3.4 recorded in D-33)"
      category: values
      status: unverified
      verification: test
      flagged: true
  artifacts:
    - "keyframe-emitter.ts exhaustive over KEYFRAME_SHAPES (10 forms) with easing-handle mapping and s:[v] scalar keyframes"
    - "shape-builder.ts complete: 5 generators (rect, ellipse, path, polyline, polystar) per the lottie spec vertex order (circle kappa 0.5519150244935105707435627)"
    - "markers.ts pose rule (7 one-shot shapes → finale, 3 loop shapes → t=0) + trigger emission from trigger_points"
    - "feature-gate.spec.ts — hard rejects + forced svg-only branch"
  key_links:
    - "keyframe shapes come from catalogue KEYFRAME_SHAPES (imported, never redeclared); easing handles from StyleSpec.easing_curves control_points (§6.3.2)"
    - "transform deltas animate with their OWN closed ranges (D-34) — slide/orbit/float/wiggle/bounce encodings never reuse the 0..1 coord bounds"
---

<objective>
Widen the tracer to the FULL motion surface: all 10 keyframe shapes (exhaustive switch, no default), all 5 shape generators, animated transform deltas for the transform/scale families, the D-15 pose rule completed in markers.ts, and the finished feature gate with its hard-reject + svg-only classification tests (COM-04).

Purpose: after this plan, compile() produces valid, gate-passing Lottie for ALL 10 recipes — the prerequisite for the 10 recipe goldens (plan 03-06) and the sanitizer proofs (plan 03-07).

Output: widened compiler modules + feature-gate/keyframe/shape spec suites green.
</objective>

<execution_context>
@.opencode/gsd-core/workflows/execute-plan.md
@.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-motion-compiler-svg-sanitizer/03-CONTEXT.md
@.planning/phases/03-motion-compiler-svg-sanitizer/03-RESEARCH.md
@.planning/phases/03-motion-compiler-svg-sanitizer/03-PATTERNS.md
@src/motion-compiler/compiler.ts
@src/motion-compiler/keyframe-emitter.ts
@src/motion-compiler/shape-builder.ts
@src/rpc/contracts/catalogue.schema.ts
@fixtures/recipe-catalogue/catalogue.json
</context>

<artifacts_this_phase_produces>
This plan completes: the exhaustive `keyframe-emitter.ts` (10 shapes), the full `shape-builder.ts` (path/polyline/polystar added), animated `transform-builder.ts` deltas, the finished `markers.ts` (pose + triggers), the enforcing `feature-gate.ts`, and the spec files `feature-gate.spec.ts`, `keyframe-emitter.spec.ts`, `shape-builder.spec.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Exhaustive keyframe-emitter — 10 shapes, trim-path, easing handles, animated deltas</name>
  <files>src/motion-compiler/keyframe-emitter.ts, src/motion-compiler/transform-builder.ts, src/motion-compiler/__tests__/keyframe-emitter.spec.ts</files>
  <read_first>src/motion-compiler/keyframe-emitter.ts, src/motion-compiler/transform-builder.ts, src/rpc/contracts/catalogue.schema.ts (KEYFRAME_SHAPES), .planning/phases/03-motion-compiler-svg-sanitizer/03-RESEARCH.md (Common Pitfalls 2/3/4/10/11, trim-path Code Example), docs/project/05_Style.md §5.5 (keyframe_shape semantics), docs/project/06_Backbone.md §6.3.2</read_first>
  <action>Replace the tracer's typed-throw stubs with real emission for ALL 10 KEYFRAME_SHAPES (imported from catalogue.schema.js — never redeclared), keeping the switch exhaustive with NO default branch and a never-type exhaustiveness check so a future enum member fails compilation (D-37). Implement per shape family, all scalars as vector keyframes s:[v] (Pitfall 3), opacity always 0..100, easing handles o={x:x1,y:y1} / i={x:x2,y:y2} from the recipe easing curve control_points on each segment's STARTING keyframe, last keyframe bare (Pitfalls 4/11): opacity-ramp (fade) animates opacity low→high by intensity; translate-in (slide) animates position from an offset proportional to amplitude × direction onto the resting position; overshoot-settle (bounce) position/scale overshoot then settle keyed by amplitude; scale-breath (pulse) scale oscillates ± amplitude around 100 with loops from MotionParams; trim-path (draw-on) emits a trim item { ty "tm", s static 0, e animated 0→100, o static 0, m 1 } on the stroke layer — percentages, never 0..1 (D-14/Pitfall 2), with the trim living alongside the path geometry; angular-in (rotate) rotation from -amplitude×360/4 to 0 degrees; pop-settle (scale-pop) scale 0→overshoot→100; sine-drift (float) position sine drift amplitude-bounded over loops; damped-oscillation (wiggle) decaying position oscillation amplitude-bounded; circular-path (orbit) position keyframes sampled on a circle of radius proportional to amplitude around the resting position (deterministic sample count from duration × fr, endpoint exact). Amplitude/intensity scaling: the recipe's resolved intensity scales amplitude within its declared range — one shared helper, unit-tested. Animated transform deltas keep their OWN closed ranges (D-34) — the translate/rotation/scale animations derive from TransformDeltaSchema-resting values plus recipe amplitude, never reinterpreting the 0..1 coord bounds. Extend transform-builder so exactly ONE property is animated per recipe family and everything else stays a:0 direct values (Pitfall 10). Spec `keyframe-emitter.spec.ts`: per shape — keyframes ascending t; last keyframe has no i/o; s arrays everywhere; opacity-shaped outputs within 0..100; trim outputs s/e within 0..100 with m 1; amplitude 0 and intensity-range minimum produce the degenerate-but-valid pose; hold keyframes (h 1) not used in Phase 3 emissions (assert absent).</action>
  <verify>
    <automated>npx vitest run src/motion-compiler/__tests__/keyframe-emitter.spec.ts; if ($?) { npx tsc --noEmit }</automated>
  </verify>
  <acceptance_criteria>
    - keyframe-emitter.ts switch covers all 10 KEYFRAME_SHAPES members with no default branch and a never-exhaustiveness guard (a comment naming the guard is fine; the switch must not silently fall through)
    - keyframe-emitter.spec.ts has ≥ 10 shape-level test groups, each asserting ascending t, bare last keyframe, s arrays, and the unit table (opacity 0..100, trim 0..100)
    - draw-on emission contains a trim item with animated e 0→100 and m 1
    - `npx tsc --noEmit` exits 0 (exhaustiveness compiles)
  </acceptance_criteria>
  <done>All 10 keyframe forms emit spec-conformant keyframes; the emitter is exhaustively closed over the locked catalogue vocabulary.</done>
</task>

<task type="auto">
  <name>Task 2: Shape generators complete (path/polyline/polystar) + markers pose rule and triggers</name>
  <files>src/motion-compiler/shape-builder.ts, src/motion-compiler/markers.ts, src/motion-compiler/__tests__/shape-builder.spec.ts</files>
  <read_first>src/motion-compiler/shape-builder.ts, src/motion-compiler/markers.ts, fixtures/recipe-catalogue/catalogue.json (trigger_points per recipe), .planning/phases/03-motion-compiler-svg-sanitizer/03-RESEARCH.md (Don't Hand-Roll: lottie spec vertex order + kappa constant), docs/project/06_Backbone.md §6.3.2-§6.3.3</read_first>
  <action>Extend shape-builder from rect+ellipse to all 5 generators, following the lottie specification's rendering algorithms (vertex order and winding direction are normative — predictable trim-path behavior for draw-on depends on them; ellipse/circle via 4 bezier segments with kappa 0.5519150244935105707435627): path and polyline map the normalized point lists through the viewBox scale (path closes per the closed flag; polyline stays open) emitting lottie shape data (ty "sh") with fmt()-formatted vertices; polystar emits ty "sr" with the star vertex algorithm (points_count 3..12, r_outer/r_inner from the closed params, rotation applied). All coordinates normalized 0..1 → viewBox units via the shared conversion helper. Complete `markers.ts`: (1) the pose rule finalized as the closed mapping over keyframe_shape — one-shot forms (opacity-ramp, translate-in, overshoot-settle, trim-path, angular-in, pop-settle, damped-oscillation) resolve the SVG pose at frame finale; loop forms (scale-breath, sine-drift, circular-path) resolve at t=0 (D-15 — derived from catalogue data, zero editing, matching the D-15 examples incl. pulse → t=0); exhaustive switch, no default; (2) trigger marker emission from the recipe's trigger_points + keyframe_shape (cm/tm/dr fields) — compiler-emitted only, never free-form triggers in input (D-34). Spec `shape-builder.spec.ts`: each generator emits the expected item type and vertex count (rect 4-corner rendering path, ellipse 4 bezier segments with the kappa constant, path/polyline vertex passthrough scaled, polystar 2×points_count vertices for star); normalized-to-viewBox conversions exact on 0.5 grids; markers.spec content folded here or in keyframe-emitter.spec — pose rule asserted for one one-shot and one loop recipe, triggers present for a recipe with 2 trigger points.</action>
  <verify>
    <automated>npx vitest run src/motion-compiler/__tests__/shape-builder.spec.ts; if ($?) { npx tsc --noEmit }</automated>
  </verify>
  <acceptance_criteria>
    - shape-builder.ts handles all 5 SHAPE_NAMES with no unimplemented branch (each catalogue recipe's canonical shape compiles)
    - shape-builder.spec.ts asserts the kappa constant appears in ellipse emission and polystar vertex count formula
    - markers.ts pose mapping covers all 10 keyframe shapes exhaustively (7 finale / 3 t=0) with no default branch
    - `npx vitest run src/motion-compiler/__tests__/shape-builder.spec.ts` exits 0
  </acceptance_criteria>
  <done>All 5 generators + the D-15 pose rule + trigger emission are complete and tested — compile() now covers every catalogue recipe.</done>
</task>

<task type="auto">
  <name>Task 3: Feature gate enforcement — hard rejects + forced svg-only branch (COM-04/D-33)</name>
  <files>src/motion-compiler/feature-gate.ts, src/motion-compiler/__tests__/feature-gate.spec.ts</files>
  <read_first>src/motion-compiler/feature-gate.ts, src/rpc/contracts/motion-compiler.schema.ts (LottieJSONSchema pins), .planning/phases/03-motion-compiler-svg-sanitizer/03-RESEARCH.md (D-33 + Open Question 3), docs/project/06_Backbone.md §6.3.4</read_first>
  <action>Finalize feature-gate.ts as the enforcing classifier over emitted/observed Lottie structures: `SupportedLottieFeature` enum derived from the lottie-web 5.13 documented subset (shapes, transforms, simple easing, masks add, matte — data-only in Phase 3, the runtime arrives in Phase 4); `assertSupported(layer|composition)` hard-rejects (typed unsupported_feature error) anything outside the subset — 3D (ddd ≠ 0), audio/video/image sequences, negative stretch (op < ip or negative scale), track matte canvas/html variants, and ANY expression (an `x` key anywhere in a property — though the schema already rejects it structurally, the gate re-asserts defense-in-depth); per D-33 there is NO bake path in Phase 3 — an expression is rejected, never baked (deliberate deviation from §6.3.4: the bake-marker convention named in D-33 is deferred to v2 — do not implement it, not even as dead code). `classify()` returns "all" | "svg-only": features ≤ 5.7.0 that a secondary renderer lacks (mask/matting-dependent compositions) classify svg-only; planning decision on Open Question 3 — the branch is forced SYNTHETICALLY in tests with a masks/matting-base fixture (no Phase 3 emission produces svg-only naturally; the real set fills in Phase 4/8). Spec `feature-gate.spec.ts`: each hard-reject case returns unsupported_feature; the synthetic masks/matting fixture classifies svg-only (branch forced — the D-33 unit test); a plain shapes+transform composition classifies all; assert classify never mutates its input.</action>
  <verify>
    <automated>npx vitest run src/motion-compiler/__tests__/feature-gate.spec.ts; if ($?) { npx tsc --noEmit }</automated>
  </verify>
  <acceptance_criteria>
    - feature-gate.spec.ts contains one test per hard-reject family (3D, negative stretch, expressions, matte variants) all asserting the unsupported_feature error code
    - The svg-only branch is exercised by a synthetic masks/matting fixture and asserts renderer_support "svg-only" (D-33 forced-branch test)
    - No bake mechanism exists: a fixed-string grep for the D-33 marker literal (two slashes, space, "lottie", colon, "bake") across src/motion-compiler returns zero matches (deferred v2, D-33)
    - `npx vitest run src/motion-compiler/__tests__/feature-gate.spec.ts` exits 0
  </acceptance_criteria>
  <done>COM-04 complete: subset enum + hard rejects + pinned v + the svg-only branch tested synthetically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Recipe params (catalogue) + resolved intensity -> emitter | Bounded inputs; amplitude scaling must never escape closed ranges |
| Emitted Lottie -> feature gate | Gate re-asserts subset invariants before renderer_support is assigned |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05a | Tampering | expressions in emission path | high | mitigate | Schema-level structural reject (plan 03-02) + gate-level defense-in-depth reject; no bake path exists (D-33) |
| T-03-05b | Tampering | negative stretch / 3D | medium | mitigate | transform-builder construction rejects (scale >= 0) + gate reject + schema reject — three independent layers |
| T-03-05c | DoS | degenerate amplitude (0 / range-min) | low | accept | Degenerate-but-valid poses allowed; non-dégénérescence (ink visible) is gated per-golden in plan 03-06 |
</threat_model>

<verification>
- `npx vitest run src/motion-compiler` green (all module suites)
- `npx tsc --noEmit` green — exhaustiveness guards compile
- `npx vitest run src/motion-compiler/__tests__/pipeline.spec.ts` still green (tracer intact after widening)
</verification>

<success_criteria>
- All 10 recipes compile gate-passing Lottie (prerequisite for the 10 recipe goldens)
- COM-04 fully enforced: enum + hard rejects + svg-only branch + no bake path
- Exhaustive no-default switches over both locked vocabularies (KEYFRAME_SHAPES, SHAPE_NAMES)
</success_criteria>

<output>
Create `.planning/phases/03-motion-compiler-svg-sanitizer/03-05-SUMMARY.md` when done
</output>
